# 🧰 Level-up-Backlog der Roadmap-Routine (Planung-Modul)

> Beobachtungen aus den Läufen der Routine `planung-roadmap-umsetzung` — **nichts hiervon wird von
> der Routine selbst umgesetzt** (Scope-Schutz). Entscheidung + Priorisierung: Dominique.
> Status je Eintrag: `neu` · `angenommen` · `verworfen` · `erledigt`.

| # | Datum | Beobachtung | Ort | Status |
|---|---|---|---|---|
| 1 | 2026-08-14 | **Verworfene/neu generierte Steps behalten ihre Kind-Steps.** `regeneriereStep` räumt jetzt nur die `geplant`/`skipped`-Kinder weg — bereits **erzeugte** Kind-Rezepte (`done`/`freigegeben`) eines neu generierten Gerichts bleiben als Steps stehen und gehören danach zu einem Entwurf, den es nicht mehr gibt. Kein Datenverlust, aber irreführende Stufen-Zähler. | `src/Services/PlanningCascadeService.php:701` (`regeneriereStep`) | neu |
| 2 | 2026-08-14 | **Kein Weg, ein geplantes Sub-Rezept abzuwählen.** `verwirfStep` greift nur bei `done`/`failed`; eine `geplant`-Zeile („brauche ich nicht") kann der Mensch nur mitziehen, indem er das ganze Gericht verwirft. Gehört zum Rest-Chunk Teil 2 von Etappe 1. | `src/Services/PlanningCascadeService.php:732` (`verwirfStep`) | erledigt (`b5275da`: `verwirfGeplantenStep`/`erzeugeGeplantenStep` + je-Zeile-Knöpfe) |
| 4 | 2026-08-14 | **Verworfenes Sub-Rezept behält eine Dangling-Dependency zum Tombstone.** `verwirfGeplantenStep` löscht die Dependency, aber die spätere Stufen-Freigabe ruft `planChildren`, das dieselbe Zutat wieder sieht (Ingredient noch un-referenziert) und die Dependency per `firstOrCreate` **neu anlegt** — jetzt auf einen `verworfen`-Step. Harmlos (kein Job: `dispatchChildren` überspringt Status ≠ `geplant`), aber ein `child_step_id`→Tombstone-Verweis bleibt liegen und könnte Dependency-Zähler verwirren. Fix-Idee: `planChildren` überspringt Ingredients, deren dedupe-Step `verworfen` ist. | `src/Services/RecipeDependencyWorkflowService.php:198` (`planChildren` `firstOrCreate`) | neu |
| 5 | 2026-08-14 | **Scope→Recipe-Typ nur am Tab getestet, nicht an der Generierung.** Die Scope-Treue (39add65) ist jetzt an der UI-Ebene abgesichert (Frei-Start öffnet den passenden Tab). Aber es fehlt weiterhin ein e2e-Test, der `goKaskade('rezept')` durchfährt und am ERZEUGTEN Rezept `is_sales_recipe=false` prüft — das einzige e2e-Cockpit-Szenario läuft mit `'gericht'` (`PlanningCascadeTest:193`), der Basisrezept-Fall prüft nur das Job-`vkModus`-Flag (`:65`), nicht das Ergebnis. Provider-los in der Sandbox → braucht einen `RecipeGeneratorService`-Fake (bind im Container), der ein Draft mit `is_sales_recipe` gemäß `$vkModus` zurückgibt. Würde die Ebene an der materiellen Weiche (`is_sales_recipe`) festnageln statt nur am Tab. | `src/Livewire/Planung/Index.php:437` (`goKaskade` `$vk`) + `src/Services/RecipeGeneratorService.php:122` (`is_sales_recipe`) | neu |
| 6 | 2026-08-14 | **`sub_rezept`-Schemabeschreibung wortgleich in beiden Generator-Prompts dupliziert.** Der ~4-zeilige Slot-Text steht identisch in `recipe.generator` und `vk.generator`. Bei Semantik-Änderung müssen beide manuell synchron gehalten werden. Fix-Idee: gemeinsame `const SUB_REZEPT_SLOT` o. Ä. in `config/foodalchemist.php` und in beide Tasks konkatenieren. | `config/foodalchemist.php:461` (recipe.generator) + `:529` (vk.generator) | neu |
| 7 | 2026-08-14 | **`PromptRegistryTest` prüft keinen Prompt-Text** → der neue Contract-Slot ist ungetestet. Ein Test, der prüft, dass `sub_rezept` in beiden Generator-Tasks vorkommt, würde den Contract festnageln, damit ihn ein späteres Prompt-Refactoring nicht still entfernt, bevor die Entscheidungsstelle ihn liest. Sinnvoll VOR dem Entscheidungsstelle-Chunk (Consumer). | `tests/Feature/PromptRegistryTest.php` | neu |
| 8 | 2026-08-14 | **Bare `sauce`-Token übertriggert bei gekauften Mehrwort-Saucen.** `SUB_SAUCEN_MARKER` trifft `sauce` token-exakt — schützt Ein-Wort-Kondimente (Sojasauce), fängt aber `Worcestershire Sauce`/`Barbecue Sauce`/`Sweet Chili Sauce` (Tokens enthalten exaktes `sauce`) → würden als `basisrezept_anlegen` statt GP/LA klassifiziert. Konservativer Fehler (legt Sub statt Match an), aber falsch für Fertigsaucen. Fix gehört ans LLM-Komponenten-Flag (»sub_rezept«), nicht an eine Blacklist. | `src/Services/Matching/MatchHeuristics.php:57` (`SUB_SAUCEN_MARKER`) | neu |
| 9 | 2026-08-14 | **`essenz`-Klassifikation ist eine offene Entscheidung.** Bei der Heuristik-Erweiterung ausgelassen, weil mehrdeutig (gemachte Klar-Essenz = Sub-Rezept vs. gekaufte Frucht-Essenz/Extrakt = GP; DoD M4-14 pinnt `Drachenfrucht-Essenz` als GP). Gehört als vierter Punkt in den »Offene Entscheidungen«-Block der Roadmap (analog Zerlegungstiefe/Standalone): Soll »Essenz« generell Sub, generell GP oder nur via LLM-Flag entschieden werden? | Roadmap »Offene Entscheidungen« + `src/Services/Matching/MatchHeuristics.php:57` | neu |
| 3 | 2026-08-14 | **`view:cache`/`view:clear` in der Sandbox brechen an einem Fremd-Fehler** (`platform-crm/src/CrmServiceProvider.php:98`, `Facade::__callStatic("hasTable")` ohne DB). `view:cache` kompiliert trotzdem durch (665 Views, 0 Lint-Fehler), aber **`view:clear` räumt nicht auf** — die Kompilate des gerade eingehängten Worktrees bleiben mit frischer mtime liegen und würden nach dem Symlink-Zurücksetzen der Parallel-Session **fremde Views** servieren. Workaround dieses Laufs: `rm -f storage/framework/views/*.php` nach dem Blade-Lint (Pflichtschritt für künftige Läufe). | `platform/modules/platform-crm` (Fremdmodul → Martin) + Routine-Guardrail | neu |
| 10 | 2026-08-14 | **408-Site braucht persistierte Zeilen-Herkunft, um flag-aware zu werden.** Die Entscheidungsstelle liest `sub_rezept` jetzt bei der Generierung (231/247), aber die zweite genannte Stelle (Kohärenz-Gate-ENTdrahtung, `$istSub = $wasSub \|\| istSubRezeptKandidat($text)`) hat kein Zugriff aufs LLM-Flag, weil es nicht am Ingredient persistiert wird. Um 408 flag-aware zu machen, müsste `syncIngredients` die Sub-Rezept-Intent der Zeile mitspeichern (Spalte/Meta) — Schema-Arbeit. Heute unkritisch: bei ENTdrahtung dominiert `$wasSub` (war real verdrahtet) das Signal. | `src/Services/RecipeGeneratorService.php:~430` (`$istSub`) + `syncIngredients` | neu |
| 11 | 2026-08-14 | **Tolerante Coercion in `llmSubRezeptFlag` ist ungetestet.** Der Helper akzeptiert bewusst auch `1`/`0`/"true"/"ja"/"nein" (LLM-Robustheit), aber die 2 neuen Tests decken nur echte PHP-Bools ab (der Fake-Provider echo't den bereits dekodierten Override-Array). Die int-/string-Zweige sind uncovered — ein späteres Refactoring könnte sie still brechen. Fix-Idee: Unit-Test direkt auf `llmSubRezeptFlag` via Reflection oder Datensatz `[1→true, 0→false, "ja"→true, "x"→null]`. | `src/Services/RecipeGeneratorService.php` (`llmSubRezeptFlag`) + `tests/Feature/RecipeGeneratorTest.php` | neu |
| 13 | 2026-08-14 | **Pre-existing Golden-Rot GT-13-11 (koriander-grounding).** `KnowledgeContextTest::GT-13-11` schlägt fehl (`### Pairing-Doku: koriander_blatt`-Count 0 statt 1) — auch mit gestashten Änderungen, also unabhängig vom regelwerk-Chunk. Der Grounding-Pfad findet die Sorten-Dokus per Präfix nicht mehr; vermutlich fehlen die `koriander_blatt`/`koriander_saat`-Anker in `foodalchemist_vocab_pairing_anchors` (Seed-Drift), da `groundingBlock` über `pairingStems()` (Anker-Vokabular) geht, der Test aber nur Docs seedet. Als Fremd-Red abgegrenzt, nicht gefixt. | `tests/Feature/Golden/KnowledgeContextTest.php:190` + `KnowledgeContextService::groundingBlock`/`pairingStems` | neu |
| 14 | 2026-08-14 | **Basis-Routings `ai_generate_recipe:{cross_cutting,domain,pairing}` nur per Import geseedet, nicht per Migration.** `seedRoutings` (Import-Command) setzt sie, aber KEINE Migration — auf einer frisch migrierten DB (Test/fresh deploy ohne `knowledge:import`) fehlen sie, weshalb jeder KnowledgeContext-Test die `cross_cutting`-Zeile manuell nachlegen muss (Concept- und jetzt Regelwerk-Test). Risiko: ein Deploy, der nur migriert und den Import vergisst, fährt den Rezept-Generator ohne Cross-Cutting-/Domain-/Pairing-Wissen. Fix-Idee: die Kern-Routings des Rezept-Generators auch als Migration seeden (idempotent, insertOrIgnore) — wie es 2026_08_07_000001 für die suggestiven Kategorien schon tut. | `src/Console/KnowledgeImportCommand.php:245-247` (nur Import) | neu |
| 15 | 2026-08-14 | **`recipe.steps` erdet nicht am Regelwerk Basisrezepte.** Der neue `regelwerk:always`-Block hängt nur an `ai_generate_recipe`. Die Step-by-Step-Generierung (`recipe.steps`, Spec 27) baut ebenfalls Zubereitungslogik und würde von §2 (Verarbeitungs-Reduktion) / §4 (Sub-Rezept-Hierarchie) profitieren — die Roadmap-Zeile nannte aber nur `ai_generate_recipe`, daher bewusst enger gehalten. Fix-Idee: eine zweite Routing-Zeile `recipe.steps:regelwerk:always` (der `regelwerkBlock` ist feature-agnostisch, greift ohne Code-Änderung). | `src/Console/KnowledgeImportCommand.php` (`seedRoutings`) + Migration | neu |
| 16 | 2026-08-15 | **Partner-Reihenfolge des `menue_kohaesion`-Blocks ist ungetestet.** `menueAromaProfil` sortiert die harmonischen Partner `level→weight→cover` (desc) über `kandidatenFuerAnker` (private). Der neue Test prüft nur *Enthaltensein* (`toContain('Dill')`), nicht die Rangfolge. Ändert ein späteres Pairing-Refactoring die Buckets/Gewichte in `kandidatenFuerAnker`, verschiebt sich der Menü-Hint still (die stärksten Partner stehen dem LLM zuerst = Wirkung). Fix-Idee: ein Fixture mit zwei Partnern unterschiedlicher `level`/`weight` und Assertion auf die Index-Reihenfolge. | `src/Services/PairingService.php` (`menueAromaProfil`/`kandidatenFuerAnker`) + `tests/Feature/MenueKohaesionGroundingTest.php` | neu |
| 17 | 2026-08-15 | **`menue_kohaesion` ist im Kontext-Inspektor unsichtbar.** Der Generator-Ergebnis-Modal „Verwendetes Wissen" speist sich aus `RecipeGenerationContextService::build` (`kontext.wissen`/`chars`/`templates`) — die Menü-Erdung fliesst aber über `IdeenService::kiDivergenzConcept` direkt in den `foodbook.kapitel_ideen`-Prompt, an diesem Audit vorbei. Der Mensch sieht am erfundenen Menü-Gericht nicht, DASS/WORAUF pairing-geerdet wurde. Gehört zur finalen UI-Etappe: `menue_kohaesion` (Anker + Partner) als Audit-Feld an die Divergenz-Rückgabe hängen und im Cockpit/Inspektor zeigen. | `src/Services/IdeenService.php` (`kiDivergenzConcept` Rückgabe) + Kontext-Inspektor | neu |
| 18 | 2026-08-15 | **Doppelte Anker-Auflösung derselben belegten Gerichte.** `kiDivergenzConcept` ruft `menueAromaProfil`→`resolveRecipeAnchorsMany` über die belegten Slot-Rezepte; der Concept-Assembler (`ConceptGeneratorService::fuelleBestehendesKonzept`/`MenuCandidatePoolService::fuerFrame`) hat dieselben Anker im selben Lauf schon aufgelöst. Bei der Fan-out-Erfindung (`fanoutConceptInvention`→`kiDivergenzConcept`) wird der Anker-Batch also ein zweites Mal gefahren (~7 Queries + Ingredient-Trees je Aufruf). Unkritisch bei kleinem Menü, aber redundant. Fix-Idee: das schon aufgelöste Anker-Set des Pools an die Divergenz durchreichen, statt neu aufzulösen. | `src/Services/IdeenService.php` (`kiDivergenzConcept`) + `src/Services/MenuCandidatePoolService.php` (`fuerFrame`) | neu |
| 19 | 2026-08-15 | **Kohäsions-Schwellen (60/35) jetzt an ZWEI Orten.** `menuKohaesionWarnung` (Service) und die `$scoreFarbe`-Zeile im `kohaesion-panel.blade.php` berechnen die drei Bänder (≥60/≥35) unabhängig aus demselben `score`. Bewusst byte-genau gespiegelt, aber Drift-Risiko: tunt jemand die Service-Schwellen, laufen Banner-Farbe (aus `warnung.stufe`) und Score-Zahl-Farbe (aus `$scoreFarbe`) auseinander. Fix-Idee: die Blade-Score-Farbe ebenfalls aus `warnung.stufe` ableiten (eine Quelle), statt den Score neu zu klassifizieren. | `resources/views/livewire/planning/partials/kohaesion-panel.blade.php:21` (`$scoreFarbe`) + `src/Services/PairingService.php` (`menuKohaesionWarnung`) | neu |
| 20 | 2026-08-15 | **Warnung-Banner-Render nur compile-gelintet, nicht render-getestet.** Der Livewire-Wiring-Test (`ConcepterEditorTest`) fährt nur den `zu_wenig`-null-Pfad (leeres Konzept) — die tatsächliche Banner-Ausgabe (Blade-`@if($warnung)`-Zweig mit `data-kohaesion-warnung`) ist nur durch das Blade-Kompilat-Lint abgesichert, nicht durch eine Render-Assertion. Die Klassifikator-Bänder sind unit-getestet, aber die End-to-End-Kette Editor→menueKohaesion→Blade-Banner nicht. Fix-Idee: ein Livewire-Render-Test mit zwei graph-verbundenen Gerichten in Slots + `assertSeeHtml('data-kohaesion-stufe')` (braucht GP+Anker+Kante-Fixture wie in `MenueKohaesionGroundingTest`). | `tests/Feature/ConcepterEditorTest.php` (kohaesionPruefen-Test) | neu |
| 12 | 2026-08-14 | **Latente Vor-Bestands-Inkonsistenz Site1↔Site2 ohne Flag.** Mit gesetztem Flag stimmen `$istBasisrezept` (Site 231) und `primaer` (Site 247) jetzt überein. OHNE Flag bleibt aber die alte Divergenz: Site 1 nutzt `queryIstHalbfabrikat`, Site 2 ODER-t zusätzlich `istSubRezeptKandidat` (breiter: creme/mousse/pesto). Eine Zeile wie »Basilikum-Pesto« → `istBasisrezept=false` (⇒ LA-Suche läuft, `la_kandidaten` gefüllt) UND gleichzeitig `primaer='basisrezept_anlegen'` = Misch-Signal an die Review-Fläche. Vorbestehend, durch meinen Chunk nicht verändert. Fix-Idee: eine einzige Sub-Entscheidung berechnen und an beiden Stellen verwenden. | `src/Services/RecipeGeneratorService.php:231` vs. `:247` | neu |
| 21 | 2026-08-15 | **Persistierte `cohesion_warning` hat noch keinen Leser.** Der Auto-Trigger schreibt die Menü-Warnung an `foodalchemist_cascade_runs.cohesion_warning`, aber kein Cockpit-Element zeigt sie — der Mensch erfährt vom Auto-Score nur, wenn er separat „Kohäsion prüfen" im Conceptor klickt (rechnet neu). Explizit als Folge-Chunk markiert (Roadmap + Commit `75d263a`): die persistierte Warnung als Banner/Badge im Planung-Cockpit sichtbar machen (Sprungliste-Detail/Stufen-Header), analog Anreicherungs-Badge. Gehört zur finalen UI-Etappe. | `src/Livewire/Planung/Index.php` (Cockpit) + `FoodAlchemistCascadeRun::$cohesion_warning` | neu |
| 22 | 2026-08-15 | **Auto-Score löst den vollen Anker-Batch je Fan-out-Abschluss neu auf.** `persistConceptCohesion` lädt `ConceptService::detail` + fährt `menuCohesion`→`anchorsForRecipe` über ALLE Gerichte, sobald der Fan-out durch ist. Bei einem Concept, das über `regeneriereStep` mehrfach neu durchläuft (jeder Voll-Abschluss triggert erneut), wird der ganze Ingredient-Tree-/Anker-Batch wiederholt. Im Queue-Worker unkritisch, aber redundant zum ohnehin im Fan-out aufgelösten Anker-Set (überschneidet sich mit #18). Fix-Idee: das aufgelöste Anker-Set einmal cachen/durchreichen statt zweimal aufzulösen. | `src/Services/PlanningCascadeService.php` (`persistConceptCohesion`) | neu |
| 23 | 2026-08-15 | **Menü-Params (`menue_gaenge`/`menue_preis_*_pp`) werden getragen, aber noch von keinem Consumer gelesen.** Teil 1 von Etappe 2a whitelistet + persistiert sie als `generation_params`, aber weder Prompt noch `PlanningFrameService` konsumieren sie — reiner Contract/Träger (bewusst, Roadmap-Punkt 2 = Frame-Feed). Risiko: bleibt der Consumer aus, ist es ein stiller No-Op (der Nutzer stellt Achsen ein, ohne Wirkung). Nächster Chunk (2a-Punkt-2) MUSS `reglerParams`→Frame-Head (`target_price_pp`/`price_min_pp`/`price_max_pp`) + Slot-`target_count` verdrahten. | `src/Livewire/Planung/Index.php` (`reglerParams` `$menue`-Zweig) + `src/Services/PlanningFrameService.php` (`setHead`) | neu |
| 24 | 2026-08-15 | **`diaet_hart` (harte Ausschluss-Multi-Select) vs. künftige Diät-*Quote* (Anteil) im selben Concept-Tab kollidieren begrifflich.** Teil 2 von Etappe 2a bringt „Diät-Quoten (vegan/vegetarisch-Anteil)" — das ist semantisch das `diet_quota`-Rule (min/percent), NICHT der bestehende harte `diaet_hart`-Constraint. Beide am Concept-Tab nebeneinander ohne klare UI-Trennung würden den Nutzer verwirren („warum vegan-Pill UND vegan-Quote?"). Bei Teil 2 sauber labeln: harter Ausschluss ≠ Portfolio-Anteil. | `resources/views/livewire/planung/partials/leitplanken.blade.php` (`diaet_hart` + künftige Quote) | neu |
| 25 | 2026-08-15 | **Euro-Parser dreifach kopiert.** `menuePreisEur` ist strukturgleich zu `zielVkEur` (nur Feld + Bounds unterscheiden sich: 0,50–500 je Portion vs. 0,50–2000 je Person). Ein gemeinsamer `private eurWert(string $roh, float $min, float $max): ?float` würde beide (und künftige Preisfelder) speisen. Bewusst dupliziert gehalten, um `zielVkEur`-Verhalten in diesem Chunk nicht anzufassen. | `src/Livewire/Planung/Index.php` (`zielVkEur` + `menuePreisEur`) | neu |
| 26 | 2026-08-15 | **⚠ ÜBERSPRUNGEN (offene Entscheidung, nicht eigenmächtig entschieden):** Roadmap-Punkt Etappe 1 „**Auto-Zerlegung** — `RecipeDependencyWorkflowService:49/90`-Gating: soll auch Standalone-Gericht (ohne Kaskade) zerlegen?" ist der direkte Spiegel der offenen Entscheidung „Standalone-Zerlegung" (Roadmap §Offene Entscheidungen). Als erster offener Top-down-Punkt beim Lauf 2026-08-15 bewusst NICHT umgesetzt — Entscheidung nur Dominique. Ebenso „**Abnahme:** Blindtest mit 3 Briefs + Golden-Eval" = Real-LLM-Abnahme, provider-los in der Sandbox nicht bewertbar → Dominique auf demo. Deshalb sprang der Lauf auf Etappe 2a (erster buildbarer Chunk). | Roadmap §Offene Entscheidungen + `src/Services/RecipeDependencyWorkflowService.php:49` | offen (Entscheid Dominique) |
| 27 | 2026-08-15 | **Diät-Quoten vegan/vegetarisch ohne Cross-Konsistenz.** `menueQuote` parst beide `_pct`-Achsen unabhängig — ein Nutzer kann `menue_quote_vegan_pct=50` + `menue_quote_vegetarisch_pct=20` setzen, was fachlich widersprüchlich ist (jedes vegane Gericht IST vegetarisch → Vegetarisch-Anteil ≥ Vegan-Anteil). Der Consumer (Frame/Prompt, 2a-Punkt-2) bekäme ein inkonsistentes Paar. Bewusst NICHT hart validiert in diesem Chunk (weiche Vorgabe, kein Filter; harte Guards nur gegen mistgetippte Zahlen). Fix-Idee bei Consumer-Bau: soft-normalisieren (`vegetarisch = max(vegetarisch, vegan)`) oder Guard-Hinweis, statt das LLM widersprüchlich zu briefen. | `src/Livewire/Planung/Index.php` (`menueQuote`/`goKaskade` concept-Guard) | neu |
| 28 | 2026-08-15 | **Frame-Feed-Chunk (2a-Punkt-2) muss AUCH die Quoten verdrahten, nicht nur Gänge/Preis.** #23 nennt für den Consumer nur `target_price_pp`/`price_*_pp`/`target_count`. Mit Teil 2 kommen `menue_quote_vegan_pct`/`menue_quote_vegetarisch_pct` dazu — sie sind derselbe „getragen, kein Leser"-No-Op. Der Frame-Feed muss die Quoten als Portfolio-/Diät-Regel in den `PlanningFrame` (bzw. Concept-Fan-out-Prompt) übersetzen, sonst bleibt die neue Achse wirkungslos. Ergänzung zu #23. | `src/Services/PlanningFrameService.php` + `src/Livewire/Planung/Index.php` (`reglerParams` `$menue`-Zweig) | neu |
| 29 | 2026-08-15 | **`menue_balance` ist der dritte „getragen, kein Leser"-No-Op** (mit #23 Gänge/Preis + #28 Quoten). Portfolio-Balance (`b325ed0`) wird whitelisted + als `generation_param` persistiert, aber kein Prompt/Frame liest sie. Der Frame-Feed-Chunk (2a-Punkt-2) muss ALLE DREI Menü-Achsen-Familien verdrahten — Gänge/Preis → Frame-Head, Quoten → Diät-Regel, `menue_balance` → Zusammenstellungs-/Vielfalts-Direktive im Concept-Fan-out-Prompt. Sonst stellt der Nutzer eine Vielfalts-Achse ein, die spurlos verpufft. Sammel-Ergänzung zu #23/#28. | `src/Services/PlanningFrameService.php` + `src/Livewire/Planung/Index.php` (`reglerParams` `$menue`-Zweig, `menueBalance`) | neu |
| 30 | 2026-08-15 | **Blade-Kompilat-Lint (Routine-Pflichtschritt) in der Sandbox blockiert, sobald MySQL aus ist.** Dieser Lauf: `view:cache` bricht diesmal KOMPLETT ab (CRM-`QueryException` „Connection refused" Port 3306, bevor auch nur eine View kompiliert — anders als #3, wo es durchlief) → 0 Kompilate in `storage/framework/views/`. Der Fallback „einzelne Blade isoliert über `BladeCompiler::compileString` linten" scheitert am `<x-foodalchemist::modal-section>`-Component-Tag (`ComponentTagCompiler` braucht den gebooteten Container → `View\Factory not instantiable`). Effektiver Ersatz dieses Laufs: (a) mechanische Direktiven-Balance-Prüfung (`@if`/`@endif`/`@foreach`/`@endforeach`-Counts + Zeilen-Trace) + (b) der Livewire-**Render**-Test (`assertSeeHtml('data-menue-balance')`) rendert das Partial durch die volle View-Pipeline und würde einen Blade-ParseError als roten Test zeigen. Für Planung-Blades ist die Render-Assertion damit die verlässlichere Absicherung als der Kompilat-Lint. Empfehlung für künftige Läufe: bei Blade-Änderung IMMER einen `assertSeeHtml`-Marker mitliefern (fängt geklebte Direktiven ohne MySQL). | Routine-Guardrail (Pest-Gate/Blade-Lint) + `platform-crm` (Fremd-Boot-DB-Zugriff → Martin) | neu |
| 31 | 2026-08-15 | **Frame-Preis-Head-Override greift NUR im Brief-Reuse-Pfad, nicht am geplanten Konzept (`generiereAusGeruest`).** `generiereAusBrief` wendet den Concept-Tab-Preis-Korridor jetzt an; aber `generiereAusGeruest(frame)` (Assembler auf einem SCHON bestehenden Frame — z.B. der künftige „KI-Kopf"/geprüftes Draft-Konzept, Etappe 2b `existing_concept_id`) bekommt die `menueAchsen` nicht durchgereicht. Sobald 2b den geplanten Pfad baut, muss die Preis-/Achsen-Verdrahtung dort GENAUSO greifen, sonst ist die Leitplanke nur im Schnell-Pfad wirksam. Heute unkritisch (2b ungebaut), aber die Achsen-Feed-Logik gehört an EINE gemeinsame Stelle (Frame-Head-Sync), nicht nur in `generiereAusBrief`. | `src/Services/ConceptGeneratorService.php` (`generiereAusGeruest` vs. `generiereAusBrief`) | neu |
| 32 | 2026-08-15 | **`menue_preis_ziel_pp` ohne Spanne-Konsistenzprüfung gegen min/max.** `menuePreisHead` mappt die drei Preis-Achsen unabhängig auf den Frame-Kopf — ein Nutzer kann `ziel=35` mit `min=40`/`max=30` setzen (fachlich widersprüchlich: Ziel außerhalb der Spanne, min>max). `setHead` speichert sie roh; kein Guard sortiert/normalisiert. Bewusst NICHT in diesem Teilschritt validiert (weiche Vorgabe; harte Guards nur gegen mistgetippte Zahlen, s. `menuePreisEur`). Fix-Idee beim Consumer-Ausbau: soft-normalisieren (`min ≤ ziel ≤ max`) oder Guard-Hinweis am `goKaskade`-Concept-Gate, analog der geplanten Quoten-Konsistenz #27. | `src/Services/ConceptGeneratorService.php` (`menuePreisHead`) + `src/Livewire/Planung/Index.php` (`goKaskade` concept-Guard) | neu |
| 33 | 2026-08-15 | **Preis-Head-Override umgeht `promptKontext`-Sichtbarkeit erst nach `replaceStructure`-Refresh.** Der zweite `setHead` (Preis-Override) läuft VOR `replaceStructure` + `fuelleBestehendesKonzept($frame->refresh())`; das ist korrekt (der Assembler sieht den überschriebenen Kopf). Aber es gibt keinen Test, der beweist, dass der überschriebene Zielpreis auch im R6-`promptKontext`-Block („Preisarchitektur: Zielpreis …") landet — nur die DB-Spalten sind assertet. Bei einem künftigen Refactor, das die Reihenfolge dreht (Struktur zuerst, Kopf danach), könnte der Slot-Assembler still den KI-Preis statt des Nutzer-Preises sehen. Fix-Idee: eine Assertion auf `promptKontext($frame)` mit dem überschriebenen Betrag. | `tests/Feature/ConceptGeneratorTest.php` (Menü-Leitplanken-Test) + `PlanningFrameService::promptKontext` | neu |
| 34 | 2026-08-15 | **`menue_gaenge`-Cap ist nur ein Deckel, kein Exakt-N.** `menueGaengeCap` schneidet überzählige `gang`-Slots ab, kann aber KEINE fehlenden Gänge nachlegen (das Gerüst-System erfindet nichts). Ein Nutzer, der 5 Gänge will, aber dessen Brief die KI nur 3 gang-Slots entlockt, bekommt still 3 — die Leitplanke wirkt asymmetrisch. Echte Exakt-N-Vorgabe ohne Erfindung geht nur prompt-seitig: `menue_gaenge` als harte Direktive in den `concept.brief_geruest`-Kontext (`$kontext['menue_gaenge']`) geben, damit die KI genau N Gänge mit sinnvollen Labels erzeugt; der Deckel bleibt als deterministischer Ceiling-Guard. Bewusst als Folge-Chunk gehalten (Teil 2 = deterministischer Frame-Deckel, mirror Teil 1). | `src/Services/ConceptGeneratorService.php` (`menueGaengeCap` + `generiereAusBrief`-Kontext) + `config/foodalchemist.php` (`concept.brief_geruest`) | neu |
| 35 | 2026-08-15 | **Label „Anzahl Gänge / Positionen" — die `/ Positionen`-Hälfte ist bei Buffets ein stiller No-Op.** `menueGaengeCap` deckelt bewusst NUR `gang`-Slots; ein Concept, das die KI als Buffet (`station`/`kapitel`-Slots) baut, ignoriert `menue_gaenge` komplett. Für Menüs korrekt (Gänge = gang-Slots), aber das UI-Label verspricht auch „Positionen" — der Nutzer eines Buffet-Briefs stellt eine Zahl ein, die verpufft. Entscheidung nötig (Dominique): entweder (a) `menue_gaenge` cappt bei Buffet auch die Stationen, oder (b) das Label wird auf „Anzahl Gänge (nur Menü)" verengt, oder (c) eine zweite Achse „Positionen je Station". Nicht eigenmächtig entschieden. | `resources/views/livewire/planung/partials/leitplanken.blade.php:89` (Label) + `src/Services/ConceptGeneratorService.php` (`menueGaengeCap`) | neu (Entscheid Dominique) |
| 36 | 2026-08-15 | **`menueGaengeCap` greift wie der Preis-Head nur im Brief-Reuse-Pfad (`generiereAusBrief`), nicht am geplanten Konzept (`generiereAusGeruest`).** Direkte Erweiterung von #31: sobald Etappe 2b den „KI-Kopf"/`existing_concept_id`-Pfad baut, muss die Gänge-Deckelung (und die künftigen Quoten-/Balance-Consumer) an DERSELBEN gemeinsamen Stelle greifen wie der Preis-Head — sonst wirken alle Menü-Achsen nur im Schnell-Pfad. Bestätigt die #31-Empfehlung, die Achsen-Feed-Logik an EINE Frame-Sync-Stelle zu ziehen (Head + Slots + Regeln gemeinsam), statt sie über `generiereAusBrief` zu verstreuen. | `src/Services/ConceptGeneratorService.php` (`generiereAusGeruest` vs. `generiereAusBrief`) | neu |
| 37 | 2026-08-15 | **Diät-Quoten-Feed greift wie Preis/Gänge NUR im Brief-Pfad (`generiereAusBrief`), nicht am geplanten Konzept (`generiereAusGeruest`).** `menueDiaetQuotenMerge` (Teil 3a) läuft in `generiereAusBrief` vor `replaceStructure`; `generiereAusGeruest(frame)` (Assembler auf bestehendem Frame — künftiger „KI-Kopf"/`existing_concept_id`, Etappe 2b) bekommt die `menueAchsen` nicht. Direkte Erweiterung von #31/#36: sobald 2b den geplanten Pfad baut, müssen Preis-Head + Gänge-Cap + Quoten-Merge an DERSELBEN gemeinsamen Frame-Sync-Stelle greifen, sonst wirken alle Menü-Achsen nur im Schnell-Pfad. | `src/Services/ConceptGeneratorService.php` (`generiereAusGeruest` vs. `generiereAusBrief`) | neu |
| 38 | 2026-08-15 | **Frame-Diät-Quote wird vom deterministischen Assembler gelesen, aber NICHT dem Concept-Erfindungs-Prompt gebrieft.** Positiv-Befund: anders als #23/#28/#29 (getragen, kein Leser) hat die frame-Ebene-`diet_quota`-Regel echte Konsumenten — `MenuAssemblyService::menuQuoten` (Bestand-Auswahl) + `CoverageService:610` (Prozent-Scoring). ABER: der LLM-Fan-out `IdeenService::kiDivergenzConcept` (erfindet Gerichte für LEERE Slots) bekommt die Quote nicht als Direktive — auf dem Erfindungs-Pfad wird die Diät-Quote also nur nachträglich gescored/geampelt, nicht vorab gesteuert (parallel zu #17 menue_kohaesion-Unsichtbarkeit). Fix-Idee: die frame-Quoten (wie den `menue_kohaesion`-Block) als Zusammenstellungs-Direktive in den Erfindungs-Prompt geben. | `src/Services/IdeenService.php` (`kiDivergenzConcept`) + `src/Services/MenuAssemblyService.php` (`menuQuoten`) | neu |
| 39 | 2026-08-15 | **Balance-Direktive greift NUR im Brief-Pfad (`generiereAusBrief`), nicht am geplanten Konzept (`generiereAusGeruest`)** — direkte Erweiterung der Familie #31/#36/#37. Anders als Preis/Gänge/Quote ist `menue_balance` aber ein PROMPT-Feed (`$kontext['menue_zusammenstellung']`), kein Frame-Sync — die in #31 empfohlene „eine gemeinsame Frame-Sync-Stelle" reicht also nicht: der 2b-Pfad (`existing_concept_id`, Assembler auf bestehendem Frame) hat gar keinen frischen KI-Gerüst-Prompt mehr, in den die Direktive fließen könnte. Bei 2b-Bau klären, wie die Zusammenstellungs-Direktive den geplanten Pfad erreicht (z.B. an den Fan-out-Erfindungs-Prompt `kiDivergenzConcept` statt an `concept.brief_geruest`). Schließt zugleich die Balance-Leg von #29 (menue_balance hat jetzt einen Leser). | `src/Services/ConceptGeneratorService.php` (`generiereAusGeruest` vs. `generiereAusBrief`) | neu |
| 40 | 2026-08-15 | **`menue_zusammenstellung`-Direktive ist im Kontext-Inspektor unsichtbar** (parallel zu #17 menue_kohaesion + #38 Diät-Quote). Der Block wird direkt in den `concept.brief_geruest`-Kontext gehängt und als JSON an die Task serialisiert (`AiGatewayService:150`), läuft aber NICHT durch `RecipeGenerationContextService::build` — der Generator-Ergebnis-Modal „Verwendetes Wissen" zeigt dem Menschen also nicht, DASS/WORAUF eine Balance-Direktive briefte. Gehört zur finalen UI-Etappe: die getragenen Menü-Direktiven (Balance, Kohäsion, Quote) als Audit-Feld sichtbar machen. | `src/Services/ConceptGeneratorService.php` (`menueBalanceDirektive`) + Kontext-Inspektor | neu |
| 41 | 2026-08-15 | **„Menü-Leitplanke" = Präfix `menue_` an VIER Stellen implizit dupliziert.** Nach der Trennung (2b6762a) hängt die Definition, welche Keys Menü- statt Rezept-Leitplanken sind, an vier unabhängigen Präfix-Prüfungen: `reglerParams` setzt sie im Concept-Zweig, `ALLOWED_GENERATION_PARAMS` listet sie, `dispatchConceptStep` filtert `str_starts_with('menue_')` in `$menueAchsen`, und jetzt strippt `sessionGenerationParams` denselben Präfix. Eine künftige Menü-Achse OHNE `menue_`-Präfix würde still in den Rezept-Fan-out durchsickern (der Strip griffe nicht) — genau der Bug, den dieser Chunk schloss, in neuer Gestalt. Fix-Idee: eine einzige Quelle (`const MENUE_LEITPLANKE_PREFIX='menue_'` oder ein Key-Set) an allen vier Stellen referenzieren, statt den Präfix je Stelle hart zu tippen. | `src/Livewire/Planung/Index.php` (`reglerParams`) + `src/Models/FoodAlchemistPlanningSession.php` (`ALLOWED_GENERATION_PARAMS`) + `src/Services/PlanningCascadeService.php` (`dispatchConceptStep`/`sessionGenerationParams`) | neu |
| 43 | 2026-08-15 | **`concept.plan` routet Wissen, aber `propose()` konsumiert Routings NICHT selbst — der nächste Chunk (`planAusBrief`) MUSS den Block aktiv bauen.** Die Routings `concept.plan:{cross_cutting:always, domain:discovery, concept:always}` sind geseedet (Migration `2026_07_27_000001` + `seedRoutings`), aber `AiGatewayService::propose` lädt nur *gebundenes* Wissen (`knowledge_bindings`) automatisch — die *gerouteten* Kategorien fließen nur, wenn der Aufrufer sie über `KnowledgeContextService` auflöst und als `options['knowledge']` mitgibt (so machen es `RecipeGeneratorService`/`ConceptGeneratorService::generiereAusBrief`). Baut `planAusBrief` den Knowledge-Block nicht, läuft der Kreativ-Kopf trotz vorhandener Routing-Zeilen wissens-blind. Kein Bug dieses Chunks (reiner Prompt-Contract), aber Pflicht für den nächsten. | `src/Services/ConceptGeneratorService.php` (`planAusBrief`, folgt) + `src/Services/Ai/AiGatewayService.php:140` (`propose`) | **geschlossen (`316e51a`)** — `fuelleCanvasAusPlan` löst die Routings über `contextFor` auf + reicht `food_dna_concept_id` durch |
| 44 | 2026-08-15 | **`geschmackswelten`-Output-Shape (`[{claim, description}]`) muss `planAusBrief` auf die Canvas-`repeatable`-Entries mappen — kein Contract erzwingt die Feldnamen.** Der Prompt fordert je Welt `{claim, description}`; das Canvas-Template `concept.geschmackswelten` deklariert `sub => {claim, description}` (passt heute). Aber nichts nagelt beide aneinander: ein späterer Canvas-Refactor, der die Sub-Keys umbenennt (z.B. `titel`/`text`), bräche die Materialisierung still, ohne dass `ConceptPlanPromptTest` (prüft nur die Feld-*Keys* auf oberster werte-Ebene, nicht die geschmackswelten-Sub-Keys) rot würde. Fix-Idee bei `planAusBrief`-Bau: die Sub-Keys aus `CanvasService::TEMPLATES['concept']['felder'][geschmackswelten]['sub']` ableiten statt hart zu tippen, ODER einen Test, der Prompt-Sub-Keys ↔ Template-Sub-Keys gegenprüft. | `config/foodalchemist.php` (`concept.plan` task) + `src/Services/CanvasService.php` (`TEMPLATES.concept.geschmackswelten.sub`) | neu |
| 45 | 2026-08-15 | **Kreativ-Kopf `planAusBrief` erbt die Concept-Menü-Leitplanken NICHT.** `generiereAusBrief` konsumiert `menueAchsen` (Preis-Korridor/Gänge-Cap/Diät-Quoten/Balance-Direktive, Etappe 2a); der neue `planAusBrief` reicht sie NICHT an die Gerüst-Reuse durch — `geruestAusBriefFuerOwner` hat gar keinen `menueAchsen`-Parameter. Folge: sobald der „KI-Kopf"-Knopf (nächster Chunk) auf `planAusBrief` verdrahtet, ignoriert der geplante Pfad die Concept-Tab-Achsen, die der Schnell-Pfad (`generiereAusBrief`) ehrt — genau die Asymmetrie, die #31/#36/#37/#39 für `generiereAusGeruest` beschreiben, in dritter Gestalt. Fix-Idee: `menueAchsen` als Parameter an `planAusBrief` + `geruestAusBriefFuerOwner` (bzw. die in #31 empfohlene EINE Frame-Sync-Stelle) durchreichen, damit alle drei Einstiegspfade dieselben Menü-Leitplanken anwenden. | `src/Services/ConceptGeneratorService.php` (`planAusBrief` → `geruestAusBriefFuerOwner`) | neu |
| 46 | 2026-08-15 | **Orphan-Cleanup bei Gerüst-Fehler in `planAusBrief` verlässt sich auf ungeprüften Frame-Cascade-Delete.** Wirft `geruestAusBriefFuerOwner` NACH `frameFor`/`setHead` (Edge „KI-Gerüst enthielt keine gültigen Slots"), existiert ein Rumpf-Frame; `planAusBrief` räumt ihn per `$this->frames->find('concept', id)?->delete()` ab. Ob `FoodAlchemistPlanningFrame::delete()` seine `frame_slots`/`frame_rules` mit-löscht (DB-Cascade oder Model-`deleting`-Hook), ist NICHT verifiziert — sonst bleiben verwaiste Slot-/Rule-Zeilen zurück. Der häufige Fehlerfall (KI liefert gar kein Gerüst) wirft VOR `frameFor` → kein Frame, kein Orphan; der Rumpf-Fall ist der Rand. Fix-Idee: Cascade der Frame-Löschung testen/absichern (oder `replaceStructure`+Head atomar in eine Transaktion, die bei leerem Slot-Ergebnis zurückrollt). | `src/Services/ConceptGeneratorService.php` (`planAusBrief`-catch) + `src/Models/FoodAlchemistPlanningFrame.php` (`delete`-Cascade) | neu |
| 42 | 2026-08-15 | **menue_*-Strip nur am Concept-Gericht-Pfad getestet, nicht am Speiseplan-Zell-Pfad.** Beide Fan-out-Erben (`materialisiereConceptGericht` + `materialisiereSpeiseplanZelle`) teilen `sessionGenerationParams`, aber die 2 neuen Tests fahren nur den Concept-Gericht-Pfad. Ein Refactor, der `materialisiereSpeiseplanZelle` den Erben umgehen ließe (z.B. eigene Param-Zusammenstellung), würde die Trennung dort still brechen, ohne einen roten Test. Fix-Idee: entweder ein zweiter Test auf `materialisiereSpeiseplanZelle` (braucht Plan+Zelle-Fixture) ODER ein direkter Reflection-Unit-Test auf `sessionGenerationParams([...menue_...]) === [...ohne menue_...]` — nagelt den Erben für beide Konsumenten fest. | `tests/Feature/PlanningCascadeTest.php` + `src/Services/PlanningCascadeService.php` (`sessionGenerationParams`) | neu |
| 47 | 2026-08-15 | **KI-Kopf verlinkt das erzeugte Draft-Concept NICHT mit der Planungs-Session (Lineage-Lücke, blockiert den nächsten Chunk).** `kiKopf` ruft `planAusBrief` und öffnet den Conceptor, aber die neue Concept-ID wird NIRGENDS an der Session persistiert (weder Spalte noch transiente Prop). Der nächste Roadmap-Bullet (Etappe 2b: `existing_concept_id` in `starteKaskade` → Concept-Step referenziert das geprüfte Draft) braucht aber genau diesen Rückgriff: nach „Plan im Conceptor prüfen" muss der Go wissen, WELCHES Draft-Concept freigegeben wurde. Heute geht die ID nach dem `dispatch` verloren (der Nutzer landet im Conceptor-Modal, das Planung-Cockpit kennt die ID nicht mehr). Fix-Idee beim `existing_concept_id`-Chunk: `kiKopf` legt `planConceptId` als transiente Prop ab (Roadmap Zeile 150 nennt sie schon) ODER persistiert `session.planned_concept_id`, damit der „Go aus geprüftem Plan" die Referenz findet. | `src/Livewire/Planung/Index.php` (`kiKopf` → fehlende `planConceptId`) + `src/Services/PlanningCascadeService.php` (`starteKaskade` `existing_concept_id`) | **geschlossen (`c528bf9`)** — `kiKopf` merkt `planConceptId` (transiente Prop), `goKaskade` liest sie als `existing_concept_id` |
| 48 | 2026-08-15 | **Provider-Stub dritte Kopie — `bindKiKopfStub` (PlanungLeitstelleTest) ≈ `bindPlanStub` (ConceptGeneratorTest) ≈ die inline-Stubs in ConceptGeneratorTest.** Beide handrollen einen vollen `LLMProviderContract` mit derselben `Concept-Canvas`→concept.plan / sonst→concept.brief_geruest-Verzweigung. Bewusst self-contained gehalten (die Routine fährt PlanungLeitstelleTest gezielt, ohne ConceptGeneratorTest zu laden → `bindPlanStub` wäre undefined), aber ein gemeinsamer Test-Fake gehört laut Memory [FA-Suite parallel] in `tests/Support/` (geteilte Helfer MÜSSEN dorthin). Fix-Idee: ein `tests/Support/FakeConceptPlanProvider.php` (Slots + optionale Plan-Werte im Konstruktor), von beiden Test-Files gebunden — eine Quelle für den concept.plan/brief_geruest-Stub. | `tests/Feature/PlanungLeitstelleTest.php` (`bindKiKopfStub`) + `tests/Feature/ConceptGeneratorTest.php` (`bindPlanStub`) + `tests/Support/` | neu |
| 49 | 2026-08-15 | **`kiKopf` hat keinen Re-Entrancy-Guard — Doppelklick erzeugt zwei Draft-Concepts.** Der Blade-Knopf ist `@disabled($laeuft)` + `wire:loading.attr=disabled`, aber die Methode selbst prüft `$laeuft` NICHT (anders als `goKaskade`, das ohnehin über den Lauf-Status geht). `$laeuft` bezieht sich zudem auf eine KASKADE, nicht auf einen laufenden KI-Kopf — während `planAusBrief` (synchroner LLM-Call, mehrere Sekunden) läuft, ist `$laeuft=false`. Ein hektischer Doppel-Request (Livewire debounct Buttons nicht hart) legt zwei `concept_plan_ui`-Drafts an, von denen nur der zweite geöffnet wird → verwaister erster Draft. Konservativ (kein Datenverlust, nur Müll-Draft), aber unsauber. Fix-Idee: ein transientes `$kiKopfLaeuft`-Flag ODER die geplante `planConceptId`-Prop (#47) als Idempotenz-Schlüssel (schon ein Draft für dieses Briefing? → wiederverwenden statt neu). | `src/Livewire/Planung/Index.php` (`kiKopf`) | neu |
| 50 | 2026-08-15 | **`existing_concept_id`-Pfad reicht die Concept-Menü-Achsen (`menue_*`) NICHT durch — sie sind am geprüften Draft aber schon materialisiert.** `dispatchConceptStep` filtert die `menue_*`-Params in den `GenerateConceptJob` (Preis-Head/Gänge-Cap/Quoten/Balance); `referenziereConceptStep` tut das nicht — der Fan-out erbt sie nur indirekt über den bereits von `planAusBrief`/`geruestAusBriefFuerOwner` gebauten Frame + die Session-`generation_params`. Heute konsistent, SOLANGE `planAusBrief` denselben Frame baut, den der Schnell-Pfad hätte (Backlog #45 flaggt, dass `planAusBrief` die Achsen noch NICHT erbt) — sobald #45 gefixt ist, muss verifiziert werden, dass der Referenz-Pfad die Achsen nicht ein ZWEITES Mal (widersprüchlich) anlegt. Klärung beim Caller-Wiring (nächster Bullet). | `src/Services/PlanningCascadeService.php` (`referenziereConceptStep` vs. `dispatchConceptStep`) | neu |
| 51 | 2026-08-15 | **Eager-`existing_concept_id`: kurzes `review`→`running`-Flackern.** `referenziereConceptStep` ruft im eager-Zweig `markStepDone` (→ recompute setzt Run kurz auf `review`, weil der `done`-Concept-Step noch kein Gericht-Kind hat) und setzt DANACH `run.status=running` + dispatcht `FanoutConceptJob`. Der final geschriebene Status ist korrekt (`running`), aber ein Poller, der exakt zwischen den beiden Writes liest, sähe `review`. Fensterbreite = zwei Statements im selben Web-Request (praktisch nie getroffen). Der Normal-Pfad (GenerateConceptJob eager) hat das Problem nicht, weil er `fanoutConceptInvention` SYNCHRON vor `markStepDone` laufen lässt (Kinder existieren am recompute). Fix-Idee falls je störend: Run vor `markStepDone` auf `running` pinnen (wie `starteFolgestufe` es tut) statt danach. | `src/Services/PlanningCascadeService.php` (`referenziereConceptStep` eager-Zweig) | neu |
| 52 | 2026-08-15 | **`planConceptId` ist NICHT an die aktive Session gebunden — Session-Wechsel nach KI-Kopf leakt den Plan.** `kiKopf` setzt die transiente Prop; `oeffne`/`waehle` (Session-Wechsel) setzen sie NICHT zurück. Wer für Session A einen KI-Kopf-Plan ausarbeitet (planConceptId=A-Concept), dann in Session B wechselt und dort den Concept-Go drückt, referenziert das FREMDE (aber team-eigene → Guard passiert) Plan-Concept von A in B's Kaskade. Kein Datenverlust, aber falsche Lineage/Zusammenstellung. Fix-Idee: `planConceptId` in `oeffne`/`waehle` auf `null` setzen (Session-Kontext-Reset), ODER die Prop gegen die Session validieren (Concept.planning_session_id == aktive Session) bevor `existing_concept_id` gereicht wird. | `src/Livewire/Planung/Index.php` (`oeffne`/`waehle`/`goKaskade`) | neu |
| 53 | 2026-08-15 | **Geplanter Pfad überlebt keinen Page-Reload — der geprüfte Plan verpufft still.** `planConceptId` ist bewusst transient (kein Migration, Roadmap Zeile 152). Folge: nach „KI-Kopf → im Conceptor prüfen" genügt ein Reload/Re-Mount der Planung-Fläche (Livewire-Neustart), und der Go fällt wortlos auf den Schnell-Pfad zurück (frische Generierung), obwohl das geprüfte Draft-Concept in der DB weiterlebt. Der Nutzer bemerkt den Pfad-Wechsel nicht (kein „Plan-vorbereitet"-Badge nach Reload, weil die Prop weg ist). Fix-Idee (falls die Persistenz-Entscheidung kippt): `session.planned_concept_id` als leichte Spalte ODER beim Öffnen das jüngste `concept_plan_*`-Draft der Session als planConceptId rehydrieren, damit der geplante Pfad reload-fest ist. Bewusst nicht in diesem Chunk (Roadmap pinnt „transiente Prop"). | `src/Livewire/Planung/Index.php` (`planConceptId`, `mount`/`oeffne`) | neu (Entscheid Dominique — Persistenz-Politik) |
| 54 | 2026-08-15 | **OFFENE ENTSCHEIDUNG — Auto-Zerlegung Standalone-Gericht (Roadmap Et.1 Zeile 116 + „Offene Entscheidungen").** `RecipeDependencyWorkflowService:49/90`-Gating: soll auch die Nicht-Kaskaden-Gericht-Erstellung (VkGenerator, Standalone) automatisch in Sub-Basisrezepte zerlegen, oder bleibt Zerlegung Kaskaden-exklusiv? Die Routine hat diesen Roadmap-Punkt bewusst NICHT umgesetzt — er steht wörtlich im „Offene Entscheidungen"-Block (Scope-Schutz: Entscheidung nur Dominique). Sobald entschieden, wird er ein normaler Umsetzungs-Chunk. | `src/Services/RecipeDependencyWorkflowService.php:49/90` | neu (Entscheid Dominique) |
| 55 | 2026-08-15 | **`skizzeAlsGericht`: der Tab-Wechsel feuert auch bei fehlgeschlagener Übernahme.** Der Knopf koppelt `wire:click="skizzeAlsGericht(id)"` (server) mit Alpine `@click="tab='gericht'"` (client) — Alpine schaltet den Tab SOFORT clientseitig, unabhängig vom Server-Ergebnis. Ist die Skizze weg/verworfen (Action setzt `fehler` + lässt den Tab leer), landet der Mensch trotzdem auf dem leeren Gericht-Tab, während oben der Fehler steht — leicht widersprüchlich. Konservativ (kein Datenverlust), aber Politur. Fix-Idee: den Tab-Wechsel an einen Erfolgs-Event der Action hängen (`$this->dispatch('skizze-uebernommen')` → Alpine `x-on:skizze-uebernommen.window="tab='gericht'"`) statt clientseitig blind. | `resources/views/livewire/planung/index.blade.php` (Skizzen-Board, „als Gericht"-Knopf) + `src/Livewire/Planung/Index.php` (`skizzeAlsGericht`) | neu |
| 56 | 2026-08-15 | **`skizzeAlsGericht` überschreibt einen bereits getippten Gericht-Brief ohne Warnung.** Hat der Mensch im Gericht-Tab schon Titel/Brief eingegeben und klickt dann auf einer Skizze „als Gericht", ersetzt das Prefill `eingabe.gericht.titel`/`brief` still. Selten und leicht rückgängig (er tippt neu), aber ein „überschreibt vorhandene Eingabe"-Hinweis oder ein Merge (anhängen statt ersetzen, wenn schon Text da ist) wäre freundlicher. Fix-Idee: bei nicht-leerem Ziel-Feld nur ergänzen ODER eine `meldung`-Warnung setzen. | `src/Livewire/Planung/Index.php` (`skizzeAlsGericht`) | neu |
| 57 | 2026-08-15 | **`skizzeGerichtId` wird bei Session-Wechsel NICHT zurückgesetzt — leakt die Skizzen-Herkunft in eine fremde Session** (exakte Parallele zu #52 für `planConceptId`). `skizzeAlsGericht` setzt die transiente Prop; `oeffne`/`waehle` (Session-Wechsel) lassen sie stehen. Wer in Session A eine Skizze in den Gericht-Tab überträgt, dann zu Session B wechselt und dort den Gericht-Go drückt, stempelt den B-Lauf mit der A-Skizze — der Fail-soft-Guard (`visibleToTeam` + nicht `verworfen`) passiert, weil die A-Skizze team-sichtbar ist. Kein Datenverlust, aber falsche Lineage (die Karte in A zeigt dann einen Lauf, der zu B gehört). Fix-Idee: `skizzeGerichtId` (wie `planConceptId`, #52) in `oeffne`/`waehle` auf `null` setzen ODER die Skizze gegen die aktive Session validieren (`planning_session_id == aktive Session`) bevor `origin_dish_idea_id` gestempelt wird. | `src/Livewire/Planung/Index.php` (`oeffne`/`waehle`/`goKaskade`) | neu |
| 58 | 2026-08-15 | **Teil 2b (Status auf die Karte) muss die `origin_dish_idea_id`↔Lauf-Beziehung defensiv lesen — es gibt keinen DB-FK und die Skizze ist soft-deletable.** Der Lauf trägt einen losen `origin_dish_idea_id` (kein `constrained()`, bewusst SQLite-safe); `FoodAlchemistDishIdea` nutzt `SoftDeletes`. Beim Surfacing gilt: (a) eine soft-gelöschte Skizze liefert über `originDishIdea()` `null` (default-Scope filtert `deleted_at`) → die Karte existiert dann gar nicht mehr, kein Problem; ABER (b) mehrere Läufe können DIESELBE Skizze referenzieren (der Mensch drückt „als Gericht" → Go, korrigiert, drückt erneut) — die Karte muss dann den JÜNGSTEN Lauf zeigen (`latest('id')` je `origin_dish_idea_id`), nicht den ersten, sonst friert der Status auf einem alten Lauf ein. Fix-Idee beim 2b-Bau: pro Skizze den neuesten `FoodAlchemistCascadeRun where origin_dish_idea_id=… latest` auflösen (ggf. als `hasMany`/`latestOfMany`-Relation am DishIdea-Model), Status defensiv auf „kein Lauf" fallen lassen. | `src/Models/FoodAlchemistDishIdea.php` (fehlende `cascadeRuns`/`latestCascadeRun`-Relation) + `src/Livewire/Planung/Index.php` (`render` Skizzen-Feed) | neu |
| 59 | 2026-08-15 | **Run-Status→deutsches Label ist jetzt eine dritte, eigene Wahrheit im Blade (`$laufStatus`).** Teil 2b mappt `running/review/done/failed` → `läuft/prüfen/fertig/fehlgeschlagen` in einer eigenen Closure im `@php`-Kopf der Planung-Landing. Der Worker-Cockpit (`partials/ergebnis.blade.php`) und der Fortschritts-Header (`index.blade.php:326-330`) benennen dieselben Run-Zustände nochmals unabhängig (dort textuell „Läuft …"/„Freigegeben …"). Driftet eine Benennung (z.B. `review` → „Sammel-Review" statt „prüfen"), sagen Karte und Cockpit Verschiedenes über denselben Lauf. Fix-Idee: ein gemeinsamer Status-Label-Helper (analog `Ui::maps()` / `$statusLabel`) für die 4 Run-STATUSES, von Karte UND Cockpit referenziert — eine Quelle. | `resources/views/livewire/planung/index.blade.php` (`$laufStatus`) + `resources/views/livewire/planung/partials/ergebnis.blade.php` + `src/Models/FoodAlchemistCascadeRun.php` (`STATUSES`) | neu |
| 61 | 2026-08-15 | **Batch-Skizzen (Teil 3) hat KEINEN Dedup-Guard — erneutes „Alle als Gerichte" startet je Skizze einen ZWEITEN Lauf.** `skizzenBatchAlsGerichte` batcht alle bearbeitbaren Skizzen unabhängig davon, ob eine davon schon einen laufenden/prüfbaren Gericht-Go trägt (`origin_dish_idea_id`). Zwei Klicks (oder Klick nach einem Einzel-`skizzeAlsGericht`→Go) legen doppelte Läufe je Skizze an; die Karte zeigt via Teil 2b nur den jüngsten, die älteren laufen aber trotzdem (Worker-Last + Kosten). Fix-Idee: vor dem Start je Skizze prüfen, ob schon ein `FoodAlchemistCascadeRun` mit `origin_dish_idea_id=idee AND status IN (running,review)` existiert → überspringen + im Summary sagen („N gestartet, M übersprungen (laufen schon)"). | `src/Livewire/Planung/Index.php` (`skizzenBatchAlsGerichte`) | neu |
| 62 | 2026-08-15 | **ENTSCHEID Dominique — Batch (Teil 3) ist quell-AGNOSTISCH, der Roadmap-Punkt heißt aber „KI-Divergenz-Skizzen".** `skizzenBatchAlsGerichte` batcht ALLE bearbeitbaren Session-Skizzen (auch manuell via `ideeHinzu` angelegte, `created_via='ui'`), nicht nur die KI-erzeugten (`source_meta.quelle='ki_divergenz'`). Bewusst so gewählt (ein Batch-Launch ist quell-egal nützlich; eine willkürliche Beschränkung auf KI-Skizzen würde manuelle Skizzen ausschließen), aber es weicht vom Roadmap-Wortlaut ab. Klärung: soll der Knopf (a) quell-agnostisch bleiben (Status quo) oder (b) nur `ki_divergenz`-Skizzen batchen (dann manuelle weiter einzeln via „als Gericht")? Nicht eigenmächtig verengt. | `src/Livewire/Planung/Index.php` (`skizzenBatchAlsGerichte`) | neu (Entscheid Dominique) |
| 63 | 2026-08-15 | **Batch-Karten-Badges refreshen nicht live (bewusster Kein-Cockpit-Hijack).** `skizzenBatchAlsGerichte` setzt `laeuft` NICHT (kein Einzel-Cockpit-Poll für einen Batch) — die Teil-2b-Status-Badges der N Läufe aktualisieren sich daher erst bei der nächsten Livewire-Interaktion, nicht sekündlich wie beim Einzel-Go (`wire:poll` an `$laeuft`). Für den ersten Teilschritt akzeptiert (der Stand ist korrekt, nur nicht auto-aktualisiert). Fix gehört zu Teil 3b: ein leichter Board-Poll, solange irgendein Skizzen-Lauf `running`/`queued` ist (z.B. `wire:poll` an einer abgeleiteten `$skizzenLaufenNoch`-Prop), unabhängig vom Einzel-Cockpit-`$laeuft`. | `src/Livewire/Planung/Index.php` (`skizzenBatchAlsGerichte` / `render` Skizzen-Feed) + `resources/views/livewire/planung/index.blade.php` (Divergenz-Board) | neu |
| 60 | 2026-08-15 | **Skizzen-Status-Badge ist reine Anzeige — kein Sprung zum Lauf.** Teil 2b zeigt „▸ prüfen/fertig" auf der Karte, aber der Mensch muss selbst in den Worker-Tab wechseln und den Lauf suchen, um zu handeln (freigeben/ansehen). Die Map trägt schon die `run_id` — ein Klick auf das Badge könnte den Worker-Tab öffnen und exakt diesen Lauf laden (`$this->laufId = run_id` + Alpine `tab='worker'`). Gehört zur finalen UI-Etappe (Details-Panel/Lineage „Im Editor öffnen", Roadmap Zeile 243), aber die Datengrundlage (run_id je Skizze) liegt jetzt schon bereit. Fix-Idee: Badge als `<button wire:click="oeffneLauf(run_id)">` mit Tab-Wechsel. | `resources/views/livewire/planung/index.blade.php` (`$skizzeLaufBadge`) + `src/Livewire/Planung/Index.php` (`render` Skizzen-Feed / neue `oeffneLauf`) | neu |
