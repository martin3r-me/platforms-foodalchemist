# 🧰 Level-up-Backlog der Roadmap-Routine (Planung-Modul)

> Beobachtungen aus den Läufen der Routine `planung-roadmap-umsetzung` — **nichts hiervon wird von
> der Routine selbst umgesetzt** (Scope-Schutz). Entscheidung + Priorisierung: Dominique.
> Status je Eintrag: `neu` · `angenommen` · `verworfen` · `erledigt`.

| # | Datum | Beobachtung | Ort | Status |
|---|---|---|---|---|
| 1 | 2026-08-14 | **Verworfene/neu generierte Steps behalten ihre Kind-Steps.** `regeneriereStep` räumt jetzt nur die `geplant`/`skipped`-Kinder weg — bereits **erzeugte** Kind-Rezepte (`done`/`freigegeben`) eines neu generierten Gerichts bleiben als Steps stehen und gehören danach zu einem Entwurf, den es nicht mehr gibt. Kein Datenverlust, aber irreführende Stufen-Zähler. | `src/Services/PlanningCascadeService.php:701` (`regeneriereStep`) | neu |
| 2 | 2026-08-14 | **Kein Weg, ein geplantes Sub-Rezept abzuwählen.** `verwirfStep` greift nur bei `done`/`failed`; eine `geplant`-Zeile („brauche ich nicht") kann der Mensch nur mitziehen, indem er das ganze Gericht verwirft. Gehört zum Rest-Chunk Teil 2 von Etappe 1. | `src/Services/PlanningCascadeService.php:732` (`verwirfStep`) | erledigt (`b5275da`: `verwirfGeplantenStep`/`erzeugeGeplantenStep` + je-Zeile-Knöpfe) |
| 4 | 2026-08-14 | **Verworfenes Sub-Rezept behält eine Dangling-Dependency zum Tombstone.** `verwirfGeplantenStep` löscht die Dependency, aber die spätere Stufen-Freigabe ruft `planChildren`, das dieselbe Zutat wieder sieht (Ingredient noch un-referenziert) und die Dependency per `firstOrCreate` **neu anlegt** — jetzt auf einen `verworfen`-Step. Harmlos (kein Job: `dispatchChildren` überspringt Status ≠ `geplant`), aber ein `child_step_id`→Tombstone-Verweis bleibt liegen und könnte Dependency-Zähler verwirren. Fix-Idee: `planChildren` überspringt Ingredients, deren dedupe-Step `verworfen` ist. | `src/Services/RecipeDependencyWorkflowService.php:198` (`planChildren` `firstOrCreate`) | neu |
| 5 | 2026-08-14 | **Scope→Recipe-Typ nur am Tab getestet, nicht an der Generierung.** Die Scope-Treue (39add65) ist jetzt an der UI-Ebene abgesichert (Frei-Start öffnet den passenden Tab). Aber es fehlt weiterhin ein e2e-Test, der `goKaskade('rezept')` durchfährt und am ERZEUGTEN Rezept `is_sales_recipe=false` prüft — das einzige e2e-Cockpit-Szenario läuft mit `'gericht'` (`PlanningCascadeTest:193`), der Basisrezept-Fall prüft nur das Job-`vkModus`-Flag (`:65`), nicht das Ergebnis. Provider-los in der Sandbox → braucht einen `RecipeGeneratorService`-Fake (bind im Container), der ein Draft mit `is_sales_recipe` gemäß `$vkModus` zurückgibt. Würde die Ebene an der materiellen Weiche (`is_sales_recipe`) festnageln statt nur am Tab. | `src/Livewire/Planung/Index.php:437` (`goKaskade` `$vk`) + `src/Services/RecipeGeneratorService.php:122` (`is_sales_recipe`) | neu |
| 6 | 2026-08-14 | **`sub_rezept`-Schemabeschreibung wortgleich in beiden Generator-Prompts dupliziert.** Der ~4-zeilige Slot-Text steht identisch in `recipe.generator` und `vk.generator`. Bei Semantik-Änderung müssen beide manuell synchron gehalten werden. Fix-Idee: gemeinsame `const SUB_REZEPT_SLOT` o. Ä. in `config/foodalchemist.php` und in beide Tasks konkatenieren. | `config/foodalchemist.php:461` (recipe.generator) + `:529` (vk.generator) | neu |
| 7 | 2026-08-14 | **`PromptRegistryTest` prüft keinen Prompt-Text** → der neue Contract-Slot ist ungetestet. Ein Test, der prüft, dass `sub_rezept` in beiden Generator-Tasks vorkommt, würde den Contract festnageln, damit ihn ein späteres Prompt-Refactoring nicht still entfernt, bevor die Entscheidungsstelle ihn liest. Sinnvoll VOR dem Entscheidungsstelle-Chunk (Consumer). | `tests/Feature/PromptRegistryTest.php` | neu |
| 8 | 2026-08-14 | **Bare `sauce`-Token übertriggert bei gekauften Mehrwort-Saucen.** `SUB_SAUCEN_MARKER` trifft `sauce` token-exakt — schützt Ein-Wort-Kondimente (Sojasauce), fängt aber `Worcestershire Sauce`/`Barbecue Sauce`/`Sweet Chili Sauce` (Tokens enthalten exaktes `sauce`) → würden als `basisrezept_anlegen` statt GP/LA klassifiziert. Konservativer Fehler (legt Sub statt Match an), aber falsch für Fertigsaucen. Fix gehört ans LLM-Komponenten-Flag (»sub_rezept«), nicht an eine Blacklist. | `src/Services/Matching/MatchHeuristics.php:57` (`SUB_SAUCEN_MARKER`) | neu |
| 9 | 2026-08-14 | **`essenz`-Klassifikation ist eine offene Entscheidung.** Bei der Heuristik-Erweiterung ausgelassen, weil mehrdeutig (gemachte Klar-Essenz = Sub-Rezept vs. gekaufte Frucht-Essenz/Extrakt = GP; DoD M4-14 pinnt `Drachenfrucht-Essenz` als GP). Gehört als vierter Punkt in den »Offene Entscheidungen«-Block der Roadmap (analog Zerlegungstiefe/Standalone): Soll »Essenz« generell Sub, generell GP oder nur via LLM-Flag entschieden werden? | Roadmap »Offene Entscheidungen« + `src/Services/Matching/MatchHeuristics.php:57` | neu |
| 3 | 2026-08-14 | **`view:cache`/`view:clear` in der Sandbox brechen an einem Fremd-Fehler** (`platform-crm/src/CrmServiceProvider.php:98`, `Facade::__callStatic("hasTable")` ohne DB). `view:cache` kompiliert trotzdem durch (665 Views, 0 Lint-Fehler), aber **`view:clear` räumt nicht auf** — die Kompilate des gerade eingehängten Worktrees bleiben mit frischer mtime liegen und würden nach dem Symlink-Zurücksetzen der Parallel-Session **fremde Views** servieren. Workaround dieses Laufs: `rm -f storage/framework/views/*.php` nach dem Blade-Lint (Pflichtschritt für künftige Läufe). | `platform/modules/platform-crm` (Fremdmodul → Martin) + Routine-Guardrail | neu |
| 10 | 2026-08-14 | **408-Site braucht persistierte Zeilen-Herkunft, um flag-aware zu werden.** Die Entscheidungsstelle liest `sub_rezept` jetzt bei der Generierung (231/247), aber die zweite genannte Stelle (Kohärenz-Gate-ENTdrahtung, `$istSub = $wasSub \|\| istSubRezeptKandidat($text)`) hat kein Zugriff aufs LLM-Flag, weil es nicht am Ingredient persistiert wird. Um 408 flag-aware zu machen, müsste `syncIngredients` die Sub-Rezept-Intent der Zeile mitspeichern (Spalte/Meta) — Schema-Arbeit. Heute unkritisch: bei ENTdrahtung dominiert `$wasSub` (war real verdrahtet) das Signal. | `src/Services/RecipeGeneratorService.php:~430` (`$istSub`) + `syncIngredients` | neu |
| 11 | 2026-08-14 | **Tolerante Coercion in `llmSubRezeptFlag` ist ungetestet.** Der Helper akzeptiert bewusst auch `1`/`0`/"true"/"ja"/"nein" (LLM-Robustheit), aber die 2 neuen Tests decken nur echte PHP-Bools ab (der Fake-Provider echo't den bereits dekodierten Override-Array). Die int-/string-Zweige sind uncovered — ein späteres Refactoring könnte sie still brechen. Fix-Idee: Unit-Test direkt auf `llmSubRezeptFlag` via Reflection oder Datensatz `[1→true, 0→false, "ja"→true, "x"→null]`. | `src/Services/RecipeGeneratorService.php` (`llmSubRezeptFlag`) + `tests/Feature/RecipeGeneratorTest.php` | neu |
| 13 | 2026-08-14 | **Pre-existing Golden-Rot GT-13-11 (koriander-grounding).** `KnowledgeContextTest::GT-13-11` schlägt fehl (`### Pairing-Doku: koriander_blatt`-Count 0 statt 1) — auch mit gestashten Änderungen, also unabhängig vom regelwerk-Chunk. Der Grounding-Pfad findet die Sorten-Dokus per Präfix nicht mehr; vermutlich fehlen die `koriander_blatt`/`koriander_saat`-Anker in `foodalchemist_vocab_pairing_anchors` (Seed-Drift), da `groundingBlock` über `pairingStems()` (Anker-Vokabular) geht, der Test aber nur Docs seedet. Als Fremd-Red abgegrenzt, nicht gefixt. | `tests/Feature/Golden/KnowledgeContextTest.php:190` + `KnowledgeContextService::groundingBlock`/`pairingStems` | neu |
| 14 | 2026-08-14 | **Basis-Routings `ai_generate_recipe:{cross_cutting,domain,pairing}` nur per Import geseedet, nicht per Migration.** `seedRoutings` (Import-Command) setzt sie, aber KEINE Migration — auf einer frisch migrierten DB (Test/fresh deploy ohne `knowledge:import`) fehlen sie, weshalb jeder KnowledgeContext-Test die `cross_cutting`-Zeile manuell nachlegen muss (Concept- und jetzt Regelwerk-Test). Risiko: ein Deploy, der nur migriert und den Import vergisst, fährt den Rezept-Generator ohne Cross-Cutting-/Domain-/Pairing-Wissen. Fix-Idee: die Kern-Routings des Rezept-Generators auch als Migration seeden (idempotent, insertOrIgnore) — wie es 2026_08_07_000001 für die suggestiven Kategorien schon tut. | `src/Console/KnowledgeImportCommand.php:245-247` (nur Import) | neu |
| 15 | 2026-08-14 | **`recipe.steps` erdet nicht am Regelwerk Basisrezepte.** Der neue `regelwerk:always`-Block hängt nur an `ai_generate_recipe`. Die Step-by-Step-Generierung (`recipe.steps`, Spec 27) baut ebenfalls Zubereitungslogik und würde von §2 (Verarbeitungs-Reduktion) / §4 (Sub-Rezept-Hierarchie) profitieren — die Roadmap-Zeile nannte aber nur `ai_generate_recipe`, daher bewusst enger gehalten. Fix-Idee: eine zweite Routing-Zeile `recipe.steps:regelwerk:always` (der `regelwerkBlock` ist feature-agnostisch, greift ohne Code-Änderung). | `src/Console/KnowledgeImportCommand.php` (`seedRoutings`) + Migration | neu |
| 16 | 2026-08-15 | **Partner-Reihenfolge des `menue_kohaesion`-Blocks ist ungetestet.** `menueAromaProfil` sortiert die harmonischen Partner `level→weight→cover` (desc) über `kandidatenFuerAnker` (private). Der neue Test prüft nur *Enthaltensein* (`toContain('Dill')`), nicht die Rangfolge. Ändert ein späteres Pairing-Refactoring die Buckets/Gewichte in `kandidatenFuerAnker`, verschiebt sich der Menü-Hint still (die stärksten Partner stehen dem LLM zuerst = Wirkung). Fix-Idee: ein Fixture mit zwei Partnern unterschiedlicher `level`/`weight` und Assertion auf die Index-Reihenfolge. | `src/Services/PairingService.php` (`menueAromaProfil`/`kandidatenFuerAnker`) + `tests/Feature/MenueKohaesionGroundingTest.php` | neu |
| 17 | 2026-08-15 | **`menue_kohaesion` ist im Kontext-Inspektor unsichtbar.** Der Generator-Ergebnis-Modal „Verwendetes Wissen" speist sich aus `RecipeGenerationContextService::build` (`kontext.wissen`/`chars`/`templates`) — die Menü-Erdung fliesst aber über `IdeenService::kiDivergenzConcept` direkt in den `foodbook.kapitel_ideen`-Prompt, an diesem Audit vorbei. Der Mensch sieht am erfundenen Menü-Gericht nicht, DASS/WORAUF pairing-geerdet wurde. Gehört zur finalen UI-Etappe: `menue_kohaesion` (Anker + Partner) als Audit-Feld an die Divergenz-Rückgabe hängen und im Cockpit/Inspektor zeigen. | `src/Services/IdeenService.php` (`kiDivergenzConcept` Rückgabe) + Kontext-Inspektor | neu |
| 18 | 2026-08-15 | **Doppelte Anker-Auflösung derselben belegten Gerichte.** `kiDivergenzConcept` ruft `menueAromaProfil`→`resolveRecipeAnchorsMany` über die belegten Slot-Rezepte; der Concept-Assembler (`ConceptGeneratorService::fuelleBestehendesKonzept`/`MenuCandidatePoolService::fuerFrame`) hat dieselben Anker im selben Lauf schon aufgelöst. Bei der Fan-out-Erfindung (`fanoutConceptInvention`→`kiDivergenzConcept`) wird der Anker-Batch also ein zweites Mal gefahren (~7 Queries + Ingredient-Trees je Aufruf). Unkritisch bei kleinem Menü, aber redundant. Fix-Idee: das schon aufgelöste Anker-Set des Pools an die Divergenz durchreichen, statt neu aufzulösen. | `src/Services/IdeenService.php` (`kiDivergenzConcept`) + `src/Services/MenuCandidatePoolService.php` (`fuerFrame`) | neu |
| 19 | 2026-08-15 | **Kohäsions-Schwellen (60/35) jetzt an ZWEI Orten.** `menuKohaesionWarnung` (Service) und die `$scoreFarbe`-Zeile im `kohaesion-panel.blade.php` berechnen die drei Bänder (≥60/≥35) unabhängig aus demselben `score`. Bewusst byte-genau gespiegelt, aber Drift-Risiko: tunt jemand die Service-Schwellen, laufen Banner-Farbe (aus `warnung.stufe`) und Score-Zahl-Farbe (aus `$scoreFarbe`) auseinander. Fix-Idee: die Blade-Score-Farbe ebenfalls aus `warnung.stufe` ableiten (eine Quelle), statt den Score neu zu klassifizieren. | `resources/views/livewire/planning/partials/kohaesion-panel.blade.php:21` (`$scoreFarbe`) + `src/Services/PairingService.php` (`menuKohaesionWarnung`) | neu |
| 20 | 2026-08-15 | **Warnung-Banner-Render nur compile-gelintet, nicht render-getestet.** Der Livewire-Wiring-Test (`ConcepterEditorTest`) fährt nur den `zu_wenig`-null-Pfad (leeres Konzept) — die tatsächliche Banner-Ausgabe (Blade-`@if($warnung)`-Zweig mit `data-kohaesion-warnung`) ist nur durch das Blade-Kompilat-Lint abgesichert, nicht durch eine Render-Assertion. Die Klassifikator-Bänder sind unit-getestet, aber die End-to-End-Kette Editor→menueKohaesion→Blade-Banner nicht. Fix-Idee: ein Livewire-Render-Test mit zwei graph-verbundenen Gerichten in Slots + `assertSeeHtml('data-kohaesion-stufe')` (braucht GP+Anker+Kante-Fixture wie in `MenueKohaesionGroundingTest`). | `tests/Feature/ConcepterEditorTest.php` (kohaesionPruefen-Test) | neu |
| 12 | 2026-08-14 | **Latente Vor-Bestands-Inkonsistenz Site1↔Site2 ohne Flag.** Mit gesetztem Flag stimmen `$istBasisrezept` (Site 231) und `primaer` (Site 247) jetzt überein. OHNE Flag bleibt aber die alte Divergenz: Site 1 nutzt `queryIstHalbfabrikat`, Site 2 ODER-t zusätzlich `istSubRezeptKandidat` (breiter: creme/mousse/pesto). Eine Zeile wie »Basilikum-Pesto« → `istBasisrezept=false` (⇒ LA-Suche läuft, `la_kandidaten` gefüllt) UND gleichzeitig `primaer='basisrezept_anlegen'` = Misch-Signal an die Review-Fläche. Vorbestehend, durch meinen Chunk nicht verändert. Fix-Idee: eine einzige Sub-Entscheidung berechnen und an beiden Stellen verwenden. | `src/Services/RecipeGeneratorService.php:231` vs. `:247` | neu |
| 21 | 2026-08-15 | **Persistierte `cohesion_warning` hat noch keinen Leser.** Der Auto-Trigger schreibt die Menü-Warnung an `foodalchemist_cascade_runs.cohesion_warning`, aber kein Cockpit-Element zeigt sie — der Mensch erfährt vom Auto-Score nur, wenn er separat „Kohäsion prüfen" im Conceptor klickt (rechnet neu). Explizit als Folge-Chunk markiert (Roadmap + Commit `75d263a`): die persistierte Warnung als Banner/Badge im Planung-Cockpit sichtbar machen (Sprungliste-Detail/Stufen-Header), analog Anreicherungs-Badge. Gehört zur finalen UI-Etappe. | `src/Livewire/Planung/Index.php` (Cockpit) + `FoodAlchemistCascadeRun::$cohesion_warning` | neu |
| 22 | 2026-08-15 | **Auto-Score löst den vollen Anker-Batch je Fan-out-Abschluss neu auf.** `persistConceptCohesion` lädt `ConceptService::detail` + fährt `menuCohesion`→`anchorsForRecipe` über ALLE Gerichte, sobald der Fan-out durch ist. Bei einem Concept, das über `regeneriereStep` mehrfach neu durchläuft (jeder Voll-Abschluss triggert erneut), wird der ganze Ingredient-Tree-/Anker-Batch wiederholt. Im Queue-Worker unkritisch, aber redundant zum ohnehin im Fan-out aufgelösten Anker-Set (überschneidet sich mit #18). Fix-Idee: das aufgelöste Anker-Set einmal cachen/durchreichen statt zweimal aufzulösen. | `src/Services/PlanningCascadeService.php` (`persistConceptCohesion`) | neu |
| 23 | 2026-08-15 | **Menü-Params (`menue_gaenge`/`menue_preis_*_pp`) werden getragen, aber noch von keinem Consumer gelesen.** Teil 1 von Etappe 2a whitelistet + persistiert sie als `generation_params`, aber weder Prompt noch `PlanningFrameService` konsumieren sie — reiner Contract/Träger (bewusst, Roadmap-Punkt 2 = Frame-Feed). Risiko: bleibt der Consumer aus, ist es ein stiller No-Op (der Nutzer stellt Achsen ein, ohne Wirkung). Nächster Chunk (2a-Punkt-2) MUSS `reglerParams`→Frame-Head (`target_price_pp`/`price_min_pp`/`price_max_pp`) + Slot-`target_count` verdrahten. | `src/Livewire/Planung/Index.php` (`reglerParams` `$menue`-Zweig) + `src/Services/PlanningFrameService.php` (`setHead`) | neu |
| 24 | 2026-08-15 | **`diaet_hart` (harte Ausschluss-Multi-Select) vs. künftige Diät-*Quote* (Anteil) im selben Concept-Tab kollidieren begrifflich.** Teil 2 von Etappe 2a bringt „Diät-Quoten (vegan/vegetarisch-Anteil)" — das ist semantisch das `diet_quota`-Rule (min/percent), NICHT der bestehende harte `diaet_hart`-Constraint. Beide am Concept-Tab nebeneinander ohne klare UI-Trennung würden den Nutzer verwirren („warum vegan-Pill UND vegan-Quote?"). Bei Teil 2 sauber labeln: harter Ausschluss ≠ Portfolio-Anteil. | `resources/views/livewire/planung/partials/leitplanken.blade.php` (`diaet_hart` + künftige Quote) | neu |
| 25 | 2026-08-15 | **Euro-Parser dreifach kopiert.** `menuePreisEur` ist strukturgleich zu `zielVkEur` (nur Feld + Bounds unterscheiden sich: 0,50–500 je Portion vs. 0,50–2000 je Person). Ein gemeinsamer `private eurWert(string $roh, float $min, float $max): ?float` würde beide (und künftige Preisfelder) speisen. Bewusst dupliziert gehalten, um `zielVkEur`-Verhalten in diesem Chunk nicht anzufassen. | `src/Livewire/Planung/Index.php` (`zielVkEur` + `menuePreisEur`) | neu |
| 26 | 2026-08-15 | **⚠ ÜBERSPRUNGEN (offene Entscheidung, nicht eigenmächtig entschieden):** Roadmap-Punkt Etappe 1 „**Auto-Zerlegung** — `RecipeDependencyWorkflowService:49/90`-Gating: soll auch Standalone-Gericht (ohne Kaskade) zerlegen?" ist der direkte Spiegel der offenen Entscheidung „Standalone-Zerlegung" (Roadmap §Offene Entscheidungen). Als erster offener Top-down-Punkt beim Lauf 2026-08-15 bewusst NICHT umgesetzt — Entscheidung nur Dominique. Ebenso „**Abnahme:** Blindtest mit 3 Briefs + Golden-Eval" = Real-LLM-Abnahme, provider-los in der Sandbox nicht bewertbar → Dominique auf demo. Deshalb sprang der Lauf auf Etappe 2a (erster buildbarer Chunk). | Roadmap §Offene Entscheidungen + `src/Services/RecipeDependencyWorkflowService.php:49` | offen (Entscheid Dominique) |
| 27 | 2026-08-15 | **Diät-Quoten vegan/vegetarisch ohne Cross-Konsistenz.** `menueQuote` parst beide `_pct`-Achsen unabhängig — ein Nutzer kann `menue_quote_vegan_pct=50` + `menue_quote_vegetarisch_pct=20` setzen, was fachlich widersprüchlich ist (jedes vegane Gericht IST vegetarisch → Vegetarisch-Anteil ≥ Vegan-Anteil). Der Consumer (Frame/Prompt, 2a-Punkt-2) bekäme ein inkonsistentes Paar. Bewusst NICHT hart validiert in diesem Chunk (weiche Vorgabe, kein Filter; harte Guards nur gegen mistgetippte Zahlen). Fix-Idee bei Consumer-Bau: soft-normalisieren (`vegetarisch = max(vegetarisch, vegan)`) oder Guard-Hinweis, statt das LLM widersprüchlich zu briefen. | `src/Livewire/Planung/Index.php` (`menueQuote`/`goKaskade` concept-Guard) | neu |
| 28 | 2026-08-15 | **Frame-Feed-Chunk (2a-Punkt-2) muss AUCH die Quoten verdrahten, nicht nur Gänge/Preis.** #23 nennt für den Consumer nur `target_price_pp`/`price_*_pp`/`target_count`. Mit Teil 2 kommen `menue_quote_vegan_pct`/`menue_quote_vegetarisch_pct` dazu — sie sind derselbe „getragen, kein Leser"-No-Op. Der Frame-Feed muss die Quoten als Portfolio-/Diät-Regel in den `PlanningFrame` (bzw. Concept-Fan-out-Prompt) übersetzen, sonst bleibt die neue Achse wirkungslos. Ergänzung zu #23. | `src/Services/PlanningFrameService.php` + `src/Livewire/Planung/Index.php` (`reglerParams` `$menue`-Zweig) | neu |
| 29 | 2026-08-15 | **`menue_balance` ist der dritte „getragen, kein Leser"-No-Op** (mit #23 Gänge/Preis + #28 Quoten). Portfolio-Balance (`b325ed0`) wird whitelisted + als `generation_param` persistiert, aber kein Prompt/Frame liest sie. Der Frame-Feed-Chunk (2a-Punkt-2) muss ALLE DREI Menü-Achsen-Familien verdrahten — Gänge/Preis → Frame-Head, Quoten → Diät-Regel, `menue_balance` → Zusammenstellungs-/Vielfalts-Direktive im Concept-Fan-out-Prompt. Sonst stellt der Nutzer eine Vielfalts-Achse ein, die spurlos verpufft. Sammel-Ergänzung zu #23/#28. | `src/Services/PlanningFrameService.php` + `src/Livewire/Planung/Index.php` (`reglerParams` `$menue`-Zweig, `menueBalance`) | neu |
| 30 | 2026-08-15 | **Blade-Kompilat-Lint (Routine-Pflichtschritt) in der Sandbox blockiert, sobald MySQL aus ist.** Dieser Lauf: `view:cache` bricht diesmal KOMPLETT ab (CRM-`QueryException` „Connection refused" Port 3306, bevor auch nur eine View kompiliert — anders als #3, wo es durchlief) → 0 Kompilate in `storage/framework/views/`. Der Fallback „einzelne Blade isoliert über `BladeCompiler::compileString` linten" scheitert am `<x-foodalchemist::modal-section>`-Component-Tag (`ComponentTagCompiler` braucht den gebooteten Container → `View\Factory not instantiable`). Effektiver Ersatz dieses Laufs: (a) mechanische Direktiven-Balance-Prüfung (`@if`/`@endif`/`@foreach`/`@endforeach`-Counts + Zeilen-Trace) + (b) der Livewire-**Render**-Test (`assertSeeHtml('data-menue-balance')`) rendert das Partial durch die volle View-Pipeline und würde einen Blade-ParseError als roten Test zeigen. Für Planung-Blades ist die Render-Assertion damit die verlässlichere Absicherung als der Kompilat-Lint. Empfehlung für künftige Läufe: bei Blade-Änderung IMMER einen `assertSeeHtml`-Marker mitliefern (fängt geklebte Direktiven ohne MySQL). | Routine-Guardrail (Pest-Gate/Blade-Lint) + `platform-crm` (Fremd-Boot-DB-Zugriff → Martin) | neu |
| 31 | 2026-08-15 | **Frame-Preis-Head-Override greift NUR im Brief-Reuse-Pfad, nicht am geplanten Konzept (`generiereAusGeruest`).** `generiereAusBrief` wendet den Concept-Tab-Preis-Korridor jetzt an; aber `generiereAusGeruest(frame)` (Assembler auf einem SCHON bestehenden Frame — z.B. der künftige „KI-Kopf"/geprüftes Draft-Konzept, Etappe 2b `existing_concept_id`) bekommt die `menueAchsen` nicht durchgereicht. Sobald 2b den geplanten Pfad baut, muss die Preis-/Achsen-Verdrahtung dort GENAUSO greifen, sonst ist die Leitplanke nur im Schnell-Pfad wirksam. Heute unkritisch (2b ungebaut), aber die Achsen-Feed-Logik gehört an EINE gemeinsame Stelle (Frame-Head-Sync), nicht nur in `generiereAusBrief`. | `src/Services/ConceptGeneratorService.php` (`generiereAusGeruest` vs. `generiereAusBrief`) | neu |
| 32 | 2026-08-15 | **`menue_preis_ziel_pp` ohne Spanne-Konsistenzprüfung gegen min/max.** `menuePreisHead` mappt die drei Preis-Achsen unabhängig auf den Frame-Kopf — ein Nutzer kann `ziel=35` mit `min=40`/`max=30` setzen (fachlich widersprüchlich: Ziel außerhalb der Spanne, min>max). `setHead` speichert sie roh; kein Guard sortiert/normalisiert. Bewusst NICHT in diesem Teilschritt validiert (weiche Vorgabe; harte Guards nur gegen mistgetippte Zahlen, s. `menuePreisEur`). Fix-Idee beim Consumer-Ausbau: soft-normalisieren (`min ≤ ziel ≤ max`) oder Guard-Hinweis am `goKaskade`-Concept-Gate, analog der geplanten Quoten-Konsistenz #27. | `src/Services/ConceptGeneratorService.php` (`menuePreisHead`) + `src/Livewire/Planung/Index.php` (`goKaskade` concept-Guard) | neu |
| 33 | 2026-08-15 | **Preis-Head-Override umgeht `promptKontext`-Sichtbarkeit erst nach `replaceStructure`-Refresh.** Der zweite `setHead` (Preis-Override) läuft VOR `replaceStructure` + `fuelleBestehendesKonzept($frame->refresh())`; das ist korrekt (der Assembler sieht den überschriebenen Kopf). Aber es gibt keinen Test, der beweist, dass der überschriebene Zielpreis auch im R6-`promptKontext`-Block („Preisarchitektur: Zielpreis …") landet — nur die DB-Spalten sind assertet. Bei einem künftigen Refactor, das die Reihenfolge dreht (Struktur zuerst, Kopf danach), könnte der Slot-Assembler still den KI-Preis statt des Nutzer-Preises sehen. Fix-Idee: eine Assertion auf `promptKontext($frame)` mit dem überschriebenen Betrag. | `tests/Feature/ConceptGeneratorTest.php` (Menü-Leitplanken-Test) + `PlanningFrameService::promptKontext` | neu |
| 34 | 2026-08-15 | **`menue_gaenge`-Cap ist nur ein Deckel, kein Exakt-N.** `menueGaengeCap` schneidet überzählige `gang`-Slots ab, kann aber KEINE fehlenden Gänge nachlegen (das Gerüst-System erfindet nichts). Ein Nutzer, der 5 Gänge will, aber dessen Brief die KI nur 3 gang-Slots entlockt, bekommt still 3 — die Leitplanke wirkt asymmetrisch. Echte Exakt-N-Vorgabe ohne Erfindung geht nur prompt-seitig: `menue_gaenge` als harte Direktive in den `concept.brief_geruest`-Kontext (`$kontext['menue_gaenge']`) geben, damit die KI genau N Gänge mit sinnvollen Labels erzeugt; der Deckel bleibt als deterministischer Ceiling-Guard. Bewusst als Folge-Chunk gehalten (Teil 2 = deterministischer Frame-Deckel, mirror Teil 1). | `src/Services/ConceptGeneratorService.php` (`menueGaengeCap` + `generiereAusBrief`-Kontext) + `config/foodalchemist.php` (`concept.brief_geruest`) | neu |
| 35 | 2026-08-15 | **Label „Anzahl Gänge / Positionen" — die `/ Positionen`-Hälfte ist bei Buffets ein stiller No-Op.** `menueGaengeCap` deckelt bewusst NUR `gang`-Slots; ein Concept, das die KI als Buffet (`station`/`kapitel`-Slots) baut, ignoriert `menue_gaenge` komplett. Für Menüs korrekt (Gänge = gang-Slots), aber das UI-Label verspricht auch „Positionen" — der Nutzer eines Buffet-Briefs stellt eine Zahl ein, die verpufft. Entscheidung nötig (Dominique): entweder (a) `menue_gaenge` cappt bei Buffet auch die Stationen, oder (b) das Label wird auf „Anzahl Gänge (nur Menü)" verengt, oder (c) eine zweite Achse „Positionen je Station". Nicht eigenmächtig entschieden. | `resources/views/livewire/planung/partials/leitplanken.blade.php:89` (Label) + `src/Services/ConceptGeneratorService.php` (`menueGaengeCap`) | neu (Entscheid Dominique) |
| 36 | 2026-08-15 | **`menueGaengeCap` greift wie der Preis-Head nur im Brief-Reuse-Pfad (`generiereAusBrief`), nicht am geplanten Konzept (`generiereAusGeruest`).** Direkte Erweiterung von #31: sobald Etappe 2b den „KI-Kopf"/`existing_concept_id`-Pfad baut, muss die Gänge-Deckelung (und die künftigen Quoten-/Balance-Consumer) an DERSELBEN gemeinsamen Stelle greifen wie der Preis-Head — sonst wirken alle Menü-Achsen nur im Schnell-Pfad. Bestätigt die #31-Empfehlung, die Achsen-Feed-Logik an EINE Frame-Sync-Stelle zu ziehen (Head + Slots + Regeln gemeinsam), statt sie über `generiereAusBrief` zu verstreuen. | `src/Services/ConceptGeneratorService.php` (`generiereAusGeruest` vs. `generiereAusBrief`) | neu |
| 37 | 2026-08-15 | **Diät-Quoten-Feed greift wie Preis/Gänge NUR im Brief-Pfad (`generiereAusBrief`), nicht am geplanten Konzept (`generiereAusGeruest`).** `menueDiaetQuotenMerge` (Teil 3a) läuft in `generiereAusBrief` vor `replaceStructure`; `generiereAusGeruest(frame)` (Assembler auf bestehendem Frame — künftiger „KI-Kopf"/`existing_concept_id`, Etappe 2b) bekommt die `menueAchsen` nicht. Direkte Erweiterung von #31/#36: sobald 2b den geplanten Pfad baut, müssen Preis-Head + Gänge-Cap + Quoten-Merge an DERSELBEN gemeinsamen Frame-Sync-Stelle greifen, sonst wirken alle Menü-Achsen nur im Schnell-Pfad. | `src/Services/ConceptGeneratorService.php` (`generiereAusGeruest` vs. `generiereAusBrief`) | neu |
| 38 | 2026-08-15 | **Frame-Diät-Quote wird vom deterministischen Assembler gelesen, aber NICHT dem Concept-Erfindungs-Prompt gebrieft.** Positiv-Befund: anders als #23/#28/#29 (getragen, kein Leser) hat die frame-Ebene-`diet_quota`-Regel echte Konsumenten — `MenuAssemblyService::menuQuoten` (Bestand-Auswahl) + `CoverageService:610` (Prozent-Scoring). ABER: der LLM-Fan-out `IdeenService::kiDivergenzConcept` (erfindet Gerichte für LEERE Slots) bekommt die Quote nicht als Direktive — auf dem Erfindungs-Pfad wird die Diät-Quote also nur nachträglich gescored/geampelt, nicht vorab gesteuert (parallel zu #17 menue_kohaesion-Unsichtbarkeit). Fix-Idee: die frame-Quoten (wie den `menue_kohaesion`-Block) als Zusammenstellungs-Direktive in den Erfindungs-Prompt geben. | `src/Services/IdeenService.php` (`kiDivergenzConcept`) + `src/Services/MenuAssemblyService.php` (`menuQuoten`) | neu |
| 39 | 2026-08-15 | **Balance-Direktive greift NUR im Brief-Pfad (`generiereAusBrief`), nicht am geplanten Konzept (`generiereAusGeruest`)** — direkte Erweiterung der Familie #31/#36/#37. Anders als Preis/Gänge/Quote ist `menue_balance` aber ein PROMPT-Feed (`$kontext['menue_zusammenstellung']`), kein Frame-Sync — die in #31 empfohlene „eine gemeinsame Frame-Sync-Stelle" reicht also nicht: der 2b-Pfad (`existing_concept_id`, Assembler auf bestehendem Frame) hat gar keinen frischen KI-Gerüst-Prompt mehr, in den die Direktive fließen könnte. Bei 2b-Bau klären, wie die Zusammenstellungs-Direktive den geplanten Pfad erreicht (z.B. an den Fan-out-Erfindungs-Prompt `kiDivergenzConcept` statt an `concept.brief_geruest`). Schließt zugleich die Balance-Leg von #29 (menue_balance hat jetzt einen Leser). | `src/Services/ConceptGeneratorService.php` (`generiereAusGeruest` vs. `generiereAusBrief`) | neu |
| 40 | 2026-08-15 | **`menue_zusammenstellung`-Direktive ist im Kontext-Inspektor unsichtbar** (parallel zu #17 menue_kohaesion + #38 Diät-Quote). Der Block wird direkt in den `concept.brief_geruest`-Kontext gehängt und als JSON an die Task serialisiert (`AiGatewayService:150`), läuft aber NICHT durch `RecipeGenerationContextService::build` — der Generator-Ergebnis-Modal „Verwendetes Wissen" zeigt dem Menschen also nicht, DASS/WORAUF eine Balance-Direktive briefte. Gehört zur finalen UI-Etappe: die getragenen Menü-Direktiven (Balance, Kohäsion, Quote) als Audit-Feld sichtbar machen. | `src/Services/ConceptGeneratorService.php` (`menueBalanceDirektive`) + Kontext-Inspektor | neu |
| 41 | 2026-08-15 | **„Menü-Leitplanke" = Präfix `menue_` an VIER Stellen implizit dupliziert.** Nach der Trennung (2b6762a) hängt die Definition, welche Keys Menü- statt Rezept-Leitplanken sind, an vier unabhängigen Präfix-Prüfungen: `reglerParams` setzt sie im Concept-Zweig, `ALLOWED_GENERATION_PARAMS` listet sie, `dispatchConceptStep` filtert `str_starts_with('menue_')` in `$menueAchsen`, und jetzt strippt `sessionGenerationParams` denselben Präfix. Eine künftige Menü-Achse OHNE `menue_`-Präfix würde still in den Rezept-Fan-out durchsickern (der Strip griffe nicht) — genau der Bug, den dieser Chunk schloss, in neuer Gestalt. Fix-Idee: eine einzige Quelle (`const MENUE_LEITPLANKE_PREFIX='menue_'` oder ein Key-Set) an allen vier Stellen referenzieren, statt den Präfix je Stelle hart zu tippen. | `src/Livewire/Planung/Index.php` (`reglerParams`) + `src/Models/FoodAlchemistPlanningSession.php` (`ALLOWED_GENERATION_PARAMS`) + `src/Services/PlanningCascadeService.php` (`dispatchConceptStep`/`sessionGenerationParams`) | neu |
| 43 | 2026-08-15 | **`concept.plan` routet Wissen, aber `propose()` konsumiert Routings NICHT selbst — der nächste Chunk (`planAusBrief`) MUSS den Block aktiv bauen.** Die Routings `concept.plan:{cross_cutting:always, domain:discovery, concept:always}` sind geseedet (Migration `2026_07_27_000001` + `seedRoutings`), aber `AiGatewayService::propose` lädt nur *gebundenes* Wissen (`knowledge_bindings`) automatisch — die *gerouteten* Kategorien fließen nur, wenn der Aufrufer sie über `KnowledgeContextService` auflöst und als `options['knowledge']` mitgibt (so machen es `RecipeGeneratorService`/`ConceptGeneratorService::generiereAusBrief`). Baut `planAusBrief` den Knowledge-Block nicht, läuft der Kreativ-Kopf trotz vorhandener Routing-Zeilen wissens-blind. Kein Bug dieses Chunks (reiner Prompt-Contract), aber Pflicht für den nächsten. | `src/Services/ConceptGeneratorService.php` (`planAusBrief`, folgt) + `src/Services/Ai/AiGatewayService.php:140` (`propose`) | **geschlossen (`316e51a`)** — `fuelleCanvasAusPlan` löst die Routings über `contextFor` auf + reicht `food_dna_concept_id` durch |
| 44 | 2026-08-15 | **`geschmackswelten`-Output-Shape (`[{claim, description}]`) muss `planAusBrief` auf die Canvas-`repeatable`-Entries mappen — kein Contract erzwingt die Feldnamen.** Der Prompt fordert je Welt `{claim, description}`; das Canvas-Template `concept.geschmackswelten` deklariert `sub => {claim, description}` (passt heute). Aber nichts nagelt beide aneinander: ein späterer Canvas-Refactor, der die Sub-Keys umbenennt (z.B. `titel`/`text`), bräche die Materialisierung still, ohne dass `ConceptPlanPromptTest` (prüft nur die Feld-*Keys* auf oberster werte-Ebene, nicht die geschmackswelten-Sub-Keys) rot würde. Fix-Idee bei `planAusBrief`-Bau: die Sub-Keys aus `CanvasService::TEMPLATES['concept']['felder'][geschmackswelten]['sub']` ableiten statt hart zu tippen, ODER einen Test, der Prompt-Sub-Keys ↔ Template-Sub-Keys gegenprüft. | `config/foodalchemist.php` (`concept.plan` task) + `src/Services/CanvasService.php` (`TEMPLATES.concept.geschmackswelten.sub`) | neu |
| 45 | 2026-08-15 | **Kreativ-Kopf `planAusBrief` erbt die Concept-Menü-Leitplanken NICHT.** `generiereAusBrief` konsumiert `menueAchsen` (Preis-Korridor/Gänge-Cap/Diät-Quoten/Balance-Direktive, Etappe 2a); der neue `planAusBrief` reicht sie NICHT an die Gerüst-Reuse durch — `geruestAusBriefFuerOwner` hat gar keinen `menueAchsen`-Parameter. Folge: sobald der „KI-Kopf"-Knopf (nächster Chunk) auf `planAusBrief` verdrahtet, ignoriert der geplante Pfad die Concept-Tab-Achsen, die der Schnell-Pfad (`generiereAusBrief`) ehrt — genau die Asymmetrie, die #31/#36/#37/#39 für `generiereAusGeruest` beschreiben, in dritter Gestalt. Fix-Idee: `menueAchsen` als Parameter an `planAusBrief` + `geruestAusBriefFuerOwner` (bzw. die in #31 empfohlene EINE Frame-Sync-Stelle) durchreichen, damit alle drei Einstiegspfade dieselben Menü-Leitplanken anwenden. | `src/Services/ConceptGeneratorService.php` (`planAusBrief` → `geruestAusBriefFuerOwner`) | neu |
| 46 | 2026-08-15 | **Orphan-Cleanup bei Gerüst-Fehler in `planAusBrief` verlässt sich auf ungeprüften Frame-Cascade-Delete.** Wirft `geruestAusBriefFuerOwner` NACH `frameFor`/`setHead` (Edge „KI-Gerüst enthielt keine gültigen Slots"), existiert ein Rumpf-Frame; `planAusBrief` räumt ihn per `$this->frames->find('concept', id)?->delete()` ab. Ob `FoodAlchemistPlanningFrame::delete()` seine `frame_slots`/`frame_rules` mit-löscht (DB-Cascade oder Model-`deleting`-Hook), ist NICHT verifiziert — sonst bleiben verwaiste Slot-/Rule-Zeilen zurück. Der häufige Fehlerfall (KI liefert gar kein Gerüst) wirft VOR `frameFor` → kein Frame, kein Orphan; der Rumpf-Fall ist der Rand. Fix-Idee: Cascade der Frame-Löschung testen/absichern (oder `replaceStructure`+Head atomar in eine Transaktion, die bei leerem Slot-Ergebnis zurückrollt). | `src/Services/ConceptGeneratorService.php` (`planAusBrief`-catch) + `src/Models/FoodAlchemistPlanningFrame.php` (`delete`-Cascade) | neu |
| 42 | 2026-08-15 | **menue_*-Strip nur am Concept-Gericht-Pfad getestet, nicht am Speiseplan-Zell-Pfad.** Beide Fan-out-Erben (`materialisiereConceptGericht` + `materialisiereSpeiseplanZelle`) teilen `sessionGenerationParams`, aber die 2 neuen Tests fahren nur den Concept-Gericht-Pfad. Ein Refactor, der `materialisiereSpeiseplanZelle` den Erben umgehen ließe (z.B. eigene Param-Zusammenstellung), würde die Trennung dort still brechen, ohne einen roten Test. Fix-Idee: entweder ein zweiter Test auf `materialisiereSpeiseplanZelle` (braucht Plan+Zelle-Fixture) ODER ein direkter Reflection-Unit-Test auf `sessionGenerationParams([...menue_...]) === [...ohne menue_...]` — nagelt den Erben für beide Konsumenten fest. | `tests/Feature/PlanningCascadeTest.php` + `src/Services/PlanningCascadeService.php` (`sessionGenerationParams`) | neu |
| 47 | 2026-08-15 | **KI-Kopf verlinkt das erzeugte Draft-Concept NICHT mit der Planungs-Session (Lineage-Lücke, blockiert den nächsten Chunk).** `kiKopf` ruft `planAusBrief` und öffnet den Conceptor, aber die neue Concept-ID wird NIRGENDS an der Session persistiert (weder Spalte noch transiente Prop). Der nächste Roadmap-Bullet (Etappe 2b: `existing_concept_id` in `starteKaskade` → Concept-Step referenziert das geprüfte Draft) braucht aber genau diesen Rückgriff: nach „Plan im Conceptor prüfen" muss der Go wissen, WELCHES Draft-Concept freigegeben wurde. Heute geht die ID nach dem `dispatch` verloren (der Nutzer landet im Conceptor-Modal, das Planung-Cockpit kennt die ID nicht mehr). Fix-Idee beim `existing_concept_id`-Chunk: `kiKopf` legt `planConceptId` als transiente Prop ab (Roadmap Zeile 150 nennt sie schon) ODER persistiert `session.planned_concept_id`, damit der „Go aus geprüftem Plan" die Referenz findet. | `src/Livewire/Planung/Index.php` (`kiKopf` → fehlende `planConceptId`) + `src/Services/PlanningCascadeService.php` (`starteKaskade` `existing_concept_id`) | **geschlossen (`c528bf9`)** — `kiKopf` merkt `planConceptId` (transiente Prop), `goKaskade` liest sie als `existing_concept_id` |
| 48 | 2026-08-15 | **Provider-Stub dritte Kopie — `bindKiKopfStub` (PlanungLeitstelleTest) ≈ `bindPlanStub` (ConceptGeneratorTest) ≈ die inline-Stubs in ConceptGeneratorTest.** Beide handrollen einen vollen `LLMProviderContract` mit derselben `Concept-Canvas`→concept.plan / sonst→concept.brief_geruest-Verzweigung. Bewusst self-contained gehalten (die Routine fährt PlanungLeitstelleTest gezielt, ohne ConceptGeneratorTest zu laden → `bindPlanStub` wäre undefined), aber ein gemeinsamer Test-Fake gehört laut Memory [FA-Suite parallel] in `tests/Support/` (geteilte Helfer MÜSSEN dorthin). Fix-Idee: ein `tests/Support/FakeConceptPlanProvider.php` (Slots + optionale Plan-Werte im Konstruktor), von beiden Test-Files gebunden — eine Quelle für den concept.plan/brief_geruest-Stub. | `tests/Feature/PlanungLeitstelleTest.php` (`bindKiKopfStub`) + `tests/Feature/ConceptGeneratorTest.php` (`bindPlanStub`) + `tests/Support/` | neu |
| 49 | 2026-08-15 | **`kiKopf` hat keinen Re-Entrancy-Guard — Doppelklick erzeugt zwei Draft-Concepts.** Der Blade-Knopf ist `@disabled($laeuft)` + `wire:loading.attr=disabled`, aber die Methode selbst prüft `$laeuft` NICHT (anders als `goKaskade`, das ohnehin über den Lauf-Status geht). `$laeuft` bezieht sich zudem auf eine KASKADE, nicht auf einen laufenden KI-Kopf — während `planAusBrief` (synchroner LLM-Call, mehrere Sekunden) läuft, ist `$laeuft=false`. Ein hektischer Doppel-Request (Livewire debounct Buttons nicht hart) legt zwei `concept_plan_ui`-Drafts an, von denen nur der zweite geöffnet wird → verwaister erster Draft. Konservativ (kein Datenverlust, nur Müll-Draft), aber unsauber. Fix-Idee: ein transientes `$kiKopfLaeuft`-Flag ODER die geplante `planConceptId`-Prop (#47) als Idempotenz-Schlüssel (schon ein Draft für dieses Briefing? → wiederverwenden statt neu). | `src/Livewire/Planung/Index.php` (`kiKopf`) | neu |
| 50 | 2026-08-15 | **`existing_concept_id`-Pfad reicht die Concept-Menü-Achsen (`menue_*`) NICHT durch — sie sind am geprüften Draft aber schon materialisiert.** `dispatchConceptStep` filtert die `menue_*`-Params in den `GenerateConceptJob` (Preis-Head/Gänge-Cap/Quoten/Balance); `referenziereConceptStep` tut das nicht — der Fan-out erbt sie nur indirekt über den bereits von `planAusBrief`/`geruestAusBriefFuerOwner` gebauten Frame + die Session-`generation_params`. Heute konsistent, SOLANGE `planAusBrief` denselben Frame baut, den der Schnell-Pfad hätte (Backlog #45 flaggt, dass `planAusBrief` die Achsen noch NICHT erbt) — sobald #45 gefixt ist, muss verifiziert werden, dass der Referenz-Pfad die Achsen nicht ein ZWEITES Mal (widersprüchlich) anlegt. Klärung beim Caller-Wiring (nächster Bullet). | `src/Services/PlanningCascadeService.php` (`referenziereConceptStep` vs. `dispatchConceptStep`) | neu |
| 51 | 2026-08-15 | **Eager-`existing_concept_id`: kurzes `review`→`running`-Flackern.** `referenziereConceptStep` ruft im eager-Zweig `markStepDone` (→ recompute setzt Run kurz auf `review`, weil der `done`-Concept-Step noch kein Gericht-Kind hat) und setzt DANACH `run.status=running` + dispatcht `FanoutConceptJob`. Der final geschriebene Status ist korrekt (`running`), aber ein Poller, der exakt zwischen den beiden Writes liest, sähe `review`. Fensterbreite = zwei Statements im selben Web-Request (praktisch nie getroffen). Der Normal-Pfad (GenerateConceptJob eager) hat das Problem nicht, weil er `fanoutConceptInvention` SYNCHRON vor `markStepDone` laufen lässt (Kinder existieren am recompute). Fix-Idee falls je störend: Run vor `markStepDone` auf `running` pinnen (wie `starteFolgestufe` es tut) statt danach. | `src/Services/PlanningCascadeService.php` (`referenziereConceptStep` eager-Zweig) | neu |
| 52 | 2026-08-15 | **`planConceptId` ist NICHT an die aktive Session gebunden — Session-Wechsel nach KI-Kopf leakt den Plan.** `kiKopf` setzt die transiente Prop; `oeffne`/`waehle` (Session-Wechsel) setzen sie NICHT zurück. Wer für Session A einen KI-Kopf-Plan ausarbeitet (planConceptId=A-Concept), dann in Session B wechselt und dort den Concept-Go drückt, referenziert das FREMDE (aber team-eigene → Guard passiert) Plan-Concept von A in B's Kaskade. Kein Datenverlust, aber falsche Lineage/Zusammenstellung. Fix-Idee: `planConceptId` in `oeffne`/`waehle` auf `null` setzen (Session-Kontext-Reset), ODER die Prop gegen die Session validieren (Concept.planning_session_id == aktive Session) bevor `existing_concept_id` gereicht wird. | `src/Livewire/Planung/Index.php` (`oeffne`/`waehle`/`goKaskade`) | neu |
| 53 | 2026-08-15 | **Geplanter Pfad überlebt keinen Page-Reload — der geprüfte Plan verpufft still.** `planConceptId` ist bewusst transient (kein Migration, Roadmap Zeile 152). Folge: nach „KI-Kopf → im Conceptor prüfen" genügt ein Reload/Re-Mount der Planung-Fläche (Livewire-Neustart), und der Go fällt wortlos auf den Schnell-Pfad zurück (frische Generierung), obwohl das geprüfte Draft-Concept in der DB weiterlebt. Der Nutzer bemerkt den Pfad-Wechsel nicht (kein „Plan-vorbereitet"-Badge nach Reload, weil die Prop weg ist). Fix-Idee (falls die Persistenz-Entscheidung kippt): `session.planned_concept_id` als leichte Spalte ODER beim Öffnen das jüngste `concept_plan_*`-Draft der Session als planConceptId rehydrieren, damit der geplante Pfad reload-fest ist. Bewusst nicht in diesem Chunk (Roadmap pinnt „transiente Prop"). | `src/Livewire/Planung/Index.php` (`planConceptId`, `mount`/`oeffne`) | neu (Entscheid Dominique — Persistenz-Politik) |
| 54 | 2026-08-15 | **OFFENE ENTSCHEIDUNG — Auto-Zerlegung Standalone-Gericht (Roadmap Et.1 Zeile 116 + „Offene Entscheidungen").** `RecipeDependencyWorkflowService:49/90`-Gating: soll auch die Nicht-Kaskaden-Gericht-Erstellung (VkGenerator, Standalone) automatisch in Sub-Basisrezepte zerlegen, oder bleibt Zerlegung Kaskaden-exklusiv? Die Routine hat diesen Roadmap-Punkt bewusst NICHT umgesetzt — er steht wörtlich im „Offene Entscheidungen"-Block (Scope-Schutz: Entscheidung nur Dominique). Sobald entschieden, wird er ein normaler Umsetzungs-Chunk. | `src/Services/RecipeDependencyWorkflowService.php:49/90` | neu (Entscheid Dominique) |
| 55 | 2026-08-15 | **`skizzeAlsGericht`: der Tab-Wechsel feuert auch bei fehlgeschlagener Übernahme.** Der Knopf koppelt `wire:click="skizzeAlsGericht(id)"` (server) mit Alpine `@click="tab='gericht'"` (client) — Alpine schaltet den Tab SOFORT clientseitig, unabhängig vom Server-Ergebnis. Ist die Skizze weg/verworfen (Action setzt `fehler` + lässt den Tab leer), landet der Mensch trotzdem auf dem leeren Gericht-Tab, während oben der Fehler steht — leicht widersprüchlich. Konservativ (kein Datenverlust), aber Politur. Fix-Idee: den Tab-Wechsel an einen Erfolgs-Event der Action hängen (`$this->dispatch('skizze-uebernommen')` → Alpine `x-on:skizze-uebernommen.window="tab='gericht'"`) statt clientseitig blind. | `resources/views/livewire/planung/index.blade.php` (Skizzen-Board, „als Gericht"-Knopf) + `src/Livewire/Planung/Index.php` (`skizzeAlsGericht`) | neu |
| 56 | 2026-08-15 | **`skizzeAlsGericht` überschreibt einen bereits getippten Gericht-Brief ohne Warnung.** Hat der Mensch im Gericht-Tab schon Titel/Brief eingegeben und klickt dann auf einer Skizze „als Gericht", ersetzt das Prefill `eingabe.gericht.titel`/`brief` still. Selten und leicht rückgängig (er tippt neu), aber ein „überschreibt vorhandene Eingabe"-Hinweis oder ein Merge (anhängen statt ersetzen, wenn schon Text da ist) wäre freundlicher. Fix-Idee: bei nicht-leerem Ziel-Feld nur ergänzen ODER eine `meldung`-Warnung setzen. | `src/Livewire/Planung/Index.php` (`skizzeAlsGericht`) | neu |
| 57 | 2026-08-15 | **`skizzeGerichtId` wird bei Session-Wechsel NICHT zurückgesetzt — leakt die Skizzen-Herkunft in eine fremde Session** (exakte Parallele zu #52 für `planConceptId`). `skizzeAlsGericht` setzt die transiente Prop; `oeffne`/`waehle` (Session-Wechsel) lassen sie stehen. Wer in Session A eine Skizze in den Gericht-Tab überträgt, dann zu Session B wechselt und dort den Gericht-Go drückt, stempelt den B-Lauf mit der A-Skizze — der Fail-soft-Guard (`visibleToTeam` + nicht `verworfen`) passiert, weil die A-Skizze team-sichtbar ist. Kein Datenverlust, aber falsche Lineage (die Karte in A zeigt dann einen Lauf, der zu B gehört). Fix-Idee: `skizzeGerichtId` (wie `planConceptId`, #52) in `oeffne`/`waehle` auf `null` setzen ODER die Skizze gegen die aktive Session validieren (`planning_session_id == aktive Session`) bevor `origin_dish_idea_id` gestempelt wird. | `src/Livewire/Planung/Index.php` (`oeffne`/`waehle`/`goKaskade`) | neu |
| 58 | 2026-08-15 | **Teil 2b (Status auf die Karte) muss die `origin_dish_idea_id`↔Lauf-Beziehung defensiv lesen — es gibt keinen DB-FK und die Skizze ist soft-deletable.** Der Lauf trägt einen losen `origin_dish_idea_id` (kein `constrained()`, bewusst SQLite-safe); `FoodAlchemistDishIdea` nutzt `SoftDeletes`. Beim Surfacing gilt: (a) eine soft-gelöschte Skizze liefert über `originDishIdea()` `null` (default-Scope filtert `deleted_at`) → die Karte existiert dann gar nicht mehr, kein Problem; ABER (b) mehrere Läufe können DIESELBE Skizze referenzieren (der Mensch drückt „als Gericht" → Go, korrigiert, drückt erneut) — die Karte muss dann den JÜNGSTEN Lauf zeigen (`latest('id')` je `origin_dish_idea_id`), nicht den ersten, sonst friert der Status auf einem alten Lauf ein. Fix-Idee beim 2b-Bau: pro Skizze den neuesten `FoodAlchemistCascadeRun where origin_dish_idea_id=… latest` auflösen (ggf. als `hasMany`/`latestOfMany`-Relation am DishIdea-Model), Status defensiv auf „kein Lauf" fallen lassen. | `src/Models/FoodAlchemistDishIdea.php` (fehlende `cascadeRuns`/`latestCascadeRun`-Relation) + `src/Livewire/Planung/Index.php` (`render` Skizzen-Feed) | neu |
| 59 | 2026-08-15 | **Run-Status→deutsches Label ist jetzt eine dritte, eigene Wahrheit im Blade (`$laufStatus`).** Teil 2b mappt `running/review/done/failed` → `läuft/prüfen/fertig/fehlgeschlagen` in einer eigenen Closure im `@php`-Kopf der Planung-Landing. Der Worker-Cockpit (`partials/ergebnis.blade.php`) und der Fortschritts-Header (`index.blade.php:326-330`) benennen dieselben Run-Zustände nochmals unabhängig (dort textuell „Läuft …"/„Freigegeben …"). Driftet eine Benennung (z.B. `review` → „Sammel-Review" statt „prüfen"), sagen Karte und Cockpit Verschiedenes über denselben Lauf. Fix-Idee: ein gemeinsamer Status-Label-Helper (analog `Ui::maps()` / `$statusLabel`) für die 4 Run-STATUSES, von Karte UND Cockpit referenziert — eine Quelle. | `resources/views/livewire/planung/index.blade.php` (`$laufStatus`) + `resources/views/livewire/planung/partials/ergebnis.blade.php` + `src/Models/FoodAlchemistCascadeRun.php` (`STATUSES`) | neu |
| 61 | 2026-08-15 | **Batch-Skizzen (Teil 3) hat KEINEN Dedup-Guard — erneutes „Alle als Gerichte" startet je Skizze einen ZWEITEN Lauf.** `skizzenBatchAlsGerichte` batcht alle bearbeitbaren Skizzen unabhängig davon, ob eine davon schon einen laufenden/prüfbaren Gericht-Go trägt (`origin_dish_idea_id`). Zwei Klicks (oder Klick nach einem Einzel-`skizzeAlsGericht`→Go) legen doppelte Läufe je Skizze an; die Karte zeigt via Teil 2b nur den jüngsten, die älteren laufen aber trotzdem (Worker-Last + Kosten). Fix-Idee: vor dem Start je Skizze prüfen, ob schon ein `FoodAlchemistCascadeRun` mit `origin_dish_idea_id=idee AND status IN (running,review)` existiert → überspringen + im Summary sagen („N gestartet, M übersprungen (laufen schon)"). | `src/Livewire/Planung/Index.php` (`skizzenBatchAlsGerichte`) | neu |
| 62 | 2026-08-15 | **ENTSCHEID Dominique — Batch (Teil 3) ist quell-AGNOSTISCH, der Roadmap-Punkt heißt aber „KI-Divergenz-Skizzen".** `skizzenBatchAlsGerichte` batcht ALLE bearbeitbaren Session-Skizzen (auch manuell via `ideeHinzu` angelegte, `created_via='ui'`), nicht nur die KI-erzeugten (`source_meta.quelle='ki_divergenz'`). Bewusst so gewählt (ein Batch-Launch ist quell-egal nützlich; eine willkürliche Beschränkung auf KI-Skizzen würde manuelle Skizzen ausschließen), aber es weicht vom Roadmap-Wortlaut ab. Klärung: soll der Knopf (a) quell-agnostisch bleiben (Status quo) oder (b) nur `ki_divergenz`-Skizzen batchen (dann manuelle weiter einzeln via „als Gericht")? Nicht eigenmächtig verengt. | `src/Livewire/Planung/Index.php` (`skizzenBatchAlsGerichte`) | neu (Entscheid Dominique) |
| 63 | 2026-08-15 | **Batch-Karten-Badges refreshen nicht live (bewusster Kein-Cockpit-Hijack).** `skizzenBatchAlsGerichte` setzt `laeuft` NICHT (kein Einzel-Cockpit-Poll für einen Batch) — die Teil-2b-Status-Badges der N Läufe aktualisieren sich daher erst bei der nächsten Livewire-Interaktion, nicht sekündlich wie beim Einzel-Go (`wire:poll` an `$laeuft`). Für den ersten Teilschritt akzeptiert (der Stand ist korrekt, nur nicht auto-aktualisiert). Fix gehört zu Teil 3b: ein leichter Board-Poll, solange irgendein Skizzen-Lauf `running`/`queued` ist (z.B. `wire:poll` an einer abgeleiteten `$skizzenLaufenNoch`-Prop), unabhängig vom Einzel-Cockpit-`$laeuft`. | `src/Livewire/Planung/Index.php` (`skizzenBatchAlsGerichte` / `render` Skizzen-Feed) + `resources/views/livewire/planung/index.blade.php` (Divergenz-Board) | erledigt (`a130d22`: `skizzenLaufAktiv`-Flag in render() + bare `wire:poll.2500ms` `data-skizzen-poll` im Board, nur bei `running`, kein `$laeuft`-Hijack — Roadmap Et.4 Teil 3b-b) |
| 64 | 2026-08-15 | **`skizzenAuswahl` (Teil 3b-a) wird bei Session-Wechsel NICHT zurückgesetzt — exakte Parallele zu #52 (`planConceptId`) / #57 (`skizzeGerichtId`).** Die angehakten Skizzen-IDs überleben `oeffne`/`waehle`. Wer in Session A Skizzen anhakt und zu Session B wechselt, trägt in B eine stale-Auswahl mit; der Batch filtert sie via `whereIn(id)` gegen B's session-scoped Kandidaten → in aller Regel `Keine der angehakten Skizzen ist startbar` (fail-safe, KEIN Fremd-Lauf, weil session-scoped), aber verwirrend: die Checkboxen wirken visuell leer (render zeigt B's Skizzen), die Prop trägt aber A's IDs. Fix-Idee: `skizzenAuswahl=[]` in `oeffne`/`waehle` — idealerweise mit #52/#57 gemeinsam als EIN Session-Kontext-Reset (planConceptId + skizzeGerichtId + skizzenAuswahl zusammen nullen). | `src/Livewire/Planung/Index.php` (`oeffne`/`waehle` vs. `skizzenAuswahl`) | neu |
| 60 | 2026-08-15 | **Skizzen-Status-Badge ist reine Anzeige — kein Sprung zum Lauf.** Teil 2b zeigt „▸ prüfen/fertig" auf der Karte, aber der Mensch muss selbst in den Worker-Tab wechseln und den Lauf suchen, um zu handeln (freigeben/ansehen). Die Map trägt schon die `run_id` — ein Klick auf das Badge könnte den Worker-Tab öffnen und exakt diesen Lauf laden (`$this->laufId = run_id` + Alpine `tab='worker'`). Gehört zur finalen UI-Etappe (Details-Panel/Lineage „Im Editor öffnen", Roadmap Zeile 243), aber die Datengrundlage (run_id je Skizze) liegt jetzt schon bereit. Fix-Idee: Badge als `<button wire:click="oeffneLauf(run_id)">` mit Tab-Wechsel. | `resources/views/livewire/planung/index.blade.php` (`$skizzeLaufBadge`) + `src/Livewire/Planung/Index.php` (`render` Skizzen-Feed / neue `oeffneLauf`) | neu |
| 65 | 2026-08-15 | **Routine-Reibung: der Blade-Lint-Schritt (`view:cache` → `php -l` auf dem Kompilat) läuft in der Sandbox nicht durch.** `php artisan view:cache` bricht am pre-existing crm-Modul-Boot-Fehler ab (`Facade::__callStatic("hasTable")` in `CrmServiceProvider::boot`, kein DB) — kompiliert die Planung-Views also gar nicht erst. Der Fallback (bare `BladeCompiler->compileString` ohne App-Boot) scheitert seinerseits an den `<x-…>`-Komponenten-Tags (`ComponentTagCompiler` braucht den Container). Folge: die Routine kann den in der SKILL geforderten „Kompilat linten"-Schritt aktuell NICHT eigenständig fahren und verlässt sich auf das Voll-Render der Livewire-Feature-Tests (die die View durch den App-Compiler ziehen → eine geklebte `@if` würde dort als ParseError auffallen). Fix-Idee: (a) ein schlanker Artisan-Command im FA-Modul, der NUR die FA-Views via `Blade::compile` durch den echten Container kompiliert (crm-Provider umgehen), oder (b) den crm-`hasTable`-Boot-Guard defensiv machen (`Schema::hasTable` in try/catch), damit `view:cache` in der Sandbox wieder durchläuft. | `sandbox` (`view:cache` vs. `CrmServiceProvider::boot`) + SKILL-Schritt „Blade-Kompilat linten" | neu |
| 66 | 2026-08-15 | **`briefVorlage` überschreibt einen bereits getippten Brief bedingungslos.** Nur der Titel ist geschützt (nur gefüllt, wenn leer) — der `brief`-Text wird immer durch die Vorlage ersetzt. Bewusst so (eine Vorlage IST der Starter, „raus aus der Blank Page"), aber wer schon ein paar Sätze getippt hat und dann eine Vorlage antippt, verliert sie ohne Rückfrage. Fix-Idee: bei nicht-leerem `brief` entweder anhängen statt ersetzen, oder eine Bestätigung/Undo (Alpine-`confirm` oder ein „letzte Vorlage rückgängig"). Für Teil 1 akzeptiert. | `src/Livewire/Planung/Index.php` (`briefVorlage`) | neu |
| 67 | 2026-08-15 | **Concept-Tab bekommt keine Brief-Vorlagen (eigener Blade-Block, nicht `erstellen-tab`).** Der Pill-Block hängt in `partials/erstellen-tab.blade.php`, das nur Basisrezept + Gericht rendert; der Concept-Tab hat ein eigenes inline-Briefing (`index.blade.php` ~Z.331–374) und trägt daher trotz sinnvoller Menü-Vorlagen (z.B. „3-Gänge-Business-Dinner", „Empfangs-Menü") keinen Vorlagen-Block. Gehört zu „Brief-Vorlagen Teil 2": eigene `scopes:['concept']`-Einträge in `BRIEF_VORLAGEN` + der Pill-Block (oder ein geteiltes Partial) auch im Concept-Briefing. `vorlagenFuer('concept')` ist schon parametrisch — es fehlt nur der Blade-Einhängepunkt + die Concept-Vorlagen. | `resources/views/livewire/planung/index.blade.php` (Concept-Tab-Briefing) + `src/Livewire/Planung/Index.php` (`BRIEF_VORLAGEN`) | neu |
| 69 | 2026-08-15 | **Session-Kontext-Reset-Lücke trifft jetzt auch das Trend-Briefing-Prefill (E4/175 Teil 1) — dieselbe Familie wie #52/#57/#64.** `seedBriefingAusTrendSession` befüllt ein Tab-Briefing nur, wenn es LEER ist (empty-only, kein Clobber). `eingabe[scope]` wird aber bei `oeffne`/`waehle` NICHT zurückgesetzt. Folge: wer erst Session A öffnet (Tab-Brief tippt), dann eine Trend-Session B via Liste öffnet, bekommt B's Trend-Brief NICHT in den bereits gefüllten Tab (der Guard unterdrückt ihn) — die Karte/Analyse zeigt den Trend, das Go-Briefing trägt aber noch A's Text. Der Primärpfad (Trendradar-Carry-in via `?open=1` = frischer `mount`) ist sauber (Tabs leer → seedet). Fix-Idee: den in #52/#57/#64 empfohlenen EINEN Session-Kontext-Reset (`planConceptId`+`skizzeGerichtId`+`skizzenAuswahl` nullen) um `eingabe`/`regler` erweitern — beim echten Session-Wechsel die Tab-Eingaben frisch aus der (Trend-)Session aufbauen statt die Prop-Reste zu tragen. | `src/Livewire/Planung/Index.php` (`oeffne`/`waehle` vs. `eingabe` + `seedBriefingAusTrendSession`) | neu |
| 70 | 2026-08-15 | **Trend-Prefill (Teil 1) füllt ALLE drei Tabs mit demselben scope-agnostischen Brief — bei Teil 2 (substantiver Brief) neu bewerten.** Der generische `ausTrend`-Einzeiler ist bewusst scope-neutral (»Konzept/Gericht/Basisrezept entwickeln«), daher ist das Fluten aller drei Tabs heute konsistent. Sobald Teil 2 den Brief aus dem strukturierten Signal baut (Kategorie/Klasse + Body-Lead), kann ein Menü-Trend im Concept-Tab anders gerahmt gehören als im Gericht-/Basisrezept-Tab (ein Menü-Brief ≠ ein Einzel-Teller-Brief). Klärung beim Teil-2-Bau: entweder EIN scope-neutraler Brief für alle (Status quo) ODER per-Scope-Rahmung (Concept = Menü-Zusammenstellung, Gericht = ein Teller). Kein Bug — Design-Frage für Teil 2. | `src/Services/PlanningSessionService.php` (`ausTrend`) + `src/Livewire/Planung/Index.php` (`seedBriefingAusTrendSession`) | Teil 3 (`3c6dbcd`) hat die per-Scope-Rahmung auf NOMEN-Ebene gelöst (Lead je Tab »ein Basisrezept/Gericht/Konzept entwickeln«, `briefFuerScope`) — die tiefere SEMANTISCHE Rahmung (Concept = Menü-Zusammenstellungs-Brief mit Gang-/Portfolio-Sprache ≠ Gericht = Einzel-Teller-Brief) ist bewusst NICHT gemacht (nur der Lead-Nomen wird getauscht, Einordnung/Kernaussage bleiben identisch). Bleibt offen als Design-Entscheid: reicht die Nomen-Schärfung, oder braucht der Concept-Tab einen menü-gerahmten Trend-Brief? |
| 71 | 2026-08-16 | **`ausTrend` liest das Trend-Meta OHNE `status`-Filter — eine `tentative` (noch nicht Review-freigegebene) Cluster-Zuordnung fließt in den Brief.** `foodalchemist_trend_meta.status` kennt `tentative`|`approved` (Review-Queue, Muster LA-First); mein `briefAusTrend` zieht `category`/`trend_class` unabhängig vom Status → ein noch nicht kuratiertes KI-Cluster-Label landet als »Einordnung: X › Y« im Brief. Konservativ vertretbar (ein tentatives Label ist meist besser als keins, und der Mensch prüft den Brief vor dem Go), aber es unterläuft die tentative→approved-Kuratierung. Klärung: nur `status='approved'`-Meta in die Einordnung ODER tentative bewusst zulassen (Status quo). Analog könnte auch `d.active`/Doc-Sichtbarkeit vs. Meta-Aktualität driften. | `src/Services/PlanningSessionService.php` (`ausTrend` Meta-Query + `trendEinordnung`) | neu (Entscheid Dominique — tentative zulassen?) |
| 72 | 2026-08-16 | **`bodyLead` nimmt blind den ersten Prosa-Absatz als Kernaussage — bei Boilerplate-Köpfen (Datum/Quelle/Meta-Zeile) trifft es die falsche Zeile.** `bodyLead` überspringt Frontmatter + `#`-Überschriften + Listen-/Zitat-Marker, nimmt dann den ERSTEN verbleibenden Absatz. Reale Trend-Importe (`07_WISSEN`) starten aber teils mit einer Metazeile (z.B. »Quelle: …«, Datum, Autoren-Zeile) ODER einer generischen »Zusammenfassung«-Intro, bevor die eigentliche Kernaussage kommt — deterministisch nicht sauber unterscheidbar. Fix-Idee: falls das Frontmatter ein strukturiertes Feld trägt (`kernaussage`/`summary`/`lead`), dieses BEVORZUGEN und erst dann auf den Body-Absatz fallen; der Reader (`KnowledgeContextService::frontmatterOf`) liegt schon vor. Erst auf demo mit echten Trend-Docs bewertbar (Sandbox-Fixtures sind sauber). | `src/Services/PlanningSessionService.php` (`bodyLead` / `briefAusTrend` — Frontmatter-Feld vor Body-Lead) | neu |
| 68 | 2026-08-15 | **Mitigation zu #65 (Blade-Kompilat-Lint OHNE `view:cache`) in diesem Lauf bestätigt.** Die Livewire-Feature-Tests ziehen die Planung-Views durch den echten App-Compiler; das Kompilat landet danach unter `storage/framework/views/*.php`. Ein `grep -rl "<data-marker>" storage/framework/views/*.php` findet die kompilierte Partial gezielt, `php -l` lintet sie (geklebte `@if`/`@endif` = ParseError sichtbar). D.h. der SKILL-Schritt „Kompilat linten" ist in der Sandbox doch fahrbar — nach einem grünen Pest-Lauf, der die geänderte View rendert, per Marker-Grep + `php -l`. Kein Ersatz für den sauberen Fix aus #65 (a/b), aber ein verlässlicher Routine-Workaround. | Routine-Schritt „Blade-Kompilat linten" (ergänzt #65) | erledigt (Workaround verifiziert `52793f4`) |
| 73 | 2026-08-16 | **Ebenen-spezifischer Trend-Lead (Teil 3) lebt NUR im Livewire-Seed — der headless/MCP-Pfad bleibt agnostisch (Lockstep-Lücke).** `briefFuerScope` schärft den Lead beim Übertragen `session.brief` → `eingabe[scope].brief` in `seedBriefingAusTrendSession` (UI). Der gespeicherte Session-Brief (`ausTrend`) bleibt bewusst agnostisch. Solange die Trend→Generierung ausschließlich über die Planung-UI läuft, ist das korrekt. Sobald ein headless-/MCP-Pfad (Roadmap Et.9: MCP-Tools für die Kaskade) einen Trend direkt in eine Kaskade schickt, würde er den agnostischen Session-Brief nehmen (»Konzept/Gericht/Basisrezept entwickeln«) — die Ebenen-Schärfung fehlt dort. Fix-Idee bei MCP-Bau: `PlanningSessionService::briefFuerScope` auch im headless Start-Pfad anwenden (der Scope steht dort am Kaskaden-Start fest) — eine Quelle, schon pure/statisch dafür ausgelegt. Memory [MCP im Lockstep]. | `src/Services/PlanningSessionService.php` (`briefFuerScope`) + künftige MCP-Kaskaden-Tools (Et.9) | neu (Lockstep bei MCP-Bau) |
| 74 | 2026-08-16 | **§-Syntax-Regeln stehen jetzt in VIER Prompt-Task-Strings dupliziert — bei Teil 2 (Service) gegen eine Quelle erden.** Mit dem Titel-Vorschlag-Contract (`5d3ccda`) gibt es `recipe.name_putzen`+`recipe.titel_vorschlag` (beide §1 «Typ: Bezeichnung») und `vk.name_putzen`+`vk.titel_vorschlag` (beide §4.4 Pipe «HG-Code: …»). Die Syntax-Regel ist in allen vier als Prosa im `task` wiederholt — driftet eine (z.B. §1.2-Typ-Vokabular ändert sich), sagen putzen und vorschlag Verschiedenes. Der Rust-Normalizer `ai_normalize_recipe_name` injiziert die erlaubten Typen schon aus `lookup_recipe_typ` (Single Source, Regelwerk §1.2). Fix-Idee beim Teil-2-Bau: der `titelVorschlag`-Service injiziert dieselbe Typ-/HG-Liste in den Prompt-Kontext (wie der Generator es tut), statt sich auf die Prosa im Task zu verlassen. | `config/foodalchemist.php` (recipe/vk .name_putzen + .titel_vorschlag) + `src/Services/TitelVorschlagService.php` (`titelVorschlag`) | **offen nach Teil 2** — der Service (`ae54996`) reicht NUR `['brief' => $brief]` durch, injiziert die §1.2-Typ-/HG-Liste NICHT (minimal + contract-treu gehalten; die §-Syntax kommt allein aus der Task-Prosa). Grounding = Nachzug bei Teil 3 (UI) oder eigener Chunk. |
| 75 | 2026-08-16 | **`vk.titel_vorschlag` verlangt einen §4.4-HG-Code — den kann das LLM aus einem freien Brief kaum zuverlässig raten.** Der Gericht-Titel-Contract fordert die Pipe-Syntax «<HG-Code>: Hauptkomponente | …». Der HG-Code ist ein kontrollierter Warengruppen-Code (Speisen-Hauptgruppen-Taxonomie), kein frei formulierbarer Text — ohne den Katalog im Prompt wird das Modell einen plausiblen, aber evtl. ungültigen Code erfinden (widerspricht »keine Erfindung«). Bei Teil 2 (Service) muss der HG-Code-Katalog (analog zur Generator-Kategorie-Injektion `recipe.category`/`vk.speisen_klasse`) in den Kontext, ODER der Titel-Vorschlag lässt den HG-Code weg und die HG-Zuordnung läuft wie beim Vollrezept über einen eigenen Klassifikator-Pass. Design-Frage für Teil 2. | `config/foodalchemist.php` (`vk.titel_vorschlag`) + `src/Services/TitelVorschlagService.php` | **offen nach Teil 2 (Design-Entscheid Dominique)** — der Service (`ae54996`) hat den HG-Code-Katalog bewusst NICHT injiziert (die Routine entscheidet die Design-Frage »Katalog injizieren vs. HG-Code weglassen + eigener Klassifikator-Pass« nicht eigenmächtig). Provider-los ist die §4.4-Konformität ohnehin nicht bewertbar → gehört in die Real-Abnahme + den Grounding-Nachzug. |
| 76 | 2026-08-16 | **Vierter handgerollter `LLMProviderContract`-Stub (`bindTitelStub`) — der Provider-Fake driftet in jedem Test-File neu.** Nach `bindKiKopfStub`/`bindPlanStub`/den inline-Stubs (#48) rollt `TitelVorschlagServiceTest` erneut einen vollen `LLMProviderContract` von Hand, nur um EIN `werte`-Feld (`name`) bzw. einen Wurf zurückzugeben — vier fast identische ~30-Zeilen-Klassen mit denselben 5 Boilerplate-Methoden (`getName/streamChat/getAvailableModels/getDefaultModel/isAvailable`). #48 empfahl einen geteilten Concept-Plan-Fake; die Wiederkehr zeigt den allgemeineren Bedarf: EIN `tests/Support/FakeProposalProvider` (Konstruktor nimmt ein prompt-marker→`werte`-Mapping ODER wirft) würde alle vier ablösen (Memory [FA-Suite parallel]: geteilte Helfer MÜSSEN nach `tests/Support/`). Bewusst wieder self-contained gelassen (die Routine fährt gezielt einzelne Files), aber die Boilerplate-Fläche wächst mit jedem KI-Chunk. | `tests/Feature/TitelVorschlagServiceTest.php` (`bindTitelStub`) + `tests/Feature/PlanungLeitstelleTest.php` (`bindKiKopfStub`) + `tests/Support/` | neu |
| 77 | 2026-08-16 | **Der neue empty-only `titelVorschlagen` (Teil 3) vererbt die Session-Kontext-Reset-Lücke der Familie #52/#57/#64/#69 in einer neuen, verwirrenderen Form.** `eingabe[scope].titel` wird bei `oeffne`/`waehle` nicht zurückgesetzt (#69). Folge fürs Titel-Feature: wer in Session A einen Titel tippt/vorschlagen lässt, dann zu Session B (anderer Brief) wechselt, sieht in B den STALE A-Titel — und der „Titel vorschlagen"-Knopf tut dort scheinbar NICHTS (empty-only-Guard überspringt still mit „Es steht bereits ein Titel"), obwohl B einen frischen Vorschlag bräuchte. Kein Datenverlust, aber der Nutzer erlebt den Knopf als kaputt. Verstärkt den Bedarf nach EINEM Session-Kontext-Reset (`planConceptId`+`skizzeGerichtId`+`skizzenAuswahl`+`eingabe`+`regler` gemeinsam beim Session-Wechsel nullen/rehydrieren). | `src/Livewire/Planung/Index.php` (`oeffne`/`waehle` vs. `eingabe[scope].titel` + `titelVorschlagen`) | neu (Teil der Sammel-Reset-Familie #52/#57/#64/#69) |
| 79 | 2026-08-16 | **Etappe-5-Roadmap war stale — die drei Voll-Kaskaden-Trigger (foodbook/speisekarte/speiseplan) sind längst gebaut, nur nicht abgehakt; Speisekarte (Zeile 194) + Speiseplan (Zeile 195) sind die IDENTISCHE Lücke wie der jetzt geschlossene Foodbook-Punkt.** Der Service-Pfad (`starteVollkaskade`/`starteSpeiseplanVollkaskade`) + die Livewire-Trigger (`Foodbooks\Index`/`Speisekarte\Index`/`Speiseplan\Editor` je `vollKaskadeStarten`) existieren seit P3/P4/P5 (`b494cab` u.a.) und sind am **Service-Layer** in `PlanningCascadeTest` gepinnt. Was fehlt(e), ist die **Livewire-Trigger-Deckung** (Session-Anlage, Redirect, Fehlerpfad). Foodbook (Zeile 193) hat sie jetzt (`0395241`, +2 Tests in `FoodbookLeitstelleTest`). **Verifiziert:** `SpeisekarteLeitstelleTest` = 0 `vollKaskade`-Refs, KEIN Speise*-Test deckt `vollKaskadeStarten`. → Die nächsten zwei Routine-Chunks (Et.5 Speisekarte + Speiseplan) folgen 1:1 dem Foodbook-Muster: 2 Livewire-Trigger-Tests je Modul (Happy-Path Session+Run+Job+Redirect · Fehlerpfad ohne Frame/Slots bzw. ohne Linien) → dann abhaken. Kein neuer Produktiv-Code nötig, nur Deckung + Häkchen. | `tests/Feature/SpeisekarteLeitstelleTest.php` + `tests/Feature/Speiseplan*Test.php` (fehlende `vollKaskadeStarten`-Deckung) | neu (nächste Et.5-Chunks) |
| 80 | 2026-08-16 | **Die drei `vollKaskadeStarten`-Trigger (foodbook/speisekarte/speiseplan) sind fast-identischer Copy-Code mit je modul-lokalem Fehler-Feld — ohne geteilten Helfer driftet der Trigger.** Alle drei: `team()`-Guard → `PlanningSessionService::create(['created_via'=>'<modul>_vollkaskade'])` → `starteKaskade('vollkaskade', ['owner_type'=>'<modul>', 'owner_id'=>…])` → `redirect()->route('foodalchemist.planung.index', [session, open=1])`, im `catch` je eine eigene Fehler-Prop (`kaskadeMeldung` bei Foodbook, andere Namen bei Speisekarte/Speiseplan). Driftet ein Detail (z.B. der Redirect-Param oder die `created_via`-Konvention), müssen drei Stellen mit — leicht zu übersehen. Fix-Idee: ein geteiltes `StartetVollkaskade`-Trait/Service-Helfer (`vollkaskadeAusOwner(team, ownerType, ownerId): ?RedirectResponse`), von allen drei Livewire-Hosts referenziert — eine Quelle, drei dünne Aufrufe. Bewusst NICHT in diesem Chunk (Scope: nur Foodbook-Deckung). | `src/Livewire/Foodbooks/Index.php` + `src/Livewire/Speisekarte/Index.php` + `src/Livewire/Speiseplan/Editor.php` (`vollKaskadeStarten`) | neu |
| 78 | 2026-08-16 | **Der #68-Blade-Lint-Workaround (Marker-`grep` auf `storage/framework/views/*.php`) ist NICHT verlässlich — dieser Lauf produzierte NULL kompilierte Views.** #68 markierte den Workaround als „erledigt": nach grünem Pest-Lauf das Kompilat unter `storage/framework/views` greppen + `php -l`. Dieser Lauf zeigt die Grenze: `php artisan view:cache` bricht am crm-`hasTable`-Boot-Fehler (#65) ab, BEVOR die Planung-Views geschrieben sind → `ls storage/framework/views/*.php` = 0 Dateien (das Verzeichnis blieb leer). Der Fallback (standalone `BladeCompiler->compileString`) scheitert an den `<x-foodalchemist::…>`-Komponenten-Tags (`ComponentTagCompiler` braucht den Container → `BindingResolutionException`). Einzige verlässliche Blade-Verifikation blieb der **Livewire-Feature-Test-Render** (`Livewire::test(...)->call('oeffne')` zieht die Partial durch den echten App-Compiler; eine geklebte `@if` = ParseError im Test). Der eigentliche Fix aus #65 (a: schlanker FA-`Blade::compile`-Command / b: crm-`hasTable` in try/catch) bleibt der saubere Weg — #68 als „erledigt" ist zu optimistisch. | Routine-Schritt „Blade-Kompilat linten" (relativiert #68, verweist auf #65) | neu |
| 81 | 2026-08-16 | **KORREKTUR zu #79: Der Speiseplan-Chunk (Et.5, letzter offener Ausgabe-Trigger) ist NICHT 1:1 der Foodbook/Speisekarte-Weg — der nächste Lauf darf `FoodbookLeitstelleTest` nicht blind kopieren.** Verifiziert: (a) Host ist `Speiseplan\Editor::vollKaskadeStarten` (`src/Livewire/Speiseplan/Editor.php:328`), NICHT `Speiseplan\Index`. (b) Service-Pfad ist der Sonderzweig `starteSpeiseplanVollkaskade` (`PlanningCascadeService:366`, delegiert ab `:71`), der über die **Zeit-Achse** (`cycle_weeks × Mo–Fr × Mittag × lines`) fächert — Steps `kind='gericht'` je LEERER Zelle (belegte Zellen werden übersprungen), NICHT `kind='concept'` je Slot. (c) Gedeckelt `SPEISEPLAN_MAX_ZELLEN=30` (Überhang → `$offen`-Zähler). (d) Fixture braucht daher einen Speiseplan mit `lines` + `start_date` + `cycle_weeks` (+ ggf. `entries` für den „Zelle belegt"-Skip), keine Rubriken/Kapitel. → Happy-Path-Test prüft: `created_via`/`scope=vollkaskade`/`source_owner_type=speiseplan`-Run + n `gericht`-Steps (= Zahl leerer Zellen, gegen Cap) + gepushter Job je Zelle (Job-Klasse am Editor/Service verifizieren — vermutlich `GenerateRecipeJob`, NICHT `GenerateConceptJob`) + Redirect; Fehlerpfad: Plan ohne Linien → ehrliche Meldung, kein Lauf/Redirect/Job. Speisekarte-Chunk (#79) ist mit `8e7a938` erledigt. | `tests/Feature/Speiseplan*Test.php` (fehlende `vollKaskadeStarten`-Deckung) + `src/Livewire/Speiseplan/Editor.php:328` + `src/Services/PlanningCascadeService.php:366` | neu (nächster Et.5-Chunk) |
| 54 | 2026-08-16 | **Speiseplan-Voll-Kaskade lässt bei „keine Menü-Linien" eine leere Review-Session zurück.** `Speiseplan\Editor::vollKaskadeStarten` legt die Session (`created_via=speiseplan_vollkaskade`) VOR dem `starteKaskade`-Call an; wirft der Service auf einen Plan ohne Linien, wird die Meldung sauber gefangen — aber die frisch angelegte Session bleibt als Waise in der DB. Foodbook/Speisekarte prüfen die Leere-Bedingung im Livewire-Handler VOR der Session-Anlage und vermeiden das. Kein Datenschaden, aber inkonsistent + Session-Müll. Fix-Idee: analog Speisekarte einen Vorab-Guard (`$plan->lines()->exists()`) in `vollKaskadeStarten` ziehen, bevor die Session entsteht. | `src/Livewire/Speiseplan/Editor.php` (`vollKaskadeStarten`, ~341-348) | neu |
| 55 | 2026-08-16 | **Routine-Doku: Pest-Gate-Pfade müssen die vendor-Symlink-Pfade sein, nicht `tests/Feature/…`.** Die Sandbox discovert FA-Tests über die Testsuite `FoodAlchemist` = `vendor/martin3r/platform-foodalchemist/tests` (phpunit.xml:14-16). `./vendor/bin/pest tests/Feature/X.php` schlägt mit „Test file not found" fehl — die Pfade müssen `vendor/martin3r/platform-foodalchemist/tests/Feature/X.php` lauten. Der SKILL.md-Ablauf sagt nur „`./vendor/bin/pest <pfade-zu-relevanten-Test-Dateien>`"; eine Zeile zum Vendor-Pfad-Präfix spart künftigen Läufen einen Fehlschlag. | `.claude/scheduled-tasks/planung-roadmap-umsetzung/SKILL.md` (Sandbox / Pest-Gate) | neu |
| 82 | 2026-08-16 | **`run->staged` ist für Ausgabe-Voll-Kaskaden rein deskriptiv — KEIN Code-Pfad KONSUMIERT das Flag dort.** Mit `e8cbf99` tragen die drei Ausgabe-Kaskaden jetzt korrekt+explizit `staged=false`, aber die Eager-Semantik ist STRUKTURELL: `starteVollkaskade`/`starteSpeiseplanVollkaskade` dispatchen alle Slot-/Zell-Jobs sofort in der Schleife und laufen NICHT durch das `staged`-Gate von `starteKaskade` (das nur die Cockpit-Scopes rezept/gericht/concept steuert). Das Flag beschreibt also die Absicht + die Freigabe-Semantik im Cockpit, treibt aber für Ausgabe-Runs keine Verzweigung. Konsequenz für Et.8 (Idempotenz/Resume): wer einen abgebrochenen Ausgabe-Lauf gestuft fortsetzen oder per `run->staged` gaten will, findet für diese Runs keinen Konsumenten — die Resume-Logik müsste den Eager-Loop selbst wiederaufnahmefähig machen (offene Steps `geplant`/`queued`/`running` erneut dispatchen), nicht am Flag hängen. | `src/Services/PlanningCascadeService.php` (`starteVollkaskade`/`starteSpeiseplanVollkaskade` vs. `staged`-Gate) | neu (relevant für Et.8 Idempotenz/Resume) |
| 83 | 2026-08-16 | **Der Et.5-Leitstellen-Testgürtel (Cascade + Leitstelle + 3 Ausgabe-Leitstellen) läuft ~135 s / 146 Tests — über dem 120-s-Bash-Timeout, zwingt zum Backgrounding.** Dieser Lauf: `PlanningCascadeTest`+`PlanungLeitstelleTest`+`Foodbook/Speisekarte/Speiseplan-LeitstelleTest` = 135310 ms. Der SKILL-Pest-Schritt sagt „gezielt, nicht die Voll-Suite", aber schon die für einen Service-Change nötige Deckung (Cascade + Leitstelle + Ausgabe-Trigger) reißt das 120-s-Fenster. Fix-Idee für die Routine: solche Multi-File-Gates von vornherein mit `run_in_background: true` starten (Notification bei Exit) statt in den Timeout zu laufen, ODER `--parallel`/`-p` prüfen (Memory [FA-Suite parallel] `./fa_test.sh`). Kein Bug — Routine-Ergonomie. | `.claude/scheduled-tasks/planung-roadmap-umsetzung/SKILL.md` (Pest-Gate: langsame Multi-File-Gruppen backgrounden) | neu |
| 84 | 2026-08-16 | **Et.1-Rest »Auto-Zerlegung« (Roadmap Z.116) ist eine Offene Entscheidung — NICHT von der Routine umgesetzt.** `RecipeDependencyWorkflowService:49/90`-Gating: soll auch die Nicht-Kaskaden-Gericht-Erstellung (VkGenerator) automatisch in Sub-Basisrezepte zerlegen, oder bleibt Zerlegung Kaskaden-exklusiv? Steht wörtlich im „Offene Entscheidungen"-Block (Z.236 »Standalone-Zerlegung«) → per Routine-Guardrail geflaggt, nicht entschieden. Entscheidung: Dominique. | `docs/PLANUNG/38_Roadmap_Planung_Leitstelle.md:116` + `src/Services/RecipeDependencyWorkflowService.php:49` | neu (blockiert — Offene Entscheidung) |
| 85 | 2026-08-16 | **Et.1-Rest »Abnahme: Blindtest 3 Briefs + Golden-Eval« (Roadmap Z.117) ist provider-los nicht bewertbar.** Die Generierungs-QUALITÄT (0 Fremdkörper, korrekte Sub-Rezept-Bildung) lässt sich nur mit echtem LLM auf demo prüfen — in der Sandbox gibt es keinen Provider. Real-LLM-Abnahme durch Dominique. | `docs/PLANUNG/38_Roadmap_Planung_Leitstelle.md:117` | neu (blockiert — Real-LLM) |
| 86 | 2026-08-16 | **Et.6-Kalkulation rechnet je Render neu — im laufenden Worker-Poll potenziell teuer.** Der neue `kalkulation`-Block in `Index::render` ruft `SalesRecipeService::cockpit()` pro Rezept-/Gericht-Step bei JEDEM Render. Der Worker-Tab pollt bei aktivem Lauf per `wire:poll.1500ms="pruefeLauf"` → bei großem Fan-out (viele Steps) sind das N cockpit()-Aufrufe alle 1,5 s (jeder mit `effektiverVk`/`marge`/`weAmpel` + Team-Settings-Lookup). Heute ok (Steps klein, cockpit rein rechnerisch), aber ein Kandidat für Memoisierung ODER Gating auf terminale Zustände (running-Steps haben ohnehin kein Rezept). Fix-Idee: cockpit-Map nur für Steps ≠ running/queued bauen (ist bereits der Fall) UND ggf. pro Poll nur die geänderten Steps neu rechnen. | `src/Livewire/Planung/Index.php` (`render` `$kalkulation`-Block) | neu |
| 87 | 2026-08-16 | **Cockpit-Marge-Kachel ohne Team-Ziel-Wareneinsatz zeigt Marge/WE stets grau (Ampel=unbekannt).** `SalesRecipeService::cockpit` liefert `ampel='unbekannt'`, solange `TeamSettingsService::zielWareneinsatzPct($team)` null ist — die neue Kachel färbt dann Marge %/WE % neutral grau statt grün/gelb/rot. Fachlich korrekt (nie geraten), aber ein Team ohne gepflegte Ziel-Quote sieht nie eine Food-Cost-Ampel im Cockpit. Fix-Idee (bei Margen-Gate, Roadmap Z.205): fehlende Ziel-Quote im Cockpit sichtbar anmahnen (»Ziel-Wareneinsatz im Team hinterlegen«) statt still grau. | `resources/views/livewire/planung/partials/step-zeile.blade.php` (Ampel-Ton) + `src/Services/MargeService.php:87` (`weAmpel`) | neu |
| 88 | 2026-08-16 | **Präzedenz-Frage Kopf-Korridor vs. Slot-Anker beim Concept→Gericht-Ziel-VK (Et.6 Z.205, `db1cdf0`).** `conceptGerichtZielVk` bevorzugt den per-Gericht-`price_anchor` des Frame-Slots vor dem gleichverteilten Kopf-`target_price_pp`. Die Et.2-Achse (`30d95ea`) macht aber den KOPF-Korridor autoritativ (User-Eingabe im Concept-Tab) — die Slot-Anker bleiben, was die KI beim Gerüst emittierte. Setzt der User den Kopf-Korridor und trägt der Frame gleichzeitig (stale) KI-Slot-Anker, gewinnen die Anker → der explizite Korridor wird beim Ziel-VK ignoriert. Bewusst so gewählt (Anker = verlustfrei/keine Verteilungs-Annahme), aber es ist ein **Policy-Entscheid**, kein Bug: Soll der autoritative Kopf-Korridor stale Slot-Anker beim Ziel-VK schlagen? → gehört in den »Offene Entscheidungen«-Block (analog Zerlegungstiefe). Entscheidung: Dominique. | `src/Services/PlanningCascadeService.php` (`conceptGerichtZielVk`) + Roadmap »Offene Entscheidungen« | neu |
| 89 | 2026-08-16 | **Concept-Slot → Frame-Slot-Zuordnung nur über Label-Match (fragil bei Namensdubletten).** `conceptGerichtZielVk` matcht den Frame-Slot per `concept_slot.role === frame_slot.label`, weil der leere Concept-Slot (`materialisiereLeereSlots`) keinen `frame_slot_id` speichert. Zwei Frame-Slots mit gleichem Label (z.B. zwei »Hauptgang«-Slots mit unterschiedlichen Ankern) → `first()` nimmt den ersten, evtl. den falschen Anker. Heute unkritisch (Labels i.d.R. eindeutig, Fallback greift), aber ein persistierter `frame_slot_id` am Concept-Slot (Schema-Arbeit in `materialisiereLeereSlots`/`ConceptService::addSlot`) würde die Zuordnung verlustfrei machen — auch für spätere per-Slot-Regel-Propagation. | `src/Services/ConceptGeneratorService.php:433` (`materialisiereLeereSlots`) + `src/Services/PlanningCascadeService.php` (`conceptGerichtZielVk`) | neu |
| 90 | 2026-08-16 | **Speiseplan-Zell-Fan-out bekommt (noch) keinen Ziel-VK aus dem Frame.** Der Et.6-Ziel-VK (`db1cdf0`) hängt nur am Concept-Fan-out (`materialisiereConceptGericht`); `materialisiereSpeiseplanZelle` (der zweite Rezept-Fan-out) erbt keinen `ziel_vk_eur` aus einem Frame. Bewusst außen vor (anderer Owner, Zeitachse statt Menü-Gänge, kein per-Person-Korridor-Konzept), aber falls Speiseplan-Zellen künftig gegen ein Kosten-Ziel gemessen werden sollen, wäre ein analoger Ableiter (aus Menü-Linie/Budget je Portion) der Anschluss. | `src/Services/PlanningCascadeService.php:482` (`materialisiereSpeiseplanZelle`) | neu |
| 91 | 2026-08-16 | **Margen-Gate (`2cb8f1b`) warnt nur bei MANUELLEM Unter-Klassen-VK — der ampel-`rot`-Fall (Food-Cost weit über Team-Ziel) triggert bewusst NICHT.** `stepsUnterAufschlagsklasse` prüft `vk.source=manuell && sales_net < vorschlag`. Ein Auto-VK (source=class) trifft die Klasse per Definition exakt und wird nie geflaggt — auch wenn der Ist-Wareneinsatz die Team-Ziel-Quote sprengt (dann steht die Cockpit-Ampel auf `rot`, aber die Freigabe schweigt). Zwei getrennte Schwellen: Klassen-Vorschlag (Rezept-markup_class) vs. Team-Ziel-Wareneinsatz. Bewusst am Klassen-Vorschlag (breit verfügbar, s. Backlog #87), aber ob die Freigabe zusätzlich bei `ampel=rot` warnen soll, ist ein Produkt-/Policy-Entscheid (zweite Warn-Achse) — nicht von der Routine entschieden. | `src/Livewire/Planung/Index.php` (`stepsUnterAufschlagsklasse`) + `src/Services/SalesRecipeService.php:488` (`ampel`) | neu |
| 92 | 2026-08-16 | **Margen-Gate berechnet `cockpit()` je freizugebendem Rezept ein ZWEITES Mal — parallel zur `render()`-`$kalkulation`-Map (Backlog #86).** `gibStufeFrei` ruft `stepsUnterAufschlagsklasse`, das für die `done`-Steps derselben Stufe `SalesRecipeService::cockpit` erneut ausführt; unmittelbar danach baut `render()` dieselben `cockpit()`-Aufrufe für die `$kalkulation`-Kachel-Map (die u.a. `vk.source`/den Vorschlag NICHT durchreicht, sonst wäre kein zweiter Call nötig). Heute vernachlässigbar (kleine Stufen, cockpit rein rechnerisch), aber wenn #86 memoisiert wird, sollte die Map `vk.source`+`vorschlag.sales_net` mitführen und das Gate aus ihr lesen statt neu zu rechnen — eine cockpit()-Wahrheit je Render statt zweier. | `src/Livewire/Planung/Index.php` (`stepsUnterAufschlagsklasse` vs. `render` `$kalkulation`) | neu |
| 93 | 2026-08-16 | **Der `EK teil-unbepreist`-Marker (`2056a27`) erbt die qs-Noise der kanonischen Zähler — »Salz, qs« zählt als unbepreiste Lücke.** Ich habe die Regel bewusst 1:1 aus `DataQualityService` gespiegelt (`ek_total_eur != null && ek_n_ingredients_priced < ..._total`, eine Wahrheit). ABER `RecipeRecomputeService::kosten()` zählt qs-Zeilen (`unit=qs`, mengeG=0) in `ek_n_ingredients_total`, ohne sie je zu bepreisen (Kommentar `:866` „qs (Faktor 0) bleibt unpriced"). Ein Draft mit »Salz, qs« + »Pfeffer, qs« (in fast jedem Gericht) trägt damit `priced < total` → der Marker feuert. Fachlich vertretbar (qs trägt real 0 €, aber der Preis IST unbekannt), doch auf demo droht der Marker »immer an« zu stehen → Signal-Entwertung. Verifizieren: wie oft feuert er auf echten LLM-Drafts? Falls Dauer-an, gehört die Kur an die QUELLE (qs aus `ek_n_ingredients_total` nehmen — platform-weit in `RecipeRecomputeService::kosten` + `DataQualityService`-Buckets), NICHT als Planung-lokaler Patch (das wäre die zweite Wahrheit, die ich vermieden habe). Platform-weiter Entscheid: Dominique. | `src/Services/RecipeRecomputeService.php:602` (`kosten` zählt qs in `ek_n_ingredients_total`) + `src/Services/DataQualityService.php` (Buckets) + `resources/views/livewire/planung/partials/step-zeile.blade.php` | neu (evtl. platform-weiter Entscheid) |
| 94 | 2026-08-16 | **Der EK-Vollständigkeits-Marker hängt an `ek_n_ingredients_*`, die NUR ein Recompute setzt — zwischen Generierung und `EnrichRecipeJob`-Abschluss ist die Aussage stumm.** `2056a27` markiert bewusst NICHT, wenn die Zähler NULL sind (keine unbelegte Behauptung, per Test gepinnt). Kehrseite: zeigt die Kachel je einen `ek_total`, BEVOR der Recompute die Zähler geschrieben hat (Reihenfolge-Frage in `RecipeOneShotService::anreichern` / `RecipeRecomputeService`), sähe der Nutzer EK/Marge OHNE den teil-unbepreist-Hinweis, obwohl die Bepreisung unvollständig ist — der Ehrlichkeits-Marker liefe dem EK-Wert hinterher. Verifizieren: setzt der Enrich-/Recompute-Pfad `ek_total_eur` und `ek_n_ingredients_*` IMMER im selben Durchgang (dann gegenstandslos)? `kosten()` tut beides in einem Aufruf → vermutlich ok, aber der gemischte Draft-Zwischenzustand (EK aus KI-Vorbepreisung, Zähler noch NULL) ist der zu prüfende Fall. | `src/Services/RecipeRecomputeService.php:602-629` (`kosten` setzt ek_total + Zähler gemeinsam) + `src/Services/RecipeOneShotService.php` (`anreichern`-Reihenfolge) | neu |
| 95 | 2026-08-16 | **Echte Kosten-Transparenz (EUR) für KI-Fotos braucht eine Preisquelle je Modell — heute nur Call-Anzahl + Modell.** Der Et.7-Chunk (`f5d0640`) hebt die geloggten KI-Bild-Calls ins Cockpit, kann aber KEINEN EUR-Betrag zeigen: `foodalchemist_ai_call_log` speichert nur `model`/`elapsed_ms`/`tokens=0` (Bilder), keinen Kostenwert, und es gibt keine Modell→Preis-Tabelle im Code → ein EUR-Wert wäre Erfindung (Guardrail »keine Erfindungen«). Ein Folge-Chunk könnte eine belegte Modell-Preis-Map (z.B. `config/foodalchemist.php` `ai_image_pricing` mit Quelle/Provenienz, wie GL-06 Kosten) einführen — dann zeigt das Cockpit echte € statt nur »2 Calls · gpt-image-1.5«. Direkt gekoppelt an die Offene Entscheidung »Bild-Kostenpolitik« (worauf sich der Kostendeckel bezieht). Entscheidung/Quelle: Dominique. | `src/Services/RecipeImageService.php` (`logCall`, kein Kostenfeld) + `database/migrations/2026_06_12_000030_create_foodalchemist_ai_call_log_table.php` + `config/foodalchemist.php` | neu (gekoppelt an Offene Entscheidung Bild-Kostenpolitik) |
| 96 | 2026-08-16 | **`step-zeile.blade.php` trägt inzwischen 5 unabhängige Anzeige-Belange in einer Partial — Kandidat für Aufteilung.** Nach dem Et.7-Bild-Call-Indikator (`f5d0640`) hält die Partial: Status/Ansehen-Aktionen, Anreicherungs-Badge (`deferred.enrich`), Et.6-Kalkulations-Kachel (EK/VK/Marge + teil-unbepreist + Formel-fehlt), Et.7-Bild-Calls, Zutaten-Editor-Toggle und »Verwendetes Wissen«. Jeder neue Cockpit-Badge (Et.7 Punkt 2 »Bild-Status«, Et.8 »Fehler-Transparenz«) wächst dieselbe Datei weiter. Kein Bug, aber die nächste Erweiterung (Bild-STATUS erzeugt/fehlgeschlagen/neu erzeugen) wäre ein guter Anlass, die Badge-Zeile (`<span class="shrink-0 …">`) und die Metrik-Zeilen in Sub-Partials (`step-badges`/`step-metriken`) zu ziehen, damit die Zeile lesbar bleibt. | `resources/views/livewire/planung/partials/step-zeile.blade.php` (5 Belange in einer Partial) | teils materialisiert (`c9d5560` fügte den Bild-Status-Badge hinzu → 6. Belang; Split-Anlass jetzt fällig) |
| 97 | 2026-08-16 | **KI-Bild-Erzeugung protokolliert KEINEN Fehler-Zustand — das ist der Blocker für Bild-Status Teil 2 (»fehlgeschlagen« + „neu erzeugen") UND eine Et.8-Fehler-Transparenz-Lücke.** `RecipeImageService::erzeugeFuerRezept` (leere `catch` je Bild) und `EnrichRecipeJob:70-77` (leerer `catch`, nur `Log::warning`) schlucken Bild-Fehler still — anders als die Anreicherung, die pro Step `deferred.enrich=failed` persistiert und darauf das »neu anreichern«-Badge baut. Folge: der Bild-Status-Badge Teil 1 (`c9d5560`) kann nur »angefordert, aber 0 Fotos« zeigen (ehrlich, aber unspezifisch — n/a-Schritte vs. echter Fehler nicht unterscheidbar) und es gibt kein „nur Bilder neu erzeugen" (heute re-triggert nur `neuAnreichern` die Voll-Anreicherung mit). Fix für Teil 2: analog `markEnrich` einen `deferred.bilder`-Status (queued→running→done|failed, +erzeugte/versuchte Zähler) im `EnrichRecipeJob` führen (Bild-Phase in eigenen try mit Status-Update statt leerem catch) + eine `reBilder(team, stepId)`-Cascade-Methode, die NUR `RecipeImageService::erzeugeFuerRezept` re-dispatcht. | `src/Services/RecipeImageService.php` (`erzeugeFuerRezept`/`generiereFoto` leere catches) + `src/Jobs/EnrichRecipeJob.php:70-77` + `src/Services/PlanningCascadeService.php` (neue `reBilder`) | **erledigt** — Teil 2a `deferred.bilder`-Persistenz `313854e`, Teil 2b `reBilder`/`bilderNeu` `359ada0`. (Et.8-Fehler-Transparenz-Aspekt für den Normal-Modus-Hard-Abort bleibt in #99.) |
| 98 | 2026-08-16 | **Der Bild-Status-Badge (`c9d5560`) zählt ALLE nicht-gelöschten Rezept-Fotos — »N Fotos ✓« heißt »vorhanden«, nicht »in DIESEM Lauf erzeugt«.** `fotoCounts` in `Index::render` zählt `foodalchemist_recipe_step_photos` je `recipe_id` ohne Lauf-/Zeitbezug. Für den Normalfall (frisch generiertes Draft, Fotos entstehen erst im `EnrichRecipeJob` nach der Freigabe) korrekt. ABER: ein re-anreicherter Draft ODER ein Rezept mit manuellem Vorab-Upload trägt die Alt-Fotos in die Zahl — der Badge sagt »2 Fotos ✓«, auch wenn dieser Kaskaden-Lauf 0 erzeugte. Gebunden durch das Gate (`status=freigegeben` + `enrich=done`), also nie an einem Reuse-`skipped`-Step. Fachlich vertretbar (»das Draft HAT 2 Fotos«), aber wenn Teil 2 »in diesem Lauf erzeugt« sauber trennen soll, braucht es den Lauf-/Zeitbezug (z.B. Fotos ab Step-Freigabe-Zeitstempel ODER die `deferred.bilder`-Zähler aus #97 statt des Roh-Counts). | `src/Livewire/Planung/Index.php` (`render` `$fotoCounts`-Query) | neu (schärfen mit #97 Teil 2) |
| 99 | 2026-08-16 | **`failed()`-Hook (harter Job-Abbruch) markiert nur `enrich`, nicht `bilder`.** `EnrichRecipeJob::failed()` ruft `markEnrich('failed')`, aber kein `markBilder` — wird der Job während der Bild-Erzeugung hart abgebrochen (Timeout/Kill, nach durchgelaufener Anreicherung), bleibt `deferred.bilder` leer, obwohl gerade die Bilder liefen. Das Cockpit fiele dann auf den Teil-1-0-Foto-Fallback (»keine Fotos erzeugt«, amber) statt »fehlgeschlagen« zu zeigen. Selten (Bilder laufen nach der Anreicherung, Timeout dort unwahrscheinlich), aber eine ehrliche Lücke. Fix-Idee: `failed()` setzt `deferred.bilder=failed` nur, wenn `kiBilder` war UND `deferred.bilder` noch nicht gesetzt ist. | `src/Jobs/EnrichRecipeJob.php:81` (`failed`) | **teils (`359ada0`)** — `failed()` markiert jetzt `deferred.bilder` im `nurBilder`-Modus (Re-Trigger). OFFEN bleibt der Normal-Modus (`kiBilder=true`): ein harter Abbruch WÄHREND der Bild-Phase markiert weiter `enrich=failed`, obwohl die Anreicherung durch war → irreführend. Fix wie beschrieben (kiBilder-Gate + „bilder noch nicht gesetzt"). |
| 100 | 2026-08-16 | **Teil 2b (»neu erzeugen« für Bilder) braucht einen Bild-only-Re-Trigger — heute sind Anreicherung + Bilder im `EnrichRecipeJob` untrennbar.** `neuAnreichern` fährt den ganzen Job (anreichern + Bilder) erneut; ein reiner Bild-Retrigger (nur Fotos neu, ohne Voll-Anreicherung) müsste `anreichern` überspringen. Fix-Idee: Konstruktor-Flag `nurBilder` am `EnrichRecipeJob` (überspringt `anreichern`, setzt nur `markBilder`) + Livewire-Action `bilderNeu($stepId)` analog `neuAnreichern`, am rose `failed`-Badge verdrahtet. | `src/Jobs/EnrichRecipeJob.php:47` (`handle`) + `src/Livewire/Planung/Index.php` (`neuAnreichern`-Muster) | **erledigt `359ada0`** (exakt so umgesetzt: `nurBilder`-Flag + `reBilder`/`bilderNeu` + `loescheKiFotos`-Ersatz statt Anhäufung). |
| 101 | 2026-08-16 | **Badge-»(N ok)«-Zähler kommt aus `$fotoCounts` (reale DB-Fotos), nicht aus `deferred.bilder.n`.** Bewusst: `$fotoCounts` zählt real existierende Fotos (inkl. manueller Uploads, minus gelöschte) und ist die ehrlichere »ok«-Zahl für den Menschen; `deferred.bilder.n` ist die im Lauf erzeugte Anzahl. Beide können divergieren (Upload/Delete nach dem Lauf). Kein Bug, aber wenn je `n` als Anzeige-Wahrheit gebraucht wird (z.B. »X von Y Fotos erzeugt«), die zwei Quellen bewusst trennen/dokumentieren. | `resources/views/livewire/planung/partials/step-zeile.blade.php:~55` (`$fotoN` vs `$bld['n']`) | neu |
| 102 | 2026-08-16 | **`loescheKiFotos` (Re-Trigger-Purge, `359ada0`) hängt am Kosten-Call-Log — ein KI-Foto ohne Log-Eintrag überlebt das „neu erzeugen" als Waise.** Der Discriminator „KI vs. manuell" ist der `foodalchemist_ai_call_log`-Eintrag (`target_id`=photo.id, `BILD_FEATURES`). `RecipeImageService::logCall` ist aber selbst fail-soft (leerer `catch`, `:154`) — schlägt das Logging fehl (DB-Hiccup), entsteht ein KI-Foto OHNE Log. Beim späteren „neu erzeugen" wird es nicht als KI erkannt → nicht gelöscht → das neue Set kommt als Dublette hinzu. Selten (Log-Fehler), aber die Purge-Garantie „ersetzen statt anhäufen" hat damit ein Loch. Alternativ-Discriminator: eine explizite `origin`/`is_ai`-Spalte an `foodalchemist_recipe_step_photos` (dann unabhängig vom Log). | `src/Services/RecipeImageService.php` (`loescheKiFotos` + `logCall:154` fail-soft) | neu |
| 103 | 2026-08-16 | **Der neue `deferred.bilder=queued`-Spinner (`359ada0`) hat KEINEN Watchdog — ohne Worker bleibt »erzeugt Fotos …« ewig stehen.** `reBilder` setzt `deferred.bilder=queued` synchron; `anreicherungOffen` hält daraufhin das `pruefeLauf`-Polling am Leben. Aber der Queue-Watchdog (`$hinweis`, „kein Worker aktiv") hängt am RUN-Status (`running`), nicht am `deferred.bilder`-Zustand — und der Run ist beim Bild-Re-Trigger längst `review`/`done`. Läuft kein `queue:work`, kippt `deferred.bilder` nie von `queued` → der Spinner dreht endlos, ohne den ehrlichen „kein Worker"-Hinweis (den die Anreicherung über den Run-Watchdog bekäme). Fix-Idee: den Watchdog auch auf ein zu lange `queued`/`running` stehendes `deferred.bilder`/`deferred.enrich` (mit `at`-Zeitstempel) ausdehnen, unabhängig vom Run-Status. | `src/Livewire/Planung/Index.php` (`anreicherungOffen` + Watchdog `$hinweis`/`pruefeLauf`) | neu |
| 104 | 2026-08-16 | **`uebernimmManuellesFoto` (Et.7 Teil 1, `80902c1`) ist »getragen, kein Leser« — das Primitive existiert, aber kein Aufrufer.** Reines Backend-Contract (Muster wie b36ba00): der Cockpit-Upload-Knopf (Teil 2, `WithFileUploads` auf `Planung\Index` → `uebernimmManuellesFoto`) fehlt noch. Risiko wie #23/#29: bleibt der Consumer aus, ist die Methode toter Code. Nächster Et.7-Chunk MUSS Teil 2 (Upload-UI am Bild-Status, als Alternative neben „neu erzeugen") bauen — und dabei entscheiden, ob das manuelle Foto direkt als Ergebnis/Hero (`endproduktSetzen`, max. 1) markiert wird oder Pool-Foto bleibt. | `src/Services/RecipeImageService.php` (`uebernimmManuellesFoto`) + `src/Livewire/Planung/Index.php` (fehlender Upload-Handler) | erledigt (`e7dc07a`: `fotoHochladen` + `uebernimmManuellesFotoFuerStep` + Upload-Knopf; #106 mit-geklärt via `$istErgebnis`) |
| 105 | 2026-08-16 | **Foto-Wiederverwendung (Et.7 Teil 3) braucht eine Lösch-Sicherheits-Entscheidung — ein geteilter `context_file_id` zwischen zwei Foto-Zeilen ist ein Waisen-/Doppel-Lösch-Hazard.** Teil 1 vermeidet das bewusst (frische ContextFile je Upload). Aber »Wiederverwendung« eines vorhandenen Fotos auf einem ANDEREN Rezept bedeutet entweder (a) eine neue Foto-Zeile auf denselben `context_file_id` (spart die Datei, aber `FoodAlchemistMediaService::delete`/`loescheKiFotos` würde die von beiden geteilte Datei löschen → das andere Rezept-Foto wird zur Waise) oder (b) die Datei physisch kopieren (kein Sharing, aber Speicher-Duplikat). Design-Entscheid für Teil 3, BEVOR gebaut wird: Sharing mit Ref-Count-/Delete-Guard vs. Copy-on-Reuse. | `src/Services/RecipeImageService.php` (künftige Reuse-Methode) + `src/Services/FoodAlchemistMediaService.php` (`delete`) | **erledigt `34f8238`** (Entscheid = **Copy-on-Reuse**: `uebernimmVorhandenesFoto` kopiert die Bytes in eine frische ContextFile am Ziel → kein geteilter `context_file_id`, kein Waisen-/Doppel-Lösch-Hazard; konsistent zur Anti-Sharing-Linie aus Teil 1). |
| 106 | 2026-08-16 | **`uebernimmManuellesFoto` legt bewusst nur ein Pool-Foto an (`is_result=false`) — die häufigste »Alternative zur KI« ist aber das HERO/Ergebnis-Bild.** Der Nutzer, der die KI-Erzeugung durch ein eigenes Foto ersetzt, will meist das fertige-Teller-Foto (`is_result`, „so soll es aussehen"), nicht ein anonymes Pool-Bild. Das Primitive überlässt die Ergebnis-Markierung bewusst `RecipeStepService::endproduktSetzen` (max.-1-Invariante), aber Teil 2 (Upload-UI) muss den Pfad wählen: entweder ein `istErgebnis`-Flag an `uebernimmManuellesFoto` durchreichen (dann intern `endproduktSetzen` nach dem Create) ODER die Markierung als separaten UI-Schritt anbieten. Sonst landet jedes manuelle Upload im Pool und der Hero bleibt leer. | `src/Services/RecipeImageService.php` (`uebernimmManuellesFoto` ohne `is_result`-Pfad) + `src/Services/RecipeStepService.php` (`endproduktSetzen`) | neu (klären bei Et.7 Teil 2) |
| 107 | 2026-08-16 | **Ein `<input type=file>` je freigegebenem Rezept-/Gericht-Draft — bei großem Fan-out viele Upload-Felder gleichzeitig gemountet.** Die Et.7-Teil-2-Upload-Zeile (`e7dc07a`) rendert für JEDE freigegebene Rezept-/Gericht-Zeile im Cockpit ein eigenes File-Input + zwei Knöpfe. Bei einer Concept-Voll-Kaskade mit vielen Gerichten ist das ein dichtes Feld an Inputs (harmlos, aber viel DOM/visuelles Rauschen). Level-up-Idee: den Upload hinter einen per-Step-Toggle legen (wie `toggleZutaten` den IngredientEditor on-demand mountet) statt ihn immer sichtbar zu halten. Kein Bug — reine UI-Dichte. | `resources/views/livewire/planung/partials/step-zeile.blade.php:~125` (`data-foto-upload`) | neu |
| 108 | 2026-08-16 | **`fotoHochladen`-Validierung `image\|max:8192` kann von der globalen Livewire-Temp-Upload-Regel geschattet werden.** Das per-Call `$this->validate(['fotoUploads.$id' => 'image\|max:8192'])` greift nach dem Upload; die HARTE Grenze beim eigentlichen Hochladen ist `config('livewire.temporary_file_upload.rules')` (Default meist `file\|max:12288`). Divergieren die zwei (z.B. Projekt setzt Temp-Upload strenger), bekommt der Nutzer einen anderen Fehler, als die per-Call-Regel suggeriert. Beim Feinschliff: die 8-MB-Grenze bewusst gegen die globale Config abgleichen. | `src/Livewire/Planung/Index.php` (`fotoHochladen` validate) | neu |
| 109 | 2026-08-16 | **`uebernimmVorhandenesFoto` (Et.7 Teil 3a, `34f8238`) ist „getragen, kein Leser" — der Reuse-Picker fehlt noch.** Reines Backend-Primitive (Muster wie #104/Teil 1): der Cockpit-Consumer (Picker: vorhandene Team-/Rezept-Fotos listen → auswählen → `uebernimmVorhandenesFoto` als Pool oder Hero) ist Teil 3b und noch nicht gebaut. Risiko wie #23/#104: bleibt der Consumer aus, ist die Methode toter Code → der nächste Et.7-Chunk MUSS den Picker bauen (Fotoquelle: `FoodAlchemistRecipeStepPhoto` team-scoped `visibleToTeam`, ggf. auf Rezepte mit Fotos gefiltert; Vorschau via `MediaService::dataUri`/`url`). | `src/Services/RecipeImageService.php` (`uebernimmVorhandenesFoto`) + `src/Livewire/Planung/Index.php` (fehlender Picker) | **erledigt `1225ec4`** (Teil 3b: `uebernimmVorhandenesFotoFuerStep` + `fotoPickerOeffnen`/`-Schliessen`/`fotoUebernehmen` + Thumbnail-Picker in `step-zeile`; Kandidaten team-scoped, ohne die eigenen Ziel-Rezept-Fotos, Cap 24). |
| 110 | 2026-08-16 | **Copy-on-Reuse dupliziert die Bild-Bytes je Übernahme — kein Dedup/Ref-Count.** Bewusst gewählt (#105: Lösch-Sicherheit vor Speicher), aber jede Wiederverwendung eines beliebten Team-Fotos legt eine physische Kopie an. Bei starker Nutzung (ein Hero auf viele Gerichte) wächst der Storage linear. Kein Bug — dokumentierter Trade-off. Falls je relevant: Content-Hash-Dedup auf ContextFile-Ebene (eine Datei, n logische Refs mit Ref-Count-Delete-Guard) wäre die verlustarme Alternative — dann aber die #105-Entscheidung teil-revidieren. | `src/Services/RecipeImageService.php` (`uebernimmVorhandenesFoto`) + `src/Services/FoodAlchemistMediaService.php` | neu |
| 111 | 2026-08-16 | **`leseFotoBytes` Legacy-Fallback nimmt den `public`-Disk fix an.** Für Fotos ohne `context_file_id` liest der neue Bytes-Leser (`34f8238`) den Legacy-`pfad` von `Storage::disk('public')` — dieselbe Annahme wie `MediaService::dataUri`/`delete`. Läge je ein Legacy-Foto auf einem anderen Disk, würde der Reuse es als „Datei fehlt" (`RuntimeException`) abweisen. Heute konsistent mit dem Bestand (alle Legacy-Fotos auf public), aber die Disk-Annahme ist an drei Stellen dupliziert — ein zentraler `fotoDisk()`-Helfer am MediaService wäre die eine Wahrheit. | `src/Services/RecipeImageService.php` (`leseFotoBytes`) + `src/Services/FoodAlchemistMediaService.php` (`dataUri`/`delete`) | neu |
| 112 | 2026-08-16 | **Reuse-Picker (`1225ec4`) zeigt die 24 JÜNGSTEN Team-Fotos ohne Suche/Filter — bei großem Foto-Bestand findet der Nutzer ein älteres Zielfoto nicht.** `render()` baut die Kandidaten als `visibleToTeam`→`orderByDesc('id')`→`limit(24)`, ohne Filter nach Rezept/Kategorie/Caption und ohne Volltextsuche. Für ein kleines Team ausreichend, aber ein Team mit hunderten Fotos sieht nur die neuesten 24 → das gewünschte Hero eines älteren Rezepts ist unerreichbar. Level-up: Suchfeld (Caption/Rezept-Name) + optional Rezept-Filter am Picker (Muster wie `composerAnkerBrowse` mit Term/Kategorie). Der Cap bleibt als Render-Guard, wird aber suchbar. | `src/Livewire/Planung/Index.php` (`render` `$fotoPickerKandidaten`-Query) + `resources/views/livewire/planung/partials/step-zeile.blade.php` (`data-foto-picker`) | neu |
| 113 | 2026-08-16 | **Picker-Thumbnails lösen je Kandidat eine `ContextFile::find()` aus (N+1 beim offenen Picker).** `FoodAlchemistRecipeStepPhoto::url()` → `MediaService::url(context_file_id, pfad)` macht pro Foto ein `ContextFile::find()`; bei 24 offenen Kandidaten sind das bis zu 24 Einzel-Lookups je Render, solange der Picker offen ist. Harmlos bei Cap 24, aber wenn der Cap je steigt oder der Picker oft offen ist, wäre ein Batch-Load der ContextFiles (whereIn über die gesammelten `context_file_id`) die eine Query. Kein Bug — reine Effizienz. | `src/Livewire/Planung/Index.php` (`render` Kandidaten-Loop `$foto->url()`) + `src/Models/FoodAlchemistRecipeStepPhoto.php` (`url`) | neu |
| 114 | 2026-08-16 | **Et.1 top-down blockiert: »Auto-Zerlegung« (`- [ ]`, Roadmap-Z.116) ist eine OFFENE ENTSCHEIDUNG (»soll auch Standalone-Gericht ohne Kaskade zerlegen?«), der letzte Et.1-Rest daneben ist der Real-LLM-Blindtest (Z.117).** Beide sind für die Routine gesperrt (Entscheidung nur Dominique / Real-LLM-Abnahme provider-los nicht bewertbar). Die Routine ist deshalb auf Et.8 gesprungen. **Entscheid-Vorlage für Dominique:** heute läuft die Sub-Rezept-Zerlegung (`afterGenerated`) NUR bei `$stepId !== null` (= innerhalb einer Kaskade); ein außerhalb der Planung erzeugtes Gericht bekommt gar kein `afterGenerated`. Da KI-Erstellung laut Memory [[project_fa_planung_leitstelle]] ohnehin NUR noch in der Planung sitzt (KI-Knöpfe aus Browsern raus), ist der „Standalone ohne Kaskade"-Fall real fast leer → die Zerlegung im Kaskaden-Pfad zu belassen ist vermutlich korrekt; zu klären ist nur der `schnellErstellen`/`cockpit_frei`-Pfad (legt der einen Step an?). | `src/Services/RecipeDependencyWorkflowService.php` (`afterGenerated` Gating) + `src/Jobs/GenerateRecipeJob.php:77` (`cascadeStepId`) | neu (Entscheidung Dominique) |
| 115 | 2026-08-16 | **Worker-Heartbeat (`d327e0e`) hat einen In-Flight-Blindfleck: ein einzelner Job länger als `STILL_SEKUNDEN=60` kann die Ampel fälschlich auf »still« kippen, obwohl der Worker legitim busy ist.** `Looping` feuert nur ZWISCHEN Jobs / im Leerlauf; `JobProcessing` stempelt nur beim Job-START. Ein langer LLM-Generate-Job (Voll-Anreicherung kann >60 s dauern) hinterlässt dazwischen keinen Stempel → nach 60 s liest `status()` »still«, obwohl gearbeitet wird. Heute akzeptabler Trade-off (den engen Hänger-Fall deckt der 90-s-Per-Lauf-Watchdog; die proaktive Ampel ist v.a. für den „vor dem Go / Worker gar nicht da"-Fall gedacht). Falls je false-still-Meldungen auftreten: (a) `JobProcessed` (Job-ENDE) als zusätzlicher Stempel engt das Zwischen-Job-Fenster, (b) die Ampel im UI mit dem laufenden Run-Zustand kombinieren (busy-Run ⇒ nie »still« zeigen). | `src/Services/WorkerHealthService.php` (`STILL_SEKUNDEN` + Listener-Set) | neu |
| 116 | 2026-08-16 | **Proaktive Worker-Ampel (`c6d60f3`) und der reaktive Watchdog-`hinweis` sind zwei getrennte »kein Worker«-Signale — sie können GLEICHZEITIG erscheinen und kollabieren zwei States auf eine Meldung.** Der neue Go-Banner (`data-worker-health`) warnt VOR dem Go bei `still`/`unbekannt` mit EINEM Text (die zwei States sind absichtlich zusammengefasst); der `hinweis` (⏱, Worker-Tab) schlägt reaktiv nach ~90 s eines hängenden Laufs an. Ein `still` gewordener Worker, dessen Lauf noch `running` ist, zeigt potenziell BEIDE. Kein Bug (verschiedene Tabs/Zeitpunkte, bewusst komplementär), aber ein Kandidat für EINE Worker-Präsenz-Komponente, die (a) `still` vs. `unbekannt` differenziert formuliert und (b) mit dem laufenden Run-Zustand kombiniert wird (busy-Run ⇒ nie »still«, s. #115). | `src/Livewire/Planung/Index.php` (`render` `$workerWarnung` + `pruefeLauf` `$hinweis`) + `resources/views/livewire/planung/partials/worker-praesenz.blade.php` | neu |
| 117 | 2026-08-16 | **`render()` ruft jetzt `WorkerHealthService::status()` bei JEDEM Render — inkl. des 1,5-s-Worker-Polls (`wire:poll.1500ms="pruefeLauf"`).** Heute vernachlässigbar (`status()` ist ein einzelnes `Cache::get`, fail-soft), aber die Ampel ist nur VOR dem Go relevant (Go-Sektionen), nicht während der Lauf schon dreht → der Poll-Fall rechnet sie unnötig mit. Zöge die Heartbeat-Quelle je von Cache auf DB (persistenter, Multi-Node), würde das ein Query-je-Poll. Level-up: `status()` nur berechnen, wenn kein aktiver Lauf läuft (`! $this->laeuft`), analog zur Kalkulations-Map-Überlegung #86. Kein Bug — reine Effizienz/Altitude. | `src/Livewire/Planung/Index.php` (`render` `$workerState`-Block) | neu |
| 118 | 2026-08-16 | **Die Worker-Ampel hat keinen Selbst-Diagnose für eine Cache-Fehlkonfiguration — ein `array`/prozess-lokaler Cache liest dauerhaft `unbekannt`/`still`, ununterscheidbar von einem echt fehlenden Worker (beim Schreiben von Runbook 39 aufgefallen).** Der Herzschlag lebt in `Cache::put('fa:worker:heartbeat')`; damit die Ampel stimmt, MÜSSEN Web-Prozess und `queue:work`-Prozess denselben persistenten Store sehen. Bei `CACHE_STORE=array` (oder getrennten Stores) sieht der Web-Prozess den Worker-Stempel nie → das Cockpit warnt „kein Worker aktiv", obwohl einer läuft. Heute nur im Runbook (§2) als Betriebs-Voraussetzung dokumentiert; im Code gibt es kein Signal, das den Misconfig-Fall vom echten „kein Worker"-Fall trennt. Level-up: in `status()` einen dritten Zustand `cache_ungeeignet` ableiten, wenn `config('cache.default')` auf einen prozess-lokalen Store (`array`/`null`) zeigt → differenzierte UI-Meldung („Cache-Store prüfen", nicht „Worker starten"). Kein Bug — Diagnose-Lücke. | `src/Services/WorkerHealthService.php` (`status`/`heartbeat` Cache-Store) + [39_Worker_Betrieb_Runbook.md](39_Worker_Betrieb_Runbook.md) §2 | neu |
| 119 | 2026-08-17 | **Der Fan-out-Cap-Vermerk (`gedeckelt_slots_offen`, `c6cc72e`) wird NUR am Run persistiert, nicht im Cockpit gezeigt — »kein stiller Deckel« gilt bislang nur auf DB-Ebene.** Gleiche Lücke wie beim Speiseplan-Cap (`gedeckelt_zellen_offen`): der Nutzer, dessen großer Menü-Brief auf 30 erfundene Gerichte gedeckelt wurde, sieht im Cockpit KEINEN Hinweis, dass N Slots übersprungen wurden — er müsste die Run-`params` in der DB lesen. Ehrlich wäre ein Cockpit-Banner (analog `data-margen-warnung`/`data-worker-health`), das `params.gedeckelt_slots_offen` bzw. `gedeckelt_zellen_offen` liest und »N Positionen wegen Kostendeckel übersprungen — separat nachfassen« zeigt. Level-up für BEIDE Caps (Concept + Speiseplan) in einem Zug. | `src/Livewire/Planung/Index.php` (`render`, kein Deckel-Consumer) + `resources/views/livewire/planung/partials/` (neuer Banner) | neu |
| 120 | 2026-08-17 | **`CONCEPT_MAX_SLOTS=30` ist ein fixer Hard-Cap ohne per-Brief-Override — ein legitim großes Menü (Foodbook-Kapitelset, langes Degustationsmenü) wird stumm auf 30 Positionen gekürzt.** Der Deckel ist ein reiner Runaway-/Kosten-Guard (richtig als Default), aber es gibt keinen Weg, ihn für einen bewusst großen Auftrag anzuheben (der Nutzer kann nur nachfassen und einen zweiten Lauf starten). Gekoppelt an die Offene Entscheidung »Bild-Kostenpolitik« (Kosten-Budget-Politik allgemein): wenn je ein Budget-/Kosten-Rahmen je Kaskade eingeführt wird, sollte der Fan-out-Cap (Concept + Speiseplan) daran hängen statt an einer Konstante. Entscheidung: Dominique. | `src/Services/PlanningCascadeService.php:49` (`CONCEPT_MAX_SLOTS`) + `:47` (`SPEISEPLAN_MAX_ZELLEN`) | neu (gekoppelt an Offene Entscheidung Kosten-/Bild-Politik) |
