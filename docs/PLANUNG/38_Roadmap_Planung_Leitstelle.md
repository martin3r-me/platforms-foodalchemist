# 🎛️ Planung-Modul — Roadmap »Mise en Place«

> **Vom Brief zum fertigen Teller — in einem geführten Fluss.**
> Die Planung ist die **KI-Leitstelle** der Food Alchemist: ein Mensch gibt eine Absicht ein, die
> KI erarbeitet Stufe für Stufe das Ganze (Concept → Gerichte → Basisrezepte → Anreicherung), und
> an **jeder Stufe entscheidet der Mensch per Freigabe**. Wie in der Küche: erst *mise en place* —
> alles vorbereitet, geprüft, an seinem Platz — dann geht es live.

- **Zweck:** gesamtheitlicher Plan + abarbeitbare Liste für das Planung-Modul (Leitstelle).
- **Zielgruppe:** Entwicklung + Produkt (Dominique).
- **Stand:** 2026-08-14. Ergänzt den zentralen [Zielbild-Umsetzungsplan](24_Zielbild_2029_Umsetzungsplan.md) um die **modultiefe** Sicht; bei Widerspruch gilt der Umsetzungsplan.
- **Statuswerte** (aus [README](../README.md)): `gebaut` · `getestet` (Sandbox) · `demo-geprüft` (am Demo-Datensatz) · `abgenommen` (Real-LLM/Kunde). „gebaut" ≠ „abgenommen".

---

## 🧭 Nordstern — wann ist das Modul „fertig"?

Ein Nutzer startet mit **einem Satz Absicht** und einem Satz Leitplanken und erhält am Ende ein
**vollständiges, kalkuliertes, geerdetes Angebot** — ohne die Kette manuell zusammenzuklicken:

1. **Ein Einstieg, jede Tiefe:** Basisrezept *oder* Gericht *oder* Concept — je nachdem, wo man startet, kaskadiert es sauber nach unten.
2. **Geführte Freigabe je Stufe:** nichts wird still live; jede Ebene hat Vorschau → Prüfen/Ändern → Freigabe.
3. **Fachlich korrekt gebaut:** ein Gericht besteht aus **Basisrezepten** (Sauce/Jus/Püree = eigenes Sub-Rezept), nicht aus flachen Zutaten — geerdet auf das [Regelwerk Basisrezepte].
4. **Kosten sichtbar mitgeführt:** EK/VK/Marge je Stufe, Zielpreis-Korridor propagiert.
5. **Ehrlicher Durchlauf:** man sieht jederzeit, ob es arbeitet, hängt, fertig ist — und ob die Anreicherung wirklich lief.
6. **Ausgabe-fähig:** das Ergebnis fließt in Foodbook / Speisekarte / Speiseplan.

---

## 🏗️ Zielarchitektur (Whiteboard 2026-08-14)

```
  Foodbook  ┐
  Speiseplan ├─►  Briefing/Planung  ─►  ┌───────────┐
  Speisekarte┘   (oder freier Brief)    │  PLANUNG  │  ← Leitstelle
                                         └─────┬─────┘
                                               ▼  dispatch (kein Web-Request rechnet LLM)
                                         ┌─────────────┐
   Erdung + Bestand (READ-only):        │ Queued Jobs │
   Wissen · Pairing-Graph · DNA         └─────┬───────┘
   Grundprodukte · Lieferantenartikel         ▼
   Basisrezepte · Gerichte  ─────►  LLM Menü ─► LLM Gerichte ─► LLM Basisrezepte
                                          └────── + LLM Foodpairing (Assist) ──────┘
                                               │
                                               ▼
              Ausgänge:  Conceptor (Menü/Pakete) · Gerichte · Basisrezepte
```

**Lesart:**
- **Eingang:** ein freier Brief ODER ein Ausgabe-Frame (Foodbook/Speiseplan/Speisekarte) liefert das Briefing → Planung. Die Ausgabe-Module sind damit **Quelle UND Ziel**.
- **Planung = Leitstelle:** zerlegt die Absicht in **Queued Jobs** — nichts Schweres läuft im Web-Request.
- **Drei LLM-Worker je Ebene, kaskadierend:** LLM Menü (Concept) → LLM Gerichte → LLM Basisrezepte. Jede Ebene: Vorschau → Freigabe → nächste Ebene.
- **LLM Foodpairing = Assist:** erdet Gerichte/Menüs am **Pairing-Graph** (Aroma-Kohärenz), statt frei zu raten.
- **Erdung/Bestand (Read-only):** die Worker reusen vorhandene Grundprodukte/Lieferantenartikel/Basisrezepte/Gerichte und erden an Wissen (Regelwerke/Domänen), Pairing-Graph und Marken-DNA.
- **Ausgänge:** Conceptor (Menü/Pakete), Gerichte, Basisrezepte — fließen in die Ausgabe-Module zurück.

---

## ⚙️ Das Prinzip (universelles Muster je Stufe)

```
Eingabe (Titel/Brief + Leitplanken)
   → Worker erzeugt VORSCHAU (Draft, noch nicht live)
      → Prüfen / Zutaten tauschen / Neu generieren
         → FREIGABE  ── legt an + reichert KOMPLETT an (als Job)
                     └─ stößt die nächste Stufe an (Fan-out)
```

- **Kaskaden-Regel (Entscheid 2026-08-14):** am Go zählen die Leitplanken des **Start-Tabs** — sie propagieren die ganze Kaskade nach unten (`generation_params` an der Session).
- **Gestuft, nicht eager:** Run-Flag `staged` + Step-`deferred` (Migration `2026_08_14_000001`).

---

## ✅ Ist-Stand (Etappe 0 — Fundament, live auf demo)

FA `main` = `eb85e3c`, demo deployt + Migration `[180] Ran`. Nachweis: `PlanningCascadeTest`/`PlanungLeitstelleTest` grün, Server-`staged` 13×, HTTP 200.

- [x] **Gestufte Kaskade** — Gate pro Ebene, `staged`/`deferred`, `FanoutConceptJob` (`demo-geprüft`)
- [x] **Per-Tab-State** — Basisrezept/Gericht/Concept je eigenes Briefing + eigene Leitplanken (`demo-geprüft`)
- [x] **Worker-Cockpit** — Stufen-Abschnitte, Fortschritts-Header, Stufen-Freigabe, Per-Step „Neu generieren" (`demo-geprüft`)
- [x] **Inline-Conceptor** — Concept „öffnen" mountet den vollen Conceptor-Editor (`demo-geprüft`)
- [x] **Freigabe = Anlage + Voll-Anreicherung als Job** (`EnrichRecipeJob` → `RecipeOneShotService::anreichern`) (`demo-geprüft`)
- [x] **KI-Fotos-Toggle** — Produktfoto + Schritt-Fotos, opt-in (Preisfrage), `RecipeImageService` (`gebaut`, Real-Abnahme offen)
- [x] **Ehrlicher Durchlauf (P0.A)** — Erfolgs-Banner „Kaskade abgeschlossen", Watchdog (done/failed = Job-Beweis, nicht mehr blind nach Freigabe), Anreicherungs-Status je Step (`deferred.enrich`), „neu anreichern", Polling hält bis Anreicherung durch (`demo-geprüft` — live bestätigt: „Kaskade abgeschlossen — 1 freigegeben, Anreicherung läuft …" + „reichert an …"-Badge sichtbar)
- [x] **Zutaten-Review inline** — `IngredientEditor` je Draft (tauschen/entfernen/ergänzen vor Freigabe) (`demo-geprüft`)

> **Offene Abnahme (Etappe 0):** (1) Anreicherung bis „angereichert ✓" abschließen (Screenshot zeigt noch „läuft"). (2) ~~**Beobachtung Dominique:** im Worker liegen die Sub-Rezepte (Consommé/Espuma) nur als 📖-Referenz IN der Zutatenliste — sie sollten als eigene **Basisrezepte-Stufe** zum Abarbeiten/Freigeben erscheinen~~ → gebaut in Etappe 1 (`d018af1`), Real-Abnahme auf demo offen. (3) Gericht-mit-Kindern 2-stufig, Worker-Stopp-Probe.

---

## 🗺️ Die Roadmap (abzuarbeiten)

### 🥇 Etappe 1 — Qualität: **Gericht = Basisrezepte** (das große Finetuning)
*Das Kernversprechen. Heute baut die Generierung flache Zutaten statt Halbfabrikate (Sauce/Jus/Püree) als eigene Sub-Basisrezepte zu zerlegen (Beispiel: Steinpilze + Rinderjus → keine „Steinpilz-Rahmsauce").*

- [x] **★ Worker führt Basisrezepte als eigene Stufe** — die generierten Sub-Rezepte (Consommé/Espuma …) erscheinen als abarbeitbare **Basisrezepte-Stufe** (Vorschau/Freigabe je Stück), nicht nur als 📖-Referenz IN der Gericht-Zutatenliste (Beobachtung Dominique 2026-08-14)
  → `d018af1` (`gebaut`, Sandbox `getestet`): neuer Step-Status **`geplant`** (Sub-Rezept benannt, noch nicht erzeugt, kein Job — wartet auf die Freigabe der Stufe darüber) wird schon beim Aufschieben angelegt; die Freigabe schaltet **denselben** Step scharf (`geplant`→`running`, keine Dublette). Direkt verdrahtete Sub-Rezepte stehen als **`skipped`** = „übernommen" (Reuse-Treffer, ansehbar, nicht freigebbar) in der Stufe. Run-Status: `geplant` → `review` (Mensch am Zug). Cockpit: Zähler „N geplant / N übernommen" + Zustand `geplant`. 6 neue Tests, 57/57 grün.
  - [x] **Rest-Chunk (Teil 2):** geplante Sub-Rezepte VOR der Gericht-Freigabe einzeln bedienen — „jetzt erzeugen" + „brauche ich nicht" (verwerfen) je `geplant`-Zeile. Heute sind sie sichtbar, aber nur als Ganzes über die Stufe darüber auslösbar.
    → `b5275da` (`gebaut`, Sandbox `getestet`): je `geplant`-Zeile zwei Knöpfe — **„jetzt erzeugen"** (`dispatchGeplantesKind`: schaltet EINEN Step scharf `geplant`→`running`, genau ein `GenerateRecipeJob`, nutzt die am Eltern-Step aufgeschobenen Kind-Params; die spätere Stufen-Freigabe sieht ihn nicht mehr als `geplant` → kein Doppel-Job) und **„brauche ich nicht"** (`verwirfGeplantenStep`: Step → `verworfen` als Tombstone, kein Hard-Delete, Dependency entfernt; die Gericht-Freigabe erzeugt ihn über den `dedupe_key`-Treffer bewusst nicht wieder). 3 neue Tests, `PlanningCascadeTest` 50/50 grün. Real-Abnahme auf demo offen.
- [x] **Scope-Treue** — ein „Freies Basisrezept"-Start erzeugte im Test eine „Gerichte"-Stufe (Tomatensuppe); der Start-Tab muss die Ebene korrekt setzen (Basisrezept ≠ Gericht)
  → `39add65` (`gebaut`, Sandbox `getestet`): Root-Cause war der Frei-Start (`schnellErstellen`) — er legte die „Freies Basisrezept"-Session an, öffnete den Editor aber auf dem Default-Tab (`tabInit='analyse'`), sodass die intendierte Ebene am Editor-Rand verloren ging und der nächste Go auf dem Gericht-Tab landen konnte. Fix: `modal.open` darf per `tab:`-Detail einen Start-Tab erzwingen (Fallback `tabInit`); `oeffne($id, ?$startTab)` reicht ihn durch; `schnellErstellen` mappt scope→Tab (`rezept`→`basisrezept`, sonst scope-Key). Das Öffnen aus der Liste (`oeffne` ohne Tab) bleibt auf dem Editor-Default. 3 neue Tests, `PlanungLeitstelleTest`+`PlanningCascadeTest` 62/62 grün. Blade-Kompilat gelintet (0 Fehler). Real-Abnahme auf demo offen.
- [x] **LLM-Contract:** `vk.generator` / `recipe.generator` (`config/foodalchemist.php`) bekommen einen Slot für **benannte Sub-Komponenten** (statt flachem `zutaten[]`)
  → `b36ba00` (`gebaut`, Sandbox `getestet`): per-Zeile-Flag **`sub_rezept`** (bool) im `zutaten[]`-Schema beider Generator-Prompts — markiert eine Zeile als eigenständiges Halbfabrikat/Sub-Basisrezept (Sauce/Jus/Fond/Sud/Essenz/Reduktion/Püree/Creme/Dressing/Vinaigrette/Espuma), das als eigenes Basisrezept anzulegen ist, statt es flach in Rohzutaten aufzulösen (§4). VK-Prompt bekommt zusätzlich die explizite Direktive „Gericht wird aus Basisrezepten gebaut" (kein «Steinpilz-Rahmsauce» aus Steinpilzen + Sahne). Name `sub_rezept` gewählt, um nicht mit dem `role`-Enum-Wert `komponente` zu kollidieren. **Reiner Contract** — das Lesen des Flags bleibt der nächste Chunk (Entscheidungsstelle, `RecipeGeneratorService`). Kein neuer Registry-Key (`PromptRegistryTest` prüft nur Key/Task/Tier). Tests: PromptRegistry/AiRetry/AiCallLog + RecipeGenerator/VkGenerator/KohärenzGate 40/40 grün. Real-Abnahme (LLM emittiert das Flag) auf demo offen.
- [x] **Heuristik erweitern:** `MatchHeuristics` Marker-Listen (`:51-65`) um `sauce/rahmsauce/jus/sud/essenz/dressing/vinaigrette` (Zwischenschritt, bleibt Keyword-Hack)
  → `2b464c5` (`gebaut`, Sandbox `getestet`): neue Konstante **`SUB_SAUCEN_MARKER`** [`sauce`, `rahmsauce`, `jus`, `sud`, `dressing`, `vinaigrette`], im `queryIstHalbfabrikat`-Gate verdrahtet (wirkt damit auch auf `istSubRezeptKandidat`, Pool-Priorität `poolLauf` und den Direktartikel-Override). **Token-EXAKT via `patternMatchesToken`** (≤ 5 Chars exakt, länger als Präfix) **statt Substring-`str_contains`** — nötig, weil der bestehende `≥4`-Substring-Gate (a) `Sojasauce`/`Fischsauce` fälschlich fangen würde (Golden-Test pinnt `Sojasauce=false`) und (b) `jus`/`sud` (3 Chars) ganz aussperrt. Exakt-Match trifft `sauce`/`jus`/`sud` nur als eigenständiges Token (Tokenizer splittet `-`/Space: `Steinpilz-Rahmsauce`→`rahmsauce`✓, `Rotwein Jus`→`jus`✓), verschont aber gekaufte Ein-Wort-Kondimente. **`essenz` bewusst ausgelassen**: mehrdeutig (gemachte Klar-Essenz = Sub vs. gekaufte Frucht-Essenz/Extrakt = GP); die DoD **M4-14** pinnt `Drachenfrucht-Essenz` als GP-Lücke — Disambiguierung gehört ans LLM-Komponenten-Flag (nächster Chunk), nicht an ein blindes Keyword. 2 neue Golden-Tests (gemachte Saucen true + Kondimente-Guard false). Tests: IngredientMatchingHeuristics/Pools + RecipeGenerator/VkGenerator + RecipeReview/Revise 101/101 grün. Real-Abnahme auf demo offen.
- [x] **Entscheidungsstelle:** `RecipeGeneratorService:247-248/408-412` liest künftig das LLM-Komponenten-Flag statt nur die Namens-Heuristik
  → `f0787a3` (`gebaut`, Sandbox `getestet`): an der Auflösung offener Zeilen (231/247) entscheidet jetzt das per-Zeile-Flag **`sub_rezept`** (Contract b36ba00), wenn gesetzt — **authoritativ in beide Richtungen** (§4): `sub_rezept:true` erzwingt Basisrezept-Anlage auch bei heuristik-blinden Fällen (gemachte Klar-Essenz — Heuristik lässt `essenz` bewusst aus), `sub_rezept:false` hält gekaufte Ware trotz Sauce/Jus/Fond-Token im LA-Pfad. Fehlt/undeutbar das Flag → Fallback auf die bisherige Namens-Heuristik (`queryIstHalbfabrikat`/`istSubRezeptKandidat`); **kein blinder `(bool)`-Cast**, damit ein fehlendes Feld die Heuristik nicht still abschaltet. Neuer Helper `llmSubRezeptFlag()` normalisiert tolerant (bool/1-0/"true"/"ja"). Die `primaer`-Zeile flag-authoritativ (LLM-Nein kippt nicht über die Button-Heuristik creme/mousse/pesto zurück zur Sub-Anlage). **408-Site (Kohärenz-Gate-ENTdrahtung) bleibt bewusst heuristisch** — dort ist das Flag nicht persistiert und `$wasSub` dominiert das Signal → eigener Chunk (bräuchte Persistenz der Zeilen-Herkunft). 2 neue Tests (Flag beide Richtungen + Fallback ohne Flag); RecipeGenerator/Hardstop/Review/Heuristik-Goldens 75/75 + McpGenerate/Cascade 63/63 grün. Real-Abnahme (LLM emittiert das Flag) auf demo offen.
- [x] **Wissens-Erdung:** Routing-Zeile `regelwerk` → `ai_generate_recipe` (analog `2026_08_07_000001`) → §2/§3/§4 [Regelwerk Basisrezepte] fließt in den Prompt
  → `85360e8` (`gebaut`, Sandbox `getestet`): Kategorie `regelwerk` war importiert, aber ungeroutet. Neue Routing-Zeile `ai_generate_recipe:regelwerk:always` (Migration `2026_08_14_000010` + `seedRoutings`-Spiegel) + dedizierter `KnowledgeContextService::regelwerkBlock` — wählt den Basisrezepte-Slug gezielt und **extrahiert die §2–§4-Region** (nicht der ganze ~53k-Text; §2 beginnt erst bei ~17k → blinder Head-Truncate würde sie verfehlen), rahmt das Food-Wissen. **Bewusst `always` statt discovery** (Abweichung von der Roadmap-Vorlage `2026_08_07_000001`): Regelwerk ist Handwerk, kein Produkt-Dossier — die generische Beschreibungs-Discovery (Slug-Token gegen die Zutaten) trifft es bei realen Rezept-Briefs nie (kein Overlap »Steinpilz« ↔ »basisrezepte«); `regelwerk` daher in die `$spezial`-Liste, damit der discovery-Loop es dem dedizierten Selektor überlässt. 7 neue Tests (`RegelwerkKnowledgeRoutingTest`), 32/33 grün (Concept/Routing/Golden/RecipeGenerator); einzige Rot ist das pre-existing GT-13-11 (koriander-grounding, unabhängig — s. Backlog). Real-Abnahme (LLM nutzt die Regeln) auf demo offen.
- [x] **LLM-Foodpairing als Assist** (Whiteboard-Baustein) — Gericht-/Menü-Erzeugung erdet am **Pairing-Graph** (Aroma-Kohärenz) statt frei zu raten (↔ Kohärenz-Gate/Anker-Graph)
  → `40cf1d2` (`gebaut`, Sandbox `getestet`): **Menü-Hälfte** geschlossen. Die **Gericht-Erzeugung** war bereits pairing-geerdet (`GenerationContextService::forGeneration` spielt je Hauptzutat Anker-Graph-Partner in den Recipe-/VK-Prompt, rollenabhängig aroma_ausschoepfen/komposition). Die **Menü-Erzeugung** war es NICHT: `IdeenService::kiDivergenzConcept` erfand Gericht-Ideen für leere Concept-Slots aroma-blind zur restlichen Folge (nur Wissen/Trend/Preis). Neu: `PairingService::menueAromaProfil(recipes)` (deterministischer Kern: `resolveRecipeAnchorsMany`→`flacheAnker`→Anker-Labels + gewichtsstärkste Partner-Expansion via `kandidatenFuerAnker`; `null` bei leerer/ungemappter Folge → Prompt byte-identisch ohne Block) fliesst als `menue_kohaesion`-Block (bereits-getragene Anker + harmonische Partner + Hinweis „auf der Aroma-Achse harmonieren, Hauptzutat nicht wiederholen") in die Ideen-Divergenz. 4 neue Tests (Kern Anker+Partner/null · Verdrahtung Block-im-Kontext/leeres-Menü-kein-Block via Provider-Spy); zusammen mit IdeenService/Anker-Batch/PairingNetz/Cascade/McpKreativModus 85/85 grün. Real-Abnahme (LLM nutzt den Block spürbar) auf demo offen.
  - [x] **Rest-Chunk:** Menü-Folge-**Kohärenz-Gate** — der `menuCohesion`-Score der erfundenen Menüfolge als sichtbare Rückkopplung/Warnung (heute erdet die Erfindung am Graphen, aber das Ergebnis wird nicht gegen die Folge-Kohäsion geprüft). Anker-Graph steht (`menuCohesion`/`composerCohesion`).
    → `0a15c26` (`gebaut`, Sandbox `getestet`): neuer reiner Klassifikator **`PairingService::menuKohaesionWarnung(array $kohaesion)`** — macht aus dem rohen `menuCohesion`-Dict eine abgestufte Warnung `{stufe: gut|schwach|kritisch, score, text}`. **Schwellen byte-genau aus dem `kohaesion-panel` gespiegelt (≥60 gut, ≥35 schwach, sonst kritisch)** — Gate und Farb-Band sagen dasselbe, keine zweite Wahrheit. `null`, wenn nichts zu beurteilen ist (zu wenig Gerichte ODER kein bewertetes Gericht-Paar) — fehlende Graph-Daten sind KEIN schlechtes Menü (T9), nur keine Aussage. Verdrahtet in `Concepter\Editor::kohaesionPruefen` (→ `menueKohaesion['warnung']`) + als farbcodiertes Banner im `kohaesion-panel.blade.php`. **Reine Rückkopplung, keine harte Filterung.** 5 neue Tests (Score-Bänder inkl. inklusiver Grenzen · weakest-pair-Hinweis nur bei schwach/kritisch · beide null-Pfade · Gate über echte Menü-Daten · Livewire-Wiring), 58/58 grün (MenueKohaesion/Concepter/Planung-Leitstelle/Pairing/Dramaturgie), Blade-Kompilat gelintet. Real-Abnahme auf demo offen.
    - [x] **Auto-Trigger (Folge-Chunk):** das Gate nach der Fan-out-**Erfindung** (`fanoutConceptInvention` → alle `MaterializeConceptIdeaJob` durch) automatisch feuern, statt auf den manuellen „Kohäsion prüfen"-Klick zu warten — die erfundene Folge wird erst NACH dem Grounding scorebar (Skizzen tragen keine Anker). Braucht einen Fan-out-Abschluss-Haken + Persistenz der Warnung am Run/Concept.
      → `75d263a` (`gebaut`, Sandbox `getestet`): der Fan-out-Abschluss-Haken sitzt am Job-Rückkanal — `markStepDone`/`markStepFailed` rufen `scoreConceptCohesionIfComplete`. Der Handler bestimmt den Concept-Step (**meldender Step selbst** = eager-Pfad, wo die Kinder inline liefen; ODER dessen **Concept-Eltern** = async-Pfad, wo der letzte `MaterializeConceptIdeaJob` abschließt) und scored erst, wenn KEIN erfundenes Gericht mehr offen ist (`geplant`/`queued`/`running`) UND überhaupt eins existiert. `persistConceptCohesion`: `menuCohesion` → `menuKohaesionWarnung`, Ergebnis am Run persistiert — neue json-Spalte **`cohesion_warning`** (Migration `2026_08_14_000011`, additiv/idempotent). **Fail-soft:** nicht-scorebare Folge (Provider-los/ungemappt/zu wenig Gerichte) persistiert `null` und kippt den Rückkanal nie. Freie Gericht-/Rezept-Läufe (kein Concept-Eltern) unberührt. 4 neue Tests (wartet-bis-komplett · eager-Concept-Abschluss · kein-Fan-out=still · freier-Lauf-unberührt); `PlanningCascadeTest` 54/54 + `MenueKohaesion`/`PlanungLeitstelle` 20/20 grün. **UI-Surfacing der persistierten Warnung im Cockpit = Folge-Chunk.** Real-Abnahme (LLM erfindet, Gate feuert sichtbar) auf demo offen.
- [ ] **Auto-Zerlegung:** `RecipeDependencyWorkflowService:49/90`-Gating — soll auch Standalone-Gericht (ohne Kaskade) zerlegen?
- [ ] **Abnahme:** Blindtest mit 3 Briefs + Golden-Eval (0 Fremdkörper, korrekte Sub-Rezept-Bildung)

### 🥈 Etappe 2 — **Concept scharf: Leitplanken nachziehen + Kreativ-Kopf**
*Ein Concept ist ein Menü, kein einzelnes Rezept. Der Concept-Tab hat heute nur Rezept-Achsen (Convenience/Frische/Bio/Aroma) — ihm fehlen die **Menü-Leitplanken**, und die Vorab-Ausarbeitung des Plans (KI-Kopf).*

**2a — Concept-Leitplanken nachziehen** (near-term, „müssen wir noch nachziehen"):
- [x] **Menü-Achsen** im Concept-Tab: Anzahl Gänge/Positionen, Zielpreis-Korridor (min/ziel/max je Person), Diät-Quoten (vegan/vegetarisch-Anteil), Portfolio-Balance
  - [x] **Teil 1 — Gänge + Zielpreis-Korridor:** Concept-only „Menü-Leitplanken"-Sektion (Anzahl Gänge/Positionen 1–20 + Zielpreis-Korridor min/ziel/max je Person).
    → `c6f1f58` (`gebaut`, Sandbox `getestet`): `REGLER_DEFAULT` + `ALLOWED_GENERATION_PARAMS` um `menue_gaenge` + drei kanonische `_pp`-Preis-Keys erweitert (kein Migration — json-Spalte + PHP-Const). `reglerParams()` parst die Roh-Eingaben **nur für den Concept-Scope** in kanonische Keys (`menue_gaenge`:int, `menue_preis_{min,ziel,max}_pp`:float); ungültig/leer = kein Key (Prompt byte-unverändert). Gericht/Basisrezept ignorieren die Menü-Achsen (bewiesen per Test). `goKaskade` sagt mistgetippte Zahlen an (fehler) statt sie still zu verwerfen (Grundsatz wie Ziel-VK). Concept-only Blade-Sektion `data-menue-leitplanken`. Die Achsen propagieren als `generation_params` in den Concept-Fan-out. 4 neue Tests, `PlanungLeitstelle`+`PlanningCascade` 70/70 grün. **Offen (Teil 2):** Diät-Quoten (vegan/vegetarisch-Anteil) + Portfolio-Balance-Achse. **Nächster Chunk (Punkt 2 unten):** diese Achsen ins `PlanningFrame` (Slots/Preis/Regeln) füttern — heute nur als Params getragen, noch nicht Frame-verdrahtet. Real-Abnahme (LLM nutzt die Achsen spürbar) auf demo offen.
  - [x] **Teil 2 — Diät-Quoten (Vegan-/Vegetarisch-Anteil):** Concept-only Portfolio-ANTEIL-Achse (0–100 %) in der Menü-Leitplanken-Sektion.
    → `e8b384c` (`gebaut`, Sandbox `getestet`): mirror Teil 1 — `ALLOWED_GENERATION_PARAMS` + `REGLER_DEFAULT` um `menue_quote_{vegan,vegetarisch}` (roh) bzw. die kanonischen `_pct`-Keys erweitert (kein Migration). `reglerParams()` parst **nur im Concept-Scope** in `menue_quote_{vegan,vegetarisch}_pct` (int 0–100) via neuen Helper `menueQuote()` (strippt `%`/Space tolerant); ungültig/leer = kein Key (Prompt unverändert). **Bewusst getrennt vom harten `diaet_hart`-Ausschluss** — die Quote ist eine WEICHE Zusammenstellungs-Vorgabe („mind. X % vegan"), kein Menü-Filter; im Blade sauber gelabelt (Anteil ≠ harter Ausschluss oben), löst die in Backlog #24 geflaggte begriffliche Kollision. `goKaskade` sagt mistgetippte Prozentwerte an (fehler). Concept-only Blade-Block `data-menue-diaet-quoten`. Gericht/Basisrezept ignorieren die Achse (bewiesen per Test). 4 neue Tests, `PlanungLeitstelle`+`PlanningCascade` 74/74 grün, Blade-Render im Livewire-Test bestätigt. **Consumer offen:** die `_pct`-Params werden getragen, aber noch von keinem Prompt/Frame gelesen (Punkt 2 unten). Real-Abnahme (LLM nutzt die Quote spürbar) auf demo offen.
  - [x] **Rest — Portfolio-Balance (Menü-Vielfalt):** Concept-only Vielfalts-Achse (ausgewogen/fokussiert) in der Menü-Leitplanken-Sektion.
    → `b325ed0` (`gebaut`, Sandbox `getestet`): letzte Menü-Achse. **Bewusst qualitatives Select (Enum `MENUE_BALANCE` = ausgewogen|fokussiert)** statt eines erfundenen numerischen Schwellwerts — „Balance" ist eine WEICHE Zusammenstellungs-Vorgabe (wie breit das Menü über Proteine/Warengruppen/Garmethoden streut), kein Filter; Semantik vollständig im UI-Hint. Mirror des `kompositions_stil`-Selects, aber concept-only. `REGLER_DEFAULT` + `MENUE_BALANCE`-Const + `ALLOWED_GENERATION_PARAMS`/`$fillable` um `menue_balance` erweitert (kein Migration). `reglerParams()`: Roh-Feld aus dem Passthrough raus; im Concept-Gate nur ein gültiger Enum-Wert gesetzt (Helper `menueBalance()` prüft `isset()` gegen den Enum → unbekannt/leer = kein Key). Kein Zahl-Guard nötig (Select unmistippbar). Gericht/Basisrezept ignorieren die Achse (bewiesen per Test). Concept-only Blade-Block `data-menue-balance`. 4 neue Tests, `PlanungLeitstelle`+`PlanningCascade` 78/78 grün, Blade-Direktiven balanciert. **Consumer offen:** wie die übrigen Achsen noch von keinem Prompt/Frame gelesen (Punkt 2 unten). Real-Abnahme (LLM nutzt die Achse spürbar) auf demo offen.
- [x] Diese Achsen speisen den **Frame** (`PlanningFrameService`: Slots/Preis/Regeln), nicht nur die Rezept-Generierung
  - [x] **Teil 1 — Preis-Korridor → Frame-Kopf:** der Concept-Tab-Zielpreis-Korridor je Person (`menue_preis_{min,ziel,max}_pp`) überschreibt autoritativ `target_price_pp`/`price_min_pp`/`price_max_pp` am Gerüst-Kopf.
    → `30d95ea` (`gebaut`, Sandbox `getestet`): `dispatchConceptStep` reicht die `menue_*`-Teilmenge der Lauf-Params an `GenerateConceptJob` (neuer Konstruktor-Param `menueAchsen` **am Ende** → die positionellen Dispatch-Calls der Voll-Kaskade bleiben unverschoben). `generiereAusBrief(…, array $menueAchsen = [])` wendet nach dem KI-`setHead` einen zweiten `setHead` nur auf die Preis-Keys an (Helper `menuePreisHead`: nur gesetzte, numerische Achsen → nur diese Keys; leer = KI-Wert bleibt). **Autoritativ** = Nordstern (die Leitplanken des Start-Tabs propagieren). 2 neue Tests (Override 35 € schlägt KI-20 € · leere Achsen lassen KI-Kopf unangetastet); ConceptGenerator + PlanningCascade 61/61 grün. **Offen (Teil 2/3):** `menue_gaenge` → Slot-`target_count`, `menue_quote_*_pct` → `diet_quota`-Rules, `menue_balance` → Zusammenstellungs-Direktive (Backlog #28/#29). Real-Abnahme (LLM/Frame spürbar) auf demo offen.
  - [x] **Teil 2 — »Anzahl Gänge« → Gerüst-Slots:** die Concept-Tab-Achse `menue_gaenge` (1–20) deckelt die `gang`-Slots des KI-Gerüsts autoritativ auf N.
    → `aec34dc` (`gebaut`, Sandbox `getestet`): `generiereAusBrief` cappt die sanitisierten Slots via Helper `menueGaengeCap` **vor** `replaceStructure` (nach dem Preis-Head aus Teil 1). **Semantik-Entscheid:** `menue_gaenge` = »Anzahl Gänge« = **Zahl der `gang`-Slots**, NICHT per-Slot `target_count` (die Teil-1-Notiz „→ Slot-`target_count`" war unpräzise) — laut `concept.brief_geruest`-Prompt ist ein Slot ein Gang/Station/Kapitel, `target_count` sind Positionen INNERHALB eines Gangs; Zeile 130 nennt als Ziel korrekt „Slots". **Bewusst nur ein Deckel:** überzählige `gang`-Slots fallen in Dramaturgie-Reihenfolge weg (erste N bleiben); produzierte die KI **weniger** Gänge als N, bleibt das Gerüst unangetastet — es werden keine Gänge erfunden (das Gerüst-System erfindet nichts, System-Prompt). `station`/`kapitel`-Slots (Buffet) sind keine Gänge → unberührt; leeres/fehlendes `menue_gaenge` = Gerüst byte-identisch. 2 neue Tests (Deckel 5→3 in Reihenfolge · station/kein-Achse-Pfad unberührt); ConceptGenerator + PlanningCascade 63/63 grün. **Offen (Teil 3):** `menue_quote_*_pct` → `diet_quota`-Rules, `menue_balance` → Zusammenstellungs-Direktive (Backlog #29). Prompt-seitige Exakt-N-Vorgabe (Undershoot ohne Erfindung) als möglicher Folge-Chunk. Real-Abnahme auf demo offen.
  - [x] **Teil 3a — Diät-Quoten → Frame-Rules:** der Concept-Tab-Vegan-/Vegetarisch-Anteil je Person (`menue_quote_{vegan,vegetarisch}_pct`) landet als autoritative frame-Ebene-`diet_quota`-Regel (operator `min`, unit `percent`) im Gerüst.
    → `e69a0f9` (`gebaut`, Sandbox `getestet`): neuer Helper `menueDiaetQuotenMerge` (mirror `menuePreisHead`/`menueGaengeCap`) legt vor `replaceStructure` je gesetzter Achse eine `diet_quota`-min-%-Regel an; »vegetarisch« → kanonisches diet_form `vegi` (DIET_FORMS). **Autoritativ** = eine gleichnamige KI-Prozent-Quote wird ERSETZT (kein Doppel-Eintrag), count-Quoten + andere Diät-Formen bleiben unangetastet. **Anteil < 1 % = keine Vorgabe** (übersprungen); fehlt/leer/ungültig die Achse, bleiben die KI-Regeln byte-identisch. Weiche Zusammenstellungs-Vorgabe, kein harter Filter (der läuft über `diaet_hart`). 2 neue Tests (Override + kein-Doppel · 0/keine-Achse lässt KI-Regeln stehen); ConceptGenerator + PlanningCascade 65/65 grün. **Offen (Teil 3b):** `menue_balance` → Zusammenstellungs-Direktive. Real-Abnahme (LLM/Assembler nutzt die Quote spürbar) auf demo offen.
  - [x] **Teil 3b — Portfolio-Balance → Zusammenstellungs-Direktive:** die Concept-Tab-Achse `menue_balance` (ausgewogen/fokussiert) landet als selbsterklärender `menue_zusammenstellung`-Block im KI-Gerüst-Prompt.
    → `d0bc649` (`gebaut`, Sandbox `getestet`): letzte Menü-Achse konsumiert. **Bewusst KEIN Frame-Kopf/Slot/Rule** — anders als Preis/Gänge/Diät-Quote ist »Balance« nichts Messbares am Gerüst, sondern eine WEICHE Vorgabe, wie breit das Menü über Proteine/Warengruppen/Garmethoden streut. Neuer Helper `menueBalanceDirektive(achsen)` mappt den Enum-Wert auf eine self-describing Direktive (ausgewogen = breit streuen, Hauptzutat/Garart nicht wiederholen · fokussiert = ein roter Faden) und hängt sie als `$kontext['menue_zusammenstellung']` an — der Prompt serialisiert den ganzen Kontext als JSON (`AiGatewayService:150`), der Block landet also beim LLM. **Die zwei Stile spiegeln 1:1 das UI-Enum `MENUE_BALANCE`** — kein erfundener Schwellwert. Nur ein gültiger Enum-Wert erzeugt den Block; leer/fremd = Prompt byte-identisch. `menue_balance` floss über den `menue_`-Prefix-Filter (`PlanningCascadeService:161`) bereits ins `$menueAchsen`-Array. 2 neue Tests (Prompt-Spy: Direktive im Prompt bei ausgewogen · kein Block bei fremdem Wert); ConceptGenerator 13/13 + Cascade/Leitstelle 78/78 grün. Real-Abnahme (LLM nutzt die Direktive spürbar) auf demo offen. **Damit sind alle vier Concept-Menü-Achsen (Preis/Gänge/Diät-Quote/Balance) an Frame bzw. Prompt verdrahtet → Parent-Punkt zu.**
- [x] Klar trennen: Rezept-Leitplanken (propagieren an Gerichte/Basisrezepte) vs. Menü-Leitplanken (steuern die Zusammenstellung)
  → `2b6762a` (`gebaut`, Sandbox `getestet`): der Fan-out-Erbe `PlanningCascadeService::sessionGenerationParams` reichte die vollen Session-`generation_params` **inkl. `menue_*`** an die Rezept-Erzeugung durch (`materialisiereConceptGericht` + `materialisiereSpeiseplanZelle`, beide Gericht-Fan-outs). Die Menü-Leitplanken (Gänge/Preis-Korridor je Person/Diät-Quoten/Balance) steuern aber die ZUSAMMENSTELLUNG (Concept-Ebene, gelesen beim Concept-Dispatch) und sind für ein einzelnes Gericht bedeutungslos — im Rezept-Prompt serialisiert würden sie die Generierung verfälschen. Fix: der Erbe filtert `menue_*` heraus (Präfix-Filter); die Rezept-Leitplanken (Niveau/Convenience/Frische/Bio/Diät/Aroma/…) propagieren unverändert. Model-Kommentar präzisiert (`menue_*` propagiert NICHT in den Rezept-Fan-out). 2 neue Tests (Rezept-Achse kommt an · kein `menue_*`-Key im Gericht-Prompt, Rezept-Achse bleibt); `PlanningCascade`+`PlanungLeitstelle` 80/80 grün. **Damit ist Etappe 2a (Concept-Leitplanken nachziehen) inhaltlich zu** — offen bleiben in Et.1 nur die beiden blockierten Punkte (Auto-Zerlegung = Offene Entscheidung, Blindtest = Real-LLM-Abnahme). Real-Abnahme auf demo offen.

**2b — Kreativ-Kopf: Concept-Plan aus Briefing** (der „alte Plan", voll eingebaut):
- [x] Prompt `concept.plan` (tier B, in `config/foodalchemist.php` + `PromptRegistryTest::REGISTRY_SOLL` + `FOOD_DNA_KEYS`) — füllt die **Canvas** (name_claim/Leitidee/USP/Inszenierung/Geschmackswelten) aus dem Brief. Wissens-Routing `concept.plan` ist schon geseedet.
  → `67a7e7a` (`gebaut`, Sandbox `getestet`): neuer Tier-B-Prompt `concept.plan` als **Schwester von `concept.brief_geruest`** — Gerüst erzeugt den deterministischen Rahmen (Slots/Preise/Regeln), Plan die **kreative Handschrift** (`werte = {name_claim, leitidee, usp_eignung, inszenierung, geschmackswelten:[{claim,description}]}`, 1:1 die kreativen Felder von `CanvasService::TEMPLATES['concept']`). System-Prompt: kreativ, DARF aus dem Brief ausformulieren (anders als der Klassifikator-Grundsatz), bleibt aber im Brief-Rahmen und erfindet **keine harten Fakten** (Preise/Portionen/Allergen-/No-Go-Vorgaben → die kommen aus dem Frame/Gerüst). In `FOOD_DNA_KEYS` (Concept-Leitidee = Marke → erbt die Team/Kunde/Foodbook-DNA-Kaskade) + `REGISTRY_SOLL`. **Reiner Contract** — die Service-Verdrahtung `ConceptGeneratorService::planAusBrief` ist der nächste Bullet. Neuer `ConceptPlanPromptTest` (Registry-Shape + Prompt↔Canvas-Feld-Gegenprobe + FOOD_DNA-Membership) + `PromptRegistryTest` 6/6 grün. Real-Abnahme (LLM füllt die Canvas spürbar) auf demo offen.
- [x] `ConceptGeneratorService::planAusBrief(team,$brief,$extra)` — legt Draft-Concept an + **Frame** (Reuse `geruestAusBriefFuerOwner`: Gänge/Zielpreis/Diät) + **Canvas** (`concept.plan`) + materialisiert die Concept-Slots (Fan-out-Ziele)
  → `3d76ab2` (`gebaut`, Sandbox `getestet`): der Service hinter dem KI-Kopf. **Draft-Concept** (`created_via=concept_plan_*`, Brief als `description`) → **Frame** via Reuse `geruestAusBriefFuerOwner` (Slots/Preise/Diät-Regeln aus dem Brief; `$extra` = Segment/Marken-DNA fließt an Gerüst- UND Plan-Prompt) → **kreative Canvas** aus `concept.plan` (`name_claim/leitidee/usp_eignung/inszenierung` als Skalare + `geschmackswelten` als repeatable: `claim`=Überschrift→`value`, `description`→`meta`, leere Welt übersprungen) → **LEERE Fan-out-Slots** je Frame-Slot (N=`target_count`, Typ-Default `gericht`, kein `sales_recipe_id/package_id` → exakt der `fanoutConceptInvention`-Filter). **Zwei tragende Entscheidungen:** (1) anders als `generiereAusBrief` läuft KEIN Assembler — die Slots bleiben leer für die spätere Kaskaden-Erfindung (dieser Service macht nur die Vorbereitung, startet KEINEN Fan-out). (2) **Fail-soft/Pflicht-Trennung:** ein leerer/gescheiterter `concept.plan` kippt den Draft NICHT (Canvas = Kür, `plan_confidence=null`), aber ein gescheitertes Gerüst (Pflicht) räumt den frisch angelegten Draft + evtl. Rumpf-Frame wieder ab statt einen Leer-Rumpf zu hinterlassen. Kein Migration (Prompt=config, Slots über `addSlot`). 2 neue Tests (Voll-Pfad: Canvas gefüllt + 3 leere Fan-out-Ziele, nichts befüllt · fail-soft: gescheiterter Plan lässt Concept/Frame/Slot stehen, Canvas leer); `ConceptGeneratorTest` 18/18 + `PlanningCascade`/`ConceptGeneratorConvenience` 60/60 grün. **Wissens-Erdung** (`316e51a`, Backlog #43 geschlossen): `fuelleCanvasAusPlan` löst die `concept.plan`-Routings (cross_cutting/domain/concept) selbst über `KnowledgeContextService::contextFor` auf und gibt sie als `options['knowledge']` mit — `propose()` lädt nur GEBUNDENES Wissen automatisch, gerouteter Kontext fließt nur aktiv (wie `generiereAusBrief`); zusätzlich `food_dna_concept_id` durchgereicht, damit die Marken-DNA-Kaskade den FOOD_DNA_KEY erdet. **Nächster Chunk:** der „KI-Kopf"-Knopf (`kiKopf()` → `planAusBrief` → inline-Conceptor). Real-Abnahme (LLM füllt Canvas + erfindet spürbar) auf demo offen.
- [x] **„KI-Kopf"-Knopf** neben dem Concept-Briefing → `kiKopf()` → `planAusBrief` → öffnet den inline-Conceptor direkt auf „Konzept & Planung" zur Prüfung/Korrektur (`Concepter\Editor::oeffnen` um optionalen Start-Tab erweitern)
  → `fa25cc0` (`gebaut`, Sandbox `getestet`): `Planung\Index::kiKopf(ConceptGeneratorService, PlanningSessionService)` spiegelt+persistiert das Concept-Briefing (wie beim Go), ruft `planAusBrief` (Draft-Concept + Gerüst + kreative Canvas + LEERE Fan-out-Slots) und öffnet den vollen inline-Conceptor via `concepter-editor.oeffnen` auf dem `'konzept'`-Tab. **Startet KEINE Kaskade** — der geprüfte Plan geht später als `existing_concept_id` in den Go (nächster Bullet). `Concepter\Editor::oeffnen` um optionalen `?string $startTab` erweitert; neue Klassen-Const `TABS` als EINE Quelle für `setTab` UND den Start-Tab (ungültig/fehlend → Aufbau-Default, Step-Zeilen-Öffnen unverändert). Blade: „KI-Kopf — Plan vorab ausarbeiten (optional)"-Sektion (btnGhost, Lade-Zustand) zwischen Briefing und Leitplanken. Reiner UI-/Verdrahtungs-Chunk (kein Migration). **Bekannt (Backlog #45):** `planAusBrief` erbt die Concept-Menü-Leitplanken (Preis/Gänge/Quote/Balance) noch NICHT — die reicht der Schnell-Pfad (`generiereAusBrief`), der geplante Pfad nicht → eigener Chunk. Tests: +2 ConcepterEditor (Start-Tab konzept + Fallback), +2 PlanungLeitstelle (KI-Kopf-Flow via Provider-Stub + Leer-Guard); PlanungLeitstelle/ConcepterEditor 46/46 + PlanningCascade/ConceptGenerator/ConcepterUi/EditorOeffnenVertrag 82/82 grün, Blade-Kompilat gelintet (79 Views, 0 Fehler). Real-Abnahme (LLM füllt Canvas + öffnet spürbar) auf demo offen.
- [x] **Engine-Option `existing_concept_id`** in `starteKaskade` → Concept-Step referenziert das geprüfte Draft-Concept (done + `deferred.fanout`) → bestehender staged-Pfad; **kein neuer Fan-out-Code**
  → `9b02aad` (`gebaut`, Sandbox `getestet`): `starteKaskade` nimmt für den `concept`-Scope `optionen['existing_concept_id']`. **Ownership-Guard (isOwnedBy, strikt `team_id`) VOR der Run-Anlage** — ein Fremd-/Fehl-Concept wirft und hinterlässt keinen Rumpf-Lauf. Neuer privater `referenziereConceptStep`: **kein `GenerateConceptJob`** — der Concept-Step wird direkt `done` (ref auf den geprüften Draft aus `planAusBrief`); im Erfinden-Modus (`voll_kreativ`/`hybrid`) werden die Fan-out-Args wie beim normalen Concept-Job (GenerateConceptJob staged-Zweig) als `deferred.fanout` am Step abgelegt → die Freigabe startet den **bestehenden** Gericht-Fan-out (`FanoutConceptJob` → `fanoutConceptInvention`), **kein neuer Fan-out-Code**. Staged (default): Run → `review`, Fan-out bei Freigabe. Eager (`staged=false`): Fan-out sofort, aber als Queue-Job (LLM raus aus dem Web-Request) — nötig, weil `gibStepFrei` `starteFolgestufe` nur bei `staged` ruft. Reuse-Modus (`datenbank`): kein Fan-out, kein `deferred.fanout`. **Nur die Engine-Option** — die Caller-Verdrahtung (`goKaskade` liest `planConceptId`, `kiKopf` persistiert die Draft-ID, Backlog #47) ist der nächste Bullet („Beide Pfade behalten"). 4 neue Tests (staged/eager/reuse/Fremd-Team-Guard); `PlanningCascade`+`PlanungLeitstelle` 86/86 grün. Real-Abnahme auf demo offen.
- [x] **Beide Pfade behalten:** Schnell (Brief → Go direkt) + Geplant (KI-Kopf → im Conceptor prüfen → Go „aus geprüftem Plan"); fail-soft, damit ein leerer Plan den Draft nicht kippt
  → `c528bf9` (`gebaut`, Sandbox `getestet`): Caller-Verdrahtung der Engine-Option `existing_concept_id` (aus `9b02aad`). Neue **transiente** Prop `Planung\Index::planConceptId` — `kiKopf` merkt nach `planAusBrief` die Draft-Concept-ID; `goKaskade('concept')` liest sie und reicht sie als `existing_concept_id` an `starteKaskade` (→ `referenziereConceptStep`, KEIN neuer `GenerateConceptJob`), sonst SCHNELL-PFAD (frische Generierung via `dispatchConceptStep`). Nach erfolgreichem Start ist die Prop **verbraucht** (→ null): der nächste Go ist wieder Schnell-Pfad; kippt der Start (catch), bleibt sie für einen Retry stehen. **FAIL-SOFT:** eine tote `planConceptId` (gelöscht/Team-fremd) blockt den Go NICHT — der Existenz-/Team-Guard (`FoodAlchemistConcept::where team_id ... exists`) VOR dem Durchreichen verwirft die Referenz still und generiert frisch (kein Hard-Fail durch den Engine-Ownership-Guard). Neue Action `planVerwerfen()` löst die Referenz manuell (zurück auf Schnell-Pfad, löscht das Draft-Concept nicht). Blade: der Concept-Go zeigt den Zustand — geprüfter Plan vorbereitet → „Go aus geprüftem Plan" + „Plan verwerfen"; sonst „Concept erzeugen". 3 neue Tests (voller Pfad kiKopf→Prop→Go referenziert · Fail-soft tote Referenz→Schnell-Pfad · planVerwerfen); `PlanungLeitstelle`+`PlanningCascade` 89/89 grün, Blade-Kompilat gelintet (0 Fehler). Real-Abnahme (LLM erfindet aus geprüftem Plan) auf demo offen.
- [x] **Kein Migration** (Prompt = config, `planConceptId` = transiente Prop) → Deploy = reiner Lock-Pin. Risiko: `fuelleBestehendesKonzept` muss leere Slots anlegen — bei Umsetzung verifizieren.
  → `c528bf9`: bestätigt — der Chunk fügt KEINE Migration hinzu (`planConceptId` ist eine transiente Livewire-Prop, keine DB-Spalte). Das Slots-Risiko ist gegenstandslos: die LEEREN Fan-out-Slots legt bereits `planAusBrief` (`materialisiereLeereSlots`, `3d76ab2`) beim KI-Kopf an; der Go referenziert diesen fertigen Draft nur noch (`referenziereConceptStep`) und legt selbst keine Slots an. Deploy = reiner Lock-Pin. **Damit ist Etappe 2b bis auf den Real-LLM-Blindtest inhaltlich zu.**

### 🖥️ Etappe 3 — (verschoben) → Hauptseite kommt ZULETZT
*Die Hauptseite / Planung-Landing wird bewusst als **letzter Bau-Schritt** geführt („erst wenn der Editor steht und funktioniert"). Der volle UI-Plan steht **ganz am Ende dieser Roadmap** (finale Etappe). Diese Nummer bleibt als Platzhalter, damit die Etappen 4–10 stabil bleiben — keine offenen Punkte hier.*

### 🥉 Etappe 4 — Eingabe-Reife
- [x] **Skizzen-Integration** — Ideen/Skizzen als Kaskaden-Eingang (bisher deferred) — alle Teilschritte (1/2a/2b/3/3b-a/3b-b) `gebaut`/`getestet`; Parent-Häkchen nachgezogen 2026-08-15.
  - [x] **Teil 1 — Skizze → Gericht-Tab (geführter Kaskaden-Eingang):** je Skizze im Divergenz-Board ein Knopf „als Gericht" (`Planung\Index::skizzeAlsGericht`) — überträgt Titel→Titel + Beschreibung→Brief in den Gericht-Tab und springt dorthin (Alpine `tab='gericht'`); der Mensch prüft die Leitplanken und drückt selbst „Go".
    → `137c1ec` (`gebaut`, Sandbox `getestet`): **bewusst Prefill statt Auto-Kaskade** — erfüllt den Nordstern „nichts läuft still" (geführte Freigabe) und reust den voll getesteten `goKaskade`-Pfad, ohne neue Fan-out-Verdrahtung/Migration/LLM-Call. **Scope-Entscheid:** eine Divergenz-Board-Skizze IST eine Gericht-Idee → Gericht-Tab (nicht Basisrezept/Concept). Die Skizze bleibt `entwurf` (Prefill verbraucht sie nicht — erst der Go erzeugt etwas; kein Doppel-Lauf-Risiko). Team-scoped + an die aktive Session gebunden + nicht-verworfen; eine fremde/gelöschte/Papierkorb-Skizze wird gesagt, nicht still verschluckt. Knopf in Einzel- UND Paket-Zeile. 2 neue Tests (Übertragung + entwurf bleibt · verworfene Skizze → fehler + Tab leer); `PlanungLeitstelle`+`PlanningCascade` 91/91 grün, Blade-Kompilat gelintet (0 Fehler). **Offen (Teil 2):** Kaskaden-Status zurück auf die Skizzen-Karte (läuft/prüfen/fertig) + Lineage. **Teil 3:** KI-Divergenz-Skizzen als Batch-Kaskaden-Eingang. Real-Abnahme auf demo offen.
  - [x] **Teil 2a — Lineage (Skizze ↔ Lauf):** der Gericht-Go aus einer übertragenen Skizze stempelt den Kaskaden-Lauf auf die Ursprungs-Skizze zurück (`origin_dish_idea_id`) — Voraussetzung für die Status-Rückkopplung auf die Karte.
    → `0ef15d9` (`gebaut`, Sandbox `getestet`): neue **nullable, indexierte Spalte `origin_dish_idea_id`** an `foodalchemist_cascade_runs` (Migration `2026_08_15_000010`, additiv/idempotent). **Bewusst KEIN DB-FK/`constrained()`** — loser Zeiger (der Lauf überlebt die Skizze) + SQLite-Harness-Kompatibilität (kein FK-`ALTER`); Beziehung lebt am Model (`FoodAlchemistCascadeRun::originDishIdea`). `starteKaskade` nimmt `optionen['origin_dish_idea_id']` und stempelt es am Run-`create` (0/fehlend = kein Ursprung, Bestandsverhalten). `Planung\Index`: **transiente Prop `skizzeGerichtId`** (Muster wie `planConceptId`) — `skizzeAlsGericht` merkt die Skizze, `goKaskade('gericht')` reicht sie **fail-soft** durch (tote/verworfene/Team-fremde Skizze kippt den Go NICHT — Prop still verworfen, Lauf ohne Herkunft) und verbraucht die Prop nach erfolgreichem Start. Reine Lineage — die **Anzeige** auf der Karte ist Teil 2b. 3 neue Tests (voller Pfad: Prop gesetzt→Lauf gestempelt→Prop null · Go ohne Ursprung=null · fail-soft verworfene Skizze); `PlanungLeitstelle` 34/34 + `PlanningCascade` 60/60 grün. Real-Abnahme auf demo offen.
    - [x] **Teil 2b — Status auf die Karte:** den Stand des verknüpften Laufs (läuft/prüfen/fertig/fehlgeschlagen) auf der Skizzen-Karte im Divergenz-Board zeigen (nutzt `origin_dish_idea_id` aus 2a) + Kurz-Lineage.
      → `1d269b6` (`gebaut`, Sandbox `getestet`): `Planung\Index::render` löst je sichtbarer Skizze den **jüngsten** verknüpften `FoodAlchemistCascadeRun` über `origin_dish_idea_id` auf (`orderByDesc('id')` → erster Treffer je Skizze gewinnt = Retry-neuester Lauf) und gibt eine Map `idea_id → {run_id,status,scope}` an die View. Blade: farbcodiertes Status-Badge (`▸ läuft`/`▸ prüfen`/`▸ fertig`/`▸ fehlgeschlagen`, Amber/Violett/Emerald/Rose) je Skizzen-Zeile in **beiden** Schleifen (Gruppe + einzel) — eine Skizze OHNE Lauf trägt kein Badge (Karte byte-unverändert). Team-scoped (`visibleToTeam`), keine Migration. 3 neue Tests (review→„prüfen" sichtbar · ohne Lauf kein Badge · jüngster Lauf `done` schlägt älteren `failed`); `PlanungLeitstelle` 37/37 + `PlanningCascade` 60/60 grün, Blade-Kompilat gelintet (0 Fehler). Real-Abnahme auf demo offen.
    - [x] **Teil 3:** KI-Divergenz-Skizzen als Batch-Kaskaden-Eingang.
      → `7f35883` (`gebaut`, Sandbox `getestet`): neue Action `Planung\Index::skizzenBatchAlsGerichte` — ein Klick startet für ALLE bearbeitbaren Session-Skizzen (nicht `verworfen` + kein `sales_recipe_id`-Bestandszeiger, das wäre Reuse statt Brief) auf einmal je einen **gestuften** Gericht-Lauf (staged → hält bei „prüfen"; geführte Freigabe je Stück = Nordstern „nichts läuft still"). **Reust `starteKaskade('gericht', …)` je Skizze — kein neuer Fan-out-Code**; jeder Lauf trägt `origin_dish_idea_id` → der Stand erscheint je Skizzen-Karte (Teil 2b), **kein Cockpit-Hijack** (`laufId`/`laeuft` unangetastet). Gericht-Tab-Leitplanken (`reglerParams`) gelten für den ganzen Batch (Start-Tab-Regel), Ziel-VK-Tippfehler wird GESAGT. Cap **`BATCH_SKIZZEN_MAX=12`** (Runaway-/Kosten-Guard, spiegelt die `kiDivergenz`-Obergrenze) → darüber gedeckelt + gesagt. Einzel-Fehlstart kippt den Batch nicht (fail-soft, Log-Warn). Blade: „Alle als Gerichte"-Knopf im Divergenz-Board (nur bei Skizzen sichtbar). 4 neue Tests (Batch stempelt je Skizze · verworfen+Bestand übersprungen · leer→fehler · Cap 13→12+gedeckelt); `PlanungLeitstelle`+`PlanningCascade` 101/101 grün, Blade-Kompilat gelintet (0 Fehler). Real-Abnahme auf demo offen.
      - [x] **Teil 3b-a — gezielte AUSWAHL:** Checkboxen je Skizze statt nur „alle" — der Batch startet wahlweise nur die angehakten Skizzen.
        → `c452c24` (`gebaut`, Sandbox `getestet`): neue Prop `Planung\Index::skizzenAuswahl` (array) + Checkbox je Skizzen-Karte in **beiden** Board-Schleifen (Gruppe + Einzel). Ist die Auswahl NICHT leer, filtert `skizzenBatchAlsGerichte` die Kandidaten auf **genau diese IDs** (Schnittmenge mit den bearbeitbaren: nicht `verworfen`, kein `sales_recipe_id`-Bestandszeiger) — leere Auswahl bleibt beim „alle"-Verhalten (Bestandsfunktion unverändert). Checkbox-Werte kommen als Strings aus Livewire → beim Filtern nach int normalisiert (`(int)` + `filter`). Auswahl nach erfolgreichem Start **verbraucht** (nächster Klick = wieder „alle"); neue Action `skizzenAuswahlLeeren` + auswahl-abhängiger Button-Text „Auswahl als Gerichte"/„Alle als Gerichte" + Zähler + „Auswahl leeren". Spezifische Fehlermeldung, wenn NUR nicht-startbare Skizzen angehakt sind. 3 neue Tests (Auswahl startet nur Angehaktes, Rest unberührt, Auswahl danach leer · nur nicht-startbare angehakt → spezifischer fehler, kein Lauf · `skizzenAuswahlLeeren`); `PlanungLeitstelle` 44/44 + `PlanningCascade` 60/60 grün, kompiliertes Blade gelintet (0 Fehler). Real-Abnahme auf demo offen.
      - [x] **Teil 3b-b (Folge-Chunk):** Live-Polling der Karten-Badges nach dem Batch (heute refreshen sie erst bei der nächsten Livewire-Interaktion, weil der Batch das Cockpit-Polling bewusst nicht anwirft).
        → `a130d22` (`gebaut`, Sandbox `getestet`): render() setzt ein Flag **`skizzenLaufAktiv`** = irgendein per `origin_dish_idea_id` verknüpfter Lauf steht auf `running`. Ist es gesetzt, blendet das Divergenz-Board ein **bare `wire:poll.2500ms`**-Element (`data-skizzen-poll`) ein → `$refresh` re-rendert, `render()` liest `$skizzenLauf` frisch aus der DB → die Karten-Badges kippen live von „läuft" auf „prüfen"/„fertig". **Bewusst nur `running` als Poll-Grund** (transient, Worker-getrieben); `review` wartet auf die Freigabe des Menschen, `done`/`failed` sind terminal → kein Auto-Wechsel → das Poll-Element entfällt und das Polling stoppt selbsttätig. **Kein Einzel-Cockpit-Hijack** (`$laufId`/`$laeuft` unangetastet — der Batch bleibt nicht-blockierend, wie in Teil 3 entworfen). Kein Migration. 3 neue Tests (running → Poll + „▸ läuft" · review → Badge ohne Poll · ohne Lauf → kein Poll); `PlanungLeitstelle`+`PlanningCascade` 107/107 grün. Blade rendert im Livewire-Test durch (Kompilat-Balance bestätigt; view:cache selbst bricht am pre-existing crm-`hasTable`-Boot-Fehler, unabhängig). Real-Abnahme auf demo offen.
- [x] **Brief-Vorlagen** je Sektor/Anlass (Schnellstart statt Blank Page)
  → `52793f4` (`gebaut`, Sandbox `getestet`): **Teil 1** — Const `BRIEF_VORLAGEN` mit 6 kuratierten Starter-Briefings (Catering-Empfang/Flying · Catering-Galadinner · Betriebsgastro-Mittagstisch · Care/Klinik-Mittag · Schule/Kita-Mittag · Restaurant-à-la-carte). `briefVorlage(scope,key)` füllt das Gericht-Tab-Briefing (Starter — ersetzt die Blank Page bewusst) und setzt Sektor/Anlass/Serviceform als Kontext-Vorschlag; der **Titel wird nur gefüllt, wenn leer** (getippter Titel bleibt). `sektor`/`occasion`/`serviceform` **spiegeln 1:1 die Leitplanken-Enums** (leitplanken.blade.php) — keine erfundenen Vokabeln. `vorlagenFuer(scope)` filtert nach Tab; die Blade blendet den Pill-Block nur ein, wo Vorlagen gelten (Teil 1: **nur Gericht** — Basisrezept ist komponentenhaft sektor-agnostisch, Concept eigener Tab). Ungültiger Key/falscher Scope → `fehler`, keine Änderung. **Kein Go** — der Mensch prüft und drückt selbst (Nordstern „nichts läuft still"). Kein Migration. 4 neue Tests, `PlanungLeitstelle`+`PlanningCascade` 111/111 grün, kompiliertes Blade gelintet (0 Fehler). **Offen (Teil 2):** Vorlagen für Concept-Tab (Menü-Briefs) + optional Basisrezept; ggf. Kurz-Preview des Brief-Texts vor dem Laden. Real-Abnahme auf demo offen.
- [x] **Trend-Anbindung** — Trendradar-Signal → vorbefülltes Briefing (Teil 1/2/3 `gebaut`/`getestet`; Parent-Häkchen 2026-08-16, `3c6dbcd`)
  - [x] **Teil 1 — Trend-Brief ins Go-Briefing:** eine aus einem Trend eröffnete Session (Carry-in via `?open=1`) befüllt jetzt das eigentliche Go-Briefing je Tab (`eingabe[scope]`) statt nur die Analyse.
    → `6bed256` (`gebaut`, Sandbox `getestet`): Root-Cause war ein Wiring-Loch — `ausTrend` legte Brief/Titel in die Session, der Deep-Link-Einstieg (`mount` `?open=1` = Trendradar-Carry-in) rief aber nur `ladeForm()`, das den Brief allein in `form` (Analyse-Tab) lud; das Go-Briefing (`eingabe[rezept/gericht/concept]['brief']`) blieb LEER → das Trend-Signal erreichte die Generierung nie (Blank-Briefing trotz Trend-Herkunft). Fix: `ladeForm()` befüllt bei `source_knowledge_document_id !== null` alle drei Tab-Briefings + Titel aus der Session (Helper `seedBriefingAusTrendSession`). **Empty-only** — ein bereits getipptes Tab-Briefing wird NIE überschrieben (Muster wie `briefVorlage`/`skizzeAlsGericht`); der Trend-Brief ist scope-agnostisch formuliert, daher alle Ebenen. Beide Editor-Einstiege (`mount`-Deeplink + `oeffne`) laufen durch `ladeForm` → eine Stelle. **Kein Go** (Nordstern: nichts läuft still) — reiner Prefill. Kein Migration (reine Livewire-Verdrahtung). 3 neue Tests (Trend-Open befüllt alle Tabs+Titel · empty-only kein Clobber · Nicht-Trend-Session bleibt leer); `PlanungLeitstelle`+`PlanningSessionService` 62/62 + `PlanningCascade` 60/60 grün. Real-Abnahme auf demo offen.
  - [x] **Teil 2 — substantiver Brief aus dem Trend-Signal:** heute flowt nur der generische Einzeiler (`Aus diesem Food-Trend … entwickeln: <Titel>`). Den Brief in `PlanningSessionService::ausTrend` aus dem strukturierten Signal bauen (Kategorie/Klasse aus `foodalchemist_trend_meta` + Body-Lead/Kernaussage statt Platzhalter) — deterministisch, keine Erfindung.
    → `9a73cf5` (`gebaut`, Sandbox `getestet`): `ausTrend` zieht jetzt das denormalisierte Trend-Meta (`category`/`trend_class`) und baut den Brief aus zwei zusätzlichen Zeilen: **`Einordnung: <Kategorie> › <Klasse>.`** (Kategorie-Label aus `foodalchemist_trend_taxonomy.description` der globalen/team-eigenen Kategoriezeile = **einzige Wahrheit**, kein hartcodierter Katalog; die Klasse trägt in `trend_class` bereits das lesbare Label — TrendClusterCommand) und **`Kernaussage: <Body-Lead>`** (neuer Helper `bodyLead`: erster Prosa-Absatz nach dem Frontmatter, Überschriften/Listen-Marker übersprungen, interne Umbrüche zu einem Satz, auf ~320 Zeichen mit Ellipse). **Byte-identischer Fallback:** fehlt Meta UND Body-Lead, bleibt der Brief exakt der alte Platzhalter (Bestandsverhalten/Teil-1-Tests unberührt). **Bewusst KEIN maturity/relevance im Brief** — die Spec nennt nur Kategorie/Klasse + Kernaussage; die Enum-Übersetzung säße als zweite Wahrheit in einem Livewire-Const → out of scope. Frontmatter-Strip in `strippeFrontmatter()` extrahiert (von `bodyAuszug` + `bodyLead` geteilt). Kein Migration (reine Lese-Logik). 3 neue Tests (Voll-Signal Einordnung+Kernaussage · nur Kernaussage ohne Meta, keine erfundene Einordnung · Platzhalter-Fallback byte-genau); `PlanningSessionService` 11/11 + `PlanungLeitstelle`+`PlanningCascade` 114/114 grün. **Offen:** der Brief bleibt scope-agnostisch (»Konzept/Gericht/Basisrezept«) — eine ebenen-spezifische Formulierung wäre ein Folge-Chunk. Real-Abnahme (LLM nutzt den reicheren Brief spürbar) auf demo offen.
  - [x] **Teil 3 — ebenen-spezifischer Lead je Tab:** der Teil-2-Folge-Chunk — der agnostische Lead (»ein Konzept/Gericht/Basisrezept entwickeln«) wird beim Seeding in die Tab-Briefings je Ebene geschärft (rezept→Basisrezept, gericht→Gericht, concept→Konzept).
    → `3c6dbcd` (`gebaut`, Sandbox `getestet`): der **Session-Brief bleibt agnostisch** (die Session hat keine Ebene, korrekt) — die Ebene entsteht erst beim Übertragen ins Tab-Briefing. Neue pure/statische `PlanningSessionService::briefFuerScope($brief, $scope)` ersetzt **nur den agnostischen Lead-Nomen-Block per erstem Vorkommen** (`substr_replace`, damit ein gleichlautender Titel nicht mit-editiert wird); Einordnung/Kernaussage bleiben scope-neutral. **Eine Wahrheit:** Nomen-Map (`TREND_SCOPE_NOMEN`) + agnostischer Block (`TREND_NOMEN_AGNOSTISCH`) als Konstanten — `briefAusTrend` baut den agnostischen Lead byte-identisch aus derselben Konstante (Teil-2-Byte-Pin `PlanningSessionServiceTest` unberührt). **Fallback = Bestandsverhalten:** fehlt der Lead (edierter/fremder Brief) oder ist der Scope unbekannt → Brief unverändert (Empty-only-Semantik von Teil 1 bleibt: der Brief ohne Trend-Lead im Empty-only-Test kippt nicht). `seedBriefingAusTrendSession` ruft die Funktion je Tab. Kein Migration (reine String-/Wiring-Logik). 3 neue Tests (scope-spezifischer Lead je Tab · Einordnung/Kernaussage bleiben neutral · Fallback ohne Lead + unbekannter Scope); `PlanningSessionService`+`PlanungLeitstelle`+`PlanningCascade` 127/127 grün. Real-Abnahme (LLM nutzt den geschärften Brief spürbar) auf demo offen.
- [x] **Titel-/Namensvorschlag** aus dem Brief (nüchtern, §-konform) — Teil 1 (Contract `5d3ccda`) + Teil 2 (Service `ae54996`) + Teil 3 (UI `87bc2b3`) `gebaut`/`getestet`; Parent-Häkchen 2026-08-16.
  - [x] **Teil 1 — LLM-Contract:** zwei nüchterne Titel-aus-Brief-Prompts, gespiegelt an der `name_putzen`-Ebenen-Trennung.
    → `5d3ccda` (`gebaut`, Sandbox `getestet`): neue Prompts **`recipe.titel_vorschlag`** (Tier B, §1-Syntax «Typ: Bezeichnung» aus dem Brief) + **`vk.titel_vorschlag`** (Tier B, Pipe-Syntax §4.4 «HG-Code: Hauptkomponente | …»). **Unterschied zu `name_putzen`:** schlägt einen Titel VOR dem Generieren aus dem freien Brief vor, statt einen fertigen Namen zu normalisieren. Beide **nüchtern** (keine Marketing-Adjektive) + **keine Erfindung** über den Brief hinaus (nur benennen, was genannt ist). **Concept bewusst außen vor** — dessen `name_claim` (via `concept.plan`) ist kreativ, nicht „nüchtern". Beide Keys in `PromptRegistryTest::REGISTRY_SOLL`; neuer `TitelVorschlagPromptTest` pinnt §-Syntax + Nüchternheit. **Reiner Contract** — Service (`titelVorschlag`) + UI-Knopf (empty-only Titel-Fill neben dem Briefing, kein Go) folgen. Tests: PromptRegistry + TitelVorschlag 7/7 grün (211 Assertions). Real-Abnahme (LLM emittiert §-konform) auf demo offen.
  - [x] **Teil 2 — Service:** `titelVorschlag(scope, brief)` ruft je Ebene den passenden Prompt (`recipe`/`vk`) und liefert einen Titel-Kandidaten (Brief-leer-Guard, fail-soft).
    → `ae54996` (`gebaut`, Sandbox `getestet`): neuer `TitelVorschlagService::titelVorschlag(scope, brief): ?string` — ebenen-getrennt wie `name_putzen`: `rezept`→`recipe.titel_vorschlag` (§1), `gericht`→`vk.titel_vorschlag` (§4.4-Pipe); `concept` + unbekannte Scopes → `null` (der kreative `name_claim` via `concept.plan` ist nicht »nüchtern«, bleibt außen vor). **Guards:** leerer Brief → `null` **VOR** dem Gateway (kein Provider-Call), fail-soft `catch \Throwable` → `null` (Provider-/Coverage-Fehler kippt nichts), leeres/namenloses KI-Ergebnis → `null` (kein leerer Titel). **Reiner Vorschlag** — nichts persistiert (Teil 3 füllt den Titel nur, wenn leer). Kontext `['brief' => $brief]` (wie `concept.plan`), Ausgabe `werte['name']`. Kein Migration, kein Blade. 6 neue Tests (Prompt-Auswahl je Scope via §1-/§4.4-Stub · concept+leerer Brief ohne Provider-Call via werfendem Stub · fail-soft · leeres Ergebnis) + Teil-1-Contract 10/10 grün. Real-Abnahme (LLM emittiert §-konform) auf demo offen. **Nächster Chunk (Teil 3):** „Titel vorschlagen"-Knopf neben dem Tab-Briefing.
  - [x] **Teil 3 — UI:** „Titel vorschlagen"-Knopf neben dem Tab-Briefing → befüllt `eingabe[scope]['titel']` **nur wenn leer** (Muster `briefVorlage`), kein Go.
    → `87bc2b3` (`gebaut`, Sandbox `getestet`): Livewire-Handler `Planung\Index::titelVorschlagen(scope, TitelVorschlagService)` verdrahtet den Titel-aus-Brief-Service (Teil 2) an die UI: ruft je Ebene den nüchternen §-Prompt und füllt `eingabe[scope]['titel']` **EMPTY-ONLY** (getippter Titel bleibt, wie `briefVorlage`/`skizzeAlsGericht`). Guards: leeres Briefing → gesagt, kein Service-Call; Service-`null` (concept / KI weg / leeres Ergebnis) → fail-soft `fehler`, kein Titel gefüllt. **Kein Go** (reiner Vorschlag). Knopf neben dem Titel-Feld in `erstellen-tab.blade.php` — nur Basisrezept-/Gericht-Tab (der Partial bedient `concept` nicht → deckt sich 1:1 mit der Service-Scope-Trennung); self-contained `@disabled`/`@svg`+`wire:loading`, keine neuen Kontroll-Direktiven. 4 neue Tests (empty-only füllt · getippter Titel bleibt/Service ungerufen · leeres Briefing → fehler · Service-null → fail-soft); Pest 130/130 grün (`PlanungLeitstelle`+`TitelVorschlagService`+`TitelVorschlagPrompt`+`PlanningCascade`), Blade vom Livewire-Render kompiliert. **Damit ist der Parent-Punkt »Titel-/Namensvorschlag« bis auf die Real-LLM-Abnahme zu.** Real-Abnahme (LLM emittiert §-konform) auf demo offen.

### 🍽️ Etappe 5 — Ausgabe-Anbindung (Frame-Kaskaden)
*Die Voll-Kaskade braucht einen Ausgabe-Owner (foodbook/speisekarte/speiseplan) — sie wird aus den Ausgabe-Modulen getriggert, nicht frei im Cockpit. Die Ausgabe-Module sind **Quelle** (liefern das Briefing) **und Ziel** (nehmen das Ergebnis auf) — siehe Zielarchitektur.*

- [x] **Foodbook-Leitstelle** — Voll-Kaskade je Kapitel-Slot (↔ Spec [29](29_Foodbook_Editor_Umbau.md))
  → `0395241` (`gebaut`, Sandbox `getestet`): der Trigger-Pfad war seit P3 (`b494cab`) gebaut — Service `starteVollkaskade` (Frame → 1 Concept-Step je Slot mit `chapter_id` + `GenerateConceptJob` mit Kapitel-Attach) + Livewire `Foodbooks\Index::vollKaskadeStarten` (legt die Review-Session an, startet die Kaskade, leitet in den Planung-Editor). Am **Service-Layer** in `PlanningCascadeTest` gepinnt; es fehlte die **Livewire-Trigger-Deckung**. Dieser Chunk: 2 Tests in `FoodbookLeitstelleTest` (Happy-Path: `created_via=foodbook_vollkaskade`-Session · `scope=vollkaskade`-Lauf am Foodbook · 1 Concept-Step · Job-Attach ans Kapitel · Redirect; Fehlerpfad: Foodbook ohne Gerüst → `kaskadeMeldung`, kein Lauf/Redirect/Job) + Korrektur des stale `PlanningCascadeService`-Klassen-Docblocks (behauptete fälschlich, `vollkaskade` werfe „bis dahin bewusst" — tatsächlich wirft nur der **ownerlose** Aufruf). Pest 77/77 grün. Real-Abnahme (LLM erzeugt Kapitel-Konzepte spürbar) auf demo offen.
- [x] **Speisekarte** — Rubriken → Gerichte-Kaskade (↔ Spec [35](35_Spec_Tagesplan_Cockpit.md))
  → `8e7a938` (`gebaut`, Sandbox `getestet`): Trigger-Pfad war seit P4 gebaut — Service `starteKaskade('vollkaskade', owner_type=speisekarte)` (Frame aus Rubriken → 1 Concept-Step je Rubrik + `GenerateConceptJob` mit Rubrik-Attach) + Livewire `Speisekarte\Index::vollKaskadeStarten` (Frame aus Sections, Review-Session, Kaskade, Redirect). Am **Service-Layer** in `PlanningCascadeTest` gepinnt; es fehlte die **Livewire-Trigger-Deckung**. Dieser Chunk: 2 Tests in `SpeisekarteLeitstelleTest` (Happy-Path: `created_via=speisekarte_vollkaskade`-Session · `scope=vollkaskade`-Lauf an der Karte · 1 Concept-Step je Rubrik · Job-Attach `attachOwnerType=speisekarte` an die Rubrik · Redirect; Fehlerpfad: Karte ohne Rubriken → `kaskadeMeldung`, kein Lauf/Redirect/Job) — 1:1 analog Foodbook (`0395241`). Kein Produktiv-Code. Pest 5/5 grün (+ Regressions-Sweep `PlanningCascadeTest`+`FoodbookLeitstelleTest` 77/77). Real-Abnahme (LLM erzeugt Rubrik-Konzepte spürbar) auf demo offen.
- [x] **Speiseplan** — Zell-Fan-out über die Zeitachse (Cap `SPEISEPLAN_MAX_ZELLEN`)
  → `58b0147` (`gebaut`, Sandbox `getestet`): Trigger-Pfad war gebaut — Service `starteSpeiseplanVollkaskade` (Zyklus `cycle_weeks` × Mo–Fr × Mittag × Linien → je LEERER Zelle ein `gericht`-Step + `MaterializeSpeiseplanCellJob`, Cap `SPEISEPLAN_MAX_ZELLEN=30`, Rest in `params.gedeckelt_zellen_offen`) + Livewire `Speiseplan\Editor::vollKaskadeStarten` (Review-Session, Kaskade, Redirect). Am **Service-Layer** in `PlanningCascadeTest` gepinnt; es fehlte die **Livewire-Trigger-Deckung** (1:1 analog Foodbook `0395241` / Speisekarte `8e7a938`). Dieser Chunk: 3 Tests in `SpeiseplanLeitstelleTest` — Happy-Path (15 leere Zellen → `created_via=speiseplan_vollkaskade`-Session · `scope=vollkaskade`-Lauf am Plan · Gericht-Step + Job je Zelle · kein Deckel) · **Cap-Path** (60 Zellen → 30 Steps + 30 Jobs + `gedeckelt_zellen_offen=30`, kein stiller Deckel) · Fehlerpfad (Plan ohne Menü-Linien → `kaskadeMeldung`, kein Lauf/Redirect/Job). Kein Produktiv-Code. Pest 85/85 grün (+ Regressions-Sweep Foodbook/Speisekarte/PlanningCascade). Real-Abnahme (LLM erzeugt Zell-Gerichte spürbar) auf demo offen.
- [x] Ausgabe-Voll-Kaskaden bleiben `staged=false` (eager, Sammel-Review) — bewusste Unterscheidung dokumentieren
  → `e8cbf99` (`gebaut`, Sandbox `getestet`): die Unterscheidung war **implicit-by-omission** — `starteVollkaskade` + `starteSpeiseplanVollkaskade` legten den Run ohne `staged`-Key an und verließen sich auf den DB-Default (`false`, Migration `2026_08_14_000001`). Fragil: ein geänderter Default hätte die Ausgabe-Kaskaden still gestuft gemacht. Fix: beide Methoden setzen `'staged' => false` jetzt **explizit** (Verhaltens-No-op) + erklärender Kommentar — eager / **Sammel-Review** (alle Slot-Konzepte/Zell-Gerichte feuern sofort, gemeinsame Prüfung im Editor), im Gegensatz zu den **gestuften** Cockpit-Scopes (rezept|gericht|concept, `staged=true` per Default in `starteKaskade`, Gate + Freigabe je Ebene). Gepinnt: die drei vollkaskade-Happy-Paths (foodbook/speisekarte/speiseplan) assertieren `staged=false`; neuer dedizierter **Kontrast-Test** stellt Cockpit-gestuft ↔ Ausgabe-eager an einer Stelle gegenüber. Reine Dokumentation/Härtung, kein Migration, kein Blade. Pest 146/146 grün (`PlanningCascade`+`PlanungLeitstelle`+`Foodbook`/`Speisekarte`/`Speiseplan`-Leitstelle, 556 Assertions).

### 💶 Etappe 6 — Kosten & Marge inline
- [x] **EK/VK/Marge je Stufe** im Worker-Cockpit sichtbar (nicht erst nach Speichern)
  → `315d963` (`gebaut`, Sandbox `getestet`): `Index::render` bündelt je Rezept-/Gericht-Step (`ref_type=recipe`, Status done/freigegeben/skipped) das Rezept per `whereIn` (kein N+1 über die Blade-Step-Schleife) und ruft den EINEN Kalkulations-Bündler `SalesRecipeService::cockpit` (→ `MargeService`; GL-02 I9: VK/Marge sind **abgeleitet**, nicht persistiert → keine neue Rechenlogik, kein Migration). Map `ref_id → {EK gesamt · VK netto · Marge % · Wareneinsatz % · Ampel · formel_fehlt}` fließt an die View; `step-zeile` rendert eine kompakte EK/VK/Marge-Kachel schon am **Draft** (nicht erst nach dem Speichern/Öffnen im VK-Editor), Ampel färbt Marge/Wareneinsatz (grün=auf/unter Ziel · gelb=drüber · rot=>50% drüber · grau=unbekannt). **Concept-Steps tragen keine Rezept-Marge** (Menü ≠ Rezept) → bewusst nicht gerechnet. Un-bepreiste Drafts zeigen ehrlich »noch nicht bepreist« / »Formel fehlt« statt einer erfundenen Zahl. **Reiner Anzeige-Consumer.** 3 neue Tests (Kachel am Gericht-Draft mit EK 2,50/VK 10,00/Marge 75,0/WE 25,0 · Concept ohne Kachel · un-bepreist zeigt Platzhalter); `PlanungLeitstelle` 63/63 + `PlanningCascade` 61/61 grün, Blades im Livewire-Render kompiliert. **Offen:** Zielpreis-Korridor-Propagation + Margen-Gate + EK-Unvollständigkeits-Marker (eigene Bullets unten). Real-Abnahme (echte Marge-Zahlen am LLM-Draft) auf demo offen.
- [x] **Zielpreis-Korridor** propagiert Concept → Gericht (aus Frame)
  → `db1cdf0` (`gebaut`, Sandbox `getestet`): `materialisiereConceptGericht` leitet für jedes erfundene Concept-Gericht einen **Ziel-VK** (netto/Portion) aus dem Concept-Frame ab und reicht ihn als `ziel_vk_eur` in die Rezept-Params — der `EnrichRecipeJob` misst das Gericht damit gegen ein echtes Ziel (`RecipeOneShotService::wirtschaftlichkeitsGlied`) statt gegen null. Neuer Helper `conceptGerichtZielVk(team, slotId)`, Quelle in Reihenfolge (keine Erfindung): (1) **Preis-Anker des passenden Frame-Slots** (per Gericht, via Concept-Slot-`role` = Frame-Slot-`label` gematcht) — die verlustfreie per-Gericht-Angabe, die der Frame direkt trägt; (2) sonst der **Frame-Kopf-Zielpreis je Person** (`target_price_pp`) gleichmäßig auf die Positionen verteilt (`target_price_pp / Σ target_count`) — ein Menü-Zielpreis je Person wird nur durch die Zahl der Gänge zu einem per-Gericht-Wert, die Gleichverteilung ist eine bewusste, **dokumentierte Allokations-Annahme** (kein erfundener Datenwert) und greift nur ohne Slot-Anker. Kein Ziel-Slot/Frame/Preis oder ein bereits gesetztes `ziel_vk_eur` (explizite Rezept-Leitplanke) → unangetastet (Bestandsverhalten). **Bewusst nur der Concept-Fan-out** (nicht die Speiseplan-Zelle — anderer Owner/Zeitachse). **Präzedenz-Frage (Backlog #88):** überschreibt der Et.2-Kopf-Korridor stale KI-Slot-Anker? — Policy-Entscheid für Dominique. 3 neue Tests (Slot-Anker gewinnt vor Kopf · Kopf-Fallback 60/3=20 · ohne Frame ungesetzt); `PlanningCascadeTest` 64/64 grün. Real-Abnahme (echter Ziel-VK-Vergleich am LLM-Draft) auf demo offen.
- [x] **Margen-Gate** — Warnung bei Freigabe unter Aufschlagsklasse
  → `2cb8f1b` (`gebaut`, Sandbox `getestet`): `gibStufeFrei` prüft die anstehenden Rezept-Drafts VOR dem Scharfschalten (danach sind sie `freigegeben`, nicht mehr `done`) gegen ihre Aufschlagsklasse. **»unter Aufschlagsklasse« = ein MANUELLER VK, der den Klassen-Vorschlag unterschreitet** (`vk.source=manuell` und `sales_net < vorschlag.sales_net`) — trifft es zu, setzt die Freigabe eine sichtbare amberfarbene `margenWarnung` mit den betroffenen Positionsnamen. **Reine Rückkopplung, keine harte Sperre** (Nordstern: der Mensch entscheidet). Reuse `SalesRecipeService::cockpit` (die EINE Kalkulations-Wahrheit, GL-02 I9) — keine neue Rechenlogik. **Nicht geraten:** Auto-VK (`source=class`) trifft die Klasse exakt → nie drunter; ohne Vorschlag (kein EK/keine Portionierung/Formel fehlt) gibt es keine Klassen-Schwelle → keine Aussage. **Bewusst am Klassen-Vorschlag statt an der Food-Cost-Ampel** (Team-Ziel-Wareneinsatz oft ungepflegt → Ampel `unbekannt`, Backlog #87); die Schwelle trägt das Rezept über seine `markup_class`. 3 neue Tests (Unter-Position warnt namentlich, saubere nicht · saubere Freigabe still · Auto-VK + fehlender Vorschlag lösen nichts aus); Pest 130/130 grün (`PlanungLeitstelle`+`PlanningCascade`), Blade durch den Livewire-Render-Test (`data-margen-warnung`) verifiziert. Real-Abnahme (echte Klassen-Unterschreitung am LLM-Draft) auf demo offen.
- [ ] Unvollständige Bepreisung sichtbar markieren (EK unvollständig)

### 🖼️ Etappe 7 — Bild & Medien
- [ ] KI-Fotos je Stufe opt-in (Produkt + Schritte), Kosten-Transparenz je Call
- [ ] Bild-Status im Cockpit (erzeugt/fehlgeschlagen/„neu erzeugen") — analog Anreicherungs-Badge
- [ ] Foto-Wiederverwendung / manueller Upload als Alternative

### 🛡️ Etappe 8 — Robustheit & Skalierung
- [ ] **Worker-Präsenz** ist mission-critical — Health-Anzeige + Doku (kein `queue:work` = Leitstelle produziert nichts)
- [ ] **Fan-out-Cap Concept** — Concept-Erfinden hat KEINEN harten Deckel (Speiseplan schon: `SPEISEPLAN_MAX_ZELLEN=30`) → Runaway-/Kosten-Risiko bei großem Menü-Brief. Analogen Cap für den Concept-Fan-out (`fanoutConceptInvention`) einziehen.
- [ ] **Save-Race Multi-Editor-Fan-out** (verify) — der Kaskaden-Fall mountet mehrere `IngredientEditor` gleichzeitig; prüfen, ob der MVP-046-Fix (eindeutige Editor-ID) auch bei parallel gemounteten Editoren hält — sonst überschreiben sich Draft-Speicherungen.
- [ ] **Fehler-Transparenz** — jeder geschluckte Job-Fehler wird sichtbar (Anreicherung ✅ erledigt; Fan-out/Images noch prüfen)
- [ ] **Idempotenz/Resume** — abgebrochene Kaskade sauber fortsetzbar
- [ ] **Multi-Tenancy** — Reads `visibleToTeam`, Writes `isOwnedBy` konsequent (Audit)

### 🔌 Etappe 9 — MCP & Automatisierung
- [ ] MCP-Tools für die Kaskade (Start/Freigabe/Status) — headless-fähig, im Lockstep mit dem UI
- [ ] Prompt-/Tool-Inventar in [26_LLM_MCP_Funktionsmatrix](26_LLM_MCP_Funktionsmatrix.md) nachziehen

### 📚 Etappe 10 — Abnahme, Betrieb & Doku
- [ ] **Real-LLM-Abnahme** Etappe 0 (s. o.) — durch Dominique auf demo
- [ ] **Benutzerhandbuch `planung.md`** — das Modul hat noch keinen Handbuch-Eintrag (Lücke)
- [ ] **Business-Case-Funktionen** in [25](25_Business_Case_Funktionsmatrix.md) mit Abnahmeevidenz eintragen
- [ ] demo → Produktiv-Team, Worker-Daemon abgesichert

---

## 🔀 Offene Entscheidungen
- **Zerlegungstiefe:** Wie tief zerlegt ein Gericht (max. Ebenen)? Regelwerk sagt Guideline 3, hart nur Zyklus/Selbstbezug.
- **Bild-Kostenpolitik:** KI-Fotos default aus — pro Stufe, pro Kaskade oder global schaltbar?
- **Standalone-Zerlegung:** Soll auch die Nicht-Kaskaden-Gericht-Erstellung (VkGenerator) automatisch zerlegen, oder bleibt Zerlegung Kaskaden-exklusiv?
- **Concept-Plan-Pflicht:** Ist der KI-Kopf (Etappe 2) optional (beide Pfade) oder der neue Standard?

## ⚠️ Risiken & Abhängigkeiten
- **Deploy:** `update.sh`/`composer update` tot am `platforms-avatar`-404 → Deploy nur via **Lock-Pin** auf FA-`main`-HEAD (main == Feature, sonst setzt `dev-main` zurück). Migrationen laufen NICHT automatisch mit `update.sh` — Forge migriert beim Push, sonst `php8.4 artisan migrate --force`.
- **Blade-Direktiven:** geklebte `@if`/`@endif` sind für Blade unsichtbar → Blade-Änderungen immer durch **Linten des kompilierten PHP** verifizieren.
- **Provider-los in Sandbox:** Generierungs-Qualität nur mit echtem LLM auf demo bewertbar.

## 📎 Verweise
- [Regelwerk Basisrezepte] = `07_WISSEN/.../Regelwerke/Regelwerk_Basisrezepte.md` (Vault, §2 Verarbeitungs-Reduktion · §3 Pürees · §4 Sub-Rezept-Hierarchie)
- Specs: [27 Step-by-Step](27_Spec_Step_by_Step_Zubereitung.md) · [29 Foodbook-Editor](29_Foodbook_Editor_Umbau.md) · [30 Produktion](30_Produktion_Ausbau.md) · [35 Tagesplan-Cockpit](35_Spec_Tagesplan_Cockpit.md) · [37 KI-Erstellen Typ/Niveau](37_Spec_KI_Erstellen_Typ_Niveau.md)
- Steuerung: [24 Umsetzungsplan](24_Zielbild_2029_Umsetzungsplan.md) · [23 MVP-Audit](23_MVP_Audit.md)

---

## 🖥️ FINALE ETAPPE — Hauptseite / Planung-Landing (Arbeitsgrundlage)

*Der LETZTE Bau-Schritt — erst wenn der Editor/Cockpit steht und rund läuft. Die Hauptseite muss Design & Flow des fertigen Editors spiegeln (nicht zwei Welten). Basis = die aktuelle Landing: „Planungen"-Liste links · „Neu erstellen" (Basisrezept/Gericht/Concept) · „KI-Leitstelle"-Intro · „Zuletzt"-Karten · Details-Panel rechts. Steht bewusst ganz am Ende, weil die Roadmap die Arbeitsgrundlage der Routine ist (top-down → Hauptseite zuletzt).*

**Linke Spalte — „Planungen"-Liste**
- [ ] Status-Badge je Eintrag (Entwurf · läuft · prüfen · freigegeben · fertig) statt nur Divergenz/Konvergenz
- [ ] Gruppierung/Filter (Kategorie · Herkunft · Typ Basisrezept/Gericht/Concept) + Suche
- [ ] laufende Planung optisch hervorheben (Puls bei `running`)

**Mitte — „Neu erstellen" + „Zuletzt"**
- [ ] „Neu erstellen"-Leiste im Editor-Look; Trend/Skizze klar als EIN Input dahinter (nicht der Rahmen)
- [ ] „Zuletzt"-Karten mit Status + Kaskaden-Fortschritt (z.B. „Gerichte 1/1 · Basisrezepte 0/3") + Direkt-Aktionen (Öffnen · freigeben · duplizieren · verwerfen)
- [ ] verwaiste Entwürfe (ohne Lauf) sichtbar machen

**Rechte Spalte — Details-Panel**
- [ ] bei Auswahl: Herkunft/Lineage · Status · Kaskaden-Stand je Stufe · Skizzen · „Im Editor öffnen"
- [ ] Kaskaden-Kurzstatus (welche Stufe offen/freigegeben/angereichert) ohne den Editor zu öffnen

**Gesamt**
- [ ] Design-/Flow-Parität zum Editor-Cockpit (gleiche Sektionen/Farben/Abstände)
- [ ] Verifikation: Sandbox-Render + kompiliertes-Blade-Lint; Real-Abnahme auf demo durch Dominique
