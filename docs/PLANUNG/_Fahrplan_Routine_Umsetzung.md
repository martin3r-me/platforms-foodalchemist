# Fahrplan — Autonome Umsetzung Rest-Programm (Routine-Arbeitsdokument)

> Einzige Fortschritts-Wahrheit der Routine `fa-specs-umsetzung` (alle 30 Min, ein Chunk pro Run). Vor jedem Run lesen (inkl. Stand-Log), dann nächstes offenes Stück gemäß Reihenfolge wählen. Checkbox erst abhaken, wenn Tests real grün.
>
> **Phase 0 wurde manuell auditiert und abgeschlossen (2026-07-25, HEAD `a55ced3`)** — die Reihenfolge unten ist das *Ergebnis* dieses Audits, nicht die ursprüngliche Annahme. Detail-Status je Spec: [_Spec_Status_Matrix.md](_Spec_Status_Matrix.md).

## ⚙️ Bau-Rahmen (vor jedem Chunk beachten)
- **Test-Gate braucht erhöhtes Memory-Limit:** `php -d memory_limit=1G vendor/bin/pest` aus der Sandbox. Im 128M-Default crasht die Suite mit *Fatal: memory exhausted* (Blade-Render + Whoops) — das ist **kein** Code-Fehler und darf nicht als solcher interpretiert werden.
- **Verifizierte Baseline 2026-07-25 (HEAD `a55ced3`): 1169 Tests · 1165 passed · 4 skipped · 0 Fail · 5558 Assertions.** Jede Regression ist ab hier zu messen. **Fortgeschriebener Ist-Stand 2026-07-26 nach Lauf 29 (03·L7b-2b): 1437 Tests · 1433 passed · 4 skipped · 0 Fail · 6774 Assertions** — die Differenz sind die Tests der Läufe 1–29 selbst (Tranche A/C/D/P/E + Spec 03 L5/L2/L4/L1/L6).
- **Laufzeit-Budget:** die Volle Suite braucht **~8,6 Min** (516 s). Bei 30-Min-Ticks bleibt nach Abzug von Suite + Commit/Push realistisch **~15–18 Min Bauzeit**. Darum: während der Arbeit gezielt filtern (`--filter=<Test>`), Voll-Suite erst am Chunk-Ende. Chunks entsprechend klein schneiden.
- Dominique committet parallel (Beleg: `2f54d15`/`a55ced3` während des Audits) → `git pull` vor Arbeit, **nur eigene Dateien** gezielt stagen.
- Kein externer Menschen-Blocker mehr (LLM-Key + Deploy self-service). Kein Deploy aus der Routine.
- **Verbesserungs-Beobachtung je Lauf → [_Verbesserungs_Backlog.md](_Verbesserungs_Backlog.md).** Was über den Auftrag hinaus auffällt (Muster, Level-up-Chancen, Regelwerk-Drift, MCP-/Test-Lücken) wird dort notiert — max. 1–3 Einträge, konkret mit Datei/Zeile, Status immer `neu`. **Nie selbst umsetzen** (Scope-Drift-Schutz); über Übernahme entscheidet nur Dominique. Ergebnis nach Gesamtlauf = fertige Verbesserungs-/Anpassliste.

---

## ✅ Phase 0 — Reconcile & Audit (ABGESCHLOSSEN 2026-07-25, manuell)
- [x] `_Spec_Status_Matrix.md` aktualisiert: Blocker-Reset im Header, Zeilen 03/08/15/19 an HEAD verifiziert, „Verdichtet"-Block mit Größen neu geschrieben.
- [x] **Overlap-Audit an HEAD** — Ergebnis:
  - **L3 ✅ via Spec 19** (Kickoff in `Foodbooks/Index`+`PlanningFrameService`+`FoodbookService` + `generiereAusBrief`) → aus dem Bauvorrat gestrichen.
  - **L2 ist NICHT absorbiert** (Button weiter `disabled` in `foodbooks/index.blade.php:226`, Prompt `foodbook.kapitel_text` fehlt) → bleibt zu bauen. *(Widerlegt die ursprüngliche Annahme.)*
  - **Spec 08 Divergenz-Hälfte ✅ via 19·E9** (Kreativ-Modi, `dish_ideas`, `PairingInspirationService`) → 08 schrumpft auf **P6**.
  - **RAG (02) ✅ gebaut** — `src/Services/Ai/SemanticRetrievalService.php`, verdrahtet in `IngredientMatchService:31/35`.
  - **12·R2.4 = XL** (ersetzt greedy Assembler `ConceptGeneratorService:152`), **13·S1 = L** — nicht die Quick-Wins der alten Liste.
  - **15**: Blocker = ~50k-Vektor-Grenze `MySqlJsonEmbeddingStore`, nicht „Vektor-DB allgemein"; Backfill+Observer jederzeit baubar.
- [x] `00_Orchestrierung_Naechste_Schritte.md` als **SUPERSEDED** markiert (Banner mit den 3 überholten Punkten + Verweis hierher) statt zu flicken — die Datei ist ein 07-18-Phasenplan, der lebende Plan ist dieses Dokument.
- [x] Audit-Ergebnis in die Spec-Dateien selbst geschrieben: Tabelle je Lücke in `03_KI_Erstell_Flaechen_512.md`, Schrumpf-auf-P6 mit Code-Belegen in `08_Planungs_und_Kreativ_Ebene.md`.

## ✅ Phase 1 — Spec 21 Signal-Ausbau (KOMPLETT 2026-07-26, alle Tranchen A/B/C/D/E/P)
> **Warum zuerst:** die Signale *messen* erst, wie gut die Rezept-/Konzept-/Foodbook-Basis wirklich ist. Danach baut man L1–L8 gegen echte Zahlen statt gegen Vermutungen. Detail + Begründung je Signal: [21_Signal_Ausbau_Qualitaetssignale.md](21_Signal_Ausbau_Qualitaetssignale.md). Die *Heilung* (Spec 05 Etappe 2) bleibt bewusst zuletzt.
- [x] **21·S0** Fundament · S — `SignalTyp`-Enum erweitert (11 Tranche-A-Typen + `istRezeptQualitaet()`), Ebene `rezeptqualitaet` im `DataQualityService` inkl. fixem Schweregrad je Check (`gap(..., $sev)`), erster Check `rezept_zutaten_ungemappt` als Muster (Zähl-Query + `queryFor` + Emission), Fixture-Basis `makeRecipe`/`makeIngredient`/`unitG`. MCP-Lockstep: `signale.SEARCH`/`signale.LIST` Typ-Enum aus `SignalTyp::cases()` statt handgepflegter 7er-Liste
- [x] **21·S1** Tranche A · M — 11 deterministische Rezept-Checks (0-Egress), Fixer-/Assist-Mapping in `SignalCockpit`; Pest je Check mit Positiv- **und** Negativfall (kein Über-Flaggen). **In zwei Teilschritte geschnitten** (M war für einen 30-Min-Tick zu groß):
  - [x] **S1a — die 5 Struktur-Checks** (reine Builder-Prädikate): `rezept_ohne_zubereitung` · `rezept_mengen_luecke` · `rezept_ein_zutat` · `rezept_kategorie_problem` · `rezept_verwaist`; dazu das Tranche-A-**Check-Register** (ein Prädikat je Check, von Zähl-Seite + `queryFor` geteilt) und der Assist-Knopf für `rezept_kategorie_problem`. Mit `rezept_zutaten_ungemappt` aus S0 sind **6 von 11** scharf.
  - [x] **S1b — die 5 Rest-Checks** (brauchen je eigene Mechanik): `rezept_yield_implausibel` (Einheiten-Gate gebaut: Ratio nur bei durchgehend massen-umrechenbaren Zutaten; der Spec-Zweig „< 0,3 × Einsatzmasse" bewusst **nicht** gebaut, s. Stand-Log + V-008) · `rezept_naming_regelwerk` (Muster-Prüfung in PHP statt SQL — dialektfrei by construction; Grammatur nur ohne Diskriminator-Funktion, §1.2a) · `rezept_dublette` (normalisierter Name je Art+Kategorie, beide Seiten geflaggt) · `rezept_allergen_unbelastbar` (kundenexponiert = 4 Kunden-Flächen + eine Sub-Rezept-Ebene) · `rezept_sub_stub_offen` (`status` in stub/draft + 0 Zutaten + referenziert) · Assist `signal.recipe_naming_suggest`
- [x] **21·S2** Tranche E · M — Zeitreihe (`signal_snapshots`) + Rausch-Guard (`signal_policies`) + Drift-Meta-Signal. **Pflicht, nicht optional** — ohne Guard kämen ~900 offene Signale statt 252 und das Cockpit stumpft ab. **In zwei Teilschritte geschnitten** (Migration + Service + MCP war für einen 30-Min-Tick zu groß):
  - [x] **S2a — E1 Zeitreihe:** Tabelle `foodalchemist_signal_snapshots` + Model + `SignalTrendService` (`schreibeSnapshot`/`serie`/`delta`/`uebersicht`) + Fold-in in `SignalDetektorService::laufen()` (Scheduler-Pfad) + `--snapshot` an `foodalchemist:data-quality` + MCP `signal_trend.GET`. **Dichte Reihe** (auch Nullen) und **ein Lauf = ein `measured_at`** sind die zwei tragenden Entscheidungen — s. Stand-Log
  - [x] **S2b — E2 Rausch-Guard + E3 Drift-Meta-Signal:** `foodalchemist_signal_policies` (`type`, `threshold`, `accepted_until`, `note`, `muted`) + aggregierte Zustands-Zeile statt n Einzel-Alarme + Meta-Signal `qualitaet_drift` (neuer `SignalTyp`) über `SignalTrendService::uebersicht()`-Delta. **Gemeinsam bauen**, weil die Policy entscheidet, ob ein Drift überhaupt alarmiert (`muted`/`threshold`) — getrennt gebaut würde Drift zweimal geschrieben. Policy-Setzen bleibt menschlich getriggert (MCP-Write explizit, Spec §8)
- [x] **21·S3** Tranche P · M–L — `Signale/DetailPanel` (heute nutzt die Signale-Seite die rechte Fläche gar nicht; 6 andere Seiten haben ein Panel). Kern-Features: **objekt-zentrische Sicht** („was hat dieses Rezept noch?" → einmal richtig fixen statt dreimal) + **Fix-Dry-Run vor dem Klick**. **In zwei Teilschritte geschnitten** (M–L war für einen 30-Min-Tick zu groß):
  - [x] **S3a — Panel + die Auflöse-Richtungen (Punkte 1+2):** `Signale/DetailPanel` + `signal-selected` + rechte Fläche auf der Signale-Seite (Scope `activity_signale`); `SignalObjectService` löst **beide** Richtungen auf (Signal→Objekte live statt Payload-Abbild · Objekt→Signale per EXISTS je Metrik über neues `DataQualityService::trifftObjekt`); volle Liste bis 300 statt inline 50, sortierbar A–Z/Z–A/Art; die 50er-Inline-Liste unter der Signal-Zeile ist **abgelöst** (eine Wahrheit)
  - [x] **S3b-1 — die Fix-Hälfte (Punkte 3+7):** Dry-Run „n Objekte, diese Felder, diese Werte" **vor** dem Klick (Punkt 3, Kern-Feature) zusammen mit Teil-Bulk-Checkboxen (Punkt 7) — beide auf demselben neuen Teilmengen-Pfad in `SignalFixService`/`SignalFixJob`, der vorher nur alles-oder-nichts kannte. Auswahl wird immer gegen das Metrik-Prädikat geschnitten (keine freie Schreib-Schnittstelle); MCP `signale.FIX` um `object_ids` + `dry_run` mitgezogen
  - [x] **S3b-2 — Policy-Regler (8) + Trend-Sparkline (4):** Regler aus E2 am Signal, wirksam auf den **Typ** (Schwelle / „akzeptiert bis" / stumm / Begründung; beim Aufklappen werden die *wirksamen*, auch geerbten Werte geladen, „Regler entfernen" nur bei eigener Zeile) + Sparkline aus `SignalTrendService::serie` über die **Signal-Seite** der Reihe (damit greift V-010 nicht: rohe DQ-Keys kommen im Panel nicht vor; erst ab 2 Messpunkten wird gezeichnet) + `SignalPolicyService::zustandFuer()` als Ein-Typ-Sicht über dasselbe `zeile()`. **Punkt 5 herausgeschnitten → S3b-3** (s. unten)
  - [x] **S3b-3 — Ursachen-Kette (5):** `br_ek_teil` → unbepreiste GPs → fehlende Lead-LAs (Kette nach unten, im Panel aufklappbar); Regelwerk-Signale → Deep-Link auf das verletzte § (`rezept_naming_regelwerk` kennt den verletzten Muster-Fall schon aus S1b — der Anker fehlt). Anders als 8/4 ist das **neue Auflöse-Logik**, keine fehlende Fläche. **Punkt 6 (Historie/„wie oft wiedergekehrt") ist mit dem heutigen Schema nicht baubar** → V-009 (kein `last_seen_at`/`seen_count` an `foodalchemist_signals`); bewusst NICHT eingeplant, sonst beginnt die Etappe mit einer Migration auf einer Bestandstabelle (und damit greift die fehlende-`mysqldump`-Warnung erstmals scharf)
- [x] **21·S4** Tranche C+D · M — Konzept- + Foodbook-Signale; `foodbook_kapitel_ohne_text` **scharf seit 2026-07-26** (S4c-3, zusammen mit 03·L2b — der Fixer war die Voraussetzung). **In drei Teilschritte geschnitten** (M war für einen 30-Min-Tick zu groß, und die 5 Konzept-Checks haben drei verschiedene Mechaniken):
  - [x] **S4a — Fundament + die zwei frame-freien Checks:** Ebene `konzept` + Check-Register (Bauart wie Tranche A) mit `konzept_slot_luecke` (unbesetzter Pflicht-Slot **oder** kein belegter Inhalts-Slot) + `konzept_ohne_wording` (Wording-Kette fällt auf den internen Pipe-Namen, inkl. Paket-Zeilen); Arbeitsmenge `konzepteInGebrauch()` = aktiv / am Angebot / im Foodbook (Vorlagen + `archiviert` draußen — ein Entwurf ist bewusst unfertig); neuer Objekt-`kind` `concept` durch `queryFor`/`betroffene`/`trifftObjekt`/`signaleAmObjekt` + Concepter-Sprung (`?tab=concepts&sel=`) und „was noch?" im Signal-Panel
  - [x] **S4b-1 — die zwei frame-gestützten Checks:** `konzept_preisband_verletzt` + `konzept_regel_verletzt` **zusammen** aus EINEM `konzepteMitFrameBefund()`-Helfer (Preis-Kopf/Slot-Preisrahmen bzw. `RULE_TYPES`-Befunde, jeweils nur `ampel='verletzt'`); Arbeitsmenge doppelt geschnitten (in Gebrauch **und** mit Gerüst — ohne Soll kein Befund); PHP-Pass wie S1b (`whereIn('id', …)`), bewusst ohne Memoisierung. **`konzept_dramaturgie` herausgeschnitten → S4b-2** (s. unten)
  - [x] **S4b-2 — `konzept_dramaturgie` (info):** gebaut als **Wiederholungs-Hälfte** — gleiche Hauptzutat über mehrere Gänge. Die offene Auslegungsfrage ist **gegen `menuCohesion`** entschieden (dort ist ein geteilter Anker der Höchstwert 1,0 ⇒ Diagnose auf dem Kopf; und seine Anker-Union je Gericht enthält jede Nebenzutat) → neues `PairingService::menuRepetitions()`. Hauptzutat = **mengenmäßig dominierende Zutat** (Einheiten-Gate wie S1b: alle beitragenden Zeilen massen-vergleichbar, sonst unbewertet), identifiziert über den Kern-Anker ihres GP/Sub-Rezepts; Sorten-Nähe über `ankerSlugMatches`, `neutral` zählt nie. **Nicht** über `recipe_anchor_mappings.role='kern'` am Gericht — das ist im Bestand ein Beutel aller Zutaten-Anker (→ V-019, beim MySQL-Gegencheck aufgefallen). **Zweite Spec-Hälfte „kein Kontrast über den Verlauf" bewusst NICHT gebaut** → V-017
  - [x] **S4c-1 — Tranche D Fundament + die zwei struktur-basierten Checks:** Ebene `foodbook` + Register (Bauart wie A/C) mit `foodbook_kapitel_leer` (Blattkapitel ohne sichtbaren `concept_ref`/`recipe_ref`-Block **oder** Buch ohne jedes befüllte Kapitel — zweiter Zweig wie `belegterInhaltsSlot` in S4a) + `foodbook_skizze_ungeerdet` (queued Freitext-Skizze 48 h nach dem Kapitel-Go, **Karenzzeit ist Pflicht**: ohne Provider bleibt sie bewusst queued); **zwei Arbeitsmengen statt einer** (`foodbooksInGebrauch` = Status/Phase≥kalkulation/Versand-Snapshot vs. der Go selbst); neuer Objekt-`kind` `foodbook` durch `queryFor`/`betroffene`/`trifftObjekt`/`signaleAmObjekt` (jetzt `SignalObjectService::KINDS`) + Leitstellen-Sprung (`?fb=`) im Signal-Panel
  - [x] **S4c-2 — Tranche D Rest:** `foodbook_ziel_verfehlt` aus den `chapter_id`-Befunden der Coverage (Ziel-Vererbung n-tief bleibt in `kapitelKettenZiel`, Ist-Rollup in `istFoodbook` — bewusst nicht nachgebaut; nur `ampel='verletzt'`, ohne Gerüst kein Befund — hier **geerbte Lücke**, weil Kapitel-Ziele auch ohne Gerüst gepflegt werden → V-022) + `foodbook_stale` über `VkSnapshotService::pending` (unveränderte Schwelle, dieselbe Wahrheit wie `vk_anpassung_empfohlen`) auf der **dritten Arbeitsmenge** `foodbooksFreigegeben` (= in Gebrauch ohne `kalkulation`: dort sollen Preise sich bewegen). Zweite Spec-Hälfte „Gerichte geändert" **nicht baubar** → V-021 (`snapshot_at`/`snapshot_json` werden von vier Stellen gelesen und nirgends geschrieben); `foodbook_kapitel_ohne_text` bleibt bis **L2** draußen (Signal ohne Fixer = Rauschen, §9)
- [x] *(21·S5 Tranche B steht in Phase 3 — sie ist Batch-Konsument von L6, kein Zweitbau)*

## ✅ Phase 2 — Quick-Wins Spec 03 (KOMPLETT 2026-07-26)
- [x] **L5** `RecipesGenerateTool` (`foodalchemist.recipes.GENERATE`) · S — Vorlage `ConceptsGenerateTool`; `vk: true` als Parameter statt zweitem Tool; `created_via=mcp` (neuer optionaler `$createdVia`-Parameter an `RecipeGeneratorService::generiere` — Default `null`, UI-Pfade byte-identisch), immer draft; Tenancy-Negativtest (#504-Muster). Kein zweiter Erzeugungs-Pfad: dasselbe `generiere()` wie beide Modals, offene Zeilen kommen ehrlich zurück statt geraten
- [x] **L2** Kapitel-Text-KI · S — Prompt in `AiGatewayService`-Allow-Liste + Button an `foodbooks/index.blade.php:226` entsperren; Vorschau + Übernehmen; Überschreib-Schutz. **In zwei Teilschritte geschnitten** (die eine Spec-Zeile deckt zwei Flächen ab, und nur die zweite schaltet das Signal frei):
  - [x] **L2a — Prompt + Foodbook-Einleitung:** Prompt-Key **`foodbook.kundentext`** (nicht `foodbook.kapitel_text` — derselbe Prompt bedient beide Ebenen über `ebene` im Kontext; ein Key pro Fläche hätte die Tonalität zweimal definiert) + in `FOOD_DNA_KEYS`; `FoodbookService::kiKundentextVorschlag` + `kundentextKontext` (Gliederung über die **Wording-Kette** = dieselben Kunden-Labels wie im PDF, nur sichtbare Blöcke, Roh-Briefing als `briefing_ist`, Leitplanken); Button `:226` scharf + **echte Vorschau-Stufe** in `Livewire/Foodbooks/Index` (`kiTextVorschau`/`kiTextUebernehmen`/`kiTextVerwerfen` — der Vorschlag berührt `form.description` nie, „Ersetzen" statt „Übernehmen" wenn schon Text steht). Kein Schema-Change, kein MCP-Nachzug (→ V-024)
  - [x] **L2b — Kapitel-Ebene + Signal:** Kapitel-Text-Feld (`foodbook_chapters.description` ist im Editor heute **gar nicht** editierbar — der Kapitel-Kopf kennt nur title/consumer_title/price_mode) + KI-Button je Kapitel über denselben Prompt (`ebene: 'kapitel'`); danach `foodbook_kapitel_ohne_text` aus Spec 21 Tranche D scharfstellen (Register + Fixer-Mapping + Pest, Muster S4c-1/-2)
- [x] **L4** KI-Slot-Vorschlag im Concepter · S–M — Slot-Ranking aus `ConceptGeneratorService` (`kandidatenPool`/`filterFuerSlot`) als per-Slot-Button; **läuft ohne LLM**; Slot ohne zulässigen Treffer sagt es ehrlich. Gebaut als `slotKandidaten()` (Faktoren + Hinweis), zu dem das bestehende `slotVorschlaege()` **Projektion** wurde (eine Ranking-Wahrheit); Kohäsions-Basis = die schon belegten Positionen inkl. Paket-Gerichte; ohne Gerüst In-Memory-Ersatz statt `frameOderAnlegen` (Lese-Pfad legt nichts an). MCP-Weg fehlt → V-026

## ✅ Phase 3 — Spec 03 Verbund L1+L6 + 21·S5 (KOMPLETT 2026-07-26)
- [x] **L1** VK-Revise · M — `✨ KI-Überarbeiten` (Freitext) + `✨ Alles anreichern` ins `VkModal`; Zutaten über den #508-Pfad (`RecipeService::syncIngredients` + `matchVorschau`), **nicht** duplizieren; Facetten-Erhalt (Servierform/Klasse/Diät). **In zwei Teilschritte geschnitten** (die Spec-Zeile deckt zwei unabhängige Knöpfe ab — Freitext-Revise ist die Strecke, Anreichern ist Wiring an einen Bestands-Service):
  - [x] **L1a — `✨ KI-Überarbeiten`:** die Revise-Strecke ist jetzt **geteilt** statt kopiert — neuer `RecipeReviseService` (`vorschau()` = ex `RecipeModal::matchVorschau`, `syncZeilen()` = ex Inline-Zeilenbau), beide Modals fahren ihn; Prompt `vk.ueberarbeiten` (Tier A, 8000 Tokens) + in `FOOD_DNA_KEYS`. **Facetten-Erhalt ist zweistufig, nicht einstufig:** sie gehen als *Vorgabe* in den Prompt (Klasse/Diätform/Darreichungen/Verkaufseinheit/Portion/Aufschlagsklasse — damit ein Fingerfood handlich und ein veganes Gericht vegan bleibt) und der Schreib-Umfang kennt sie gar nicht (Whitelist `REVISE_TEXTE` = description/plating_text/sales_wording_standard + Komponenten-Sync; `role` je Zeile bleibt aus dem Original). Nebenbefund im Zug: der Alt-Revise nullte beim Übernehmen `role`/`is_value_relevant`/`quantity_max`/`trimming_loss_pct` (Voll-Ersatz-Semantik von `syncIngredients`) — im neuen Service per Original-Fallback geschlossen, die Wurzel → **V-027**. Kein MCP-Nachzug (Begründung im Stand-Log)
  - [x] **L1b — `✨ Alles anreichern`:** Button + Wiring der `BulkEnrichService`-Strecke ins `VkModal` (RecipeModal-Muster `allesAnreichern`/`bulkAlleUebernehmen` + Status-Zeile im `render`). Die VK-Schrittfolge ist **`SCHRITTE_VK = description|wording|plating|speisen_klasse`** — `category` (186er-Rezept-Kategorie) fliegt raus, die Klassifikation am Gericht heißt `speisen_klasse` und läuft über `SpeisenKlassenService::classify`/`acceptKlasse` (Taxonomie-Aufbau, Aktiv-Filter, Besitzer-Regel und Accept-Stempel liegen dort schon — kein zweiter Klassifikations-Pfad). Namens-Regel festgeschrieben: **Schritt-Name = Suffix des Prompt-Keys** (gilt im Bestand schon durchgehend). Ebenen-Schutz zweifach: `starteVk()` schneidet die Arbeitsmenge auf `->verkauf()`, und `proposeFeld` wirft bei den drei `NUR_GERICHT`-Schritten auf einem Basisrezept — Fehlzeile statt Unsinn-Vorschlag
- [x] **L6** Rezept-Copilot · M–L — `RecipeReviewService`; Prompts `recipe.review`/`vk.review` sind in `AiGatewayService:45-46` **bereits reserviert**; Findings (`menge/einheit/entfernen/fehlt/hinweis` + Confidence), „fehlt" sofort matchen (`auto_applicable` nur bei Match), granularer Apply + `recomputeAndPropagate`; Button in RecipeModal **und** VkModal. **In zwei Teilschritte geschnitten** (M–L war für einen 30-Min-Tick zu groß; der Schnitt liegt zwischen Motor und Fläche, nicht zwischen Basis und VK — die Ebenen-Wahl ist ein `is_sales_recipe`-Zweig im Service):
  - [x] **L6a — Motor + MCP:** `RecipeReviewService::pruefe()` (read-only, Zweig-Wahl über `is_sales_recipe`) + `uebernehmen()` (genau ein Befund je Aufruf, über `syncIngredients` wegen Grounding + `recomputeAndPropagate`); die Anwendbarkeits-Entscheidung fällt EINMAL im Service (`auto_applicable` + `status` mit dem Warum: `kein_ziel`/`ohne_wert`/`kein_treffer`/`schon_drin`/`letzte_zutat`/`nur_hinweis`), UI und MCP lesen sie. Hard-Stop bei `fehlt` ohne Bestands-Treffer. `RecipeReviseService::bestandsZeile()` herausgezogen (V-027-Voll-Ersatz umgangen, nicht geheilt). Prompt-Verträge `recipe.review`/`vk.review` ersetzt (`{befunde:[{schwere,text}]}` kannte kein Schreibziel) → erste Konsumenten der reservierten Keys. MCP `recipes.REVIEW` read-only im selben Commit
    - [x] **L6b — die zwei Flächen:** Findings-UI (Karten mit art-Farbe + Confidence + Begründung, 1-Klick-Apply je Befund, „Alle übernehmen" nur `auto_applicable`, Hard-Stop-Zeile mit „GP anlegen"/„Stub anlegen") + Knopf `🧑‍🍳 Copilot` in `RecipeModal` **und** `VkModal`. Gebaut als **EIN** Trait (`HatRezeptCopilot`) + **EINE** anonyme Blade-Komponente (`x-foodalchemist::copilot-box`, nur `prefix`/`zeilenWort` je Fläche) statt zweier Kopien — die Ebenen-Wahl fällt schon im Service über `is_sales_recipe`. Neu dazugekommen ist eine Regel, die die Spec nicht kannte: **nach jedem Apply werden die übrigen Befunde neu bewertet** (`RecipeReviewService::bewerte()`, ohne KI-Call) — ein `entfernen` löscht die Zielzeile eines anderen Befunds, dessen Knopf darf danach nicht grün bleiben. #511-Event-Kette (`zutatenVersion`++ / `recipe-gespeichert`) je Apply
- [x] **21·S5** Tranche B · S–M — direkt nach L6: Batch-Lauf des Copilot über den Bestand, Findings über Confidence-Schwelle werden Signale (`rezept_plausi_ki`, `rezept_gericht_vs_komponente`). Fängt die Pfefferkörner→Pfefferrahm-Fehlerklasse (Skript 215) künftig **laufend** statt einmalig. **In zwei Teilschritte geschnitten** (die Spec-Zeile setzt eine Ablage voraus, die es nicht gab — V-031; Ablage + Batch waren mit den Signal-Checks zusammen zu groß für einen 30-Min-Tick):
  - [x] **S5a — Ablage + Batch-Motor:** `foodalchemist_recipe_findings` (Migration `…_000003`) + `ai_reviewed_at` an `recipes` (`…_000004`) + Model + `RecipeFindingService` (`arbeitsmenge`/`pruefeUndAblegen`/`speichere`/`entscheide`/`offeneUeberSchwelle`) + Command `foodalchemist:recipe-findings` + MCP `recipe_findings.SEARCH`/`.PUT`. Drei tragende Entscheidungen: **der Fingerprint kennt den Wert nicht** (`art + Zielzeile`, sonst wäre jede Ablehnung durch einen anderen Vorschlagswert aushebelbar) · **`verworfen` hält, `uebernommen` darf zurückkommen** (Ruhigstellung vs. „der Fix hat nicht gegriffen") · **die Arbeitsmenge ist change-driven** (`ai_reviewed_at IS NULL OR < updated_at`; der Stempel läuft roh, damit er nicht selbst `updated_at` bewegt). `pruefe()` bleibt read-only — der Batch ist der einzige Schreiber, damit `recipes.REVIEW` seine `read_only`-Zusage behält
  - [x] **S5b-1 — `rezept_plausi_ki` + der Landeplatz:** Register-Zeile in `rezeptQualitaetChecks` über `offeneUeberSchwelle()` als Sub-Builder (kein nachgebautes EXISTS: die Befund-Zeilen sind bewusst nicht team-hierarchisch, ihr Scope steckt im Service) + `SignalTyp::RezeptPlausiKi`/`istKiUrteil()` + Sprung aus dem Panel ins Rezept-/VK-Modal **mit den abgelegten Befunden** (`copilotAusAblage()`, kein Prüf-Call: das Signal zählt die Ablage, ein Live-Pass zeigte womöglich anderes und kostete Egress je Sprung) + Loop-Closure `entscheide()` bei Übernahme/„Lass das so" (sonst zählt das Signal einen erledigten Befund bis zum nächsten Batch). Bewusst **kein** Knopf im `SignalCockpit` — hinter dem Signal liegt schon ein KI-Urteil je Befund, ein Sammel-`propose()` wäre ein zweites darüber (→ V-033: „kein Knopf" und „kein Weg" sehen im Panel gleich aus)
  - [x] **S5b-2 — `rezept_gericht_vs_komponente`:** eigener Erzeuger `RecipeBauartService` + Prompt `recipe.bauart` (Tier B, Klassifikator ⇒ **keine** Food-DNA), Maßstab 269er „Wie gebaut?" nie „Wo eingesetzt?". Die Ablage-Frage ist zugunsten **derselben Tabelle mit eigener `kind`-Art** (`bauart`) entschieden — und damit wurde aus „ein Erzeuger" das Konzept **Pass**: `RecipeFindingService::PAESSE` bindet je Pass die zulässigen Arten UND einen eigenen Prüf-Stempel (`structure_reviewed_at`, Migration `…_000005`). Beides ist Pflicht, nicht Kosmetik: mit geteilter Arten-Liste schlösse jeder Lauf die offenen Befunde des anderen als „nicht mehr gemeldet", mit geteiltem Stempel nähmen sich die Läufe gegenseitig die Fälligkeit. **Der Fingerprint ist für rezept-weite Arten `art` allein** (der Text-Rückfall auf die Begründung hätte jede Ablehnung durch eine neue Formulierung ausgehebelt). Nie `auto_applicable` — die Umstellung kippt `is_sales_recipe` samt Taxonomie und Verkaufs-Facetten (`applicability = strukturentscheidung`). Register-Zeile S5b-1 ist **mit-eingeschränkt** (`ARTEN_COPILOT`), sonst stünde ein Rezept in zwei Signalen für einen Sachverhalt

## ✅ Phase 4 — Flaggschiff L7 + L8 (ein Kaskaden-Umbau) — KOMPLETT 2026-07-27
- [x] **L7** One-Shot-Vollerstellung · M–L — **KOMPLETT 2026-07-26** (L7a · L7b-1 · L7b-2a · L7b-2b) — Toggle „⚡ Voll anreichern" (Default AN); Kette: Generieren → Grounding (+ LA-First-Mint Spec 07) → Accept/Aggregation → Auto-Enrichment über **bestehende** `BulkEnrichService`-Strecke → Kohärenz; Sub-Rezepte v1 = Stub+Flag; alles `draft`, user-editierte Felder nie überschreiben. **In zwei Teilschritte geschnitten** (M–L war für einen 30-Min-Tick zu groß; der Schnitt liegt zwischen Motor und Fläche wie bei L6):
  - [x] **L7a — Kaskaden-Motor + MCP:** `RecipeOneShotService::anreichern()` verkettet Generierung und Anreicherungs-Pass zu einem Durchlauf. Drei tragende Entscheidungen: **die bestehende Strecke, nicht eine zweite** (dieselben Prompts, derselbe Vorschlags-Speicher `foodalchemist_bulk_proposals` als Audit-Spur, derselbe Accept-Pfad `alleUebernehmen()` mit Override-First je Feld) · **Auto-Übernahme nur in LÜCKEN** (neu: `BulkEnrichService::ZIELFELDER` + `luecken()` — GL-07 verbietet Auto-Persistenz gegen menschliche Pflege, nicht gegen Leerstellen; ein gefülltes Feld kostet keinen zweiten Call) · **synchron im ohnehin asynchronen Kontext** (`verarbeiteRezept()` direkt statt `starte()`, sonst liefe der dispatchte `BulkEnrichJob` auf demo parallel und `alleUebernehmen()` fände nichts). Graceful by construction; `voll_anreichern` an `recipes.GENERATE` (Default **aus**, weil der Tool-Call synchron ist — Begründung im Stand-Log) und `vollAnreichern` am `GenerateRecipeJob`
  - **L7b — die Flächen.** Die V-035-Frage aus der ursprünglichen Zeile ist **entschieden: Strecke vereinheitlichen** (nicht „VK ohne Toggle") — damit war L7b für einen Tick zu groß und ist geschnitten:
    - [x] **L7b-1 — eine Generier-Strecke + der Toggle:** `VkGeneratorModal` von synchron auf `GenerateRecipeJob` (`vkModus: true`) gehoben, Lauf-Mechanik (Dispatch · `wire:poll` · Cache-Ergebnis · One-Shot-Flag) in den geteilten Trait `HatGeneratorLauf` gezogen; Toggle „⚡ Voll anreichern" (Default AN) + Fortschritts-/Ergebnis-Zeile als **zwei geteilte Blade-Komponenten** (`oneshot-toggle`/`oneshot-ergebnis`) an beiden Flächen; Job-Timeout mit aktiver Kaskade 300 → 540 s
    - **L7b-2 — der Rest der Fläche.** Vier Punkte, davon zwei am Kaskaden-Ergebnis und zwei an der Eingabe-Interaktion; für einen Tick zusammen zu groß, darum geschnitten:
      - [x] **L7b-2a — die zwei Ergebnis-Glieder:** **Kohärenz-Glied** (`RecipeOneShotService::kohaerenzGlied()` — `CoherenceService::judge` als GL-10-Achse 2 ans Ende der Kaskade, nur VK und nur ab 2 Komponenten, in EIGENEM try/catch weil es von der Anreicherung nicht abhängt; läuft auch bei `schritte === []`, darum ist der Früh-Return aus L7a aufgelöst) + **Sub-Rezept-Stub-Flag** (`statistik['stubs']` nennt die neu angelegten Stubs beim Namen; der *dauerhafte* Flag bleibt bewusst das Signal `rezept_sub_stub_offen` aus 21·S1b — keine neue Spalte). Beide Zeilen an beiden Flächen über geteilte Blades (`oneshot-ergebnis` erweitert, `stub-offen` neu)
      - [x] **L7b-2b — der Paritäts-Check an der Eingabe:** Hard-Stops sind **Wege** statt Text — `HardstopResolveService` (Ein-Zeilen-Pfad statt Voll-Sync: gezielter Row-Update + genau ein `recomputeAndPropagate`, Guards wie im Voll-Sync) + Trait `HatHardstopAktionen` + geteilte Blade `hardstop-zeilen` an beiden Generator-Flächen. Drei Wege, bewusst ungleich: „🧩 Basisrezept anlegen" legt an und verknüpft · „🤔 Meintest du?" bindet einen Shortlist-Kandidaten (als **Override** — der Mensch übersteuert ein no_match) · „📥 Beschaffung anstoßen" schreibt einen Sourcing-Wunsch und legt **keinen GP** an (LA-First: der Mint hat im selben Lauf schon erfolglos gesucht, deshalb IST die Zeile ein Hard-Stop) — die Zeile bleibt danach ehrlich offen. Abweichung von der Spec-Zeile („GP anlegen") ist damit gewollt und dokumentiert; der Alt-Text an den drei Copilot-/Revise-Flächen → V-039, die fehlende MCP-Entsprechung → V-040
- [x] **L8** Wirtschaftlichkeits-Glied · M — **KOMPLETT 2026-07-27** (L8a · L8b-1 · L8b-2) — damit ist **Phase 4 (L7 + L8) abgeschlossen**. Nach Enrichment: Portion (`quantity_per_unit_g`) + Default-AK + Standard-Darreichung → Auto-VK via `MargeService` (heute in **keinem** Generator verdrahtet) → W%-Ampel gegen `target_food_cost_pct`; unbepreiste Zutaten schlagen als „vorläufig" durch; `recipes.GENERATE` (L5) liefert EK/VK/W% mit. **In zwei Teilschritte geschnitten** (M war für einen 30-Min-Tick zu groß; Schnitt wie bei L6/L7 zwischen Motor und Fläche):
  - [x] **L8a — Kaskaden-Glied + MCP:** `RecipeOneShotService::wirtschaftlichkeitsGlied()` am Ende der Kaskade (nur VK) — Aufschlagsklasse Klasse-vor-HG in die Lücke, Standard-Darreichung über `ensureStandard`, Preis danach aus der **bestehenden** Maschine (`price_mode='auto'`, kein Auto-Publish); Schreibweg ist `SalesRecipeService::updateVk` (dort hängt die Darreichungs-Selbstheilung schon dran), Provenienz bleibt `vk_generator`. W%-Ampel + Signal teilen **eine** Zahl: `SignalDetektorService::wareneinsatzUeberZielFuer()` aus dem Batch-Detektor herausgezogen und mit optionaler W%-Übergabe. **Die Portion wird bewusst NICHT abgeleitet** (`yield_kg / sales_unit_count` würde die Darreichungs-Formel `× Anzahl` doppelt zählen → V-041) — fehlt sie, ist sie eine benannte Lücke. `recipes.GENERATE` liefert `sales_net` + `anreicherung.wirtschaftlichkeit`
  - **L8b — die Fläche + die Portions-Quelle.** Drei Punkte, davon zwei am *Ergebnis* der Kaskade und einer an der *Eingabe*; für einen Tick zusammen zu groß, darum geschnitten:
    - [x] **L8b-1 — Ergebnis-Zeile + Portions-Quelle:** Wirtschaftlichkeits-Block in der geteilten `oneshot-ergebnis`-Blade (VK · W%/Ziel mit derselben Ampel-Leiter wie das Signal · Portion · „vorläufig" · Lücken-Zeile · Signal-Quittung) an **beiden** Generator-Flächen; `portion_g` im Prompt-Vertrag `vk.generator` + Schreibweg `RecipeGeneratorService::portionG()` mit Plausibilitäts-Band 20–3000 g (unplausibel ⇒ still auf die benannte Lücke, nie ein falscher Preis). Dazu die **dritte Vorbedingung sichtbar gemacht**: `ensureStandard() === null` (Varianten ohne Standard-Flag / fehlendes Servierform-Vokabular) war bis hierher ein stummer Null-Preis → neue Lücke `darreichung`. MCP-Description nachgezogen (die Portion kommt aus dem Vorschlag, nicht aus dem Yield)
    - [x] **L8b-2 — die Ziel-VK-Pill (Eingabe):** optionales Feld „Ziel-VK" am **VK-Generator-Modal** → als Constraint in den Prompt-Kontext (`vk.generator`) **und** nach der Kalkulation ehrlich gegen das Ergebnis gehalten. KEIN Solver — Brücke zur R2.4-Denke. **Abweichung von „an beiden Modals", bewusst:** das Basisrezept-Modal hat keine Verkaufseinheit, keinen VK und kein Wirtschaftlichkeits-Glied (`null`) — eine Pill ohne Folge wäre die stumme Bedienfläche, die L8b-1 gerade beseitigt hat. Der Abgleich hat **keine eigene Toleranz-Schwelle**: `ziel_wareneinsatz_pct` = derselbe Bruch mit dem Ziel-VK im Nenner, beurteilt von derselben Ampel-Leiter (eine Leiter, drei Anzeigen). Ein Transportweg für beide Wirkungen (`parameter.ziel_vk_eur`), Band 0,50–500 € mit **Rückfrage statt stiller Verwerfung** (Absender ist ein Mensch — Gegenstück zu `portionG`); MCP `recipes.GENERATE` bekommt `ziel_vk` (nur mit `vk=true`)

## Phase 5 — Spec 08 Rest
- [ ] **P6** Wissens-Ebene · M — Kategorie `concept` befüllen (Destillation 109/110/111), `concept`/`konzept`-Dublette konsolidieren, Routings `concept.plan`/`foodbook.plan` in `foodalchemist_knowledge_routings` (heute nur `foodbook.kapitel_ideen`). **In zwei Teilschritte geschnitten** — der Schnitt liegt zwischen *Leitung* und *Inhalt*, weil die zweite Hälfte gar kein Modul-Code ist:
  - [x] **P6a — Kategorie + Leitung (Code):** Kategorie `concept` im Vokabular + defensive `konzept`-Konsolidierung + die Routings `foodbook.plan`/`concept.plan` als Migration in die Bestands-DBs (der Seed in `KnowledgeImportCommand` kennt sie seit E6.4, hat sie aber nur bei einem Import gesetzt) + **der Konsum-Pfad** `concept:always` im `KnowledgeContextService` (ohne ihn wäre die Routing-Zeile eine Zeile ohne Wirkung → V-043) + MCP-Enums nachgezogen
  - [ ] **P6b — Befüllung (Daten, NICHT Modul-Code):** Vault-Ordner `07_WISSEN/07.01_Lebensmittel_und_Gastronomie/Concepting/` anlegen und per Destillation (Skripte 109/110/111) mit Concepting-Handwerk füllen, dann `foodalchemist:knowledge-import` — der Import-Pfad steht seit P6a (`concept.`-Präfix). **Dominique-Aufgabe / eigener Lauf:** die Routine arbeitet im Modul-Repo, der Vault ist nicht ihr Scope, und ohne Quell-Material wäre jeder „Inhalt" erfunden (GL: keine Erfindungen). Bis dahin ist die Kategorie leer = kein Prompt-Block (Invariante 6), die Leitung aber vollständig und getestet

## Phase 6 — Große eigene Etappen (jede mehrere Runs)
- [ ] **12·S2 · R2.4 Marge-optimale Assemblierung · XL** — ersetzt den greedy Schritt `ConceptGeneratorService:152`; reutzt `PlanningFrameService`-Constraints (`target_price_pp`, Bänder, `RULE_TYPES`), `kandidatenPool`/`filterFuerSlot`, `CoverageService::coverage` als Feasibility-Gate; Perf-Vorlage `MargeImpactService::impactFuerGps`+`gpSetExposure`. **In Teilschritte zerlegen, nicht in einem Run.**
- [ ] **13·S1 · Kanal-B Datei-Import · L** — `FileArticleImportService` + Command `foodalchemist:import-articles {--file --supplier --team --dry-run --apply}`; schreibt in **vorhandene** Tabellen (kein neues Schema); `PriceService::createFor` → `recomputeAndPropagate` → `preisSprungMargeImpact`; idempotent/resumefähig
- [ ] 13·S2 Lieferbedingungen · S · und 13·S3 MCP `ingest.STATUS` · S
- [ ] **05·Etappe 2** KI-Datenqualität · M — via OpenAI über Core-Provider (Entscheid: serverseitig auf demo, Review in demo-UI); u.a. `--mode=ki` für mehrdeutige Prep-Texte (Prozessanker), unbestimmte Servierformen je Gericht

## Phase 7 — Vektor-DB-Track (Store-Frage)
- [ ] **15 · Backfill + Observer** — laut Spec §79 **vom Ingest entkoppelt und jederzeit baubar**; kann bei Bedarf vorgezogen werden
- [ ] **15 · LA-Pool** — erst wenn die ~50k-Grenze beantwortet ist (Qdrant/Partitionierung = die geplante Vektor-DB); dann RAG-Speicher als **Backend-Swap** umhängen, Additiv-Fallback halten

## Deferred / extern (NICHT anfassen)
12·R2.3 (gated auf Q2-Format-Spec aus echter Bankettprofi-Datei = Dominique-Aufgabe) · 13·S4 Sales-Ist (dito) · 13·E6 Kanal A (extern) · 09·R6.10 produktiv (Q1-Contract) · 11·KI-Narrativ (optional) · #492-Blindtest (braucht 3 echte Kunden-Briefs — Dominique liefert; nicht erfinden).

---

## Stand-Log (neueste oben)
- **2026-07-27 · 08·P6a — Kategorie `concept` + die Planungs-Routings, samt Konsum-Pfad ✅ (Routine-Lauf 33)** — Commit `8e3df22`. Gebaut: Migration `2026_07_27_000001_add_concept_knowledge_category_and_plan_routings.php` (Kategorie + defensive `konzept`-Konsolidierung + 6 Routing-Zeilen) · `KnowledgeContextService` (`conceptBlock()` + `CONCEPT_MAX_DOCS`/`CONCEPT_TRUNCATE_CHARS` + `concept:always`-Zweig in `contextFor`) · `KnowledgeImportCommand` (Seed-Parität + Import-Ordner `Concepting/` mit `concept.`-Präfix) · MCP-Lockstep `KnowledgeListTool`/`KnowledgeSearchTool`/`KnowledgeCreateTool` (Kategorie-Enums + Beschreibung). Tests: 7 in `ConceptKnowledgeRoutingTest`, 1 angepasst (`KnowledgeImportTest`: Routing-Soll 12 → 14). **Suite 1471 · 1467 passed · 4 skipped · 0 Fail · 6904 Assertions (Vorlauf 1464/1460, +7 Tests), 11,1 Min.**
  — **Erster Befund: die Routings fehlten nicht in der Liste, sie fehlten in der DB.** `KnowledgeImportCommand::seedRoutings` kennt `foodbook.plan`/`concept.plan` seit E6.4 (`4effdae`) — gesetzt werden sie aber ausschließlich bei einem Wissens-Import. Am lokalen MySQL stehen 8 Routing-Zeilen, keine davon eine Planungs-Zeile. `IdeenService::kiDivergenz:276` ruft seither `contextFor('foodbook.plan', …)` und bekam still einen **leeren Wissens-Block**, während der Docblock zwei Zeilen darüber das Routing als Teil des Kontext-Vertrags nennt. Darum Migration statt Seed-Ergänzung: eine Zeile, die nur bei einem manuellen Import entsteht, ist keine Verdrahtung.
  — **Zweiter Befund: eine Routing-Zeile allein wirkt nicht.** `contextFor` prüfte vier fest verdrahtete `category:mode`-Kombinationen (`cross_cutting:always`, `domain:discovery`, `pairing:discovery`, `pairing:grounding`). `concept:always` wäre ohne den neuen `conceptBlock()`-Zweig eine Zeile in der DB ohne jede Wirkung geblieben — sichtbar, plausibel, folgenlos. Das ist die eigentliche Falle der Tabelle (freie Strings, kein FK, kein Enum, keine Warnung) → **V-043**.
  — **Dritter Befund: die Dublette gibt es hier nicht.** Der Spec-08-Nit (`concept` UND `konzept` doppelt) trifft am lokalen Stand nicht zu — 9 Kategorien, keine konzeptseitige darunter. Die Konsolidierung ist trotzdem drin (Docs mitziehen + Slug-Präfix + Alt-Zeile deaktivieren, Muster `skill`→`workflow`), weil demo die deutsche Variante tragen kann und die Migration dort sonst eine zweite anlegte statt einzusammeln. Nicht zurückgedreht wird sie in `down()` — nach dem Merge weiß niemand mehr, welche Zeile vorher deutsch war.
  — **Bewusst `always` statt `discovery` für die Kategorie:** Concepting-Wissen beschreibt Handwerk (Dramaturgie, Gang-Aufbau, Anlass-Fit), nicht Produkt. Eine Beschreibungs-Discovery würde es nach Zutaten filtern und genau dann verwerfen, wenn der Brief noch keine Zutaten nennt. Der Deckel steht stattdessen als Daten in der Routing-Zeile (4 Docs × 4000 Zeichen), die Reihenfolge ist Slug-stabil, und der Block steht **vor** dem Food-Wissen — er rahmt es.
  — **MySQL-Smoke: offen (vermerkt, nicht gefahren).** Erste Migration dieses Programms, die eine **Bestandstabelle** anfasst (`knowledge_categories`/`_routings` der geteilten Dev-DB) — die Routine mutiert die nicht blind. Dialekt-Prüfung am Text: `insertOrIgnore` (→ `INSERT IGNORE`), `REPLACE()` und die Existenz-Guards sind in beiden Dialekten gültig; die SQLite-Strecke ist durch die Suite belegt (Migrationen laufen je Test). **Manuell offen** außerdem: Browser-Klickstrecke entfällt (keine UI-Änderung).
  — **Office-Dev-Modul: nichts abgehakt, weil nichts zuordenbar ist.** Package 23 / Board 53 führt kein Issue zu Spec 08 (durchsucht: „Planungs", „Wissen", „Kreativ", „Divergenz"). Am nächsten liegen #505 (Generator-Grounding — andere Ebene: Rezept, nicht Plan) und #547 (Foodbook-Planungs-Cockpit — enthält den Kreativ-Tab, aber nicht die Wissens-Leitung). Ein Issue anzulegen ist nicht Routine-Auftrag ⇒ hier vermerkt. **Kandidat für Dominique:** ein Issue „Spec 08 P6 Wissens-Ebene" mit P6a ✅ / P6b offen.
  — **Nächster Schritt:** P6b ist bewusst kein Routine-Chunk (Vault-Daten, s. Phase 5) ⇒ weiter mit **Phase 6 · 12·S2 R2.4** (XL, in Teilschritte zerlegen) oder — falls ein kleinerer Einstieg gewünscht ist — **13·S1 Kanal-B Datei-Import**.
- **2026-07-27 · 03·L8b-2 — die Ziel-VK-Pill ✅ · damit L8 und PHASE 4 komplett (Routine-Lauf 32)** — Commit `a13c5bc`. Gebaut: `VkGeneratorModal` (`zielVk` + `zielVkEur()`) · `vk-generator-modal.blade.php` (Feld) · `config/foodalchemist.php` (`vk.generator`-Constraint) · `GenerateRecipeJob::zielVk()` · `RecipeOneShotService::anreichern(..., ?float $zielVk)` + Ziel-Abgleich im Wirtschaftlichkeits-Glied · `oneshot-ergebnis.blade.php` (Ziel-Zeile + geteilte Ampel-Zuordnung) · `RecipesGenerateTool` (`ziel_vk` + Validierung + Description). Tests: 5 in `RecipeOneShotWirtschaftlichkeitTest`, 5 in `GeneratorOneShotToggleTest`, 2 in `McpRecipesGenerateTest`. **Suite 1464 · 1460 passed · 4 skipped · 0 Fail · 6879 Assertions (Vorlauf 1452/1448, +12 Tests), 11,1 Min.**
  — **Erste Entscheidung: gehalten, nicht durchgesetzt — und deshalb ohne eigene Schwelle.** Die naheliegende Lesart von „Gericht für 8,50 €" wäre, den VK auf 8,50 € zu setzen. Dann stimmte die Zahl und die Marge wäre unsichtbar falsch — dasselbe Muster wie der grün gemeldete Falschpreis aus L8b-1, nur eine Ebene höher. Der Preis bleibt also gerechnet; beantwortet wird stattdessen die Frage, die wirklich zählt: *was würde der Zielpreis bedeuten?* → `ziel_wareneinsatz_pct` ist derselbe Bruch wie die Ist-Quote, nur mit dem Ziel-VK im Nenner, beurteilt von **derselben** Ampel-Leiter (`weAmpel`). Damit gibt es hier **keine dritte Schwelle**: „Ziel erreichbar" heißt exakt „bei diesem Preis liegt der Wareneinsatz noch im Ziel". Eine eigene Toleranz (±5 % o. ä.) hätte eine vierte Zahl erfunden, die niemand pflegt. `ziel_delta_eur` (Ist − Ziel) steht faktisch daneben. Das ist die Brücke zur R2.4-Solver-Denke — bewusst ohne Solver.
  — **Zweite Entscheidung: ein Transportweg für beide Wirkungen.** Der Ziel-VK wirkt zweimal (Prompt-Constraint *vor* der Generierung, Maßstab *nach* der Kalkulation) und reist trotzdem nur einmal: als `parameter.ziel_vk_eur`. Der Job liest ihn dort wieder heraus und reicht ihn an `anreichern()`. Ein zweiter Konstruktor-Parameter wäre bequemer gewesen und hätte die beiden Wirkungen auseinanderlaufen lassen (Prompt sagt 8,50 €, Abgleich rechnet gegen etwas anderes). Geschrieben wird der Wert nirgends — er gilt für DIESEN Lauf; Pest hält fest, dass `sales_net` nach einem 6,00-€-Ziel unverändert 9,60 € ist.
  — **Dritte Entscheidung: Rückfrage statt stiller Verwerfung — spiegelbildlich zu L8b-1.** `portionG()` lässt Unplausibles still fallen, weil der Absender ein Modell ist, dem man nicht antworten kann. Hier ist der Absender ein Mensch: „850" statt „8,50" ist keine Halluzination, sondern ein Tippfehler, gegen den nachher jedes Ergebnis „viel zu billig" aussähe. Also Band 0,50–500 € mit Fehlzeile und ohne Lauf; über MCP dieselbe Grenze als `VALIDATION_ERROR`, ebenso `ziel_vk` ohne `vk=true` (ein ignorierter Constraint sähe für den Client aus wie eine Antwort auf seine Vorgabe). Der Service selbst kennt nur „positiv oder keine Vorgabe" — Bänder gehören an die Eingänge, wo es einen Absender gibt.
  — **Abweichung vom Checkbox-Text „an beiden Generator-Modals", bewusst:** das Basisrezept-Modal hat keine Verkaufseinheit, keinen VK und das Glied liefert dort `null`. Eine Pill ohne Folge wäre genau die stumme Bedienfläche, die L8b-1 gerade beseitigt hat. Ohne Ist-VK (Lücke `aufschlagsklasse`) steht der Ziel-Wareneinsatz dagegen trotzdem — er braucht nur den EK je Portion, und dort ist er am nützlichsten (er sagt, ob das Ziel überhaupt tragfähig ist, bevor der Preis existiert).
  — **Kein Schema-Change, keine Migration, kein neues SQL** → MySQL-Smoke entfällt sachlich. **MCP-Lockstep im selben Commit** (`ziel_vk` + Beschreibung beider Wirkungen). **Browser-Klickstrecke bleibt manuell offen** (headless nicht fahrbar) — Feld und Ziel-Zeile sind in Livewire-Render-Tests belegt (Eingabe normalisiert · leer bleibt leer · unplausibel meldet · Zeile mit Delta + Ziel-W% · ohne Vorgabe keine Zeile). Backlog: **V-042** (jede neue Generator-Option erfindet ihren eigenen Transportweg — drei Optionen, drei Muster, alle Fehlermodi still). **Nächster Schritt: Phase 5 · Spec 08 P6** (Wissens-Ebene: Kategorie `concept` befüllen, `concept`/`konzept`-Dublette konsolidieren, Routings `concept.plan`/`foodbook.plan`).
- **2026-07-27 · 03·L8b-1 — die Fläche + die Portions-Quelle ✅ (Routine-Lauf 31)** — Commit `b3d080a`. L8b nennt drei Punkte; zwei hängen am *Ergebnis* der Kaskade, einer an der *Eingabe* → geschnitten: **L8b-1 = Ergebnis-Zeile + Portions-Quelle** (hier), **L8b-2 = Ziel-VK-Pill** (Fortsetzungspunkt in der Checkbox). Gebaut: `config/foodalchemist.php` (`vk.generator` um `portion_g`) · `RecipeGeneratorService::portionG()` + Schreibweg im vkModus-Block · `oneshot-ergebnis.blade.php` um den Wirtschaftlichkeits-Block · `RecipeOneShotService` (neue Lücke `darreichung`) · `RecipesGenerateTool` Description. Tests: 3 in `VkGeneratorTest`, 4 in `GeneratorOneShotToggleTest`, 1 in `RecipeOneShotWirtschaftlichkeitTest`. **Suite 1452 · 1448 passed · 4 skipped · 0 Fail · 6836 Assertions (Vorlauf 1444/1440, +8 Tests), 11,0 Min.**
  — **Erste Entscheidung: die Portion kommt aus dem Vorschlag, aber nur durch ein Band.** L8a hatte die Ableitung aus dem Yield verworfen (V-041) und die fehlende Portion zur benannten Lücke gemacht — im Regelfall also zur Dauer-Lücke. Die Quelle ist jetzt der Vertrag selbst (`portion_g`, „Gramm je Verkaufseinheit — was EIN Gast bekommt, nicht die Charge"). Was dabei anders ist als bei jedem anderen Vertragsfeld: dieser Wert läuft ungebremst in die Preis-Formel (`ek_portion = EK/g × Grammatur × Anzahl`). Eine Verwechslung der Größenordnung erzeugt keinen Fehler, sondern einen **falschen VK, den die Ampel grün meldet** — schlechter als kein VK. Darum `portionG()` mit hartem Band **20–3000 g** (Amuse bis Platte je Einheit) und stillem Rückfall auf `null`: unplausibel ⇒ die Lücke aus L8a greift weiter, kein Werfen (das Rezept selbst ist verwertbar). Der Basisrezept-Pfad setzt die Portion nie — er hat keine Verkaufseinheit.
  — **Zweite Entscheidung: eine Fläche, dieselbe Ampel-Leiter.** Der Block hängt in der geteilten `oneshot-ergebnis`-Blade (Muster L7b-2a/L7b-1), die Modals bekommen kein eigenes Markup; am Basisrezept-Modal rendert er gar nicht, weil das Glied dort `null` liefert. Die W%-Pille färbt sich **nach der `ampel` aus dem Service**, nicht nach eigener Schwelle im Blade — sonst wäre die Leiter zum dritten Mal formuliert (Service, Signal-Detektor, View) und ein Generator-Ergebnis könnte „grün" zeigen, während das Cockpit dasselbe Gericht warnt. Lücken sind Klartext statt Kürzel (`portion` → „Portionsgröße"), und der Signal-Fall bekommt eine Quittung: „als Signal im Cockpit vermerkt" — sonst wirkt der Ampel-Ausschlag wie eine Anzeige ohne Folge.
  — **Dritter Punkt, im Zug geschlossen: die dritte Vorbedingung war stumm — und das fiel erst durch die neue Fläche auf.** `ensureStandard()` gibt bewusst `null` zurück, wenn es nichts raten darf (Varianten ohne Standard-Flag) oder das Servierform-Vokabular `unbestimmt` fehlt; dann gibt es keine Preis-Zeile und damit keinen VK. In `luecken` stand dieser Fall nicht — die neue Zeile hätte also **weder Preis noch Grund** gezeigt, exakt der stille Null-Preis, den die DoD ausschließt. Neue Lücke `darreichung` (+ Pest). Bewusst als Teil dieses Chunks und nicht als Backlog-Eintrag: eine Fläche kann nur zeigen, was der Service benennt — ohne das wäre die Etappe unehrlich fertig gewesen.
  — **Kein Schema-Change, keine Migration, kein neues SQL** → MySQL-Smoke entfällt sachlich. **MCP-Lockstep im selben Commit:** die Description von `recipes.GENERATE` behauptete seit L8a „Portionsgroesse … wird gesetzt" — das hielt kein Code. Jetzt sagt sie, dass die Portion aus dem Vorschlag kommt, **nicht** aus dem Yield abgeleitet wird, welches Band gilt und welche drei Werte `luecken` kennt. **Browser-Klickstrecke bleibt manuell offen** (headless nicht fahrbar) — der Block ist in Render-Tests in allen vier Zweigen belegt (bepreist / Lücke / vorläufig+Signal / Basisrezept ohne Block), kompiliert also nachweislich. Backlog: **keine neue V-Nummer**, stattdessen **V-030 um einen zweiten Fundort ergänzt** (der Prompt-Vertrag trägt mit `portion_g` erstmals eine preis-relevante Zahl; der einzige Schutz ist ein privates Band im Konsumenten statt ein Wertebereich an der Naht). Nächster Schritt: **Phase 4 · L8b-2** — Ziel-VK-Pill an beiden Generator-Modals (Prompt-Constraint) + Ist-gegen-Ziel als Zeile im jetzt stehenden Wirtschaftlichkeits-Block; damit ist L8 und damit Phase 4 komplett.
- **2026-07-26 · 03·L8a — die KI-Kaskade endet bepreist ✅ (Routine-Lauf 30)** — Commit `8456d1b`. Gebaut: `RecipeOneShotService::wirtschaftlichkeitsGlied()` (neu, hinter dem Kohaerenz-Glied) · `SignalDetektorService::wareneinsatzUeberZielFuer()` (aus dem Batch-Detektor herausgezogen) · `RecipesGenerateTool` (Ergebnis + Description). Tests: 7 in `RecipeOneShotWirtschaftlichkeitTest`. **Suite 1444 · 1440 passed · 4 skipped · 0 Fail · 6812 Assertions (Vorlauf 1437/1433, +7 Tests), 10,9 Min.**
  — **Erste Entscheidung: der Preis wird nirgends geschrieben.** Gesetzt werden nur Aufschlagsklasse (Klasse-vor-HG-Default) und, falls fehlend, die Standard-Darreichung; der VK entsteht danach in `DarreichungService::recomputePreise` aus EK × Aufschlag mit `price_mode='auto'`. Damit erfüllt sich die DoD-Zeile „kein Auto-Publish" nicht durch eine Prüfung, sondern durch die Bauform — es gibt keinen Pfad, auf dem die Kaskade einen Preis festschreiben könnte, und die R2.5-Trennung Live-Marge ↔ freigegebener Snapshot bleibt unberührt. Geschrieben wird über `SalesRecipeService::updateVk`, weil dort die Selbstheilung der Standard-Darreichung schon hängt; `ensureStandard` deckt nur noch den Fall „nichts zu ändern, aber auch keine Darreichung da". Einziger Zusatz: die Provenienz wird nach `updateVk` von `vk_editor` auf `vk_generator` zurückgestellt — hier hat kein Mensch editiert.
  — **Zweite Entscheidung: die Portion wird nicht geraten — und das ist der eigentliche Fund des Laufs.** Die naheliegende Ableitung `yield_kg × 1000 / sales_unit_count` (so zeigt `cockpit()` die Grammatur an) lieferte im ersten Testlauf 96,00 € statt 9,60 €: die Darreichung rechnet `ek_portion = EK/g × Grammatur × Anzahl` und multipliziert mit derselben Anzahl wieder hoch. Dahinter stecken zwei gegensätzliche Lesarten derselben Spalte — Einheiten je Verkauf (Darreichung, multipliziert) vs. Portionen je Charge (`KalkulationService`, `PaketService`, Cockpit-Fallback, dividieren) —, und beide treffen sich in `recipeHk`, dessen Wareneinsatz-Quote dadurch bei jedem Gericht mit mehr als einer Einheit systematisch **zu niedrig** ausfällt. Genau darauf steht das Signal `wareneinsatz_ueber_ziel`. Welche Lesart gilt, ist eine Fach-Entscheidung über die Spalte samt Bestandsprüfung, kein Beifahrer-Fix → **V-041**. L8a weicht sauber aus: fehlende Portion = benannte Lücke (`luecken: ['portion']`) statt geratener Chargenpreis, und der Wareneinsatz kommt aus der Darreichung selbst (`ek_portion` gegen `sales_net` — dieselbe Menge auf beiden Seiten, bei Cost-plus per Konstruktion `1/(1+Aufschlag)`). Dass die Lücke damit heute der Regelfall ist, ist ehrlich und benannt; sie verschwindet in **L8b**, wenn `portion_g` im Prompt-Vertrag `vk.generator` steht (DoD-Zeile 1 sagt „aus dem Proposal", nicht „aus dem Yield").
  — **Dritte Entscheidung: Ampel und Signal teilen eine Zahl.** Statt einer zweiten Prüfstelle wurde die Per-Gericht-Emission aus dem Batch-Detektor herausgezogen (`wareneinsatzUeberZielFuer()`, optionale W%-Übergabe): eine Schwelle, eine Schweregrad-Leiter (über Ziel = Warnung, über 1,5 × Ziel = kritisch), ein Dedup-Key, ein Text. Der Ausreißer eines frisch generierten Gerichts steht damit sofort im Cockpit statt erst nach dem nächsten Scheduler-Lauf. Bewusst **nicht** die Foodbook-Ampel-Regel (`ziel + food_cost_tolerance_pp`) — das ist ein Buch-Feld ohne Gegenstück am Gericht, und zwei Schwellen hießen: Generator sagt „grün", Cockpit sagt „Warnung".
  — **Vierte Entscheidung: das Glied läuft am Ende, nicht im Generator.** Die Speisen-Klasse entsteht erst im Anreicherungs-Pass; ein Glied im Generator hätte den Klasse-vor-HG-Default bei der Hälfte der Gerichte ins Leere laufen lassen. **MCP-Lockstep im selben Commit:** `recipe.sales_net` immer, `anreicherung.wirtschaftlichkeit` mit `voll_anreichern=true`, Description nennt das Glied als **provider-freien** Schritt (es kostet keinen Call — wichtig, weil `voll_anreichern` sonst als „noch teurer" gelesen wird). **Kein Schema-Change, keine Migration, kein neues SQL** → MySQL-Smoke entfällt sachlich. **Browser-Klickstrecke entfällt** (L8a hat keine UI-Fläche). Dev-Modul: Package 23, Issue #512 um eine abgehakte L8a-Zeile ergänzt; die L8-Umbrella-Zeile bleibt offen. Neu im Backlog: **V-041**. Nächster Schritt: **Phase 4 · L8b** — Ergebnis-Zeile (VK/W%/Ampel/„vorläufig"/Lücken) als geteilte Blade an beiden Generator-Modals, `portion_g` in den Prompt-Vertrag `vk.generator`, optionale Ziel-VK-Pill.
- **2026-07-26 · 03·L7b-2b — Hard-Stops sind Wege statt Text ✅ (Routine-Lauf 29) · damit L7 gebaut bis auf L8** — Commit `ae4d9e0`. Gebaut: `src/Services/HardstopResolveService.php` (neu) · `src/Livewire/Concerns/HatHardstopAktionen.php` (neu) · `resources/views/components/hardstop-zeilen.blade.php` (neu) · beide Generator-Modals + Blades verdrahtet. Tests: 8 in `GeneratorHardstopTest`. **Suite 1437 · 1433 passed · 4 skipped · 0 Fail · 6774 Assertions (Vorlauf 1429/1425, +8 Tests), 10,9 Min.**
  — **Erste Entscheidung: Ein-Zeilen-Pfad statt Voll-Sync.** Der naheliegende Weg wäre `syncIngredients` gewesen (der Schreibweg der Zutatenliste). Dagegen zwei Dinge: er ist Voll-**Ersatz** — für „binde genau diese eine Zeile" müsste das gesamte, gerade generierte Rezept fehlerfrei durchgereicht werden — und er nullt dabei `role`/`is_value_relevant`/`quantity_max`/`trimming_loss_pct` (V-027, im Revise nur per Fallback umgangen). Also gezielter Row-Update + **genau ein** `recomputeAndPropagate`, aber mit denselben Guards wie der Voll-Sync (XOR, Besitzer-Team/D1, `pruefeVerknuepfung`, Platzhalter-Ausschluss) — kein zweiter Schreibweg mit eigener Regel-Auslegung. Dazu ein Idempotenz-Guard, der im Voll-Sync gar nicht nötig war: die Zeile wird nur angefasst, **solange sie offen ist** (`whereNull` auf beide Ziel-Spalten). Doppelklick oder zweiter Tab bekommen eine Fehlzeile statt die erste Auflösung zu überschreiben; das ist getestet.
  — **Zweite Entscheidung: der GP-Knopf legt keinen GP an — und das ist der Kern, nicht ein Rand.** Die Spec-Zeile sagt „GP anlegen". Unter LA-First (kein GP ohne LA) ist das nicht baubar: `LaFirstGpService::mintFromLa` hat im selben Lauf, an genau dieser Zutat, schon erfolglos gesucht — **deshalb** ist die Zeile überhaupt ein Hard-Stop. Ein Knopf, der hier trotzdem schriebe, wäre der Guardrail-Bruch, den Spec 07 ausdrücklich ausschließt. Also heißt er „📥 Beschaffung anstoßen", schreibt einen (deduplizierten) Beschaffungs-Wunsch über `GpProposalService` und lässt die Zeile **offen stehen** — sichtbar, mit Quittung darunter. Die Abweichung steht in der Spec-Zeile; dass drei andere Flächen (Copilot, Revise ×2) weiter „GP anlegen" sagen, ist **V-039** (nicht mitgeändert: Scope).
  — **Dritte Entscheidung: die drei Wege bleiben ungleich, auch in der Provenienz.** „🧩 Basisrezept anlegen" legt an und verknüpft in einem Zug (`createSubRecipeStub`, idempotent per Namens-Dedupe) und bindet als `recipe_ref` — dieselbe Mechanik wie im Generator, nur menschlich ausgelöst, denn vorher existierte nichts. „🤔 Meintest du?" bindet einen Shortlist-Kandidaten als **Override** (`override_gp`/`override_subrecipe`) und **ohne** `match_confidence`: hier übersteuert ein Mensch bewusst ein `no_match` des Matchers, und ein geerbter Score würde diese Entscheidung als Messwert tarnen. Die Kandidaten selbst waren längst da (`offene[].shortlist` aus `RecipeGeneratorService:231`) — gezeigt wurde nur ihre Anzahl.
  — **Vierte Entscheidung: eine Fläche, und eine ehrliche Statistik.** Muster wie L6b/L7b-1: ein Trait + eine anonyme Blade, die Modals reichen nur `prefix` als Test-Hook durch. Die Ergebnis-Statistik wird nach einer Auflösung **lokal** korrigiert statt den Lauf neu zu laden (das Cache-Ergebnis ist ein Protokoll des Laufs, es wird nicht rückwirkend umgeschrieben) — und ein frisch angelegter Stub zählt als `stub_neu` **samt Namen in `statistik['stubs']`**, nicht als `bestand_sub`: sonst behauptete die Zeile einen Reuse, den es nicht gab, und die Bringschuld-Liste aus L7b-2a wäre unvollständig.
  — **Kein Schema-Change, keine Migration, kein neues SQL** → MySQL-Smoke entfällt sachlich. **MCP bewusst nicht nachgezogen und begründet:** das einzige schreibende Zutaten-Tool ist der Voll-Sync — ein Client, der dem `hinweis` in `recipes.GENERATE` folgt, repariert eine Zutat und beschädigt per V-027 die anderen. Eine Fassade (`recipe_ingredients.RESOLVE`) auf den neuen Service ist als **V-040** mit fertigem Vorschlag notiert; ein neues Tool ist eine Vertrags-Entscheidung, keine Beifahrer-Änderung. **Browser-Klickstrecke bleibt manuell offen** (headless nicht fahrbar) — die neue Blade ist in Render-Tests in beiden Zweigen belegt (zugeklappt/aufgeklappt, GP-Fall/Halbfabrikat-Fall), kompiliert also nachweislich. Neu im Backlog: **V-039**, **V-040**. Nächster Schritt: **Phase 4 · L8** Wirtschaftlichkeits-Glied (Portion + Default-AK + Standard-Darreichung → Auto-VK via `MargeService` → W%-Ampel; `MargeService` ist heute in **keinem** Generator verdrahtet).
- **2026-07-26 · 03·L7b-2a — Kohärenz-Glied + Sub-Rezept-Stub-Flag ✅ (Routine-Lauf 28)** — Commit `fd5c20b`. L7b-2 nennt vier Punkte; zwei hängen am *Ergebnis* der Kaskade, zwei an der *Eingabe*-Interaktion → geschnitten: **L7b-2a = Kohärenz-Glied + Stub-Flag** (hier), **L7b-2b = Hard-Stop-Inline-Anlage + „Meintest du?"-Disambig** (Fortsetzungspunkt in der Checkbox). Gebaut: `RecipeOneShotService::kohaerenzGlied()` + `CoherenceService`-Injektion · `RecipeGeneratorService` sammelt `statistik['stubs']` · `resources/views/components/stub-offen.blade.php` (neu) an beiden Generator-Blades · `oneshot-ergebnis.blade.php` um die Urteils-Zeile erweitert · `RecipesGenerateTool` Description + Ausgabe-Kommentare. Tests: 4 in `RecipeOneShotTest`, 3 in `GeneratorOneShotToggleTest`, `RecipeGeneratorTest` um die Stub-Namen erweitert. **Suite 1429 · 1425 passed · 4 skipped · 0 Fail · 6738 Assertions (Vorlauf 1422/1418, +7 Tests), 10,8 Min.**
  — **Erste Entscheidung: welche Kohärenz.** GL-10 §1 kennt zwei Achsen und verbietet, sie zu verrechnen. Achse 1 (deterministischer Aroma-Score, `PairingService::recipeCohesion`) lief im Generator schon — direkt nach dem Zutaten-Sync, ohne Provider-Call. Was fehlte, ist Achse 2: das kulinarische Urteil `vk.kohaerenz`, bis hierher **nur** on-demand im VK-Detail-Panel. Damit war das per KI erstellte Gericht genau das, über das nie geurteilt wurde. Gebaut über dieselbe `CoherenceService::judge`-Strecke inkl. Cache (kein zweiter Pfad, keine zweite Wahrheit). Das Feld heißt bewusst **`kohaerenz_urteil`**, nicht `kohaerenz`: im MCP-Output liegen jetzt zwei 0–100-Skalen in einer Antwort, und ein Client ohne Hinweis würde sie mitteln — das ist als **V-038** notiert (nicht selbst umgesetzt: es beträfe einen bestehenden Tool-Vertrag).
  — **Zweite Entscheidung: zwei Gates, beide inhaltlich.** *Nur VK* (ein Basisrezept IST eine Komponente — es gibt keinen Teller zu beurteilen) und *erst ab zwei Komponenten* (Kohärenz ist eine Aussage über Zusammenspiel; auf einem Ein-Zeiler wäre ein 100er-Score eine Falschaussage und der Call bezahltes Rauschen). Beide Gates sind negativ getestet mit `shouldReceive('propose')->never()` — „kein Urteil" muss auch „kein Call" heißen, sonst ist das Gate nur Kosmetik am Rückgabewert.
  — **Dritte Entscheidung: eigenes try/catch, und der Früh-Return aus L7a musste weichen.** Das Urteil hängt an den Komponenten, die nach dem Generieren schon stehen — nicht am Anreicherungs-Pass. Im gemeinsamen catch hätte ein gescheiterter Enrichment-Schritt das Urteil mitgenommen, und umgekehrt hätte ein fehlendes Urteil (`judge()` wirft absichtlich ohne verwertbaren `score`, FakeProvider-Grenze) als Anreicherungs-Fehler gelesen. Dazu: bei `schritte === []` kehrte `anreichern()` bisher sofort zurück — ein Gericht mit schon gefüllten Textfeldern hätte das Glied stumm übersprungen. Genau dieser Fall ist jetzt der erste Test.
  — **Vierte Entscheidung: der Stub-Flag ist eine Sichtbarkeits-, keine Schema-Frage.** Naheliegend wäre eine Spalte `needs_recipe_detail` am Stub gewesen. Dagegen: das Signal `rezept_sub_stub_offen` (21·S1b) erkennt exakt diesen Zustand schon aus dem Bestand (status stub/draft + 0 Zutaten + referenziert) — eine Spalte daneben wäre eine zweite Wahrheit mit Drift-Garantie. Was wirklich fehlte, ist die Sichtbarkeit **im Moment der Entstehung**: „3 Stubs neu" sagt nicht, WAS auszurezeptieren ist, und ohne Namen erfährt man es erst beim nächsten Signal-Lauf. `statistik['stubs']` trägt `id`+`name` der NEUEN Stubs (ein wiederverwendeter Bestands-Stub ist keine Bringschuld dieses Laufs), die Zeile rendert auch mit **Toggle aus** — Stubs entstehen im Generier-Schritt, nicht im Anreicherungs-Pass.
  — **Kein Schema-Change, keine Migration, kein neues SQL** → MySQL-Smoke entfällt sachlich. **MCP-Lockstep strukturell erfüllt** (`statistik`/`anreicherung` fließen als ganze Blöcke durch `recipes.GENERATE`, `stubs`/`kohaerenz_urteil` also automatisch mit) — nachgezogen ist die *Zusage*: die Tool-Description nennt den zusätzlichen Judge-Call und die Achsen-Trennung, statt sie den Client raten zu lassen. **Browser-Klickstrecke bleibt manuell offen** (headless nicht fahrbar); beide neuen Zeilen sind in Livewire-Render-Tests belegt, in beiden Zweigen (Urteil vorhanden / Urteil fehlt), die neue Blade kompiliert also nachweislich. **Nebenbefund mit Beweis:** der Paritäts-Check der L7-DoD ist geprüft und **negativ** — Hard-Stop-Inline-Anlage und Disambig fehlen beide („GP anlegen" ist an beiden Flächen nur Text, von der schon gelieferten `offene[].shortlist` wird nur die Anzahl gezeigt); in der Spec als geprüft-mit-Fundort vermerkt statt weiter offen zu raten. Neu im Backlog: **V-038**. Dev-Board: **#512** um zwei DoD-Zeilen ergänzt — `L7b-2a` ✅ (mit Commit + Suite-Zahl) und `L7b-2b` offen mit den belegten Fundorten; die L7-Umbrella-Zeile bleibt bewusst unchecked (Muster wie L1a/L1b, L6a/L6b in demselben Issue). Nächster Schritt: **03·L7b-2b** (Hard-Stop-Inline-Anlage „GP anlegen"/„Stub anlegen" als echte Knöpfe + „Meintest du?"-Disambig aus der Shortlist).
- **2026-07-26 · 03·L7b-1 — eine Generier-Strecke für beide Flächen + der One-Shot-Toggle ✅ (Routine-Lauf 27)** — Commit `36301eb`. L7b war für einen Tick zu groß, deshalb geschnitten: **L7b-1 = Strecke + Toggle + Ergebnis-Zeile** (hier), **L7b-2 = Kohärenz-Glied + Sub-Rezept-Stub-Flag + Paritäts-Check Hard-Stop-Inline-Anlage** (Fortsetzungspunkt in der Checkbox). Gebaut: `src/Livewire/Concerns/HatGeneratorLauf.php` (neu) · `GeneratorModal` + `VkGeneratorModal` auf den Trait gehoben · zwei geteilte Blade-Komponenten `resources/views/components/oneshot-toggle.blade.php` + `oneshot-ergebnis.blade.php` · Poll-Block + Lauf-Button im VK-Blade · Job-Timeout dynamisch. Tests: `tests/Feature/GeneratorOneShotToggleTest.php` (8). **Suite 1422 · 1418 passed · 4 skipped · 0 Fail · 6713 Assertions** (Vorlauf 1414/1410, +8 Tests), 10,8 Min.
  — **Die V-035-Frage aus der Checkbox ist entschieden: Strecke vereinheitlichen, nicht Toggle weglassen.** Die Wahl stand zwischen (a) VK-Modal ohne Toggle — Feature fehlt auf der wichtigeren der beiden Flächen, weil VK-Gerichte das verkaufte Produkt sind; (b) Toggle im synchronen Web-Request — 1–4 zusätzliche Provider-Calls obendrauf, also ein Knopf, der garantiert timeoutet; (c) das VK-Modal auf `GenerateRecipeJob` heben. (c), weil der Job `vkModus` schon als Parameter kannte und die Umstellung damit kein Neubau ist, sondern das Nachziehen einer 2026-07-20 halb gelassenen Migration. **Damit ist V-035 durch diese Etappe faktisch geschlossen** — der Eintrag bleibt im Backlog auf `neu` stehen (Status setzt nur Dominique), aber er beschreibt keinen Bestand mehr; kein Selbstauftrag, sondern die in der Checkbox verlangte Vorentscheidung.
  — **Zweite Entscheidung: die geteilte Mechanik ist ein Trait, die geteilte Fläche sind zwei Blade-Komponenten.** Naheliegend wäre gewesen, die ~30 Zeilen Poll-Logik ins VK-Modal zu kopieren (so stand es auch im V-035-Vorschlag als Minimalweg). Dagegen spricht genau die Erfahrung aus L1: zwei Kopien derselben Strecke driften, und der Unterschied fällt erst auf, wenn eine Fläche etwas kann, was die andere nicht kann — hier wäre der One-Shot-Toggle sofort der erste Driftpunkt gewesen (zwei Defaults, zwei Reset-Listen, zwei Fehlerpfade). Gebaut ist deshalb `HatGeneratorLauf` mit `starteLauf()`/`pruefeErgebnis()`/`$vollAnreichern`/`$laeuft`/`$runId`/`$anreicherung` und **einem** abstrakten Punkt, an dem sich die Flächen wirklich unterscheiden: `auswahlEvent()` (`recipe-selected` vs. `vk-recipe-selected`). Vorlage ist `HatRezeptCopilot` aus L6b, dieselbe Bauart. Bei der Fläche dasselbe Motiv: `oneshot-toggle` und `oneshot-ergebnis` sind anonyme Komponenten, die Modals reichen nur `marker` (Test-Hook) und `schritte` (die Ebenen-Schrittfolge im Hinweistext, Basis vs. VK) durch — der Bestand kannte für diesen Schnitt schon `x-foodalchemist::copilot-box`.
  — **Dritte Entscheidung: das Job-Timeout wächst mit der Kaskade** (300 → 540 s im Konstruktor, sobald `vollAnreichern`; bleibt unter dem demo-Worker-Timeout von 600 s). Das ist kein Streicheln einer Zahl, sondern die Lücke, die der Default-AN-Toggle aufreißt: der Pass selbst wirft nie (L7a fängt jeden Schritt einzeln), aber gegen einen **harten Timeout-Kill** hilft das nicht — `failed()` schreibt dann `status=error`, die UI liest „Generierung abgebrochen", und das Rezept existiert trotzdem. Genau das „halbe Wrack", das die L7-DoD ausschließt. Getestet ist die Zahl beider Fälle plus `tries=1` (kein stiller Auto-Retry der KI-Kosten).
  — **Vierte Entscheidung: die Ergebnis-Zeile nennt drei Zahlen statt „fertig".** Gefüllte Felder · Schritte mit schon belegtem Ziel-Feld · offene Vorschläge in der Review-Liste. Die mittlere Zahl ist die wichtigste und wäre am ehesten weggelassen worden: „schon belegt" ist die GL-07-Grenze in Aktion (`luecken()` schneidet die Schrittfolge, es wird nichts überschrieben und kein Call bezahlt) — ohne sie liest ein Nutzer „2 von 4 gefüllt" als Teil-Ausfall. Und ein gescheiterter Pass erscheint als **amberfarbene Lücken-Zeile am erfolgreichen Ergebnis**, nicht in `$fehler`: „Rezept steht, Anreicherung unvollständig" von „Generierung fehlgeschlagen" zu trennen ist der ganze Sinn des graceful-Verhaltens, und in einer gemeinsamen Fehlerzeile wäre er verloren. Beide Fälle sind getestet, ebenso dass ein echter Job-Fehler auf dem **VK**-Pfad weiter als Fehler durchkommt (der alte synchrone Zweig fing `\RuntimeException` selbst — die Meldung darf durch die Umstellung nicht verschwinden).
  — **Kein Schema-Change, keine Migration, kein neues SQL** → MySQL-Smoke entfällt sachlich (dieser Teilschritt ist UI + Wiring; die einzige Verhaltens-Änderung an der Datenseite ist, *wo* derselbe Service läuft). **Kein MCP-Nachzug:** `voll_anreichern` steht seit L7a an `recipes.GENERATE`, hier ändert sich keine Tool-Fähigkeit. **Browser-Klickstrecke bleibt manuell offen** (headless nicht fahrbar) — beide Modals sind aber in Livewire-Tests in **beiden** Zweigen gerendert (Eingabe mit Toggle, Ergebnis mit Anreicherungs-Zeile), die neuen Blades kompilieren also nachweislich. Neu im Backlog: **V-037** (der Kapitel-Go, `FoodbookService:1060` aus `MaterializeIdeaJob`, ist der dritte `generiere()`-Aufrufer und bleibt beim Gerüst — genau der Pfad, der die meisten Gerichte auf einmal erzeugt und bei dem niemand danebensitzt; Folge: `foodbook_kapitel_ohne_text`/`konzept_ohne_wording` feuern für Gerichte, die dieselbe KI in derselben Sekunde hätte füllen können). Dev-Board: **#512** DoD-Zeile L7 auf „FAST" (L7a + L7b-1 ✅ mit Commit + Suite-Zahl, L7b-2 als offener Rest) — bewusst **nicht** abgehakt. Nächster Schritt: **03·L7b-2** (Kohärenz-Glied · Sub-Rezept-Stub-Flag · Paritäts-Check Hard-Stop-Inline-Anlage + „Meintest du?"-Disambig).
- **2026-07-26 · 03·L7a — Generieren und Anreichern werden ein Durchlauf; Phase 4 angefangen (Routine-Lauf 26)** — Commit `0e4723b`. Gebaut: `src/Services/RecipeOneShotService.php` (`anreichern()`) · `BulkEnrichService::ZIELFELDER` + `luecken()` + `laufAnlegen()` (Insert-Extraktion, `starte`/`starteGp` fahren sie jetzt) · `vollAnreichern`-Parameter am `GenerateRecipeJob` inkl. `anreicherung`-Block im Cache-Payload · `voll_anreichern` an `foodalchemist.recipes.GENERATE` (Schema + Beschreibung + Ergebnis-Feld). Tests: `tests/Feature/RecipeOneShotTest.php` (5) + 2 in `McpRecipesGenerateTest`. **Suite 1414 · 1410 passed · 4 skipped · 0 Fail · 6686 Assertions**, 11,2 Min. Kein Schema-Change, keine Migration.
  — **Die eigentliche Frage von L7 ist nicht „wie verkette ich", sondern „darf die Kaskade selbst schreiben".** GL-07 sagt: keine Auto-Persistenz. Die Anreicherungs-Strecke ist genau darauf gebaut (Vorschläge in die Review-Liste, Mensch entscheidet je Zeile) — ein One-Shot, der das erbt, liefert kein volles Rezept, sondern wieder eine Aufgabenliste. Aufgelöst über die Lesart, die das Verbot präzise nimmt: **verboten ist das Überschreiben menschlicher Pflege, nicht das Füllen einer Leerstelle.** Darum die neue Gap-Regel `luecken()`: die Schrittfolge wird vor dem Lauf auf die Schritte geschnitten, deren Ziel-Feld leer ist. Das schützt doppelt — `manual`-Werte werden nicht einmal *vorgeschlagen* (Override-First in `uebernehmen()` bleibt als zweite Wand), und ein Feld, das der Generator schon geschrieben hat, kostet keinen zweiten Provider-Call. Der MCP-Test macht das sichtbar: mit einem Stub, der `taste_direction` liefert, setzt der Generator es selbst und die Kaskade fährt nur noch `category`.
  — **Keine zweite Strecke, mit einer bewussten Ausnahme.** Prompts, Vorschlags-Speicher und Accept-Pfad sind die vorhandenen; neu ist nur die Verkettung. Die Ausnahme ist der Einstieg: `starte()` dispatcht einen `BulkEnrichJob`, und auf demo (database-Queue) liefe der **parallel** zum aufrufenden Generier-Job — `alleUebernehmen()` fände dann noch keine Vorschläge und der One-Shot wäre stumm leer. Darum ruft der Motor `verarbeiteRezept()` direkt und legt die Lauf-Zeile über das neu herausgezogene `laufAnlegen()` an (drittes Vorkommen desselben Inserts entfernt, vgl. V-032). Die Lauf-Zeile bleibt trotzdem: sie ist Fortschritts-Anker fürs Polling **und** Audit-Spur — die Vorschläge stehen in der Review-Queue, auch wenn sie in derselben Sekunde übernommen wurden.
  — **MCP-Default bewusst gegen den UI-Default.** Der Toggle in der Fläche steht auf AN (Spec-DoD), der Tool-Parameter auf **aus**: im Modal läuft die Kaskade in der Queue (Timeout 300 s), im Tool-Call synchron im HTTP-Request — 3–4 zusätzliche Provider-Calls würden die Antwortzeit vervielfachen. Der Client entscheidet, ob er sie zahlt; die Beschreibung sagt, was sie kostet. Dieselbe Fehlerklasse, gegen die `GenerateRecipeJob` überhaupt gebaut wurde (502 durch fastcgi-Timeout).
  — **Manuell offen:** die Browser-Klickstrecke ist für L7a nicht nötig (keine UI-Fläche — sie ist L7b). Kein Deploy. Office-Dev **#512** (Package 23, Features-Board): DoD-Zeile L7 auf den L7a-Stand geschrieben, Haken bleibt **offen** bis L7b (inkl. der V-035-Vorbedingung im Text). Verbesserungs-Backlog: **V-035** (der 502-Fix hängt nur am Basisrezept-Modal — `VkGeneratorModal` ruft die KI weiter im Web-Request; das blockiert den L7b-Toggle auf der wichtigeren Fläche) und **V-036** (Feld-Zuordnung der Schritte steht jetzt zweimal: `ZIELFELDER` und der Accept-`match`). Nächster Schritt: **L7b** — zuerst die V-035-Frage entscheiden (VK-Modal auf den Job heben oder Toggle dort auslassen), dann Toggle + Fortschritts-/Ergebnis-Zeile in beiden Modals.
- **2026-07-26 · 21·S5b-2 — „Gericht oder Komponente?" bekommt einen eigenen Pass; Tranche B und Phase 3 komplett (Routine-Lauf 25)** — Commit `b70f99a`. Gebaut: `src/Services/RecipeBauartService.php` (Erzeuger, read-only) · Prompt `recipe.bauart` (Tier B, `max_tokens` 1200, **nicht** in `FOOD_DNA_KEYS` — Klassifikator) · Migration `2026_07_26_000005_add_structure_reviewed_at_to_foodalchemist_recipes` · Pass-Konzept in `RecipeFindingService` (`PASS_COPILOT`/`PASS_BAUART` + `PAESSE`, durchgereicht an `arbeitsmenge`/`pruefeUndAblegen`/`speichere`) · Arten-Split in `RecipeReviewService` (`ARTEN_COPILOT`/`ARTEN_STRUKTUR`/`ARTEN`) + `bauart`-Zweig in `normalisiere()` · `SignalTyp::RezeptGerichtVsKomponente` + Label/Icon + `istKiUrteil()` auf zwei Typen · Register-Zeile `rezept_gericht_vs_komponente` (und Einschränkung der S5b-1-Zeile auf `ARTEN_COPILOT`) · `--pass=` am Batch-Command · Karten-Farbe/-Wort + `strukturentscheidung`-Satz in `copilot-box.blade.php` · MCP `recipe_findings.SEARCH` (Beschreibung + `signal_kandidaten` je Pass). Tests: `tests/Feature/RecipeBauartTest.php` (6) + angepasste Zählungen in `RezeptQualitaetSignaleTest` (12→13, deterministisch bleibt 11), `McpRecipeFindingsTest`, `PromptRegistryTest`. **Suite 1407 · 1403 passed · 4 skipped · 0 Fail · 6640 Assertions**, 10,9 Min.
  — **Die Ablage-Frage aus S5b-1 ist zugunsten derselben Tabelle mit eigener `kind`-Art entschieden — und daraus wurde die eigentliche Arbeit: das Konzept „Pass".** Zwei Erzeuger auf einer Tabelle brauchen zwei Grenzen, und beide sind Pflicht, nicht Kosmetik. **(a) Die Arten-Grenze:** `speichere()` schließt am Ende jedes Laufs alle offenen Befunde, die nicht mehr gemeldet wurden — ohne Einschränkung auf die Arten des Passes hätte der Bauart-Lauf reihenweise Mengen-Befunde des Copilot als „verschwunden" abgeräumt. **(b) Der Stempel:** die Arbeitsmenge ist change-driven über `ai_reviewed_at < updated_at`; würden sich beide Pässe diesen Stempel teilen, nähme der zuerst laufende dem anderen die Fälligkeit weg und kein Rezept käme je durch beide. Die schema-freie Alternative (Fälligkeit aus den Befund-Zeilen ableiten) scheitert am Regelfall: ein Rezept, dessen Bauart stimmt, erzeugt gar keine Zeile — es bliebe für immer „nie geprüft" und würde in jedem Lauf erneut bezahlt. Darum die Migration `…_000005`.
  — **Der Fingerprint war die zweite Falle, und die Spec hat sie benannt** („kennt kein Ziel ohne Zutat"): der Bestands-Fingerprint fällt ohne Zielzeile auf die *Begründung* zurück. Bei einem rezept-weiten Befund heißt das, dass jede neu formulierte Begründung eine neue Zeile und ein wiederauferstandenes Signal ergäbe — „Lass das so" wäre wirkungslos. Gelöst über einen Kurzschluss: für `ARTEN_STRUKTUR` ist der Schlüssel **die Art allein**, weil es je Rezept genau einen solchen Befund gibt. Ein Test hält es fest (zwei Läufe, völlig andere Begründung, `seen_count` 2, Status bleibt `verworfen`).
  — **Nie `auto_applicable`, mit Absicht.** Die „Übernahme" dieses Befunds wäre ein Kippen von `is_sales_recipe` — daran hängen Taxonomie, Verkaufs-Facetten und Darreichungen. Er trägt `applicability = strukturentscheidung`, das Panel schreibt hin, was von Hand zu tun ist, und der einzige Knopf ist „Lass das so". Konsequent kein Cockpit-Fixer/-Assist (Begründung in `SignalCockpit` ergänzt).
  — **Zwei Signale statt eines**, deshalb musste die S5b-1-Zeile mit angefasst werden: `offeneUeberSchwelle()` hat einen optionalen Arten-Filter bekommen, `rezept_plausi_ki` zählt jetzt `ARTEN_COPILOT`. Ohne das stünde dasselbe Rezept in zwei Signalen für einen Sachverhalt, den nur eines von beiden beschreibt. Dieselbe Trennung im MCP: `signal_kandidaten` liefert `{copilot, bauart}` statt einer irreführenden Summe.
  — **Zwei echte Funde aus den Testläufen, beide behoben:** (1) der Bauart-Befund landete zunächst mit `applicability = 'anwendbar'` in der Ablage, weil `speichere()` dieses Feld defaultet und der neue Erzeuger nicht durch `normalisiere()` läuft — der Erzeuger setzt es jetzt selbst, die Wurzel ist **V-034**; (2) `PromptRegistryTest` (Inventar-Disziplin) schlug beim ersten Voll-Lauf an, weil `recipe.bauart` im Soll-Inventar fehlte — nachgetragen. Das ist der Test, der genau dafür da ist.
  — **Manuell offen:** die Migration ist auf der **geteilten Dev-MySQL nicht gefahren** (Routine-Regel „nicht blind gegen die geteilte Dev-DB mutieren") — sie ist additiv und nullable, muss aber vor dem nächsten MySQL-Smoke einmal laufen. Und die Browser-Klickstrecke (Cockpit → Bauart-Signal → Objekt-Liste → Rezept-Modal mit dem Bauart-Kasten) ist headless nicht fahrbar. Kein Deploy. Verbesserungs-Backlog: **V-034**. Office-Dev **#618** (Package 23, Features-Board): DoD-Zeile S5 auf den S5b-2-Stand geschrieben, **abgehakt** (6/6) und das Issue nach *Done* verschoben (`is_done`) — Spec 21 ist mit allen Tranchen A/B/C/D/E/P abgeschlossen. **Damit ist Tranche B komplett und Phase 3 abgeschlossen.** Nächster Schritt: **Phase 4 · L7** (One-Shot-Vollerstellung) — vor dem Bau die L7-Kette gegen HEAD verifizieren (`BulkEnrichService`-Strecke, LA-First-Mint aus Spec 07) und in Teilschritte schneiden, M–L ist für einen 30-Min-Tick zu groß.
- **2026-07-26 · 21·S5b-1 — der KI-Befund wird ein Signal, und der Klick landet auf ihm (Routine-Lauf 24)** — Commit `fde5c8c`. Gebaut: `SignalTyp::RezeptPlausiKi` + `istKiUrteil()` (Docblock von `istRezeptQualitaet` nachgezogen: die Ebene umfasst jetzt A **und** B) · Register-Zeile `rezept_plausi_ki` in `DataQualityService::rezeptQualitaetChecks()` · `RecipeFindingService::offeneBefundeFuer()` · `finding_id`-Durchreichung in `RecipeReviewService::normalisiere()` · `HatRezeptCopilot::copilotAusAblage()` + `copilotBefundVerwerfen()` + Loop-Closure in `copilotUebernehmen()` · `copilot`-Parameter an `RecipeModal::oeffnen`/`VkModal::oeffnen` · Sprung + „Lass das so"-Knopf in `detail-panel.blade.php`/`copilot-box.blade.php` · Nicht-Mapping-Begründung in `SignalCockpit`. Tests: `tests/Feature/RezeptPlausiKiSignalTest.php` (4) + 3 in `RecipeCopilotUiTest`, `RezeptQualitaetSignaleTest` Enum-Zählung 11→12 (davon 11 deterministisch). **Suite 1401 · 1397 passed · 4 skipped · 0 Fail · 6599 Assertions**, 10,4 Min.
  — **Drei tragende Entscheidungen.** (1) **Die Auswahl-Regel wird geliehen, nicht nachgebaut:** `whereIn('id', offeneUeberSchwelle($t)->select('recipe_id'))` statt eigenem EXISTS — die Befund-Zeilen sind nicht team-hierarchisch (Messreihen-Ausnahme S5a), ihr Scope steckt im Service; nachgebaut wäre er die zweite Wahrheit. (2) **Der Sprung zeigt die Ablage, nicht einen frischen Pass** — das Signal zählt die abgelegten Zeilen, ein Live-Call beim Klick kostete Egress *und* könnte anderes liefern. Frisch ist nur die Anwendbarkeit (`bewerte()`, dieselbe Stelle wie nach jedem Apply). (3) **Die Schleife schließt am Befund, nicht am Batch** — sonst zählt das Signal einen erledigten Befund bis zum nächsten Lauf weiter; Übernehmen ⇒ `uebernommen` (darf wiederkommen), „Lass das so" ⇒ `verworfen` (hält). Kein Cockpit-Knopf, mit Begründung im Code: hinter dem Signal liegt schon ein KI-Urteil je Befund. Kein Schema-Change, keine Migration, kein Provider-Call in den Tests. MCP-Lockstep ohne Arbeit (Typ-Enum kommt seit S0 aus `SignalTyp::cases()`). Neue Beobachtung: **V-033** (`planFor` → 29 von 37 Typen fallen auf `null` und das Panel rendert dazu gar nichts; Vorschlag dritte Art `navigate`). **Manuell offen:** die Browser-Klickstrecke Cockpit → Objekt-Liste → Modal mit aufgeklappten Befunden ist headless nicht fahrbar. Office-Dev #618 (Package 23, Features-Board): DoD-Zeile S5 auf den S5b-1-Stand geschrieben, Haken bleibt offen bis S5b-2. Nächster Schritt: **21·S5b-2** (`rezept_gericht_vs_komponente`, eigener Erzeuger; zuerst die Ablage-Frage klären).
- **2026-07-26 · 21·S5a — der Copilot-Befund wird eine Zeile, der Batch läuft über den Bestand (Routine-Lauf 23)** — Commit `28deaee`. Gebaut: Migrationen `2026_07_26_000003_create_foodalchemist_recipe_findings_table` + `…_000004_add_ai_reviewed_at_to_foodalchemist_recipes` · Model `FoodAlchemistRecipeFinding` · `src/Services/RecipeFindingService.php` (`arbeitsmenge`/`pruefeUndAblegen`/`speichere`/`entscheide`/`offeneUeberSchwelle`, Konstante `KONFIDENZ_SCHWELLE = 0.7`) · `src/Console/RecipeFindingsCommand.php` (`foodalchemist:recipe-findings`, Lauf-Bookkeeping als `type=review` in `foodalchemist_bulk_runs`) · MCP `RecipeFindingsSearchTool` + `RecipeFindingsPutTool` · `tests/Feature/RecipeFindingServiceTest.php` (8) + `tests/Feature/McpRecipeFindingsTest.php` (4). **Suite 1394 · 1390 passed · 4 skipped · 0 Fail · 6549 Assertions**, 10,3 Min.
  — **Der erste Voll-Lauf hat einen echten Fund geliefert** (1393 · 1 Fail): `PolicyTest` „Trait-Vertrag" verlangt `BelongsToTeamHierarchy` an *jedem* Model, und das neue trägt es bewusst nicht. Aufgelöst über die im Vertrag vorgesehene Ausnahmeliste (`$messreihen`, bisher nur `FoodAlchemistSignalSnapshot`) statt durch Umbiegen des Models — mit Begründung im Docblock: ein Befund ist ein Urteil **plus die Entscheidung darüber**; geerbte Sichtbarkeit würde ein fremdes `verworfen` in die eigene Inbox mischen und den Befund dort stumm stellen. Eltern und Kind dürfen zum selben Eltern-Rezept getrennt urteilen.
  — **S5 ist in zwei Teilschritte geschnitten, und der Schnitt liegt an der Stelle, die die Spec nicht kannte:** Tranche B setzt eine Ablage voraus (V-031), die es nicht gab. S5a = Ablage + Motor, S5b = die zwei Signal-Typen. Dieser Lauf schreibt **bewusst noch keine Signale** — sonst wäre die Signal-Definition an eine Ablage geknüpft, die im selben Tick entstanden ist und nie an echten Daten stand.
  — **Der Dedup-Fingerprint kennt den Wert NICHT** (`art + Zielzeile`). Mit Wert wäre jede Ablehnung aushebelbar: der Folgelauf schlägt 1250 statt 1200 vor, neuer Fingerprint, neue Zeile, wieder Signal — dasselbe Rausch-Muster, gegen das S2b die `signal_policies` gebaut hat, nur eine Ebene tiefer. Der Wert wird an der bestehenden Zeile nachgeführt, der Status bleibt.
  — **Asymmetrie mit Absicht: `verworfen` hält, `uebernommen` darf zurückkommen.** „Lass das so" ist eine Entscheidung über den Sachverhalt; ein *angewendeter* Befund, der wieder auftaucht, ist dagegen die Nachricht „der Fix hat nicht gegriffen" und geht auf `offen`. `seen_count`/`first_seen_at`/`last_seen_at` tragen die Wiederkehr — das ist Punkt 6 aus Spec 21 §7 (am Signal weiter offen als V-009) auf der Befund-Ebene.
  — **Die Arbeitsmenge ist change-driven, und genau daran hängt eine Falle:** fällig ist `ai_reviewed_at IS NULL OR ai_reviewed_at < updated_at`. Ein Eloquent-`save()` für den Prüf-Stempel hätte `updated_at` mitgezogen und das Rezept sofort wieder als geändert markiert — Dauer-Egress. Der Stempel läuft darum als roher `DB::table`-Update; ein Test hält fest, dass die Arbeitsmenge nach der Prüfung leer ist. Zusammen mit `--limit` (Default 25) arbeitet der Batch den Bestand über mehrere Läufe ab.
  — **`pruefe()` bleibt read-only, und das kostet etwas.** Der neue Service ist der einzige Schreiber, damit `foodalchemist.recipes.REVIEW` seine `read_only`-Zusage behält. Preis dafür: der **UI-Pass legt weiter nichts ab**, und `RecipeReviewService::uebernehmen()` stempelt keine Befund-Zeile. Beide Hälften sind in V-031 als „Teil-Umsetzung im Auftrag" vermerkt und bleiben Dominiques Entscheid — sie zusammenzuführen ist der eigentliche Level-up, nicht ein Nebenschritt dieser Etappe.
  — **MySQL:** beide Migrationen gefahren (additiv + idempotent-geguarded), Schema-Backup der `recipes`-Tabelle unter `database/backups/PRE_S5A_recipes_schema.sql` — ein voller Dump ist auf dem Host weiter nicht möglich (kein `mysqldump`, die Warnung aus S3b-3 steht damit erstmals scharf bei einer Bestandstabelle; abgesichert wurde stattdessen über Additivität + `Schema::hasColumn`-Guard + `down()`). **Read-only-Smoke:** die Fälligkeits-Abfrage läuft dialektfrei (`ai_reviewed_at IS NULL DESC` als Sortier-Ausdruck) und meldet **82 von 95** Rezepten fällig; `offeneUeberSchwelle` = 0 (nichts abgelegt), beide Tools registriert. Kein Provider-Call, nichts geschrieben.
  — **Nächster Schritt:** **21·S5b** — `rezept_plausi_ki` als Register-Zeile über `whereExists` auf offene Befunde ≥ 0,7 (Bauart Tranche A/C/D) + Fixer-Mapping „Modal mit offenem Copilot". **Vorher entscheiden:** `rezept_gericht_vs_komponente` hat **keinen Erzeuger** — der Copilot-Prompt beurteilt Zutaten, nicht die Bauart-Klassifikation; entweder eigener Prompt/Pass (Vorlage Skript 272, 68 geflaggt) oder der Typ bleibt in S5b draußen. Ein echter Batch-Lauf gegen den Provider (Egress) ist bewusst **nicht** gefahren — der ist Dominiques Freigabe, nicht die der Routine. Browser-Klickstrecke: für S5a nicht nötig (keine UI-Fläche).
- **2026-07-26 · 03·L6b — der Copilot bekommt seine zwei Flächen; L6 komplett (Routine-Lauf 22)** — Commit `31e4ea3`. Gebaut: `src/Livewire/Concerns/HatRezeptCopilot.php` (Trait: `copilotPruefen`/`copilotUebernehmen(int $i)`/`copilotAlleUebernehmen`/`copilotVerwerfen`/`copilotZuruecksetzen`) + `resources/views/components/copilot-box.blade.php` (Befund-Karten mit art-Farbe, Konfidenz, Begründung, Einzel-Apply, Hard-Stop-Zeile) + `RecipeReviewService::bewerte()` (Neubewertung ohne KI-Call) + Einbindung in `RecipeModal` (`data-copilot`) und `VkModal` (`data-vk-copilot`); `tests/Feature/RecipeCopilotUiTest.php` (8). **Suite 1382 · 1378 passed · 4 skipped · 0 Fail · 6484 Assertions**, 9,9 Min. Kein Schema-Change, keine Migration, kein MCP-Nachzug (der Apply bleibt bewusst UI-only, s. L6a).
  — **Eine Fläche, zwei Einbau-Orte statt zwei Flächen.** Der L1-Fehler war die Kopie; hier trägt ein Trait die Livewire-Hälfte und eine anonyme Blade-Komponente die Darstellung — die Modals reichen nur `prefix` (Marker `data-vk-*`) und `zeilenWort` (Zutat/Komponente) durch. Die Ebenen-Wahl (Basis vs. Gericht) passiert ohnehin eine Etage tiefer im Service über `is_sales_recipe`, das UI-Verhalten ist danach identisch — es gibt schlicht nichts zu differenzieren.
  — **Die Entscheidung, die in der Spec fehlte: was passiert mit den ÜBRIGEN Befunden nach einem Apply?** Ein `entfernen` löscht die Zielzeile, auf die eine zweite Karte zeigt — deren Knopf bliebe grün und schriebe gegen eine Zeile, die es nicht mehr gibt. Darum wird die Restliste nach jedem Apply durch `RecipeReviewService::bewerte()` geschickt: dieselbe `normalisiere()`-Mechanik wie beim Prüf-Pass, aber **ohne** zweiten LLM-Call (die normalisierte Ausgabe ist gültige Eingabe — die Bewertung ist idempotent). Damit bleibt die Anwendbarkeits-Entscheidung an genau einer Stelle, so wie L6a sie gesetzt hat. „Alle übernehmen" ist deshalb auch keine Schleife über eine Momentaufnahme, sondern nimmt jeweils den ersten anwendbaren Befund und bewertet neu — mit Fortschritts-Garantie (Liste muss kürzer werden) gegen Endlosschleifen.
  — **Nicht anwendbare Befunde bleiben sichtbar, statt weggefiltert zu werden.** Ein `fehlt` ohne Bestandstreffer ist kein Fehler, sondern der Hard-Stop: die Karte sagt „erst GP anlegen"/„erst Basisrezept (Stub) anlegen" und lässt den Koch entscheiden. Genau diese Hälfte ist für ihn die interessantere. Der Knopf dorthin fehlt bewusst — ein Anlege-Flow aus dem offenen Modal heraus ist eine eigene Strecke, keine Zeile in dieser Etappe.
  — **🐞 Bug-Fund nebenbei (NICHT gefixt, gehört auf das Bugs-Board):** `RecipeModal::ladeRezept()` (`src/Livewire/Recipes/RecipeModal.php:73`) resettet nur `fehler` + `form` — die L1a-Revise-Vorschau (`ueberarbeitung`/`ueberarbeitenOffen`/`anweisung`) und `bulkRunId` überleben den Rezept-Wechsel; `VkModal::formZuruecksetzen()` resettet sie dagegen explizit. Über die Sub-Navigation (Rezept A → Sub-Rezept B, `navStack`) steht damit die Vorschau von A über B, und „Übernehmen" schreibt A's Zutatenliste in B. **Verifiziert** mit einer Wegwerf-Probe (Vorschau von A überlebt `oeffnen(B)`, `recipeId` = B), die danach wieder gelöscht wurde. Für die eigene Fläche ist das Loch zu: `copilotZuruecksetzen()` hängt in beiden Modals im Wechsel-Pfad.
  — **Nächster Schritt:** **21·S5 Tranche B** (Batch-Lauf des Copilot über den Bestand → Findings über Schwelle werden Signale). Vorher lesen: **V-031** — der Copilot-Befund hat heute keine Ablage (nur Livewire-State), S5 braucht genau die. Browser-Klickstrecke für L6 gesamt bleibt manuell offen. **Dev-Board nachgezogen:** #512 — L6-Zeile abgehakt + vier Zeilen ergänzt (L6a ✅, L6b ✅, L6-Rest Klickstrecke, L6-Nachzug V-031); der Bug-Fund liegt als **#638** (Bugs-Board, `high`) mit Fix-Vorschlag + DoD.
- **2026-07-26 · 03·L6a — der Copilot prüft und rät nicht: Motor + MCP ✅ (Routine-Lauf 21)** — Commit `3c0ed84`. Gebaut: `src/Services/RecipeReviewService.php` (`pruefe()` read-only mit `is_sales_recipe`-Zweig · `normalisiere()` mit `auto_applicable` + `status` je Befund · `uebernehmen()` als EIN Befund je Aufruf über `syncIngredients`) + `RecipeReviseService::bestandsZeile()` herausgezogen (`syncZeilen()` baut jetzt darauf) + Prompt-Verträge `recipe.review`/`vk.review` ersetzt (max_tokens 6000) + `src/Tools/RecipesReviewTool.php` (read-only) + Registrierung; `tests/Feature/RecipeReviewServiceTest.php` (7) + `tests/Feature/McpRecipesReviewTest.php` (4). **Suite 1374 · 1370 passed · 4 skipped · 0 Fail · 6441 Assertions**, 10,0 Min. Kein Schema-Change, keine Migration, kein Blade.
  — **Der Kern der Etappe ist nicht der Prompt, sondern „ist dieser Befund anwendbar?".** Ein `hinweis` hat kein Schreibziel, eine erfundene `zutat_id` auch nicht, und ein `fehlt` ohne Bestands-Treffer darf nicht geraten werden. Diese Entscheidung fällt einmal im Service und wird als `auto_applicable` + `status` (das Warum) mitgeliefert — sonst hätten UI (L6b) und MCP sie je einmal getroffen und wären gedriftet. Drei Griffe für reale Modell-Ausgaben: Zielzeile auch über den Namen (exakt, keine Fuzzy-Suche), unbekannte `art` wird zum `hinweis` entschärft statt verworfen, Konfidenz als Zahl/Prozent/hoch-mittel-niedrig lesbar.
  — **Zweite Entscheidung: der granulare Apply umgeht V-027, statt ihn zu heilen.** `syncIngredients` ist Voll-Ersatz — der Copilot schickt darum den kompletten Bestand über die neue `bestandsZeile()` und mutiert genau die Zielzeile. Der Umweg über den Sync ist Absicht (dort hängen #508-Grounding und `recomputeAndPropagate`); ein direkter Zeilen-Write hätte beides zweimal. Die Wurzel bleibt offen und gehört Dominique (V-027 Level-up: `patchIngredient` + id-Treffer-Fallback in `syncIngredients`).
  — **Nebenbefund:** die reservierten Prompt-Keys trugen einen Vertrag (`{befunde:[{schwere,text}]}`), den keine Fläche verwenden kann. Er wurde ersetzt, ohne dass irgendetwas Mechanisches davon Notiz genommen hat → **V-030** (Ausgabe-Vertrag nur als Prosa im `task`, `AiProposal` prüft nur den Umschlag). Die erste Hälfte von **V-004** (Keys ohne Konsumenten) ist damit faktisch erledigt — Status setzt Dominique.
  — **Nächster Schritt:** L6b (Findings-UI + Knopf `🧑‍🍳 Copilot` in `RecipeModal` UND `VkModal`; Apply je Karte gegen `RecipeReviewService::uebernehmen`, danach `zutatenVersion`-Remount wie im Revise). Browser-Klickstrecke für L6 gesamt bleibt manuell offen. Dev-Board-Nachzug (#512) in diesem Lauf **nicht** gemacht — Nachzug mit L6b.
- **2026-07-26 · 03·L1b — „✨ Alles anreichern" am Gericht, auf der richtigen Ebene ✅; L1 komplett (Routine-Lauf 20)** — Commit `b693a8a`. Gebaut: `BulkEnrichService::SCHRITTE_VK` (`description|wording|plating|speisen_klasse`) + `NUR_GERICHT` + `starteVk()` + `$type`-Parameter an `starte()` (Lauf-Typ `enrich_vk`) + zwei neue `proposeFeld`-Arme (`vk.wording`/`vk.plating`, geteilter `gerichtKontext()`) + Klassen-Weg über `SpeisenKlassenService` + zwei neue `uebernehmen()`-Arme + `uebernehmeSpeisenKlasse()`; in `src/Livewire/Verkauf/VkModal.php`: `allesAnreichern`/`bulkAlleUebernehmen`/`bulkRunId` (+ im `formZuruecksetzen`-Reset) + Lauf-Status im `render`; Blade: Knopf im Actions-Slot + Poll-Status-Zeile; `tests/Feature/VkBulkEnrichTest.php` (7 Tests). **Suite 1363 · 1359 passed · 4 skipped · 0 Fail · 6388 Assertions**, 9,8 Min.
  — **Die Etappe bestand daraus, die Spec-Annahme zu widerlegen.** „BulkEnrichService kann VK schon — nur Button/Wiring" stimmt nicht: `SCHRITTE` = `description|category|geschmack` ist die Basisrezept-Folge, `category` ist die 186er-Produktions-Kategorie. Wörtlich verdrahtet hätte der Knopf am Gericht die falsche Ebene angereichert (genau die Falle, die die DoD-Zeile aus Lauf 19 vorhergesagt hat). `geschmack` bewusst NICHT in `SCHRITTE_VK`: am Gericht liegt es schon unter dem ✨-Eigenschaften-Knopf. Namens-Regel dabei festgeschrieben: **Schritt-Name = Suffix des Prompt-Keys** — gilt im Bestand ohnehin durchgehend (`recipe.description`/`recipe.category`/`recipe.geschmack`), macht aber erst benannt jeden Vorschlag auf seine Registry-Zeile zurückführbar.
  — **Zweite Entscheidung: der Klassen-Schritt bekommt keine eigene Logik.** `classify()` für den Vorschlag, `acceptKlasse()` für die Übernahme — Taxonomie-Aufbau (nur aktive HGs, E7), Klassen-Validierung, Override-First, Besitzer-Regel D1 und der `accepted_at`-Stempel liegen dort schon und werden vom Detail-Panel gefahren. Der Preis dafür: das Veto kommt als Exception statt als `null`, also fängt `uebernehmeSpeisenKlasse` `\Throwable` und lässt den Vorschlag offen. Das ist billiger als eine zweite Stelle, an der die Taxonomie-Regeln gepflegt werden müssten.
  — **Dritte Entscheidung: Ebenen-Schutz zweifach.** `starteVk()` schneidet die Arbeitsmenge auf `->verkauf()` (Basisrezept fällt raus statt Unsinn zu erzeugen) **und** `proposeFeld` wirft bei den drei `NUR_GERICHT`-Schritten auf einem Basisrezept (wer `starte()` direkt mit `SCHRITTE_VK` füttert, bekommt Fehlzeilen). Einer allein hätte gereicht, um den UI-Pfad zu schützen — beide, damit auch der Service-Pfad ehrlich ist; je ein Test.
  — **Kein MCP-Nachzug, und diesmal aus einem anderen Grund als bei L1a:** die gesamte Bulk-Proposal-Fläche hat kein Tool (weder Rezept- noch GP-Seite) — ein Client kann keinen Lauf starten, keine Vorschläge sehen, keinen übernehmen. Das ist eine eigene Fähigkeit (Lauf + Liste + granularer Accept), nicht ein Anhängsel dieser Etappe. Kein Schema-Change: nur neue Werte in `bulk_proposals.field` und `bulk_runs.type`.
  — **Beobachtungen:** **V-029** (Review-Liste rendert `bulk_proposals.value` roh — `category` zeigt eine bare ID, der Reviewer entscheidet über eine Zahl; `speisen_klasse` legt darum ID+Label ab, womit drei Wert-Formen in einer Spalte liegen) · zweiter Fundort für **V-028** samt der unangenehmeren Gegenrichtung: die ✨-Einzelknöpfe schreiben in `$form`, `updateVk` stempelt beim Speichern genau diese Felder auf `manual` — ein nie handgeschriebener Text blockt danach jeden KI-Accept (das ist der Grund, warum der Bulk-Weg eine eigene Proposal-Strecke hat statt den Einzelknopf zu fahren).
  — **Dev-Board:** #512 — L1b-Zeile ergänzt (checked), die **L1-DoD-Zeile jetzt gecheckt** (L1 komplett) samt L1a-Zeile, plus neue unchecked Zeile „L1-Rest: Browser-Klickstrecke im VK-Editor". Browser-Abnahme headless nicht möglich → **manuell offen**.
  — **Nächster Schritt:** **L6** (Rezept-Copilot, M–L) — teilt sich die Matching-/Apply-Strecke mit L1a und ist der größere Hebel; beim Bau die Prompt-Registry befolgen (`recipe.review`/`vk.review` sind reserviert, die DoD-Zeile nennt veraltete Namen) und V-027 mitdenken (granularer Apply braucht ein Teil-Update).
- **2026-07-26 · 03·L1a — „mach das Gericht vegan": Freitext-Revise am VK-Gericht ✅, Phase 3 angebrochen (Routine-Lauf 19)** — Commit `9386796`. L1 war als „Revise + Anreichern" geplant und ist **geschnitten**: L1a = die Revise-Strecke, L1b = der Anreichern-Knopf (Begründung in der Checkbox — die Bulk-Schrittfolge ist heute die Basisrezept-Ebene und muss für VK erst festgelegt werden, sonst reichert der Knopf am Gericht `recipe.category` statt Wording/Plating an). Gebaut: `src/Services/RecipeReviseService.php` (`vorschau()` + `syncZeilen()`) · Prompt `vk.ueberarbeiten` in `config/foodalchemist.php` + `AiGatewayService::FOOD_DNA_KEYS` + `PromptRegistryTest`-Soll · `VkModal::kiUeberarbeiten`/`facetten`/`ueberarbeitungUebernehmen`/`ueberarbeitungVerwerfen` + Revise-Box in `vk-modal.blade.php` (Zutaten-Sektion, neben 🎭) · `tests/Feature/VkReviseTest.php` (6 Tests). Suite **1356 / 1352 passed / 4 skipped / 0 Fail** (6355 Assertions, 9,7 Min).
  — **Die tragende Entscheidung war „extrahieren statt portieren".** Die Spec sagt „Portierung, keine Neuentwicklung", und wörtlich genommen hätte das geheißen: `matchVorschau` (40 Zeilen) + den Zeilenbau aus `ueberarbeitungUebernehmen` (25 Zeilen) ins VkModal kopieren. Dann hätte der #508-Grounding-Fix zwei Orte gehabt — und die DoD-Zeile „geteilte Strecke, nicht dupliziert" wäre formal erfüllt (beide rufen `syncIngredients`) und faktisch verletzt gewesen. Stattdessen liegt die Lese-/Übersetz-Hälfte jetzt einmal im `RecipeReviseService`; `RecipeModal::matchVorschau` ist ein Durchgriff geblieben, damit Blade und der Bestands-Test ihren Aufruf behalten.
  — **Facetten-Erhalt ist zweistufig gebaut, weil eine Stufe nicht reicht.** Nur den Schreib-Umfang zu beschränken (Facetten kommen nicht zurück) hätte den Fall „mach es vegan" trotzdem kaputt gemacht: die KI hätte Komponenten vorgeschlagen, die zur Diätform der Speisen-Klasse nicht passen, und die Facette wäre formal intakt, inhaltlich unterlaufen. Darum gehen die Facetten als **Vorgabe** in den Prompt (`facetten()`: Klasse, Diätform, Darreichungs-Formen, Verkaufseinheit, Portion, Aufschlagsklasse) mit der expliziten Anweisung, sie nicht auszugeben und einen Widerspruch stattdessen in `aenderungs_notiz` zu melden — und der Schreibpfad kennt sie zusätzlich gar nicht (`REVISE_TEXTE`-Whitelist + `role` aus dem Original). Der Pest-Fall dazu schickt Facetten-Felder bewusst MIT und beweist, dass keins ankommt.
  — **Kein MCP-Nachzug, bewusst** (DoD lässt „begründen oder L5 mitlösen"): ein Revise-Tool wäre entweder eine Auto-Persistenz (verletzt GL-07 — der Vorschau-/Übernehmen-Schnitt ist der Kern des Features) oder es gäbe eine Vorschau zurück, die kein Client übernehmen kann, weil der Accept-Zustand in der Livewire-Komponente lebt. Der schreibende Weg über MCP existiert schon und ist granular: `recipe_ingredients.PUT` + `recipes.PUT`. Ein LLM-Client, der „mach das vegan" umsetzen soll, plant selbst und schreibt gezielt — er braucht kein zweites Reasoning-Feld im Modul.
  — **Nebenbefund, der zum Fund geworden ist:** beim Extrahieren fiel auf, dass der Alt-Revise beim Übernehmen `role`, `is_value_relevant`, `quantity_max`, `trimming_loss_pct` und `cooking_loss_source` auf Default zurücksetzte — `syncIngredients` baut seine `$attrs` bei UPDATE wie bei INSERT aus dem Payload, „Feld fehlt" heißt dort „lösche es". Im neuen Service per Original-Fallback geschlossen (deshalb überlebt die VK-Rolle das Revise, siehe Test). Die Wurzel bleibt und trifft als nächstes L6 (granularer Copilot-Apply) → **V-027**; zweite Beobachtung des Laufs → **V-028** (KI-Accepts schreiben am `updateVk`-Gate vorbei, relevant sobald L8 Preise schreibt).
  — **Dev-Board:** Issue #512 (Package 23, To Do) um eine L1a-Zeile ergänzt; die **L1-DoD-Zeile bleibt unchecked**, weil L1b noch offen ist (dieselbe Handhabung wie bei L2a). Nebenbefund beim Nachtrag: `dev.issues.GET` gibt DoD-Items in der Liste **nicht** heraus — man sieht sie erst in der Antwort eines `dev.issues.PUT`. Ein blinder `toggle`-Index wäre geraten gewesen, darum der additive Weg. Zweiter Nebenbefund: die L6-DoD-Zeile nennt Prompts `recipe.copilot`/`copilot_vk`, reserviert sind in `AiGatewayService:45-46` aber `recipe.review`/`vk.review` — beim L6-Bau der Registry folgen, nicht dem Issue-Text.
  — **Nächster Schritt:** L1b (Anreichern-Knopf, erst VK-Schrittfolge festlegen) oder direkt **L6** (Rezept-Copilot) — L6 ist der größere Hebel und teilt sich die Strecke mit L1a; L1b ist unabhängig davon.
- **2026-07-26 · 03·L4 — „schlag mir für diese Position was vor": Top-3 mit Begründung, ohne KI ✅ (Routine-Lauf 18) — Phase 2 komplett** — Commit `1dc7828`. Gebaut: `ConceptGeneratorService::slotKandidaten()` (gerankte Kandidaten + `faktoren` + `begruendung` + `hinweis`) · `slotVorschlaege()` als dessen Projektion (Foodbook-Weg-B unverändert im Verhalten) · `rankingBegruendung()` · in `src/Livewire/Concepter/Editor.php`: `vorschlagFuerSlot`/`vorschlagUebernehmen`/`vorschlagVerwerfen` + `frameSlotZurRolle` + `kreativLeitplanken` + `belegteGerichtIds` · `✨ Vorschlag` an jeder leeren Position in `concepter/editor.blade.php` (Faktoren-Zeile, „übernehmen"/„✕", Hinweis-Zeile) · `tests/Feature/ConcepterSlotVorschlagTest.php` (10 Tests). — **Erstens: keine zweite Rangliste.** Die Spec sagt „Slot-Ranking aus `ConceptGeneratorService` als per-Slot-Button" — der Weg-B-Pfad `slotVorschlaege()` (Foodbook-Leitstelle) macht genau das schon, nur ohne Begründung und ohne Kohäsions-Basis. Deshalb ist der neue Weg der breitere und der alte seine **Projektion** (`array_map` auf vier Felder), nicht ein Nachbau: dieselben harten Filter (`filterFuerSlot`), dieselbe Sortierung (`besterKandidat`). Zwei Ranglisten hätten dieselbe Frage an zwei Flächen unterschiedlich beantwortet — und die Foodbook-Fläche driftet mit, wenn das Ranking sich ändert. — **Zweitens: der Editor-Fall ist NICHT der Generator-Fall — dort ist das Menü schon halb gebaut.** `slotVorschlaege` startet mit leerer Anker-Basis (der Generator baut von Null). Im Editor sind meist Positionen belegt, und genau die sind der Bezugspunkt („passt zum Rest"), nicht ein leeres Blatt. Also `$belegteRecipeIds` als Basis: ihre Anker kommen **aus dem Kandidaten-Pool selbst** (keine zweite Anker-Auflösung; ein Gericht außerhalb des Pools — Draft, Slot-Variante — liefert eben keine), und sie fallen aus der Kandidatenliste. Die **Paket-Ebene gehört dazu** und war im ersten Entwurf vergessen: ein Menü, dessen Gänge in Paketen stecken, wäre für den Vorschlag halb leer gewesen. Belegt per Negativ-Kontrolle: mit abgeschalteter Paket-Auffaltung fällt der Paket-Test (Alphabet-Tiebreak gewinnt), mit ihr rankt das Aroma-Geschwister oben. Die Auffaltung musste **von Hand neben `kohaesionPruefen` wiederholt** werden, zehn Zeilen entfernt → V-018 um diesen siebten Fundort ergänzt. — **Drittens: Faktoren als Text, nicht als Punktzahl — und zwei Stellen, die still falsch geworden wären.** Die Begründung folgt der Sortier-Reihenfolge („Hauptgruppe passt zur Rolle · Aroma-Nähe … · n Aroma-Anker · Preis-Abstand …"); Faktoren ohne Entscheidungskraft im Kontext (kein Preis-Anker, kein Ziel-Niveau, neutrale Convenience) **fehlen** statt als Null-Wert Rauschen zu machen. (a) `preisnaehe` ist in ZWEI Fällen 0,0 — Preis genau am Anker **und** gar kein VK-Preis; entschieden wird deshalb am `sales_net`, nicht am Vorzeichen, sonst behauptet die Zeile „kein VK-Preis" beim perfekten Treffer. (b) Ohne Kante zur Basis steht ausdrücklich „keine Aroma-Kante zur gesetzten Folge" da — weglassen hieße, der Kandidat sei nach Aroma ausgewählt worden. — **Viertens: der Knopf trägt auch ohne Gerüst, und legt trotzdem keines an.** Zuordnung Position↔Gerüst-Zeile über `role`↔`label` (normalisiert) — genau so hängt es im Bestand zusammen, `fuelleBestehendesKonzept` schreibt `role = frameSlot->label`. Ohne Gerüst-Treffer eine **nicht persistierte** Ersatz-Zeile (Slot-Regeln/Preisrahmen fallen weg, Frame-Regeln mit `slot_id NULL` wirken weiter); ohne Gerüst überhaupt ein **In-Memory-Frame** mit leeren Relationen. `frameOderAnlegen()` wäre ein Schreibzugriff auf Knopfdruck „Vorschlag" gewesen — ein Test hält fest, dass danach kein `planning_frame` existiert. Ziel-Niveau/Convenience über die vorhandene Kaskade (Konzept-`level` → Team-Segment), eine Ebene kürzer als `FoodbookService::leitplanken`. Ehrlichkeit an beiden Enden: leere Menge → „Kein Gericht erfüllt die Vorgaben (`filterBeschreibung`)", Teil-Treffer → „Nur 2 zulässige Treffer". — **Suite 1350 / 1346 passed / 4 skipped / 0 Fehler / 6323 Assertions** (+10 Tests, Vorlauf 1340/1336), 9,8 Min. Kein Schema-Change, keine Migration, kein Bestands-Test angepasst. **Kein MCP-Nachzug — Lücke, nicht Entscheidung (wie L2a):** `concepts.GENERATE` kann alles, `concept_slots.POST` kann eine bekannte ID setzen, der Schritt dazwischen fehlt — und `fillSlot` prüft Ownership, nicht die Gerüst-Regeln. Also ist die Auswahl über MCP weiter Freihand, genau da, wo „keine Erfindungen" am leichtesten kippt → **V-026** mit Vorbedingung (Kontext-Auflösung erst aus der Livewire-Komponente in einen Service, sonst baut das Tool sie nach → V-023-Muster). **Blade-Gate:** kompiliert **und Kompilat gelintet**, sauber. **Pint:** die drei berührten Dateien meldet `pint --test` mit denselben Fixern wie unberührte Bestandsdateien (`FoodbookService`, `ConceptService`) — repo-weite Drift, nicht durch diesen Lauf entstanden; nicht reformatiert (das hätte Dominiques Parallelarbeit zugemüllt). **MySQL read-only gegengeprüft:** 26 VK-Gerichte im Pool / 22 mit Anker, 3 Konzept-Gerüste, `role`↔`label` deckungsgleich (`Vorspeisen` mit Preis-Anker 8,50 € · `Hauptgang` · Frame-No-Go „Innereien") — die Dev-Fixture hat aktuell **keine** leere Position, die Fläche ist dort also nicht sichtbar; nichts geschrieben. **Nebenbefund, nicht in diesem Commit:** `Livewire/Foodbooks/Index.php:118`/`:172` und `FoodbookService::uebernehmeVorschlag:584` nehmen eine **client-gelieferte** `slot_id` per `findOrFail` ohne Prüfung gegen das eigene Gerüst — der Schreibpfad ist durch `ownedKapitel` gedeckt (verifiziert), aber Preis-Anker/Label eines fremden Gerüst-Slots können in die eigene Ansicht bzw. als Slot-Rolle in eigene Daten wandern (#504-Klasse). Als Bug behandeln, nicht im Verbesserungs-Backlog. Mein neuer Pfad hat die Lücke nicht (Slot wird über `ConceptService::detail($team,…)->slots` aufgelöst). **Browser-Klickstrecke manuell offen** (headless nicht möglich): ✨ an leerer Position · Faktoren-Zeile · übernehmen/✕ · zweiter Klick klappt zu. Neue V-Nummer: **V-026**, dazu **V-018 um den siebten Fundort ergänzt**. Dev-Board: **#512** L4-Kriterium abgehakt und ausformuliert, zwei neue Zeilen (offene Browser-Klickstrecke · MCP-Nachzug V-026); der Nebenbefund ist als **Bug #630** auf dem Bugs-Board angelegt (Board 54, mit Fix-Vorschlag + DoD) statt im Backlog geparkt. Nächster Schritt: **Phase 3 · L1+L6** (VK-Revise + Rezept-Copilot als geteilte Strecke — `RecipeReviewService`, Prompts `recipe.review`/`vk.review` sind in `AiGatewayService:45-46` reserviert; Zutaten über den #508-Pfad, nicht duplizieren).
- **2026-07-26 · 03·L2b + 21·S4c-3 — die Kapitel-Hinführung, und ein Kundentext, der bisher nirgends ankam ✅ (Routine-Lauf 17)** — Commit `593c206`. Gebaut: Feld „Hinführung“ im Kapitel-Kopf (`kapitelForm.description` + Textarea, `KAPITEL_FELDER` konnte `description` längst — nur der Editor kannte es nicht) · `FoodbookService::kiKapitelKundentextVorschlag` + `kapitelKundentextKontext` über denselben Prompt `foodbook.kundentext` mit `ebene: 'kapitel'` · geteilte Positions-Auflösung `kundentextKapitelZeile` (Buch- und Kapitel-Sicht zeigen dasselbe) · `kiKapitelText`/`kiTextZiel`/`kiTextHolen` in `src/Livewire/Foodbooks/Index.php` · Vorschau-Partial `foodbooks/partials/ki-text-vorschau.blade.php` (vorher inline, jetzt von beiden Ebenen geteilt) · `SignalTyp::FoodbookKapitelOhneText` + Register-Eintrag `foodbook_kapitel_ohne_text` + `DataQualityService::kapitelOhneText()` · `tests/Feature/FoodbookKapitelTextSignalTest.php` (10 Tests) + 8 L2b-Tests in `FoodbookKiKundentextTest`. — **Erstens, der eigentliche Befund: der fehlende Fixer war nicht die einzige Lücke — die Ausgabe fehlte auch.** `foodbook_chapters.description` war über `foodbook_kapitel.PUT` schreibbar, im Editor unerreichbar **und wurde von keiner Projektion gelesen** — `dokumentDaten` gab es nicht heraus, PDF und Präsentation druckten es nie. Nur das Formularfeld zu bauen hätte einen Kundentext erzeugt, den der Kunde nicht sieht, und das Signal hätte eine wirkungslose Lücke gemeldet. Darum ist die Projektion mitgezogen (`'text'` in der Kapitel-Zeile, Ausgabe in `dokumente/foodbook.blade.php` + `foodbooks/praesentation.blade.php`, leer/nur-Leerzeichen → `null`) — bewusst als Teil der Etappe, nicht als Backlog-Eintrag, weil L2b sonst nichts bewirkt. Der gleichartige Fall `claim` bleibt liegen → **V-025**. — **Zweitens: ein Prompt-Key, zwei Ebenen — die L2a-Entscheidung hat getragen.** Der Kapitel-Kontext ist auf DIESES Kapitel geschnitten (plus Unterkapitel; Nachbar-Kapitel gehören nicht dazu), das Buch-Briefing kommt getrennt als `rahmen_einleitung` (die Hinführung soll weiterführen, nicht wiederholen — dafür eine Zeile im `task`), ein bestehender Kapitel-Text ist `briefing_ist`, und `leitplanken` werden MIT Kapitel aufgelöst. `target_table`/`target_id` der Audit-Zeile zeigen auf das Kapitel, sonst wären n Kapitel-Calls am selben Buch nicht unterscheidbar. — **Drittens: EIN Vorschau-Zustand für beide Flächen, geroutet über `kiTextZiel` statt verdoppelt.** Buch-Kopf und Kapitel sind Master-Detail und nie gleichzeitig sichtbar. Zwei Sicherungen gehören dazu, ohne die es still falsch schreibt: Kapitel-Wechsel (`ladeKapitelForm`) und Rücksprung auf den Kopf (`kopfAnzeigen`) verwerfen den Vorschlag — sonst landete der Text des vorigen Kapitels im nächsten. Das Partial prüft die Ebene zusätzlich gegen `kiTextZiel`, damit die Buch-Fläche keinen Kapitel-Vorschlag zeigt, falls sich das Layout je ändert. — **Viertens, zum Signal: `info`, und gegenseitig ausschließend mit `foodbook_kapitel_leer`.** Gemessen wird nur, was Inhalt TRÄGT (dieselbe Inhalts-Definition wie der Nachbar-Check) — ein leeres Kapitel braucht Gerichte, nicht Text; beides zu melden wäre ein Sachverhalt zweimal (§9). Blatt-Filter ist deshalb unnötig: eine reine Klammer hat keine eigenen Blöcke. `info` statt `warnung`, weil das Kapitel druckbar und inhaltlich vollständig ist; als Warnung stünde eine Formulierungs-Aufgabe neben `foodbook_stale`. Leer-Prüfung im Haus-Muster (`LENGTH(TRIM(COALESCE(...)))=0`, dialektfrei). — **Suite 1340 / 1336 passed / 4 skipped / 0 Fehler / 6293 Assertions** (+18 Tests, Vorlauf 1322/1318), 9,6 Min. Zwei Bestands-Tests bewusst angepasst statt aufgeweicht: die Register-Liste in `FoodbookZielStaleSignaleTest` (jetzt Prefix-Vergleich + 5 Typen) und die „saubere“ Fixture in `FoodbookQualitaetSignaleTest` (bekommt einen Kapitel-Text, sonst ist sie nicht mehr sauber — genau die Aussage des neuen Checks). Kein Schema-Change, keine Migration. **MCP-Lockstep erfüllt ohne Nachzug:** der neue Typ kommt automatisch in `signale.LIST`/`.SEARCH`/`signal_policy.PUT` (Enum aus `SignalTyp::cases()`), und das Zielfeld ist über `foodbook_kapitel.PUT` schreibbar — anders als L2a, wo `foodbooks.PUT` fehlt (V-024 bleibt offen). **Blade-Gate nach der L2a-Lehre:** kompiliert **und Kompilat gelintet**, sauber. **MySQL read-only gegengeprüft:** 3 reale Kapitel, genau eines trägt Inhalt ohne Text („Sommerfest-Buffet“, 3 sichtbare Positionen) — von der Arbeitsmenge zurückgehalten, weil sein Buch `draft`/`befuellung` ist (Grenze greift); kein Kapitel im Bestand hat heute einen gepflegten Text. Nichts geschrieben, kein LLM-Call dort. **Browser-Klickstrecke manuell offen** (Kapitel-Kopf-Feld · ✨ je Kapitel · Hinführung im PDF/in der Präsentation). Neue V-Nummer: **V-025**. Dev-Board: **#369** — L2b-Kriterium abgehakt und mit dem Projektions-Nachtrag ausformuliert, drei neue Zeilen (Signal scharf · offene Browser-Klickstrecke · V-025-Beobachtung); L2 ist dort damit vollständig. Nächster Schritt: **Phase 2 · L4** (KI-Slot-Vorschlag im Concepter — Slot-Ranking aus `ConceptGeneratorService::kandidatenPool`/`filterFuerSlot` als per-Slot-Button, läuft ohne LLM; Slot ohne zulässigen Treffer sagt es ehrlich).
- **2026-07-26 · 03·L2a — Kundentext-KI für die Foodbook-Einleitung, der Knopf ist scharf ✅ (Routine-Lauf 16)** — Commit `d0f9029`. Gebaut: Prompt `foodbook.kundentext` in `config/foodalchemist.php` (Tier A, 1500 Tokens, strikt reproduktives System-Prompt) · Key in `AiGatewayService::FOOD_DNA_KEYS` · `FoodbookService::kiKundentextVorschlag()` + privater `kundentextKontext()` (direkt neben dem M11-08-`kiAndockKontext`, das genau dafür angelegt worden war) · Vorschau-Stufe in `src/Livewire/Foodbooks/Index.php` (`kiTextVorschau`/`kiTextConfidence`/`kiTextHinweis` + `kiEinleitung`/`kiTextUebernehmen`/`kiTextVerwerfen`) · Button `foodbooks/index.blade.php:226` entsperrt + Vorschau-Kasten · `tests/Feature/FoodbookKiKundentextTest.php` (9 Tests) · `REGISTRY_SOLL` in `tests/Feature/PromptRegistryTest.php` nachgezogen. — **L2 ist in zwei Teilschritte geschnitten**, und das ist der erste Befund: die eine Spec-Zeile („Prompt `foodbook.kapitel_text` … Button an `:226` entsperren") beschreibt **zwei verschiedene Flächen**. Der Button an `:226` sitzt an der **Foodbook-Einleitung** (`foodbooks.description`); das Signal `foodbook_kapitel_ohne_text` aus Tranche D braucht dagegen den **Kapitel-Text** (`foodbook_chapters.description`) — und der ist im Editor **überhaupt nicht editierbar** (der Kapitel-Kopf kennt nur `title`/`consumer_title`/`price_mode`). L2b muss also erst ein Feld bauen, bevor es einen Fixer geben kann; deshalb bleibt `foodbook_kapitel_ohne_text` weiter draußen. — **Zweitens: ein Prompt-Key für beide Ebenen, mit anderem Namen als in der Spec.** `foodbook.kundentext` statt `foodbook.kapitel_text`, unterschieden über `ebene: foodbook|kapitel` im Kontext. Zwei Keys hätten die Tonalität an zwei Orten definiert (und V-004 sagt, was mit Keys passiert, die niemand führt); ein Key, der nach nur einer der beiden Flächen heißt, wäre in Zeile zwei falsch. Der Key steht in `FOOD_DNA_KEYS`, weil ein Kundentext die Marken-Stimme *ist* — ohne die Kaskade (Team → Kunde → Foodbook → Schreibstil) wäre er beliebig. — **Drittens: der Kontext liest die Wording-Kette, nicht die internen Namen.** Die Gliederung kommt aus `dokBlockLabel` — demselben `WordingResolver`, den das PDF benutzt — und erbt dessen `visible`-Filter: was nicht gedruckt wird, sieht die KI nicht (eigener Test). Dazu das Roh-Briefing als `briefing_ist` (die KI *formt* es statt daneben zu schreiben) und die Leitplanken; Deckel 12 Positionen je Kapitel / 20 Kapitel. — **Viertens: der Überschreib-Schutz ist der Ablauf, nicht ein Feld.** Ein `notizen_manual`-Gegenstück gibt es an `foodbooks` nicht, und das Haus-Muster (`kiKundentext` am Block) schreibt den KI-Text **direkt** ins Formularfeld — für ein handgeschriebenes Briefing genau die Backup-Lehre 2026-06-30. Der Vorschlag lebt darum in eigenem Zustand, berührt `form.description` nie, und der Knopf heißt „**Ersetzen**" (plus Warnzeile), sobald schon Text im Feld steht. Eine leere KI-Antwort wirft, statt als Erfolg durchzugehen — sonst hätte „Übernehmen" das Feld *geleert*. — **Fünftens, der eigentliche Nebenertrag: eine Blade-Falle in einer dritten Variante, und die Erkenntnis, dass unsere Blade-Zusicherung keine ist.** `@if` direkt an ein Wortzeichen geklebt (`… übernommen@if(…)`) bleibt uncompiliert (Blades Regex verlangt `\B` vor dem `@`), das `@endif` dahinter wird kompiliert → verwaistes `endif`, Fehler zeigt aufs **Dateiende**. Entscheidend: **`php artisan view:cache` meldete „cached successfully"** für genau dieses kaputte Blade (es schreibt PHP heraus, ohne es zu parsen), und `php -l` auf die Quelle sieht nur HTML. Die Bau-Rahmen-Zeile „Blade vor Push `php -l`/kompilieren" trägt also nicht. Was trägt, ist kompilieren **plus Kompilat linten** — als Einzeiler aus der Sandbox: `php artisan view:clear && php artisan view:cache && for f in storage/framework/views/*.php; do php -l "$f" | grep -v "No syntax errors"; done` (leer = sauber, danach `view:clear`). Ab jetzt in jedem UI-Lauf so prüfen. — **Sechstens: ein bestehender Wächter hat gegriffen.** Die erste Voll-Suite fiel mit *einem* Fehler: `PromptRegistryTest` („keine unbekannten Keys — Inventar-Disziplin") meldete den neuen Key als nicht inventarisiert. Genau dafür ist der Test da; nachgezogen wurde das Inventar, nicht der Test aufgeweicht. — **Suite 1322 / 1318 passed / 4 skipped / 0 Fehler / 6236 Assertions** (+9 Tests, Vorlauf 1313/1309), 9,4 Min. Kein Schema-Change, keine Migration. **Kein MCP-Nachzug — hier eine Lücke statt einer Entscheidung:** die KI-Fläche ist UI-only wie ihr Vorbild `vk.marketing`, aber es gibt **kein `foodbooks.PUT`** (Kapitel und Blöcke haben PUT/POST/DELETE, der Kopf nur POST + Reads). Das Feld, das L2 füllt, ist für einen LLM-Client also unerreichbar → **V-024** mit Bauvorschlag. **MySQL read-only gegengeprüft:** Prompt registriert (Tier A/1500), Key in `FOOD_DNA_KEYS`, Kontext an beiden realen Büchern durchgespielt — FB #1 = 3 Kapitel / 3 sichtbare, korrekt aufgelöste Kunden-Zeilen („Ochsenherztomate | Burrata | Basilikum-Schaum"), FB #8 = 113-Zeichen-Briefing, **keine** Kapitel → Bogen ohne konkrete Position (korrekt, der Prompt darf nichts erfinden). Kein Generator-Lauf dort (LLM-Call + Schreibzugriff auf die geteilte Dev-DB). Nichts geschrieben. **Browser-Klickstrecke manuell offen** (headless nicht möglich). Neue V-Nummern: **V-024** neu, **V-012 um dritten Fundort + die `view:cache`-Falle ergänzt**. Dev-Board: **#512 Referenz-Zeile L2 auf L2a-Teilstand aktualisiert** (bleibt unchecked, L2b offen) · **#369 DoD nachgetragen** (das Issue hatte gar keine `acceptance_criteria`). Nächster Schritt: **Phase 2 · L2b** (Kapitel-Text-Feld im Kapitel-Kopf + KI-Button je Kapitel über denselben Prompt mit `ebene: 'kapitel'`, dann `foodbook_kapitel_ohne_text` scharfstellen — Register + Fixer-Mapping + Pest nach Muster S4c-1/-2).
- **2026-07-26 · 03·L5 — MCP `recipes.GENERATE`, Phase 2 angebrochen ✅ (Routine-Lauf 15)** — Commit `af75bf6`. Gebaut: `src/Tools/RecipesGenerateTool.php` (15 Schema-Felder) · Registrierung in `FoodAlchemistServiceProvider` neben `RecipesPostTool` · neuer optionaler Parameter `?string $createdVia = null` an `RecipeGeneratorService::generiere()` (durchgereicht an `RecipeService::create`) · `tests/Feature/McpRecipesGenerateTest.php` (8 Tests). — **Erste Entscheidung: `vk` ist ein Parameter, kein zweites Tool** (so sah es die Spec vor, und der Code stützt es: die UI-Trennung in `GeneratorModal`/`VkGeneratorModal` ist eine *Flächen*-Grenze, dahinter steht derselbe Call `generiere(..., vkModus: true)`). Ein `verkaufsrezepte.GENERATE` hätte einen zweiten Namen für dieselbe Fähigkeit vergeben — und der LLM-Client hätte raten müssen, welcher der richtige ist. Ein Test hält fest, dass dasselbe Tool beide Sorten liefert. — **Zweite: `created_via` wird ein Service-Parameter, nicht ein Nachtrags-`update()` im Tool.** Naheliegend war, im Tool nach der Generierung `created_via='mcp'` zu schreiben — dann läge die Lineage aber außerhalb der Transaktion und wäre bei jedem künftigen Aufrufer erneut zu erinnern. Default bewusst `null` statt `'generator'`, damit die UI-Pfade **byte-identisch** bleiben: dass der Generator seine eigene Herkunft heute gar nicht stempelt (Spalten-Kommentar listet `generator` als Wert, geschrieben wird er nirgends), ist ein Bestandsbefund und kein Auftrag dieser Etappe. — **Dritte: `name` gibt es im Schema absichtlich NICHT.** `concepts.GENERATE` hat ihn, weil ein Konzept ein Behälter ist, den man benennt; ein Rezept-Name ist ein **Ergebnis** (Basisrezept-Regelwerk §1, die KI benennt nach Schema). Ein Namens-Parameter hätte den Regelwerk-Pfad umgangen; Umbenennen bleibt `recipes.PUT`. — **Vierte: das Tool meldet die Lücken, statt sie zu verbergen.** `offene[]` kommt kompakt mit (Text · primäre Aktion `gp_anlegen`/`basisrezept_anlegen` · Top-3-Shortlist) und der Hinweis nennt den Weg (`gps.MATCH` → GP anlegen → `recipe_ingredients.PUT`). Ein Test hält fest, dass die ungematchte Zeile wirklich `unmatched` bleibt — kein stilles Raten. — **Fünfter Punkt, der eigentliche Nebenertrag: beim Schreiben des Happy-Path-Tests ist eine Lineage-Drift im Bestand aufgefallen** (→ Bugs-Board, s. u.): `RecipeGeneratorService` setzt je GP-Treffer `match_method='gemini_proposed'` + `match_confidence`, `RecipeService::syncIngredients` liest das Feld des Aufrufers aber **nie** (nur den eigenen `$groundedMethod`, der nur bei GP-freien Zeilen gesetzt wird) und schreibt im `create`-Fallback `'manual'`. Jede KI-gematchte Zutat sieht in der DB handverlesen aus, die Konfidenz fällt weg. Der Test prüft deshalb bewusst die `gp_id` statt den Ist-Wert von `match_method` — den festzuschreiben hätte den Drift zementiert. — **Suite 1313 / 1309 passed / 4 skipped / 0 Fehler / 6204 Assertions** (+8, Vorlauf 1305/1301), 9,3 Min. Kein Schema-Change, keine Migration. **MCP-Lockstep ist diese Etappe selbst** (das fehlende Tool war die Schuld aus #505); Registrierung read-only gegen die Dev-MySQL gegengeprüft (`artisan tinker`: Tool im Registry, `required=[description]`, 15 Properties) — **nicht ausgeführt**, weil ein Generator-Lauf einen LLM-Call und einen Schreibzugriff auf die geteilte Dev-DB bedeutet hätte. Nichts geschrieben. Neue V-Nummer: **V-023**. Dev-Board: **#512 DoD-Zeile L5 abgehakt** (Index 4, mit Ergebnis-Notiz) · Lineage-Drift als Bug **#627** auf dem Bugs-Board angelegt. *Randnotiz für künftige Läufe:* die S4b-1-Notiz „kein lesendes `dev.*`-Tool gibt `dod_items` heraus" gilt nur für `dev.issues.GET` — **`dev.issue.GET` (Singular) liefert sie als `acceptance_criteria` mit**, inklusive Reihenfolge. Damit ist der Index vor dem Schreiben bekannt und der additive Zwischenschritt entfällt. Nächster Schritt: **Phase 2 · L2** (Kapitel-Text-KI: Prompt `foodbook.kapitel_text` in die `AiGatewayService`-Allow-Liste + Button `foodbooks/index.blade.php:226` entsperren, Vorschau + Übernehmen, `notizen_manual`-Schutz) — danach ist `foodbook_kapitel_ohne_text` aus Tranche D freigeschaltet.
- **2026-07-26 · 21·S4c-2 — verfehlte Kapitel-Ziele + überholte Kundenpreise ✅, Tranche D damit komplett (Routine-Lauf 14)** — Commit `ec28abb`. Gebaut: `DataQualityService::foodbooksMitKapitelZielBefund()` + `foodbooksMitPreisDrift()` + `foodbooksFreigegeben()` + `foodbookGerichtIds()` + zwei Register-Einträge · `SignalTyp::FoodbookZielVerfehlt`/`FoodbookStale` · `VkSnapshotService` per Konstruktor injiziert · `tests/Feature/FoodbookZielStaleSignaleTest.php` (11 Tests). — **Erste Entscheidung: die Ziel-Vererbung n-tief NICHT nachbauen.** Sie steht in `CoverageService::kapitelKettenZiel` (self + Eltern hoch) und der Ist-Rollup in `istFoodbook`; der Check liest ausschließlich die Befunde mit `chapter_id` (die stempelt `pruefeKapitel` auf) und davon nur `ampel='verletzt'` — Bauart und Begründung wie S4b-1. Eine eigene Auflösung wäre die zweite Wahrheit gegenüber der Planungs-Rail. — **Zweite: „ohne Gerüst kein Befund" ist hier eine GEERBTE Lücke, kein sauberer Schnitt.** Kapitel-Ziele pflegt man in der Leitstellen-Rail unabhängig vom Gerüst, `coverage()` steigt ohne Frame aber vorher aus ⇒ ein so gesetztes Ziel wird nirgends ausgewertet, auch nicht in der UI. Bewusst nicht einseitig im Signal-Layer repariert (sonst ein Befund ohne Fläche) → **V-022**. — **Dritte: dritte Arbeitsmenge `foodbooksFreigegeben`** (= in Gebrauch **ohne** `kalkulation`): dort sollen Preise sich bewegen, ein Alarm wäre §9-Über-Flaggen. Test zeigt dasselbe Buch in `kalkulation` still und in `freigabe` gemeldet. — **Vierte: Drift-Schwelle unverändert aus `VkSnapshotService::pending()`**, derselben Liste wie `vk_anpassung_empfohlen` — zwei Objekte, eine Wahrheit; kein Duplikat, weil die Frage eine andere ist („welches Dokument steht falsch beim Kunden" statt „welcher Preis muss nachgezogen werden"). Gezählt über beide Wege der Duality (`recipe_ref` + `concept_ref`→Slots/Pakete), nur sichtbare Blöcke. — **Fünfte: die zweite Spec-Hälfte „Gerichte geändert" ist NICHT baubar** → **V-021**: `foodbook_chapters.snapshot_at`/`snapshot_json` werden von vier Stellen gelesen und im ganzen Modul nie geschrieben; ohne „seitdem" und ohne Inhalts-Abzug lässt sich „geändert" nicht formulieren. — **Suite 1305 / 1301 passed / 4 skipped / 0 Fehler / 6160 Assertions** (+11), 9,3 Min. Kein Schema-Change, keine Migration, kein MCP-Nachzug (Typ-Enum aus `SignalTyp::cases()`; kein deterministischer Fixer für Foodbooks). Ein Test des S4c-1-Files auf `toContain` gelockert (die vollständige, geordnete Metrik-Liste hält jetzt der jüngste Tranche-D-Test — dieselbe Konvention wie in Tranche C). **MySQL read-only gegengeprüft:** beide Metriken laufen fehlerfrei und stehen auf 0, beide **ehrlich leer** — kein einziges Kapitel im Bestand trägt ein Ziel (bei genau 1 Foodbook-Gerüst), und `foodalchemist_vk_price_snapshots` ist **komplett leer**: R2.5 ist nie benutzt worden, obwohl 32 von 39 Darreichungen einen VK führen. Nichts geschrieben. Neue V-Nummern: **V-021**, **V-022**. Nächster Schritt: **Phase 2 · L5** (`RecipesGenerateTool`, Vorlage `ConceptsGenerateTool`, `vk: true` als Parameter, Tenancy-Negativtest).
- **2026-07-26 · 21·S4c-1 — Tranche D Fundament + die zwei struktur-basierten Foodbook-Checks ✅ (Routine-Lauf 13)** — Commit `283ff55`. S4c war für einen Tick zu groß und hat zwei Mechaniken, deshalb geschnitten: **S4c-1 = Fundament + die zwei struktur-basierten Checks** (reine Builder-Prädikate über Kapitel/Blöcke/Skizzen), **S4c-2 = `foodbook_ziel_verfehlt` (Ziel-Vererbung n-tief über `CoverageService`/`kapitelZiele`) + `foodbook_stale` (Snapshot-Vergleich)**. Gebaut: Ebene `foodbook` im `DataQualityService` + Register `foodbookChecks()` mit `foodbook_kapitel_leer` + `foodbook_skizze_ungeerdet` · `foodbooksInGebrauch()`/`foodbooksNichtArchiviert()` · `kapitelOhneInhalt()`/`kapitelMitInhalt()`/`ungeerdeteSkizze()` · `foodbookItems()` + `kind`-Dispatch · `queryFor()` schlägt jetzt in drei Registern nach · `SignalObjectService::KINDS` (Allowlist statt Inline-Liste, das Blade liest sie) · Leitstellen-Sprung `?fb=` im `Signale/DetailPanel` · zwei neue `SignalTyp`-Fälle + `istFoodbookQualitaet()` · Fixtures `makeFoodbook`/`makeChapter`/`makeFoodbookBlock`/`makeDishIdea` · `tests/Feature/FoodbookQualitaetSignaleTest.php` (15 Tests). — **Erste und tragende Entscheidung: ZWEI Arbeitsmengen, nicht eine.** In Tranche C war `konzepteInGebrauch` für alle fünf Checks richtig; auf der Foodbook-Ebene wäre dieselbe Übertragung falsch. `foodbook_kapitel_leer` braucht sie (ein Entwurf hat leere Kapitel, weil er ein Entwurf ist), `foodbook_skizze_ungeerdet` nicht: dort hat jemand **Kapitel-Go** gedrückt, und dieser Knopf ist die Erklärung „das soll jetzt entstehen" — unabhängig davon, ob das Buch nebenbei noch `draft` heißt. Wer eine Skizze verliert, verliert sie auch im Entwurf; ein Test hält beide Seiten fest (dasselbe Buch: Kapitel-Check 0, Skizzen-Check 1). Nebenbefund beim Bestimmen der Menge: **die `phase` ist der belastbarere Marker als der `status`** — sie führt die Leitstelle selbst, den Status setzt jemand von Hand, und in der Dev-MySQL stehen beide Bücher auf `draft`, während ihre Phasen differenzieren (`befuellung`/`kontext`). Aufgenommen sind deshalb drei Wege: Status · `phase` ab `kalkulation` (ab da ist die Befüllung erklärt-fertig, s. `PhaseService::PHASEN`) · ein Kapitel mit Versand-`snapshot_at`. — **Zweite Entscheidung: der Skizzen-Check braucht eine Karenzzeit, und das ist keine Bequemlichkeit.** `materialisiereFreitextIdee` lässt eine Skizze **absichtlich** `queued`, wenn kein Provider da ist (graceful, retrybar) — ein Check ohne Zeitgrenze hätte den geplanten Zustand als Fehler gemeldet, also genau das Rauschen aus §9, und zwar sekundenweise nach jedem Go. 48 h, gemessen gegen `released_at`: der Go dispatcht die Jobs sofort, kürzer wäre ein Provider-Ausfall über Nacht, länger würde verlorene Kreativarbeit verschweigen. `fehlgeschlagen` zählt bewusst NICHT mit — dort ist die KI gelaufen und hat verloren, das meldet die Leitstelle am Kapitel; hier geht es um den **stillen** Fall. — **Dritte Entscheidung: das Objekt ist das BUCH, nicht das Kapitel.** Naheliegend wäre ein `kind` `chapter` gewesen (die Signal-Namen lauten „Kapitel …"), aber dann hätte eine Tranche zwei Objekt-Typen, und `foodbook_stale` (S4c-2) hängt ohnehin am Buch. Dieselbe Wahl wie in Tranche C, wo das Konzept und nicht der Slot das Objekt ist. Kosten davon: der Sprung landet auf dem Buch, nicht am Kapitel — `?kap=` gibt es, aber der Befund kann n Kapitel betreffen, und einen von n willkürlich zu wählen wäre die schlechtere Auskunft. `foodbookItems()` benennt `label` → `name` um, statt dem Panel einen zweiten Feldnamen beizubringen. — **Vierte Entscheidung: gemessen wird, was DRUCKT.** Als Inhalt zählt nur ein `visible=true`-Block vom Typ `concept_ref`/`recipe_ref` — geprüft, dass `FoodbookService::dokumentDaten` genau so filtert; ein ausgeschalteter Block ist im Kundendokument nicht da, und das Kapitel druckt leer. Struktur-Blöcke (`header`/`text`/`spacer`/`image`) beschreiben Inhalt, tragen ihn nicht (Spiegel von `ConceptService::STRUKTUR_TYPEN`). Geprüft werden nur **Blattkapitel**: ein Eltern-Kapitel ist eine Klammer, sein Inhalt hängt an den Nachfahren — ohne diese Grenze wäre jede saubere Baum-Gliederung ein Befund (eigener Test, inkl. Gegenrichtung: fällt der Inhalt des Unterkapitels weg, ist es als Blatt selbst der Befund). Der zweite Zweig „Buch ohne jedes befüllte Kapitel" ist die Lehre aus S4a (`belegterInhaltsSlot`): ein Buch mit null Kapiteln hat kein *leeres* Kapitel und wäre unauffällig — real ist genau das FB #8 der Dev-DB. — **Fünfte Entscheidung, und der eigentliche Nebenertrag des Laufs: beim Grounding der Arbeitsmenge ist ein Bug in der SCHON AUSGELIEFERTEN Tranche C aufgefallen** (→ Bugs-Board **#620**, To Do). Das Status-Vokabular ist gespalten: die Migrationen kommentieren `draft|aktiv|…`, aber `ConceptService::setStatus` validiert `active`, die Concepter-Views mappen `active`, `ConceptsSearchTool` führt `active` im MCP-Enum, und das Leitstellen-Dropdown (`foodbooks/index.blade.php:135`) schreibt `active`. Damit filtert `konzepteInGebrauch()` (seit `f71d1eb` produktiv) auf einen Wert, den die UI **nie schreibt** — ein über die UI aktiviertes Konzept wird nur noch über Angebot/Foodbook-Referenz erfasst, ein aktives Konzept ohne beides gar nicht. Die Pest-Tests sind grün, weil `makeConcept` selbst `aktiv` schreibt: **die Fixture bestätigt die Annahme, die sie enthält** — dieselbe Falle wie V-019, eine Ebene höher. Behandelt wurde das bewusst NICHT durch einen Fix (welche Schreibweise kanonisch ist, ist eine offene Entscheidung — die README-Regel „Schema englisch" betrifft Identifier, Enum-*Werte* sind im FA ausdrücklich auch deutsch erlaubt, s. `DishIdea::STATUSES`), sondern durch `FOODBOOK_STATUS_IN_GEBRAUCH = ['aktiv','active','versendet']` mit Kommentar-Verweis und eigenem Test: der neue Check darf kein live geschaltetes Buch stillschweigend verfehlen, solange die Frage offen ist. Als Krücke deklariert, Rückbau ist DoD-Punkt in #620. — **Suite 1294 · 1290 passed · 4 skipped · 0 Fail · 6130 Assertions** (Vorlauf 1279/1275, +15 Tests), 9,7 Min. **Kein Schema-Change, keine Migration, kein MCP-Nachzug** — Typ-Enum der `signale.*`-Tools kommt seit S0 aus `SignalTyp::cases()`, `signal_causes.GET` bleibt bei `recipe`/`gp` (`SignalCauseService::fuerObjekt` gibt für `foodbook` sauber `[]` zurück, wie für `concept`), Teil-Bulk-Checkbox und `fixbareItems()` bleiben bei `recipe`/`gp` (kein deterministischer Foodbook-Fixer — Stelle im Blade kommentiert). Geprüft: die DQ-Ampel hat **keinen** UI-Konsumenten (nur CLI + Signal-Emission), es gibt also keine Ebenen-Liste in einem Blade nachzuziehen. **MySQL-Smoke echt gefahren, read-only, mit Erkenntniswert:** `foodalchemist:data-quality --team=1` zeigt beide Metriken ohne SQL-Fehler auf 0 — und dieses 0 ist die Arbeitsmengen-Entscheidung, nicht eine leere Datenlage. Dasselbe Prädikat *ohne* die Grenze (als reine Lese-Query gegengeprüft, damit die `NOT EXISTS`-Verschachtelung auf MySQL wirklich ausgeführt wird und nicht nur auf einer leeren Menge) trifft **beide** Bücher: FB #1 „Spec20 Klickstrecke FB" hat zwei leere Blattkapitel neben einem befüllten (würde also flaggen, sobald es live geht), FB #8 „Foodbook 2027" hat gar kein Kapitel (zweiter Zweig). Nichts geschrieben. — **Nächster Schritt: 21·S4c-2** (`foodbook_ziel_verfehlt` + `foodbook_stale`); danach ist Tranche D bis auf das L2-gebundene `foodbook_kapitel_ohne_text` komplett und Phase 1 endet mit S5 in Phase 3. **Neu im Verbesserungs-Backlog: V-020** (20 `status`-Spalten, 2 Model-Consts — das Vokabular lebt nur im Migrations-Kommentar, also an der einen Stelle, die kein Code liest).
- **2026-07-26 · 21·S4b-2 — `konzept_dramaturgie`, Tranche C damit komplett ✅ (Routine-Lauf 12)** — Commit `9159cab`. Gebaut: `PairingService::menuRepetitions()` + `kernSlugs()` · `DataQualityService::konzepteMitWiederholung()` + `konzeptGerichtIds()` + Register-Eintrag · `SignalTyp::KonzeptDramaturgie` · `tests/Feature/KonzeptDramaturgieSignalTest.php` (10 Tests). — **Erste Entscheidung: die offene Auslegungsfrage ist gegen `menuCohesion` entschieden.** Der Fahrplan sah ihn als Quelle vor; `cohesionFor` liest einen geteilten Anker aber als Paar-Typ `gleich` mit **1,0, also maximaler Nähe** — für den Teller richtig, für die Menüfolge die Diagnose auf dem Kopf (zweimal Lachs *hebt* dort den Score, obwohl es der Mangel ist). Denselben Wert einmal als Stärke und einmal als Befund zu lesen wären zwei Wahrheiten in einer Zahl. Dazu legt `menuCohesion` je Gericht die **Union aller Zutaten-Anker** an: ein geteilter Anker heißt dort auch „beide enthalten Butter". Beide Gründe zusammen ⇒ eigener, schmaler Weg statt eines weiteren Rückgabe-Schlüssels dort. — **Zweite Entscheidung — und das ist der eigentliche Ertrag des Laufs: der erste Entwurf war falsch, und nur der MySQL-Gegencheck hat ihn gekippt.** Er nahm `recipe_anchor_mappings.role='kern'` als Identitäts-Anker des Gerichts (das Feld liest sich so, und `resolveRecipeAnchors` behandelt es beim Sub-Rezept als Einzelwert). An echten Daten ist es ein **Beutel**: 352 Zeilen auf 89 Rezepte, Spitze 24 an einem Rezept, durchweg `source='ki'` mit `ai_confidence = NULL`, `role` ohne zweiten Wert im ganzen Bestand — „Crumble: Grana Padano" trägt kern = butter, kaese, ei, olivenoel, salz, pfeffer. Die Tests waren grün (Fixtures legen genau ein Mapping an), die Auswahl per `ORDER BY COALESCE(ai_confidence,1.0) DESC, id` ist faktisch „der zuerst eingefügte", und der Check hätte reale Wiederholungen verfehlt und dafür „beide fangen mit Butter an" gemeldet. **Lehre fürs Muster: ein grüner Fixture-Test sagt nichts über die Semantik eines Feldes im Bestand** — die Verteilung muss man ansehen. Notiert als **V-019**. — **Dritte Entscheidung: Hauptzutat = mengenmäßig dominierende Zutat**, identifiziert über den Kern-Anker ihres GP bzw. Sub-Rezepts (dieselbe Wahl, die `resolveRecipeAnchors` je *Zeile* trifft — dort ist sie belastbar, weil ein GP-Kern die Identität eines GP ist). Mit dem **Einheiten-Gate aus S1b**: bewertet wird nur, wenn ALLE beitragenden Zeilen massen-vergleichbar sind. Ohne das Gate gewinnt die schwerste *messbare* Zeile — im Bestand real: „Mayonnaise: Petersilie (vegan)" mit 0,25 l Sojamilch und 0,5 l Öl hätte **Salz** als Hauptzutat gehabt. Lieber unbewertet als falsch; ein eigener Test hält fest, dass eine einzige Volumen-Zeile das ganze Paar aus der Wertung nimmt. — **Vierte Entscheidung: `info`, nicht `warnung`** — ein Themen-Menü (Kürbis-Menü) wiederholt bewusst; als Warnung wäre das Über-Flaggen nach §9. Gruppiert wird über `ankerSlugMatches` statt Anker-ID (`lachs` / `lachs_wild` = dieselbe Hauptzutat für den Gast), `neutral` bildet nie eine Wiederholung. — **Suite 1279 · 1275 passed · 4 skipped · 0 Fail · 6080 Assertions** (Vorlauf 1269/1265, +10 Tests), 9,1 Min. **Kein Schema-Change, keine Migration, kein MCP-Nachzug** — Typ-Enum der `signale.*`-Tools kommt seit S0 aus `SignalTyp::cases()`, für Konzepte gibt es weiterhin keinen deterministischen Fixer (S4a-Entscheid), und `SignalCauseService::fuerObjekt` gibt für `kind='concept'` weiter sauber `[]`. Zwei Bestands-Tests, die die *vollständige* Metrik-Liste der Konzept-Ebene festhielten, sind auf ihre eigenen Keys umgestellt (`toContain`) — die geordnete Gesamtliste hält jetzt nur noch der jüngste Tranche-C-Test, sonst müsste jede neue Etappe drei Stellen ändern. **MySQL-Smoke echt gefahren und read-only, mit Erkenntniswert:** `foodalchemist:data-quality --team=1` zeigt die neue Metrik ohne SQL-Fehler auf 0 — und das 0 ist wieder strukturell (die Dev-DB hat genau ein lebendes Konzept: Entwurf, ein einziges Gericht, das nicht einmal einen Anker trägt). Beobachtbar ist der Check dort nur über den direkten Service-Aufruf: über 58 reale Rezepte meldet er **drei** Gruppen, alle plausibel — 3× „Crumble: Mandel*" über `weizen`, 2 kartoffel-dominierte Hauptgänge (Gnocchi / Rosmarinkartoffeln), 3 „Roggenmischbrot"-Sandwiches. Derselbe Lauf meldete **vor** dem Einheiten-Gate zusätzlich `salz` und `creme_fraiche` — das ist der Beleg, dass das Gate trägt. Nichts geschrieben. **Verbesserungs-Backlog: drei neue Einträge** — **V-017** (die zweite Spec-Hälfte „kein Kontrast über den Verlauf" ist heute nur als erfundene Schwelle formulierbar und stünde quer zum Cockpit; Level-up: `cohesionFor` gibt seine Paar-Matrix heraus), **V-018** (die Gerichte eines Konzepts werden an sechs Stellen neu zusammengesetzt — Editor, Coverage ×2, Planungsblatt, Wording, jetzt Signale; ob ein doppelt gelistetes Gericht einmal oder zweimal zählt, beantwortet jede Stelle für sich), **V-019** (s. o., der Kern-Beutel — trifft auch jede Sub-Rezept-Auflösung im Pairing-Netz). Nächster Schritt: **21·S4c** (Tranche D · Foodbook-Signale: `foodbook_kapitel_leer` · `foodbook_ziel_verfehlt` · `foodbook_skizze_ungeerdet` · `foodbook_stale`; `foodbook_kapitel_ohne_text` bleibt bis nach L2 draußen — Signal ohne Fixer wäre Rauschen).
- **2026-07-26 · 21·S4b-1 — die zwei frame-gestützten Konzept-Checks ✅ (Routine-Lauf 11)** — Commit `9665d1f`. S4b war als „Preisband + Regel + Dramaturgie" geplant und ist **auf Preisband + Regel geschnitten**: die beiden gehören zusammen (ein `coverage()`-Pass), `konzept_dramaturgie` ist eine andere Mechanik (Anker-Graph statt Gerüst-Soll) und die einzige Tranche-C-Prüfung, die *ohne* Gerüst greift — in einem Tick zusammengebaut wären zwei Arbeitsmengen und zwei Auflöse-Wege in einer Etappe gelandet → neue Etappe **S4b-2**. Gebaut: `DataQualityService::konzepteMitFrameBefund()` + zwei Register-Einträge + `FRAME_REGEL_DIMENSIONEN` · `CoverageService` per Konstruktor injiziert · zwei neue `SignalTyp`-Fälle · `tests/Feature/KonzeptFrameSignaleTest.php` (8 Tests). — **Erste und tragende Entscheidung: eine ZWEITE Arbeitsmengen-Grenze über der aus S4a — ohne Planungs-Gerüst kein Befund.** Das ist keine Optimierung, sondern der Unterschied zwischen den beiden Hälften von Tranche C: S4a misst das Konzept gegen sich selbst (hat es Slots, haben die Zeilen Kundentexte — das kann man immer beantworten), S4b misst es gegen ein SOLL, das jemand gesetzt hat. Wo keins gesetzt ist, sind 30 € p. P. weder richtig noch falsch, und ein Signal darüber wäre eine erfundene Erwartung. Nebeneffekt: der teure Pass läuft nur über eine doppelt geschnittene Menge (in Gebrauch **und** mit Gerüst). — **Zweite Entscheidung: gemeldet wird nur `ampel='verletzt'`, also exakt das, was das Concepter-Cockpit rot zeigt.** Die Versuchung war, auch `teilerfuellt` zu nehmen (>10 % über dem Zielpreis *ist* eine Kalkulations-Frage). Genau da entstünden zwei Wahrheiten über dasselbe Konzept: gelb im Cockpit, Alarm in der Inbox — und niemand könnte sagen, welche Schwelle gilt. Zwei Tests halten die Kante: derselbe Ist-Preis (30 €) wird bei `price_max_pp=28` gemeldet und bei `price_max_pp=40` nicht, obwohl er weiter 20 % über dem Zielpreis liegt; dieselbe No-Go-Regel wird als hart gemeldet und als `severity=weich` nicht. — **Dritte Entscheidung: `konzept_regel_verletzt` deckt NUR die Regel-Dimensionen ab** (`diaet`/`nogo`/`saison` = `RULE_TYPES`). `menge`/`dramaturgie` kommen aus den Gerüst-*Slots*, sind ein anderer Sachverhalt („Struktur nicht gefüllt" ≠ „Zusage gebrochen") und würden zwei Befunde in einem Signal vermischen — auch nicht von `konzept_slot_luecke` gefangen, denn der misst `concept_slots`, nicht `planning_frame_slots`. Die Lücke ist als **V-016** notiert, nicht mitgebaut (Scope-Drift-Schutz); ein eigener Test hält fest, dass rote `menge`/`dramaturgie`-Ampeln hier bewusst **nicht** zählen, damit die Abgrenzung nicht später versehentlich aufgeweicht wird. — **Vierte Entscheidung: kein Builder-Prädikat, aber dieselbe Schnittstelle** — PHP-Pass wie `namingVerstossIds` (S1b), Rückgabe als `whereIn('id', …)`, damit `betroffene()`/`countFor()`/`trifftObjekt()`/Panel unverändert funktionieren. Bewusst **ohne Memoisierung**, mit derselben Begründung wie bei `nameScan`: der Service wird auch NACH einem Fix erneut gefragt (`countFor` ⇒ Signal schließen), ein Cache meldete dort den alten Stand. `CoverageService` hängt jetzt per Konstruktor am `DataQualityService` — vorher geprüft, dass kein Container-Zyklus entsteht (seine drei Abhängigkeiten kennen den Service nicht; das war bei `SignalObjectService` anders und der Grund für dessen Auslagerung). — **Suite 1269 · 1265 passed · 4 skipped · 0 Fail · 6054 Assertions** (Vorlauf 1261/1257, +8 Tests), 9,0 Min. **Kein Schema-Change, keine Migration, kein MCP-Nachzug** — das Typ-Enum der `signale.*`-Tools kommt seit S0 aus `SignalTyp::cases()`, und für Konzepte gibt es weiterhin keinen deterministischen Fixer (S4a-Entscheid), also auch keine Fixer-/Assist-Zuordnung nachzuziehen; geprüft per Grep, dass die S4a-Typen an keiner weiteren Stelle im Code auftauchen. Lockstep damit erfüllt. **MySQL-Smoke echt gefahren und read-only:** `foodalchemist:data-quality --team=1` zeigt beide neuen Metriken ohne SQL-Fehler, beide 0 — **und das 0 ist begründet, nicht bloß leer**: die Dev-DB hat kein einziges lebendes Konzept mit Gerüst. Per PDO gegengeprüft: 1 lebendes Konzept (`#486 Smoke Concept`, `draft`, ohne Gerüst) gegen 3 Konzept-Gerüste (je 2 Slots + 2 Regeln), die **alle** an am 2026-07-21 soft-gelöschten Konzepten hängen. Die Checks sind an echten Daten damit **nicht beobachtbar** — dort tragen die 8 Tests, und das ist beim Lesen der Zahl mitzudenken. Nichts geschrieben. **Verbesserungs-Backlog:** **V-016** neu (rot im Coverage-Cockpit, still in den Signalen — die Struktur-Hälfte des Gerüsts wird nicht gemeldet; Level-up-Vorschlag: Befund-Shape von `coverage()` selbst signalfähig machen, dann gilt es auch für Tranche D) **+ V-015 um einen zweiten Fundort ergänzt** (die Soft-Delete-Waisen betreffen auch das Planungs-Gerüst; weil dessen Verknüpfung polymorph ist, greift dort nicht einmal ein FK-Cascade als Netz — ein relations-getriebenes Kaskaden-Concern würde genau diesen Fall nicht automatisch mitnehmen). Dev-Board: **#618** DoD-Zeile S4 bleibt „TEIL" (S4a + S4b-1 ✅, S4b-2 + S4c als offener Rest) — bewusst **nicht** abgehakt. *Randnotiz zum Weg dorthin:* kein lesendes `dev.*`-Tool gibt `dod_items` heraus (weder `dev.issues.GET` mit `filters`/`search_fields` noch `dev.board.GET`) — die Liste ist nur als **Antwort eines `dev.issues.PUT`** sichtbar. Ein blindes `update_text` auf geratenem Index wäre ein destruktiver Schreibzugriff auf fremde Zeilen gewesen; deshalb erst ein additiver `add` (liefert die volle Liste zurück), dann das In-Place-Update auf dem nun bekannten Index 4 und das Entfernen der Zwischenzeile. Endstand: 6 DoD-Zeilen, keine Dublette. Nächster Schritt: **21·S4b-2** (`konzept_dramaturgie` (info) über `PairingService::menuCohesion`; offene Auslegungsfrage im Fahrplan notiert — `cohesionFor` kennt die Wiederholung intern als Paar-Typ `gleich`, gibt sie aber nicht heraus).
- **2026-07-26 · 21·S4a — Tranche C Fundament + die zwei frame-freien Konzept-Checks ✅ (Routine-Lauf 10)** — Commit `f71d1eb`. S4 war für einen Tick zu groß und hat drei verschiedene Mechaniken, deshalb geschnitten: **S4a = Fundament + SQL-Checks**, **S4b = die zwei frame-gestützten (Preisband + Regelverletzung, zusammen weil sie aus EINEM `CoverageService::coverage()`-Pass fallen) + Dramaturgie**, **S4c = Tranche D (Foodbook)**. Gebaut: Ebene `konzept` im `DataQualityService` + Register `konzeptChecks()` (Bauart wie Tranche A) mit `konzept_slot_luecke` + `konzept_ohne_wording` · `konzepteInGebrauch()` · `offenerPflichtSlot()`/`belegterInhaltsSlot()`/`slotOhneWording()` · `conceptItems()` + `kind`-Dispatch in `betroffene()` · `queryFor()` schlägt jetzt in beiden Registern nach · `SignalObjectService::signaleAmObjekt` kennt `concept` · Concepter-Sprung + „was noch?" im `Signale/DetailPanel` · zwei neue `SignalTyp`-Fälle + `istKonzeptQualitaet()` · Fixture `makeConcept`/`makeConceptSlot`. — **Erste und tragende Entscheidung: die Arbeitsmenge, nicht das Prädikat.** Naheliegend wäre gewesen, alle Konzepte zu prüfen (so lesen sich die Spec-Zeilen: „Slots unbesetzt"). Genau das wäre falsch: anders als ein Rezept ist ein Konzept über längere Zeit **bewusst** unfertig — der Entwurf IST der Arbeitsstand, und ein Check darüber hätte die normale Arbeit als Fehler gezählt (§9 Nicht-Ziel „kein Rauschen"). Gemessen wird deshalb nur, was in Gebrauch ist: `status='aktiv'` · `offer_id` · Angebots-Zuordnung (`foodalchemist_offer_concept`) · in einem Foodbook-Block referenziert; `is_template` und `archiviert` bleiben draußen. Zwei Tests halten beide Seiten fest (Entwurf/Vorlage/archiviert ohne einen einzigen Slot ⇒ 0; derselbe Entwurf in einem Foodbook ⇒ 1). Das ist dieselbe Logik wie `kundenExponiert` bei `rezept_allergen_unbelastbar` — geprüft wird, wo es zählt. — **Zweite Entscheidung: `konzept_slot_luecke` prüft zwei Lagen, nicht eine.** „Unbesetzter Pflicht-Slot" allein lässt das schlimmste Konzept durch: eines **ohne jeden** Inhalts-Slot hat keinen offenen Pflicht-Slot und wäre unauffällig. Zweiter Zweig ist deshalb „kein einziger belegter Inhalts-Slot"; Struktur-Slots (`ConceptService::STRUKTUR_TYPEN`) zählen nie als Inhalt (Test mit einem Header-only-Konzept), ein unbesetzter **optionaler** Slot ist kein Befund. — **Dritte Entscheidung: der Wording-Check spiegelt den `WordingResolver`, statt eine eigene Regel zu formulieren** — inkl. der Paket-Zeilen, die keinen Slot-Override kennen und direkt auf `sales_wording_standard` gehen (sonst wäre die halbe Kundensicht ungeprüft). Der **Foodbook-Override bleibt bewusst außen vor**: er hängt am Buch, nicht am Konzept, und würde die Lücke an einer von n Verwendungsstellen kaschieren. Gemeldet wird also genau der Fall, in dem der interne Pipe-Name (`[HG] A | B`) druckt. Bewusst NICHT gebaut: „Intro fehlt" (`concepts.description`) — das ist ein anderer Sachverhalt (Beschreibungstext, nicht Bezeichnung) und hätte zwei Befunde in einem Signal vermischt. — **Vierte Entscheidung: der `kind` gehört ans Check-Register, nicht an den Aufrufer.** `queryFor` lieferte bisher hart `'recipe'` für Register-Treffer; jetzt läuft es über beide Register und gibt den Objekt-Typ mit. Damit kann eine Konzept-Metrik kein Rezept treffen, auch nicht bei gleicher ID (Test) — und `betroffene()`/`trifftObjekt()`/`signaleAmObjekt()`/Panel funktionieren für Konzepte ohne einen vierten Query-Pfad. Die **Teil-Bulk-Checkbox bleibt bei `recipe`/`gp`** (kein deterministischer Fixer für Konzepte ⇒ ein Knopf ohne Wirkung wäre schlechter als keiner); die Stelle ist im Blade kommentiert, damit ein künftiger Konzept-Fixer beide Enden zusammen ändert. — **Suite 1261 · 1257 passed · 4 skipped · 0 Fail · 6026 Assertions** (Vorlauf 1250/1246, +11 Tests), 8,9 Min. **Kein Schema-Change, keine Migration, kein MCP-Nachzug** — das Typ-Enum der `signale.*`-Tools kommt seit S0 aus `SignalTyp::cases()` (die zwei neuen Typen erscheinen automatisch), und die Objekt-/Ursachen-Tools laufen über dieselben Services; `SignalCauseService::fuerObjekt` gibt für `kind='concept'` sauber `[]` zurück (kein Bruch, keine erfundene Kette). Lockstep damit erfüllt, nichts nachzuziehen. **MySQL-Smoke echt gefahren und read-only:** `foodalchemist:data-quality --team=1` (ohne `--signals`) zeigt die neue Ebene ohne SQL-Fehler. Mit Erkenntniswert: das einzige Konzept der Dev-DB ist **über die Angebots-Zuordnung in Gebrauch** — die Arbeitsmengen-Klausel greift dort also real und blendet nicht einfach alles aus —, und beide Checks messen echte 0 (0 offene Pflicht-Slots, 2 belegte Inhalts-Slots, 0 Wording-Lücken; gegengeprüft mit den Roh-Prädikaten). Nichts geschrieben. **Fund am Weg:** von 23 lebenden `concept_slots`-Zeilen hängen **21 an einem soft-gelöschten Konzept** — `ConceptService::delete()` löscht die Slots nicht mit, `cascadeOnDelete` greift nur beim harten Delete. Die Checks dieses Laufs sind immun (sie setzen am Konzept an und fragen Slots per EXISTS), die naheliegende Variante „zähle unbesetzte Pflicht-Slots" hätte 21 Papierkorb-Zeilen gemeldet → **V-015**. Dev-Board: **#618** DoD-Zeile S4 auf „TEIL" (S4a ✅ mit Commit + Suite-Zahl, S4b + S4c als offener Rest ausformuliert) — bewusst **nicht** abgehakt. Nächster Schritt: **21·S4b** (`konzept_preisband_verletzt` + `konzept_regel_verletzt` aus einem `coverage()`-Pass je Konzept + `konzept_dramaturgie`; Achtung: erster Konzept-Check, der kein Builder-Prädikat ist — Muster ist der PHP-Pass aus S1b mit `whereIn('id', …)`).
- **2026-07-26 · 21·S3b-3 — Ursachen-Kette ✅, Tranche P (S3) damit komplett (Routine-Lauf 9)** — Commit `25d0195`. Gebaut: `src/Services/SignalCauseService.php` (read-only) · `DataQualityService::namingBefundeFuer()` + `namingVerstoss()` → `namingBefunde()` (Liste statt bool) · Ursachen-Block im aufgeklappten Objekt des `Signale/DetailPanel` · MCP `foodalchemist.signal_causes.GET`. — **Erste Entscheidung: die Kette gehört dem OBJEKT, nicht dem Signal.** Naheliegend wäre eine Sektion am Signal gewesen (so steht Punkt 5 in der Spec, direkt neben Verlauf und Rausch-Guard) — nur deckt `br_ek_teil` n Rezepte ab, und „welche GPs sind unbepreist" hat je Rezept eine andere Antwort; eine Signal-Sektion hätte entweder über alle Rezepte mitteln oder das erste als repräsentativ ausgeben müssen. Gebaut ist deshalb der Block **in derselben aufgeklappten Fläche wie die objekt-zentrische Sicht aus S3a**: ein Klick auf „was noch?" liefert beides, was hier hängt und warum. Zugeklappt wird die Kette gar nicht erst gerechnet (Test hält das fest) — jedes Glied kostet Auflöse-Fragen, und die Liste zeigt bis zu 300 Objekte. — **Zweite Entscheidung: was unbepreist IST, entscheidet weiter allein `RecipeRecomputeService::zeilenKosten()`.** Der Service rechnet keine Preise nach, er fragt dieselbe T3-Kaskade, die auch `ek_n_ingredients_priced` füllt, und diagnostiziert erst danach die **Beschaffungs**-Lage des GP (LAs vorhanden? bepreist? Lead gesetzt?). Das ist bewusst eine andere Frage als „was kostet er" — eine eigene Preis-Nachbildung wäre die zweite Wahrheit, an der die Erklärung irgendwann von der Zahl abweicht, und zwar unbemerkt. — **Dritte Entscheidung — und das war eine Korrektur an der eigenen Annahme, die erst der Test aufgedeckt hat: ein fehlender Lead-LA bricht die EK-Kette NICHT.** `preisProGrammFuer`/`preisProStueckFuer` fallen ohne Lead auf den **Durchschnitt** über die aktiven bepreisten LAs zurück; die Zeile ist dann bepreist, das Rezept gilt als vollständig. Der erste Testfall („kein Lead → Kette bricht, fixbar") war damit sachlich falsch aufgestellt und ist umgedreht worden: er hält jetzt fest, dass die Kette den Lead-Mangel **nicht als EK-Ursache behauptet** — sonst schickte das Panel jemanden auf eine Reparatur, die die Zahl nicht bewegt. Preis-blockierend sind nur „GP ohne Lieferantenartikel" und „kein LA mit gültigem Preis"; die Lead-Frage hängt als Nebenbefund dran. Dafür trägt `gpBeschaffung()` ein eigenes `loest_auf` neben `status` — die zwei sind eben **nicht** dasselbe. — **Vierte Entscheidung: der §-Deep-Link ist echt, aber doppelt geguardet.** Die Regelwerke liegen real als `regelwerk.*`-Dokumente im Wissens-Modul (an der Dev-MySQL verifiziert: beide Slugs da, `…/wissen?doc=841` baut), die ID wird über den **Slug** zur Laufzeit aufgelöst, weil sie je Umgebung abweicht; fehlt das Dokument **oder** ist die Route nicht registriert (Konsolen-/Tool-Pfade, Test-Host), bleibt der §-Text ohne Link stehen. Ein toter Link wäre schlechter als keiner. Dafür musste `namingVerstoss()` von bool auf eine **Liste** von Fall-Schlüsseln umgestellt werden (bricht nicht beim ersten Treffer ab — wer einen Namen korrigiert, soll alle gerissenen Regeln auf einmal sehen); die Zuordnung Fall → § liegt bewusst im Cause-Service, nicht im `DataQualityService`: der misst, er formuliert nicht. — **Suite 1250 · 1246 passed · 4 skipped · 0 Fail · 5981 Assertions** (Vorlauf 1239/1235, +11 Tests), 8,8 Min. **Kein Schema-Change, keine Migration.** **MCP-Lockstep erfüllt:** `signal_causes.GET` im selben Commit registriert, read-only, mit Tenancy-Negativtest (`NO_TEAM` bei User ohne Team) + `VALIDATION_ERROR` bei fremdem `kind`; ein zweiter Test prüft, dass das Tool dieselbe Kette liefert wie die UI (eine Wahrheit, zwei Flächen). **MySQL-Smoke echt gefahren und read-only — mit Erkenntniswert:** über 6 real teil-/unbepreiste Rezepte der Dev-MySQL löst die Kette ohne SQL-Fehler auf und liefert **vier verschiedene** Ursachen (R#461/1269/1398 „GP ohne Lieferantenartikel", R#1382/1581 „kein LA mit gültigem Preis", R#1523 „Menge nicht umrechenbar") — vorher stand an all diesen Rezepten dasselbe Wort „teil-unbepreist". Auf der GP-Seite bestätigt und schärft sie den S3b-1-Befund: von 5 gesampelten `gp_ohne_lead`-GPs sind **4 gar nicht lead-los, sondern LA-los** (nicht fixbar), nur 1 ist der Fall, den der Fixer heilen kann. Nichts geschrieben. **Naming-Kette an echten Daten nicht beobachtbar** (0 Verstöße im Bestand, s. S1b) — dort trägt der Test. Neu im Backlog: **V-014** (ein „bepreistes" Rezept kann still auf einem Lieferanten-Durchschnitt stehen — `ek_total_eur` unterscheidet nicht zwischen „über den gewählten Artikel" und „über einen Mittelwert aus 8,90 und 22,00"; trifft direkt L8 und die Marge-Arbeit). Dev-Board: **#618** DoD-Zeile S3 auf ✅ KOMPLETT (S3a + S3b-1 + S3b-2 + S3b-3). Nächster Schritt: **21·S4** (Tranche C+D — Konzept- + Foodbook-Signale; `foodbook_kapitel_ohne_text` bleibt bis nach L2 stumpf).
- **2026-07-26 · 21·S3b-2 — Policy-Regler + Trend-Sparkline im Panel ✅ (Routine-Lauf 8)** — Commit `4f712cb`. S3b-2 war als „8 + 4 + 5" geplant und ist **auf 8 + 4 geschnitten**: Punkt 5 (Ursachen-Kette) wandert in eine neue Etappe **S3b-3**, weil 8 und 4 „Service da, Fläche fehlt" sind, 5 dagegen neue Auflöse-Logik ist (GP→Lead-LA-Kette + §-Deep-Links) — in einem Tick zusammen mit zwei UI-Flächen wäre das eine halbe Etappe geworden. Gebaut: `SignalPolicyService::zustandFuer()` (Ein-Typ-Zustandszeile über dasselbe private `zeile()` wie `zustand()`, plus `gesetzt`/`geerbt`) · `Signale/DetailPanel` um `policyForm`/`pThreshold`/`pAcceptedUntil`/`pNote`/`pMuted` + `policyFormUmschalten`/`policySpeichern`/`policyEntfernen` + `sparkline()` · zwei neue Blade-Sektionen „Verlauf" und „Rausch-Guard". — **Die tragende Entscheidung: der Regler gilt für den TYP, nicht für das Signal, aus dem er gestellt wurde.** Technisch war das durch `signal_policies.type` vorgegeben, fachlich ist es die gefährliche Stelle: ein Formular direkt unter einem einzelnen Befund liest sich als „diesen Befund akzeptieren", und wer so eine ganze Klasse stummschaltet, merkt es nicht. Deshalb drei Dinge zusammen: der Formularkopf benennt es ausdrücklich („gilt für **alle** Signale vom Typ …"), ein Test stellt den Regler an Signal A und prüft die Wirkung an Signal B desselben Typs, und `zeige()`/`signalOeffnen()` schließen das Formular — sonst stünden beim Signalwechsel die Werte von Typ A im Formular für Typ B und würden beim Speichern dorthin geschrieben. — **Zweite Entscheidung: aufgeklappt werden die *wirksamen* Werte geladen, nicht die eigenen.** `policyFormUmschalten` zieht über `fuer()` (also inkl. geerbter Eltern-Policy). Naheliegend wäre die eigene Zeile gewesen — dann sähe ein Kind-Team ein leeres Formular über einer aktiven Eltern-Entscheidung und „Speichern" hätte sie stillschweigend auf leer zurückgesetzt. Die Zeile weist die Herkunft aus („geerbt vom Eltern-Team — Speichern legt eine eigene Zeile an, die sie überstimmt"), „Regler entfernen" erscheint nur bei eigener Zeile, und ohne eigene Zeile sagt der Klick, dass die geerbte bestehen bleibt (Test fährt die Kette 7 geerbt → 99 eigen → entfernt → wieder 7). — **Dritte Entscheidung: die Sparkline zeichnet erst ab zwei Messpunkten.** Eine Waagerechte aus einem Punkt behauptet eine Stabilität, die nie gemessen wurde; bei einem Punkt steht der Satz „der Verlauf entsteht ab dem zweiten Detektor-Lauf". Bei flacher Reihe läuft die Linie mittig statt am Rand (am Rand liest sie sich als Extremwert). Die Normierung liegt im Component, nicht im Blade — sie ist Logik und damit testbar (`'0,24 50,12 100,0'` für 1→2→3). — **Vierte Entscheidung: gelesen wird die Signal-Seite der Reihe** (`source=signals`, Schlüssel = `SignalTyp`-Wert). Damit greift **V-010 hier by construction nicht**: die label-lose Ampel-Hälfte kann im Panel nicht auftauchen, weil das Panel nie einen DQ-Metrik-Key kennt. Für den Delta-Text nutzt `zustandFuer` `SignalTrendService::delta()` statt `uebersicht()` — bei der dichten Reihe (E1) ist „zwei jüngste Zeilen dieser Metrik" dasselbe Paar wie „zwei jüngste Läufe", und die schmale Variante ist eine Query statt drei. — **Suite 1239 · 1235 passed · 4 skipped · 0 Fail · 5945 Assertions** (Vorlauf 1235/1231, +4 Tests), 8,8 Min. **Kein Schema-Change, keine Migration, kein MCP-Nachzug** — `signal_policies.GET`/`signal_policy.PUT` existieren seit S2b unverändert; das Panel ist eine zweite Fläche auf denselben Service (Lockstep damit erfüllt, nichts nachzuziehen). **MySQL-Smoke echt gefahren und read-only:** über alle 4 offenen Signal-Typen der Dev-MySQL liefert `zustandFuer()` Bestand/Vorlauf/Delta ohne SQL-Fehler (3/3/4/1 offen, alle delta 0 — die zwei S2a-Läufe sind identisch, 100 Snapshot-Zeilen, 0 Policies gesetzt) und jede Serie hat 2 Punkte ⇒ die Sparkline zeichnet real. Nichts geschrieben, keine Policy angelegt. — **Fund am Weg (~10 Min):** die Blade-Roh-Block-Falle aus V-012 schlug im selben Panel-Blade **erneut** zu, in einer Variante, die der dortige Level-up-Vorschlag nicht abdeckt: (a) ein neuer Block-`@php` *unterhalb* einer vorhandenen `@php(...)`-Kurzform reißt sie mit (Fehlerbild `unexpected identifier "title"` = unkompilierter Komponenten-Tag) — Regel für die Vorlage: Block-Form nur **oben vor allen** Kurzformen; (b) nennt man die Direktivennamen in einem **Kommentar innerhalb** des Blocks, beendet das erwähnte Schluss-Tag den Block genau dort (Fehlerbild `unexpected token "\`"`). Beides in der Datei kommentiert. **Kein neuer V-Eintrag** — stattdessen V-012 um diesen zweiten Fundort + die zweite Variante ergänzt (der vorgeschlagene Compile-Test hätte nur (a) gefangen); sonst ist in diesem Lauf keine eigenständige neue Beobachtung angefallen. Geprüft und **verworfen**: „das Stumm-Häkchen auf einem `qualitaet_drift`-Signal wäre ein toter Knopf" — es ist keiner, `muted` lässt dort weiterhin die Einzel-Alarme zu einer Zustands-Zeile zusammenfallen, und „kein Drift über den Drift" ist eine bewusste E3-Entscheidung. Dev-Board: **#618** DoD-Zeile S3 bleibt „FAST" (S3a + S3b-1 + S3b-2 ✅, S3b-3 als offener Rest) — bewusst **nicht** abgehakt. Nächster Schritt: **21·S3b-3** (Ursachen-Kette nach unten: `br_ek_teil` → unbepreiste GPs → fehlende Lead-LAs; Regelwerk-Signale → Deep-Link auf das verletzte §).
- **2026-07-26 · 21·S3b-1 — Fix-Vorschau + Teil-Bulk ✅ (Routine-Lauf 7)** — Commit `4975406`. S3b war für einen Tick zu groß, deshalb geschnitten: **S3b-1 = die Fix-Hälfte (Punkte 3+7)**, **S3b-2 = Policy-Regler (8) + Sparkline (4) + Ursachen-Kette (5)** (Fortsetzungspunkt in der Checkbox, inkl. des V-010-Hinweises für die Sparkline). Gebaut: `SignalFixService::execute($team,$sig,$ids)` + neues `satz()` + `vorschau()` + fünf lesende Dry-Run-Zwillinge (`dryAllergen`/`dryLeadLa`/`dryRecipeAnker`/`dryGpAnker`/`dryRecompute`) · `SignalFixJob` mit `?array $ids` · `Signale/DetailPanel` um Checkboxen, `alleWaehlen`/`auswahlLeeren`/`vorschauZeigen`/`teilFixAusfuehren` · Blade-Aktionsleiste + Dry-Run-Block · MCP `signale.FIX` um `object_ids` + `dry_run`. — **Die tragende Entscheidung: die Teilmenge ist eine Einschränkung, keine zweite Autorisierung.** Naheliegend wäre gewesen, die IDs aus dem Panel direkt an die Fixer zu geben (sie sind ja aus der Liste geklickt) — damit wäre `SignalFixService` aber zur allgemeinen Schreib-Schnittstelle geworden: wer den Livewire-Call oder das MCP-Tool mit beliebigen IDs aufruft, hätte ein Recompute/Lead-LA-Repick auf jedem Objekt seines Teams auslösen können, ohne dass ein Detektor das je als Befund gesehen hat. Gebaut ist deshalb `satz()`: **eine** Stelle, aus der Fix *und* Vorschau ziehen, und mit Auswahl wird gegen `betroffene()` geschnitten (Test: eine ID mit gesetztem EK trifft `br_ek_null` nicht und bleibt unangetastet, `fixed=0`). Derselbe Schnitt macht Fix und Vorschau mengengleich — sonst beschriebe die Vorschau eine andere Menge als der Knopf darunter bearbeitet. — **Zweite Entscheidung: der Dry-Run ist ein Zwilling im selben Service, keine eigene Klasse.** Jeder Zweig spiegelt seinen Fixer rein lesend über die Fähigkeiten, die die Kern-Services schon haben (`backfillAllergenKonfidenz($gp, false)` rechnet ohne zu schreiben, `pickLeadLa` ohne `setLeadLa`, `resolveRecipeAnchors`/`resolveByName` ohne Mapping-Write, `betroffeneRezepte` statt `recomputeAndPropagate`). Eine Vorschau in einer eigenen Klasse hätte die Fixer-Bedingungen nachgebaut und wäre beim ersten Fixer-Edit zur Lüge geworden — sie steht bewusst direkt über `applyFixer`, damit ein Edit beide Seiten im Blick hat (im Klassen-Docblock begründet). — **Dritte Entscheidung: `recompute` sagt, was es nicht weiß.** Die vier anderen Fixer können ihr Zielfeld exakt nennen; die EK-Kette kann es nicht, weil sie erst beim Rechnen entsteht. Statt einen Wert zu erfinden zeigt die Vorschau dort die einzige exakt vorhersagbare Größe — die Größe der Propagations-Kaskade (`betroffeneRezepte`, also auch „wirkt auf n übergeordnete Rezepte") — und benennt den Rest als offen. Das ist die ehrliche Variante der Frage „welche Werte?". — **Vierte Entscheidung: die Zähler sind ausgewiesen gekappt.** `wirkt`/`wirkt_nicht` gelten nur über die aufgeschlüsselten Objekte (`VORSCHAU_LIMIT = 25`), weil jedes Objekt eine Auflöse-Frage kostet (Anker-Resolve, Lead-Pick, Kaskaden-BFS) — eine Vorschau über 3.000 Zeilen wäre so teuer wie der Fix selbst. Panel und MCP-Antwort weisen die Kappung aus, damit die Zahl nicht als Vollaussage gelesen wird. — **Fünfte Entscheidung: das Panel bekommt genau den Knopf, den die Zeile NICHT hat.** S3a hatte festgelegt, dass Lifecycle- und KI-Knöpfe in der Zeile bleiben (eine Wahrheit). Der Teil-Fix verletzt das nicht, weil er einen anderen Scope hat; ohne Auswahl tut er bewusst nichts und sagt, dass „alles fixen" in der Zeile liegt — sonst hätte dieselbe Aktion zwei Auslöser mit unterschiedlichem Default. Aus dem gleichen Motiv verwirft `updatedAuswahl()` die Vorschau (getestet): eine Feld-/Wert-Liste zu einer anderen Auswahl als der Knopf darunter ist genau die Fehlinformation, die der Dry-Run abschaffen soll. — **Suite 1235 · 1231 passed · 4 skipped · 0 Fail · 5918 Assertions** (Vorlauf 1222/1218, +13 Tests), 8,7 Min. **Kein Schema-Change, keine Migration** (die mysqldump-Warnung bleibt für die erste Etappe an einer Bestandstabelle stehen). **MySQL-Smoke echt gefahren und read-only — mit Erkenntniswert:** die Vorschau über alle **9** deterministisch fixbaren Signale der Dev-MySQL läuft ohne SQL-Fehler und zeigt, dass bei `gp_ohne_lead` real nur **3 von 15** Objekten geändert würden (12 = „kein bepreister LA wählbar", also echte Sourcing-Lücken) und bei `gp_anker_fehlt`/`br_anker_fehlt` **0 von 2** bzw. **0 von 2** — der Fixer wäre dort ein reiner No-op. Genau diese Information war vorher erst nach dem Klick zu haben. Zusätzlich der Auswahl-Schnitt gegen eine Fantasie-ID gegengeprüft (angefragt 2 → im Satz 1). Nichts geschrieben. **MCP-Lockstep erfüllt:** `object_ids` + `dry_run` am `SignaleFixTool` im selben Commit, inkl. Tenancy-Negativtest (`NO_TEAM`) und der Abweisung eines `dry_run` auf einem Assist-Plan (`ACTION_NOT_AVAILABLE` statt still zu schreiben). Neu im Backlog: **V-013** (ein fehlgeschlagener Auto-Fix hinterlässt keine Spur — `SignalFixService` verschluckt drei `catch`-Zweige vollständig, anders als das Vorbild `RecipeRecomputeService`, das jeden Fehlschlag loggt; damit ist „echte Datenlücke" nicht von „technischer Fehlschlag" zu unterscheiden, und der Teil-Bulk macht das häufiger sichtbar). Dev-Board: **#618** DoD-Zeile S3 auf „FAST" (S3a + S3b-1 ✅ mit Commits + Suite-Zahl, S3b-2 als offener Rest) — bewusst **nicht** abgehakt. Nächster Schritt: **21·S3b-2** (Policy-Regler aus E2 im Panel — `SignalPolicyService::setzen` existiert, es fehlt nur die Fläche; dann Sparkline + Ursachen-Kette).
- **2026-07-26 · 21·S3a — Signal-Panel + beide Auflöse-Richtungen ✅ (Routine-Lauf 6)** — Commit `be8dc44`. S3 war für einen Tick zu groß, deshalb geschnitten: **S3a = das Panel + Punkte 1/2**, **S3b = die Fix-Hälfte (3+7) mit Policy/Trend/Ursachen** (Fortsetzungspunkt in der Checkbox, inkl. der Begründung, warum Punkt 6 draußen bleibt). Gebaut: `src/Livewire/Signale/DetailPanel.php` + `resources/views/livewire/signale/detail-panel.blade.php` · `SignalObjectService` · `DataQualityService::trifftObjekt()` · rechte Fläche auf der Signale-Seite (`activity_signale`, eigener Sidebar-Scope) · `signal-geaendert`-Dispatch nach jeder Lifecycle-Aktion. — **Die tragende Entscheidung: die objekt-zentrische Sicht wird RÜCKWÄRTS gefragt, nicht aus den Trefferlisten zusammengesetzt.** Die naheliegende Variante wäre gewesen, für jedes offene Signal `betroffene()` zu laden und die Objekte zu schneiden — bei 3.138 „Im Review" und 788 teil-unbepreisten wären das Zehntausende Zeilen für eine Frage nach EINEM Rezept. Gebaut ist deshalb `DataQualityService::trifftObjekt()`: dasselbe `queryFor`-Prädikat wie Zählung und Liste (kein vierter Query-Pfad), aber mit `whereKey(...)->exists()` — **ein EXISTS je Metrik statt n Zeilen**, dazu ein Cache je `metrik|kind` innerhalb eines Aufrufs. Der `kind`-Vergleich ist dabei nicht Kosmetik: ohne ihn würde eine GP-Metrik ein Rezept mit derselben ID treffen (Test deckt genau das ab). — **Zweite Entscheidung: `total` kommt aus der Live-Query, nicht aus dem Payload.** Die alte Inline-Liste zeigte „50 von 42" nach dem Payload-Stand des letzten Detektor-Laufs; wer zwischendurch fixt, sah die alte Zahl. Jetzt zählt bei gekappter Liste `countFor()` nach — ein Fix senkt die Zahl sofort, und der Test setzt bewusst Payload 7 gegen eine echte 1. — **Dritte Entscheidung: die Inline-50er-Liste ist abgelöst, nicht verdoppelt.** `toggleDetail`/`detailPanelId`/`betroffeneFuer` sind aus `ReviewQueue` heraus (−95 Zeilen), die Auflösung liegt jetzt EINMAL im `SignalObjectService`. Zwei Listen mit unterschiedlichem Limit und unterschiedlicher `total`-Semantik nebeneinander wären genau die zweite Wahrheit, die das Cockpit-Redesign abschaffen wollte. Aus demselben Grund bekommt das Panel **keine** eigenen Erledigt-/Ignorieren-/KI-Knöpfe — die bleiben in der Zeile; das Panel zeigt den KI-Plan nur als Erklärtext. — **Eigener Service statt `SignalService`-Methode**, weil die Auflösung `DataQualityService` braucht und der am `SignalService` hängt: eine Injection dort wäre ein Container-Zyklus (im Klassen-Docblock vermerkt). — **Suite 1222 · 1218 passed · 4 skipped · 0 Fail · 5864 Assertions** (Vorlauf 1216/1212, +6 Tests), 8,6 Min. **Kein Schema-Change, keine Migration, kein MCP-Nachzug** (rein lesende UI-Schicht auf vorhandenen Prädikaten; die `signale.*`-Tools liefern dieselben Signale wie vorher). **MySQL-Smoke echt gefahren und read-only:** gegen die Dev-MySQL beide Richtungen über 4 offene Signale — `betroffene()` löst 15/8/2/5 Objekte ohne SQL-Fehler auf, `signaleAmObjekt()` liefert für den jeweils ersten GP genau 1 offenes Signal (im Fixture-Bestand hat kein Objekt Mehrfach-Befunde). Nichts geschrieben. — **Fund am Weg (kostete ~15 Min):** Blades Raw-Block-Regex greift ab dem ersten `@php` bis zum ersten `@endphp` — die Kurzform `@php(extract(Ui::maps()))` **vor** einem `@php … @endphp`-Block lässt die halbe View unkompiliert und meldet `unexpected token "class"` auf die Compile-Datei. Im Panel-Blade jetzt ein Block oben, Kurzformen erst danach, mit Warn-Kommentar an der Stelle; der Bestand ist geprüft und nirgends betroffen. Daraus **V-012** (Test-Lücke: kein einziger Test prüft, dass ein Modul-Blade überhaupt kompiliert — 105 Blades, abgedeckt nur, was zufällig in einem Feature-Test gerendert wird). **Pint bewusst NICHT angewendet:** der Sandbox-Pint (Laravel-Preset, kein `pint.json` im Modul) formatiert das Modul gegen seinen committeten Stil um (`' . '` → `.'`, leere Konstruktor-Bodies) — 140 Zeilen Fremd-Churn allein in `DataQualityService`; zurückgedreht, neue Dateien an den Modul-Stil angepasst. Dev-Board: **#618** DoD-Zeile S3 auf „HALB" (S3a ✅ mit Commit + Suite-Zahl, S3b als offener Rest ausformuliert) — bewusst **nicht** abgehakt. Nächster Schritt: **21·S3b** (Dry-Run + Teil-Bulk brauchen zuerst einen Teilmengen-Pfad in `SignalFixService`/`SignalFixJob` — der kennt heute nur den vollen betroffenen Satz; das ist der eigentliche Kern der Etappe, nicht die UI).
- **2026-07-26 · 21·S2b — E2 Rausch-Guard + E3 Drift ✅, Tranche E damit komplett (Routine-Lauf 5)** — Commit `ec2b365`. Gebaut: Migration `2026_07_26_000002_create_foodalchemist_signal_policies_table` · Model `FoodAlchemistSignalPolicy` · `SignalPolicyService` (`alle`/`fuer`/`setzen`/`loeschen`/`zustand`/`aggregierteTypen`/`driftStumm`) · `exclude_types` an `SignalService::paginate` · Zustands-Zeilen im Signale-Tab der `ReviewQueue` · Detektor-Schritt `SignalDetektorService::qualitaetsDrift()` + neuer `SignalTyp::QualitaetDrift` · MCP `foodalchemist.signal_policies.GET` (read-only) + `foodalchemist.signal_policy.PUT` (Write, menschlich getriggert). — **Die tragende Auslegungs-Entscheidung: der Guard greift an der Präsentation, nicht an der Entstehung.** Die naheliegende Variante wäre gewesen, ein Signal über Schwelle gar nicht erst zu erzeugen (oder zu löschen) — das hätte die Zahl geschönt und, schwerwiegender, dem Detail-Panel aus S3 die Objekte weggenommen: dessen Kern-Features (betroffene Objekte, objekt-zentrische Sicht, Teil-Bulk) brauchen genau die Einzelzeilen, die der Guard sonst wegwürfe. Gebaut ist deshalb: die Signale bleiben vollständig in der Tabelle, `paginate()` blendet aggregierte Typen **nur in der ungefilterten Ansicht** aus, und ein Klick auf die Zustands-Zeile setzt den Typ-Filter und holt sie zurück (Test deckt beide Richtungen ab + prüft, dass die Zeilen physisch da sind). — **Drei getrennte Regler statt einem Schwellwert**, weil sie drei verschiedene Aussagen sind: `threshold` = Darstellung · `accepted_until` = bewusste Akzeptanz **mit Ablaufdatum** (Stichtag inklusive; danach kippt die Zeile automatisch auf `frist_abgelaufen` = wieder Alarm, ohne dass jemand die Policy anfassen muss — ein Guard ohne Verfallsdatum wäre ein Deckel) · `muted` = „interessiert nicht" und **als einziger auch drift-wirksam**. Daraus die Regel, die den Guard vom Wegsehen trennt: **Schwelle und Frist dämpfen den Bestand, ein Zuwachs meldet sich weiter.** Eine akzeptierte Lage darf nicht heimlich wachsen — sonst könnte man eine sich verschlechternde Lücke wegkonfigurieren. — **E3 mit vier Sperren gegen Fehlalarm:** (1) `previous === null` (im Vorlauf nicht gemessen) alarmiert nie — dafür wurde die Reihe in S2a dicht geschrieben, und genau dieser Fall ist getestet; (2) ein Zuwachs braucht **beides**, ≥ 20 % **und** ≥ 5 absolut (sonst wäre 1 → 2 ein „+100 %"-Alarm), Ausnahme ist das **Neuauftreten 0 → n**, das mengenunabhängig zählt, weil ein behobener und zurückgekehrter Befund die stärkste Aussage der Reihe ist; (3) dieselbe Lage wird oft doppelt gemessen (Ampel-Metrik *und* offene Signale desselben Typs) → die Ampel-Seite gewinnt, sonst zwei Drift-Signale für einen Sachverhalt; (4) kein Drift über den Drift. Dedup je `drift:<source>:<metric_key>` — ein Lauf ohne neue Messung aktualisiert statt zu doppeln (getestet). — **Reihenfolge in `laufen()` ist jetzt Detektoren → Snapshot → Drift**, weil der Vergleich den frischen Punkt gegen den Vorlauf braucht. Bewusst in Kauf genommen: die Drift-Signale eines Laufs erscheinen erst im *nächsten* Snapshot — sonst zählte sich der Drift selbst mit. Im Docblock begründet. — **Das Drift-Signal bekommt bewusst keinen Knopf** (kein `metrik` im Payload): über eine Trend-Zeile einen Massen-Fix auszulösen wäre genau der Blind-Klick, den Spec 21 §7 Punkt 3 abschaffen will; repariert wird am zugrundeliegenden Befund. — **Policies erben wie der Katalog** (`BelongsToTeamHierarchy`): Kind-Team sieht die Eltern-Policy, eigene Zeile überstimmt sie, die Eltern-Zeile bleibt unangetastet; damit war **keine** neue Ausnahme im Trait-Vertrag der `PolicyTest` nötig (anders als bei der Snapshot-Messreihe in S2a — dort ist die Zeile eine Messung, hier eine übernommene Entscheidung, und Entscheidungen erbt man im Katalog-Modell). — **Suite 1216 · 1212 passed · 4 skipped · 0 Fail · 5844 Assertions** (Vorlauf 1200/1196, +16 Tests), 8,6 Min. **MySQL:** Migration gegen die Dev-MySQL gefahren (384 → 385 Tabellen; Spalten inkl. `date`/`tinyint(1)` und beide Indizes per `SHOW COLUMNS`/`SHOW INDEX` verifiziert), danach **read-only** gegengeprüft: `zustand()` liefert 4 Zustands-Zeilen mit korrektem Delta, `uebersicht()` meldet 0 gestiegene Metriken ⇒ ein echter Drift-Lauf würde nichts emittieren. An Fach-Daten wurde nichts angefasst, keine Signale geschrieben. `mysqldump` fehlt auf dem Host weiterhin (s. S2a) — hier wieder verantwortbar, weil reines `CREATE TABLE` mit `hasTable`-Guard; **die Warnung bleibt für die erste Etappe stehen, die eine Bestandstabelle ändert.** MCP-Lockstep erfüllt (beide Tools im selben Commit registriert, Tenancy-Negativtest `NO_TEAM` für Read **und** Write). Neu im Backlog: **V-011** (eine Lücke, die auf 0 fällt, schließt ihr Signal nicht — geschlossen wird ausschließlich über den Fixer-Knopf; Phantome halten Zustands-Zeile und Signal-Trend hoch, während die Ampel-Hälfte längst 0 misst — trifft direkt die Schicht, die dieser Lauf gebaut hat). Dev-Board: **#618** DoD-Zeile S2 auf ✅ KOMPLETT gesetzt. Nächster Schritt: **21·S3** (Tranche P — `Signale/DetailPanel`; **Achtung:** Punkt 6 „Historie/wie oft wiedergekehrt" ist laut V-009 mit dem heutigen Schema nicht baubar, die Etappe müsste dafür mit einer Migration an `foodalchemist_signals` beginnen — dann greift die mysqldump-Warnung erstmals scharf).
- **2026-07-26 · 21·S2a — E1 Zeitreihe ✅ (Routine-Lauf 4)** — Commit `d8b52bf`. S2 war für einen Tick zu groß (Migration + Service + MCP + E2/E3), deshalb geschnitten: **S2a = E1 Zeitreihe**, **S2b = E2 Rausch-Guard + E3 Drift** (bewusst zusammen, s. Checkbox). Gebaut: Migration `2026_07_26_000001_create_foodalchemist_signal_snapshots_table` · Model `FoodAlchemistSignalSnapshot` · `SignalTrendService` (`schreibeSnapshot`/`serie`/`delta`/`uebersicht`/`laufZeitpunkte`) · Fold-in in `SignalDetektorService::laufen()` · `--snapshot` an `foodalchemist:data-quality` · MCP `foodalchemist.signal_trend.GET`. — **Die zwei tragenden Entscheidungen:** (1) **dichte Reihe** — ein Lauf schreibt *alle* Metriken inkl. der Nullen (25 Ampel-Lücken + 25 Signal-Typen = 50 Zeilen/Team/Lauf). Sparsam wäre billiger, macht aber „behoben" (0) von „damals nicht gemessen" (Check gab es noch nicht) unterscheidbar — und **genau diese Unterscheidung ist die Voraussetzung für E3**: ein neu gebauter Check darf nicht als Verschlechterung alarmieren (`previous=null` ⇒ kein Drift). Test deckt den Fall explizit ab. (2) **ein Lauf = ein `measured_at`** (auf die Sekunde), darauf beruht das Paaren „letzter Lauf ↔ Vorlauf" in `uebersicht()`; Unique-Index `(team_id, source, metric_key, measured_at)` macht einen Doppellauf zum Überschreiben statt zum Doppeln (getestet). — **Zwei Quellen in einer Tabelle** (`source`): `data-quality` (Lücken-Metriken der Ampel) **+** `signals` (offene Signale je Typ, mit `severity_counts`). Die zweite Hälfte ist **nicht** redundant: die 10 Detektor-Typen (Preis-Anomalie, Vertragsfrist, Widerspruch Wissen↔Graph …) haben keine Ampel-Metrik und wären ohne sie trendlos. — **Geschrieben wird im Detektor-Lauf**, nicht in einem Zweit-Job: `SignalDetektorService::laufen()` ruft am Ende (nach allen Emissionen, sonst zählt die Signal-Seite den Stand *vor* diesem Lauf) `schreibeSnapshot()`. Grund: der Scheduler auf demo fährt `signale-detektor`, und genau so ist 2026-07-14 auch die DQ-Ampel eingehängt worden — ein separater Snapshot-Command wäre lokal grün und auf demo nie aktiv. Der `--snapshot`-Schalter an `data-quality` bleibt für die Messung außer der Reihe. — **Drei bewusste Abweichungen/Feinentscheide:** (a) Spec nennt für die Tabelle „`type`/`gap_key` + `severity_counts`"; gebaut ist `metric_key` + `source` + `signal_type`, weil derselbe Schlüsselraum zwei Bedeutungen hat (SignalTyp-Wert vs. Ampel-Metrik-Key) und ein Trend-Reader sonst raten müsste, was er vor sich hat. `severity_counts` wird nur für die Signal-Seite gefüllt (die Ampel-Metrik hat genau *eine* Severity). (b) **Kein `visibleToTeam` auf der Snapshot-Zeile** — eine Zeitreihe gehört dem messenden Team; ein Kind-Team zählt den Eltern-Katalog bereits mit, eine vererbte Eltern-Serie läge als zweite Serie derselben Objekte darüber. Das kollidierte mit dem repo-weiten Trait-Vertrag in `PolicyTest` („ALLE Models tragen BelongsToTeamHierarchy") → dort eine **zweite, benannte Ausnahme-Kategorie `$messreihen`** ergänzt statt das Model in die vorhandene „Satelliten"-Liste zu schmuggeln (die meint „scopet über sein Eltern-Aggregat", etwas anderes). Alternative wäre gewesen, den Trait mitzunehmen und nie zu benutzen — das ist eine Falle für den Nächsten. (c) Die **Zähl**-Seite gegen `foodalchemist_signals` nutzt trotzdem `visibleToTeam`, weil `SignalService::offeneCount`/`paginate` (= die Zahl auf dem Bildschirm) es tun; eine Trendzahl, die vom Cockpit abweicht, wäre schlimmer als die Vererbungs-Unschärfe. Beides im Code begründet. — **`pct` bleibt bei Nullbasis `null`** (von 0 auf 5 ist ein Neuauftreten, keine „+500 %") — E3 entscheidet über `delta`, nicht über Prozent. — Additiv nötig war ein `kind`-Marker (`gap`/`info`) an den Metrik-Konstruktoren des `DataQualityService`: `severity='info'` kann **beide** Arten treffen (ein als Info deklarierter Lücken-Check wird nie rot), die Zeitreihe braucht aber genau die Lücken und nicht die Bestands-Totale („GPs approved"). — **Suite 1200 · 1196 passed · 4 skipped · 0 Fail · 5788 Assertions** (Vorlauf 1189/1185, +11 Tests), 8,4 Min. **MySQL diesmal voll durchgefahren** (Sandbox läuft auf lokalem MySQL, Tests auf SQLite `:memory:`): Migration gefahren → 383 → 384 Tabellen (nur die neue; `SHOW COLUMNS`/`SHOW INDEX` verifiziert: `severity_counts` als echtes `json`, beide Indizes da), dann **zwei** Snapshot-Läufe über `data-quality --team=1 --snapshot` → 100 Zeilen, `uebersicht()` liefert `measured_at`/`previous_at` korrekt gepaart, `delta` rechnet (242 → 242, delta 0). Das ist reine Mess-Schreibung in die neue Tabelle — an Fach-Daten wurde nichts angefasst, `--signals` bewusst **nicht** gesetzt. ⚠️ **`mysqldump` ist auf diesem Host nicht installiert** (weder auf PATH noch in DBngin-Pfaden; die früheren `storage/PRE_*.sql` stammen von einer Umgebung, die es hatte) → statt Dump als Absicherung die Tabellen-Zählung vor/nach + Schema-Verifikation; verantwortbar, weil die Migration ein reines `CREATE TABLE` mit `hasTable`-Guard ist (kein ALTER, kein Datenzugriff) und `down()` nur die neue Tabelle dropt. **Bei S2b (Policy-Tabelle) gilt dasselbe; sobald eine Etappe eine Bestandstabelle ändert, ist der fehlende Dump ein echter Blocker → dann `mysqldump` bereitstellen (Dominique) oder die Etappe supervidiert fahren.** — MCP-Lockstep erfüllt: neues read-only Tool `signal_trend.GET` im selben Commit registriert (Tenancy-Negativtest: Kontext ohne Team **und** User ohne `current_team_id` ⇒ `NO_TEAM`; die Basisklasse fällt bewusst auf `currentTeamRelation` zurück, das ist im Test dokumentiert). Neu im Backlog: **V-009** (Wiederkehr eines Signals wird nirgends gemessen — kein `last_seen_at`/`seen_count`; damit ist Spec 21 §7 Punkt 6 „Historie/wie oft wiedergekehrt" heute **nicht baubar**, S3 müsste mit einer Migration beginnen) + **V-010** (Ampel-Metrik-Labels existieren nur während der Messung → die Trend-Antwort zeigt für die Ampel-Hälfte rohe Keys, für die Signal-Hälfte Labels). Dev-Board: **#618** DoD-Zeile S2 auf „HALB" gesetzt (S2a ✅ mit Commit + Suite-Zahl + den drei Entscheiden + dem mysqldump-Hinweis, S2b als offener Rest ausformuliert) — bewusst **nicht** abgehakt, S2 ist erst mit S2b fertig. Nächster Schritt: **21·S2b** (E2 `signal_policies` + E3 `qualitaet_drift`).
- **2026-07-26 · 21·S1b — Tranche A KOMPLETT (11/11) ✅ (Routine-Lauf 3)** — Commit `fae3658`. Die 5 Checks mit eigener Mechanik gebaut: `rezept_yield_implausibel` · `rezept_naming_regelwerk` · `rezept_dublette` · `rezept_allergen_unbelastbar` (kritisch) · `rezept_sub_stub_offen`. **Etappe S1 damit abgeschlossen.** — **Die Einheiten-Falle beim Yield ist entschärft, aber der Spec-Zweig „< 0,3 × Einsatzmasse" ist bewusst NICHT gebaut:** genau dieser Wert ist das *reguläre* Recompute-Ergebnis, sobald Putz-/Garverlust deklariert sind (Reduktionen/Fonds/Jus erreichen 70–90 %), und die Verlust-Kaskade (`effektiverPutzverlust`/`effektiverGarverlust`) zieht Defaults von GP und Team-Warengruppe — der Check hätte jede Reduktion geflaggt und wäre nicht deterministisch entscheidbar. Gebaut sind die zwei belastbaren Hälften: „Ausbeute fehlt trotz Zutaten" und „Ausbeute > Roh-Einsatzmasse" (physikalisch unmöglich, Verluste senken nur) — letzteres nur, wenn **jede** beitragende Zutat (ohne `is_optional`, ohne Einheit `qs`, gespiegelt aus `RecipeRecomputeService::yieldUndZaehler`) eine Massen-Einheit mit Gramm-Faktor trägt; sonst zählt eine Bund-/Stück-Zutat mit 0 g und die Ausbeute wirkt fälschlich zu hoch (Test deckt genau diesen Negativfall ab). Der belastbare Zwilling des gestrichenen Zweigs ist ein **Drift-Check über die vorhandenen `*_aggregated_at`-Stempel** → **V-008** (am Bestand gemessen: 4 von 95 Rezepten haben eine Zutat, die nach der letzten Aggregation geändert wurde; kein Check und kein Detektor liest diese Stempel bisher). — **Zweite Abweichung: Naming + Dubletten laufen in PHP, nicht in SQL.** Normalisierung + Muster wären in SQL entweder dialekt-gebunden (`REGEXP` kennt MySQL, SQLite nicht) oder eine REPLACE/LOWER-Kette mit unterschiedlichem Umlaut-Verhalten je Dialekt; ein Pass über `(id, name)` ist bei Tausender-Beständen billig, bit-identisch auf beiden Dialekten und im Register unsichtbar (die `q`-Closure liefert weiter einen Builder via `whereIn('id', …)`, also funktionieren `betroffene()`/`countFor()` unverändert). Bewusst **ohne** Memoisierung — der Service wird nach einem Fix erneut gefragt (`countFor` ⇒ Signal schließen), ein Cache würde dort den alten Stand melden. — **Regelwerk hat gegen Spec gewonnen (dritte Abweichung):** Spec 21 §2 listet „Grammatur im Namen" als Verstoß, Regelwerk_Verkaufsgerichte **§1.2a** (und Basisrezepte §1.8) erlauben sie ausdrücklich als Diskriminator zwischen sonst gleichnamigen Gerichten („(17g)" vs. „(65g)") → sie zählt nur, wenn dieser Zwilling nicht existiert (Basis-Namen-Zähler im selben Scan). VK-Namen werden zusätzlich gegen **[HG]-Präfix + Pipe-Skelett** (§1.1) und die Katalog-Marker `CC:`/`STF:`/`MS:`/`(SG)`/`(BOX)`/`ADD ON`/`[FC]` (§1.2) geprüft; die §1.2-Regel „keine Verbindungswörter (mit/auf/an)" **nicht** umgesetzt, weil sie legitime Bausteinnamen („Kartoffelgratin mit Trüffel") mitreißen würde — Rauschen ist teurer als eine fehlende Teilregel. Dubletten gruppieren je **Rezept-Art + Kategorie**, weil Regelwerk_Basisrezepte §1 die Kategorie als Diskriminator vorsieht; geflaggt werden **beide** Seiten. — **Allergene nur wo es haftet:** kundenexponiert = eine der 4 Kunden-Flächen (Konzept-Slot/Foodbook-Block/Speiseplan/Paket) **plus eine Sub-Rezept-Ebene** (die Sauce im verkauften Gericht); tiefer wäre rekursiv (§4 erlaubt 3 Ebenen) und für eine Trefferliste unverhältnismäßig — Grenze im Code dokumentiert. Sub-Stub prüft `status` in **stub/draft** (Spec sagt nur draft, `RecipeService::createSubRecipeStub()` setzt real `stub`). — **Fixture-Härtung war Pflicht, nicht Kosmetik:** `makeRecipe` setzt jetzt saubere Allergen-Defaults (DB-Default ist `unbekannt`/`unknown` → jedes exponierte Fixture-Rezept hätte sofort geflaggt) und `makeIngredient` führt die Ausbeute mit der eingewogenen Masse mit (1,0 kg aus 200 g Zutaten ist unmöglich) — sonst wäre der Negativfall „Fixture-Rezept löst KEINEN Check aus" verloren gegangen. Zwei bewusst offene Grenzen des Versprechens im Fixture-Docblock notiert (VK-Namen macht der Test selbst regelkonform; rohe Zutaten-Inserts umgehen die Ausbeute-Mitführung). **KI-Assist:** `rezept_naming_regelwerk` → neuer Prompt `signal.recipe_naming_suggest` (Vorschlag, nie Auto-Rename — der Name hängt in Angeboten und Foodbooks). Der **Inventar-Test aus `PromptRegistryTest` hat den neuen Key sofort als „unbekannt" gemeldet** — genau wie gedacht, Soll-Liste nachgezogen. Register-Reihenfolge auf die dokumentierte Lese-Logik gebracht (drei kritische Checks zuerst). **Suite 1189 · 1185 passed · 4 skipped · 0 Fail · 5742 Assertions** (Vorlauf 1184/1180, +5 Tests), 8,4 Min. **MySQL-Smoke diesmal echt gefahren:** die Sandbox läuft auf lokalem MySQL (`DB_CONNECTION=mysql`, Tests dagegen SQLite `:memory:` per `phpunit.xml`) → `foodalchemist:data-quality` **read-only** (kein `--signals`) liefert alle 11 Checks ohne SQL-Fehler: u. a. 7 kundenexponierte Rezepte mit unbelastbaren Allergenen, 1 offener Sub-Stub, 1 Ausbeute-Fall; Naming/Dubletten = 0 und das ist **echt** (gegengeprüft: alle 31 VK-Gerichte tragen das `[HG]`-Präfix, 64 Basisrezepte ohne Doppel-Leerzeichen/Rand-Trenner). Dev-DB unverändert (0 Rezept-Signale, letztes Signal weiter 2026-07-14). Kein Schema-Change, keine Migration, **kein MCP-Nachzug nötig** (das Typ-Enum der `signale.*`-Tools kommt seit S0 aus `SignalTyp::cases()`, die neuen Typen erscheinen automatisch). Dev-Board: **#618** DoD-Zeile S1 auf ✅ KOMPLETT gesetzt (beide Commits + Suite-Zahl + die drei bewussten Abweichungen). Nächster Schritt: **21·S2** (Tranche E — `signal_snapshots` + `signal_policies` + Drift-Meta-Signal; erste Etappe mit Migration → Backup-Regel beachten).
- **2026-07-26 · 21·S1a Tranche A (5 von 10 Rest-Checks) ✅ (Routine-Lauf 2)** — Commit `608da7d`. S1 war für einen Tick zu groß, deshalb geschnitten: **S1a = die 5 Checks, die ein reines Builder-Prädikat sind** (`rezept_ohne_zubereitung` · `rezept_mengen_luecke` · `rezept_ein_zutat` · `rezept_kategorie_problem` · `rezept_verwaist`), **S1b = die 5, die je eigene Mechanik brauchen** (Fortsetzungspunkt oben in der Checkbox notiert, inkl. der Einheiten-Falle bei `yield`). Mit S0 sind **6 von 11** Tranche-A-Checks scharf. Struktur: alle Tranche-A-Prädikate liegen in **einem Check-Register** (`rezeptQualitaetChecks()`), aus dem Zähl-Seite, `betroffene()` und `countFor()` gemeinsam ziehen — das ist bewusst nur für die *neuen* Checks gebaut (V-005 bleibt Dominiques Entscheidung für den Altbestand, s. Nachtrag dort). **Drei Stellen, an denen die Spec vom Code abwich und der Code gewonnen hat:** (1) Spec sagt „`status != draft`", real gibt es auch `stub` (RecipeService setzt das für Auto-Stubs) und `deprecated` → geprüft wird nur `review`/`approved`, sonst flaggt der Zubereitungs-Check jeden Stub, den `rezept_sub_stub_offen` ohnehin fängt. (2) Spec sagt „Kategorie zeigt auf eine `is_inactive`-Hauptgruppe" — das gilt **nur** für VK-Gerichte (`dish_main_groups`); Basisrezepte hängen an `recipe_main_groups`, das gar kein `is_inactive` hat (→ V-007), dort bleibt nur „Kategorie fehlt". (3) Der Info-Schweregrad war bisher nur am Signal wirksam, die Metrik-Ampel blieb mengengesteuert → ein Info-Check wird jetzt nie rot (300 verwaiste Rezepte sind kein roter Zustand). **KI-Assist:** nur `rezept_kategorie_problem` bekommt einen Knopf (`signal.recipe_category_suggest`, Bauart-Logik, kein Food-DNA-Key — DNA würde die Klassifikation verzerren); der Rest bleibt bewusst knopflos, weil „ohne Zubereitung"/„Mengen-Lücke" Küchen-Wissen am Einzelfall braucht und ein Sammel-`propose()` über 15 Beispiele nur Scheinsicherheit liefert. Dabei aufgefallen und gleich abgesichert: ein Assist-Mapping mit unbekanntem Prompt-Key fiel bisher **erst beim Klick** auf → neuer Test läuft über **alle** `SignalTyp::cases()` und verlangt für jeden Assist-Plan einen registrierten Prompt-Key. **Suite 1184 · 1180 passed · 4 skipped · 0 Fail · 5696 Assertions** (Vorlauf 1176/1172, +8 Tests), 8,3 Min. Zusätzlich gegen **echte Daten** gefahren (`foodalchemist:data-quality`, read-only, kein `--signals`): alle 6 Checks liefern ohne SQL-Fehler. **MySQL-Smoke: nicht gefahren** (nur SQLite verifiziert) — die einzige dialekt-nahe Stelle ist `LENGTH(TRIM(COALESCE(preparation,'')))`, in MySQL Bytes statt Zeichen, bei Schwelle 20 fachlich belanglos (im Code vermerkt). Keine Migration, kein Schema-Change. Neu im Backlog: **V-006** (`rezept_verwaist` kann über `updated_at` nie feuern — jeder Recompute setzt die Uhr zurück; 2095 unreferenziert, aber 0 älter als 180 d) + **V-007** (zwei Hauptgruppen-Bäume, nur der VK-Baum kann stillgelegt werden). Dev-Board: **#618** DoD-Zeile S1 auf „halb" gesetzt (S1a ✅ mit Commit + Suite-Zahl, S1b als offener Rest ausformuliert) — bewusst **nicht** abgehakt, S1 ist erst mit S1b fertig. Nächster Schritt: **21·S1b**.
- **2026-07-25 · 21·S0 Fundament ✅ (Routine-Lauf 1)** — Commit `a826225`. Enum um die 11 Tranche-A-Typen erweitert (Label+Icon+`istRezeptQualitaet()`); neue Ebene `rezeptqualitaet` im `DataQualityService` (bewusst über **beide** Rezept-Arten, weil Regelwerk_Basisrezepte für Basisrezept wie VK gilt); `gap()` nimmt jetzt einen **fixen Schweregrad** entgegen — sonst wäre „ohne Zubereitung" bei 3 Treffern nur Warnung und „verwaist" bei 300 kritisch (Mengen-Heuristik ROT_SCHWELLE=100 passt für Tranche A nicht). Erster Check `rezept_zutaten_ungemappt` end-to-end als Muster für S1 (Zähl-Query · `queryFor` · `betroffene`/`countFor` · Emission). Fixture-Basis in `SeedsTeamHierarchy`: `makeRecipe()` erzeugt bewusst ein **qualitativ unauffälliges** Rezept, damit jeder S1-Test genau ein Feld verschlechtert und den Negativfall (kein Über-Flaggen) gratis bekommt. MCP-Lockstep: das Typ-Enum in `signale.SEARCH`+`signale.LIST` war handgepflegt und schon auf 7 von 14 Typen zurückgefallen → jetzt aus `SignalTyp::cases()` abgeleitet. **Suite 1176 · 1172 passed · 4 skipped · 0 Fail** (Baseline 1169/1165, +7 neue Tests), 8,4 Min. Nebenbefund zu **V-003**: dessen Test-Hälfte (Pest über `cases()` erzwingt Label+Icon) ist mit diesem Lauf real da — offen bleibt dort nur noch der Map-Refactor. Neu: **V-005** (Lücken-Prädikate doppelt in `DataQualityService`; lohnt sich **vor** Tranche A). Dev-Board: Spec 21 hatte **kein** Issue — neu angelegt als **#618** (Package 23 / Features / In Progress) mit DoD S0–S5, S0 abgehakt; künftige Läufe haken dort weiter ab. Nächster Schritt: **21·S1** — die restlichen 10 Tranche-A-Checks nach diesem Muster, je mit Positiv-/Negativfall + Fixer-/Assist-Mapping in `SignalCockpit`.
- **2026-07-25 · Spec 21 angelegt (Session, nach Signal-Audit)** — Ist-Katalog kartiert: 14 `SignalTyp`-Typen aus `DataQualityService` (14 Gaps) + `SignalDetektorService` (10 Detektoren) + `SignalCockpit` (FIX_DET/ASSIST). Befund: Rezept-Ebene prüft nur EK/Anker/Servierform, **Konzept+Foodbook 0 Signale**, Signale-Seite ohne rechtes Panel (6 andere Seiten haben eins). Neue Spec 21 mit Tranchen A/B/C/D/E/P + Etappen S0–S5; als **Phase 1 vor Spec 03** eingehängt (Entscheid Dominique), Folgephasen umnummeriert. Nächster Schritt: **21·S0**.
- **2026-07-25 · Phase 0 (manuell, supervidiert)** — Voll-Audit aller Specs 01–20 an HEAD `a55ced3`. Ergebnis: L3 + Spec-08-Divergenz via 19 erledigt, L2 dagegen offen (Annahme widerlegt); 6/8 Lücken in Spec 03 offen; R2.4=XL und Kanal-B=L neu eingeordnet; Spec-15-Blocker als 50k-Store-Grenze präzisiert; Suite-Memory-Falle dokumentiert. Matrix + dieser Fahrplan neu geschrieben, Orchestrierung als SUPERSEDED markiert. Baseline gemessen: **1169/1165 passed/4 skip/0 Fail** (8,6 Min Laufzeit → Tick-Budget dokumentiert). Alle 15 Kern-Artefakte der „fertig"-Specs an HEAD gegengeprüft (114 MCP-Tools, 193 Test-Dateien). Nächster Schritt: **Phase 1 · L5** (`RecipesGenerateTool`).
