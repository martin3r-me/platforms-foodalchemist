# 19 — Foodbook-Leitstelle A–Z: Phasen-Cockpit + volle Kaskaden-Verdrahtung

> **Status:** 🟢 bau-reif, freigegeben Dominique 2026-07-23. Nachfolger der Foodbook-Hälfte von Spec 08 (dort Banner).
> **Betrieb:** Autonome Routine arbeitet die Teilschritt-Checkliste (§Ende) ab — **diese Datei ist der Übergabepunkt zwischen Läufen.** Jeder Lauf: Checkliste lesen → EINEN Teilschritt bauen → verifizieren → Checkliste fortschreiben → committen.

## Kontext — warum

Das Foodbook-Cockpit (Phasen 1–6, gepusht) ist gebaut, aber die Planung ist **flach** und **konvergent**: Slots = freie Labels, Ziele hängen am flachen Frame-Slot, jedes übernommene Gericht landet sofort in einem Konzept, Kreativ-Ideen ohne DB-Anlage gibt es nicht. Dominiques reales Doing (Broich Foodbook 2026: 4 Ebenen tief, 139 Überschriften, Excel-Preis-Cockpit mit WE-Ampel, Duality Pakete/Einzelartikel) ist ein **mehrphasiger Baukasten**. Ziel: Das Foodbook wird die echte Leitstelle — 7 Arbeits-Phasen führen von Bedarf bis Preis, alle Dimensionen werden am Foodbook/Kapitel definiert und kaskadieren nach unten (Foodbook ⊃ Kapitel(n-tief) ⊃ Konzept/Gericht ⊃ Rezept ⊃ GP/LA), und **nichts wird angelegt, bevor ein Kapitel freigegeben ist**.

Regeln: nur dieses Modul + Sandbox; gezielt stagen (NIE `add -A`); ROADMAP + Office-Dev-Package 23 mitziehen; MCP-Lockstep als DoD; Migrationen additiv/idempotent (Live-Daten!).

## Verbindliche Entscheidungen (Dominique, 2026-07-23)

1. **Kapitel-Struktur beliebig tief** (n Ebenen) — Kapitel-Baum (`parent_id`) existiert, es fehlt Bedienung + tiefen-feste Coverage.
2. **Freigabe PRO KAPITEL** — das Go-Gate ist kapitelweise.
3. **Phasen soft mit EINEM harten Gate** — 7 Phasen als Checkliste (springen erlaubt); hart ist nur: ohne Kapitel-Go wird nichts angelegt.
4. **Eigenes Zielgruppen-Vokabular** (Kapitel wählt 1–n); Outlet nur optionaler Tag, KEINE primäre Ebene.
5. **Kapitel tragen Konzepte UND direkte Gerichte**: Konzept = Paket mit gebündeltem €/Gast-Preis; Einzelgericht = `recipe_ref`-Block direkt am Kapitel (€/Position). Invariante wird „0–n Konzepte + 0–n Einzel-Blöcke pro Kapitel"; ROADMAP-Entscheid „Weg B exklusiv" teilrevidiert.
6. **Vokabular-Pflicht — keine freien Klassifikations-Strings**: Jede Klassifikations-Dimension referenziert Vokabular/Klasse per FK bzw. kanonische Konstante. Freitext nur für echten INHALT (Ideen-Titel/Beschreibung, Namen, Notizen, Wording). Zielgruppen-Stempelung über Pivot `concept_target_groups` — NICHT als Text ins Legacy-Feld `concepts.target_group` (bleibt unangetastet; Prompts lesen aus `leitplanken()`).

## Ziel-Architektur

- **5er-Phasen-Maschine bleibt unangetastet** (`PhaseService::PHASEN`). Die **7 User-Phasen sind eine abgeleitete Checkliste** (nie persistiert): 1 Bedarf→kontext · 2 Struktur/3 Tiefe/4 Kapitel-Aufbau→struktur · 5 Kreativ/6 Anlegen→befuellung · 7 Preise→kalkulation · Versand→freigabe (bestehendes Coverage-Gate).
- **Ziele wandern vom Slot ans Kapitel** (kein Slot-Spiegelbaum, kein Hybrid): das Kapitel IST der n-tiefe Baum. Frame bleibt Foodbook-globaler Rahmen (Kopf-Preis p.P., frame-weite Quoten, Kickoff-Landeplatz, R2.4-Constraint-Quelle); `strukturAusGeruest` stempelt Slot-Ziele einmalig aufs Kapitel.
- **Kreativ = Skizzen-Ebene** (`dish_ideas` + `dish_idea_groups`): frei geschrieben ODER Bestand-Ref, Einzel ODER Paket-Gruppe — Status entwurf, erdet NICHTS.
- **Kapitel-Go „Anlegen"** (UI-Wording NIE „Freigabe"): Paket-Gruppen → je EIN Konzept + Slots + concept_ref-Block; Einzel-Ideen → `recipe_ref`-Block; Freitext → KI-Queue (L7/L8, provider-gated; partielle Materialisierung ist der DoD-Fall).
- **Dimensionen kaskadieren**: Kapitel(+Eltern) → Foodbook-Default → Segment. Servierform wirkt automatisch auf Preise (Konzept-Stempel → `DarreichungResolver`-Fallback; Einzel-Pfad → `blocks.presentation_id` + additive `DarreichungResolver::fuerBlock`).

## Datenmodell (5 Migrationen, additiv; ALTERs auf Bestandstabellen mit `unsignedBigInteger+index` statt FK)

**M1 `create_foodalchemist_target_groups_tables`** (Muster `2026_07_03_000006`): `foodalchemist_target_groups` (id, uuid, team_id, name, description, sort_order, is_inactive, timestamps, softDeletes, unique(team_id,name); Seeds idempotent z.B. Tagungsgast/Bankett-Gast/Mitarbeiter/VIP-Gala) + Pivots `foodbook_target_groups` (Default 1–n), `chapter_target_groups` (Kapitel 1–n), `concept_target_groups` (Stempel-Ziel, Entscheidung 6).

**M2 `add_dimension_defaults_to_foodalchemist_foodbooks`**: `foodbooks` += `default_event_type_id`, `default_serving_form_id`, `target_food_cost_pct` decimal(5,2), `food_cost_tolerance_pp` decimal(5,2) (Code-Default 5.0) + Pivot `foodbook_service_moments` (Tagesablauf 1–n).

**M3 `add_ziele_und_anlage_to_foodalchemist_foodbook_chapters`**: SOLL-Spalten `target_count` int, `price_anchor/price_min/price_max` decimal(10,2), `niveau` string (KANONISCH — Übergang zu `concept.level` IMMER via `denormNiveauFuerConcept`), `service_moment_id`, `serving_form_id`, `pricing_mode` string(12) (paket|einzel|gemischt, Model-Const, weiche Prüfung), `target_food_cost_pct`; Anlage-Spalten `released_at`, `released_by` (users.id ohne Cross-Modul-FK), `release_note`, `release_result` json. `chapters.status` (draft|sent|archived) bleibt Versand-Status.

**M4 `create_foodalchemist_dish_ideas_tables`**: `dish_idea_groups` (id, uuid, team_id, chapter_id/concept_id FK nullable XOR, name → wird concept.name, target_price_pp decimal(10,2), position, materialized_concept_id, timestamps, softDeletes) + `dish_ideas` (id, uuid, team_id, chapter_id/concept_id XOR, position, titel, beschreibung, sales_recipe_id (nur echte VK-Gerichte), ziel_form string(12) default 'einzel' (einzel|paket; paket ⇒ group_id), group_id FK nullOnDelete, status entwurf|verworfen|freigegeben, created_via, quelle_meta json, generation_status (null|queued|erstellt|fehlgeschlagen), generated_recipe_id, materialized_at, materialized_ref json ({block_id} einzel / {concept_id,concept_slot_id} paket), timestamps, softDeletes). **Invariante: Ideen erzeugen NIE Rezepte/GPs/Konzepte — erst das Go.**

**M5 `add_presentation_to_foodalchemist_foodbook_blocks`**: `foodbook_blocks` += `presentation_id`. Optional abkoppelbar: `foodalchemist_outlets` (Vokabular + `color` string(7)) + `chapters.outlet_id` — reines Tag, NICHT in leitplanken().

**Kein-Migrations-Fund (E1):** `recipe_ref` existiert in Schema + ALLEN Lesepfaden (`blockPreis` FoodbookService ~:500, Coverage `$block->dish` ~:193, Dokument/Vorschau/Picker `gerichtKandidaten` ~:714). Gesperrt NUR Schreibpfad: `BLOCK_TYPES` ~:342 + MCP-Enum. Unlock + Doktrin-Kommentar :337–341 + `docs/foodbook.md` im selben Commit.

## Services (Kern-Signaturen)

**FoodbookService:**
- `leitplanken(Team, $fb, ?Concept = null, ?Kapitel = null): array` — += `zielgruppen[]`, `event_type_id`, `serving_form_id`, `service_moment_ids[]`, `quellen[]`; Kaskade Kapitel(+Eltern) → Foodbook → Segment (Segment-Boden nur niveau/convenience). DER Auflösungs-Punkt für Vorschläge, Kickoff, Canvas, Anlage-Stempel.
- `kapitelZiele(Team, Kapitel): array` — aufgelöste SOLL-Sicht mit Vererbung + Quellen.
- `uebernehmeGericht(Team, $fbId, $chapterId, $recipeId, ?$rolle, $createdVia, ?int $conceptId = null)` — Kern aus `uebernehmeVorschlag`; `$conceptId` null = heutiges Verhalten (Wrapper bit-identisch), gesetzt = Gericht in DIESES Konzept. **Dedup kapitelweit** (Konzept-Slots ∪ recipe_ref-Blöcke), quer nur WEICH melden.
- `kapitelFreigeben(Team, $kapitelId, ?$note, ?$userId): array` — transaktional, **idempotent**. Routing: Paket-Gruppe → Konzept (name/target_price_per_person aus Gruppe; Stempel serving_form_id/event_type_id FK, Einsatzmomente-Pivot-Sync, Zielgruppen via `concept_target_groups`-Pivot; `created_via='kapitel_freigabe'`) + Slots + concept_ref-Block; einzel+Ref → recipe_ref-Block (type+sales_recipe_id+opt. wording+presentation_id); Freitext → `generation_status='queued'` (GenerateRecipeJob-Muster → L7/L8; ohne Provider typisierte Exception → „wartet auf KI", **Go scheitert nicht**). Setzt released_* + Activity-Log. Return: {kapitel_id, konzepte[], bloecke_einzel, materialisiert, queued, uebersprungen, protokoll[]}.
- `wareneinsatzAmpel(Team, $fb, Kapitel, ?$pax): array` — IST aus `kapitelAggregat().food_cost_percent`; SOLL chapters→foodbooks→`zielWareneinsatzPct()` (30); grün ≤Ziel / gelb ≤Ziel+Toleranz / rot / unbekannt. Pauschal-Blöcke ohne EK → „partiell"-Hinweis.
- `blockPreis()`: recipe_ref respektiert `price_basis` (person|pauschal) + `fuerBlock(...)?->sales_net ?? dish->sales_net`.
- `strukturAusGeruest`: stempelt Slot-Ziele einmalig aufs neue Kapitel (protokoll += ziele_uebernommen). `KAPITEL_FELDER` erweitern.

**DarreichungResolver** (additiv, Muster `fuerPaketGericht`): `fuerBlock(Block, ?$servingFormId): ?Darreichung` — 1. block.presentation_id → 2. Gericht-Darreichung zur Kapitel-/Foodbook-Servierform → 3. standardFuer(). **Alles null ⇒ bit-identisch heute** (keine sichtbaren Preisänderungen im Bestand).

**CoverageService:** Blocker-Fix Tiefe — `istFoodbook` iteriert flach + zählt nur direkte Blöcke, `vk_pp` ist rekursiv → Ziel-/Slot-Scope = „Kapitel + alle Nachfahren" (`descendantKapitelIds` existiert ~:315); Model-Kommentar `FoodAlchemistFoodbook.php:32` fixen. Neu `pruefeKapitel(Kapitel,$ist)` (Befund-Shape identisch, chapter_id additiv) + WE-Ampel-Sektion. **Vorrangregel:** Kapitel-Ziel gewinnt → `pruefeSlot` skippt Menge/Preis für Slots, deren Kapitel eigene Ziele hat.

**PlanningFrameService:** `replaceStructure`-**Rerun-Guard** — Kickoff-Rerun löscht heute Slots samt chapter_id → Duplikat-Kapitel; Label-Match erhält chapter_id, sonst Warnung.

**Neu `IdeenService`:** liste/add/update/setStatus (nur entwurf|verworfen), addGruppe/updateGruppe, `kiDivergenz(Team,$chapterId,$anzahl)` → Entwurfszeilen `created_via='ai_gateway'` via Prompt-Key **`foodbook.kapitel_ideen`** (Kontext = leitplanken() + promptKontext() + KnowledgeContextService). Erledigt Spec-08-P6-Routings (`foodbook.plan`/`concept.plan`).

**Neu `LeitstelleService`:** `checkliste(Team,$fb)` (7 Schritte abgeleitet, Sprungziele tab+anker), `kapitelStand(Kapitel)`, `speisenBaum($fb)` (Kapitel → [Paket→Gerichte | Gericht direkt | Ideen], Badges). Panels als **Nested-Livewire** (Muster `KundeDnaPanel`).

## UX

1. **Checkliste + Stepper:** Stepper von Briefing-Karte (index.blade.php ~:131) auf Tab-Leisten-Ebene; 7-Chip-Checkliste (offen/teil/erledigt, klickbar → Tab+Anker via Alpine-Event-Bus; `freigabe` NIE über Checkliste). Partial `leitstelle-checkliste.blade.php`.
2. **Rechte Rail — KONTEXTSENSITIV** (User-Vorgabe per Screenshot): **Kopf gewählt** = Umschalter Fortschritt (Checkliste + Kapitel-Matrix, [Go]-Shortcut, Komplexitäts-Hinweis >8 Kapitel/>12 Positionen) · Speisen (heterogener Baum, Badges [Paket]€/Gast·[Einzel]€/Pos·Entwurf/angelegt/bepreist/KI-Queue) · Kalkulation (Portfolio + WE-Ampel je Kapitel); Auto-Default je Tab nur ohne manuellen Pin (localStorage). **Kapitel gewählt** = Kapitel-Planung: Zielgruppen-Chips, Niveau-Override, Einsatzmoment, Servierform, Mengenziel, Preisziel, WE-Ziel (M3-Spalten) + Kapitel-Coverage (Scope-Param) + Kapitel-Kalkulation + Ideen-Stand mit [Go]. Hauptfläche bleibt Kapitel-Kopf + Blöcke; Unterkapitel im linken Baum.
3. **Kreativ-Tab = Skizzenfläche** (per-Slot-Vorschläge im Planung-Tab ENTFERNT/weitergeleitet — „Übernehmen" erzeugt künftig Entwurf-Idee, KEIN Konzept): [+ Idee] frei · [aus Bestand] (Reuse `slotVorschlaege`) · [KI frei] (provider-gated, disabled+Tooltip). Paket-Bündelung per Mehrfachauswahl → „zu Paket bündeln" (Muster `markiere()`/`wahlGruppeBilden()`); „aus Paket lösen"/„Paket auflösen". Kapitel-Fill fragt [als Einzelpositionen]/[als ein Paket] (Vorbelegung aus slot_type).
4. **Anlage-Modal** (Wording „Kapitel anlegen"): Zusammenfassung beider Wege, ☑-Liste je Paket/Idee, Ziel-/Niveau-Anzeige. Undo „Anlage zurückziehen" solange draft + created_via='kapitel_freigabe' + kein Snapshot/Versand.
5. **Preise-Tab (neu):** Kapitel-Baum-Tabelle, Art Paket €/G vs. Einzel €/Pos · EK · VK · WE% · Ampel · VK-Editor-Link; nach Foodbook-Freigabe R2.5-Snapshot-Badges.
6. **Vorschau/Dokument:** depth-basierte Überschriften (h3/h4/h5); Entwurf-Ideen NIE in Kundensicht; €/Gast vs. €/Position konsistent. `dokumentDaten` rendert Baum+recipe_ref schon — Regression absichern.
7. **Bestands-Foodbooks:** Slot-Konzepte bleiben gültig, Anzeige als [Paket] („Slot-Übernahme"); Matrix „angelegt — Ideen-Phase übersprungen". Keine Datenmigration.

## Prinzip: KI-/MCP-Führung durch die Planungsebenen (verbindlich)

1. **Ein Kontext-Vertrag:** `leitplanken()` + `kapitelZiele()` — JEDER KI-Aufruf im Foodbook-Umfeld bekommt den aufgelösten Block, immer mit Kapitelbezug wenn ein Kapitel im Spiel ist; dazu DNA-Kette (`cascadeKontext`).
2. **MCP-Lesefläche:** neues `foodalchemist.leitstelle.GET(foodbook_id, ?chapter_id)` — Checkliste, Kapitel-Matrix, pro Kapitel aufgelöster Planungs-Kontext (Ziele, Zielgruppen, Dimensionen, Coverage, WE-Ampel, Ideen-Stand). Eine KI kann den User durch die Phasen führen.
3. **MCP-Schreibfläche je Phase:** P1 `foodbooks.PUT`-Defaults + `zielgruppen.*` · P2/3 `planning.PUT` + `foodbook_kapitel.POST/PUT` · P4 `foodbook_kapitel.PUT` (Ziele) · P5 `kapitel_ideen.*` (nur Entwürfe) · P6 human-only (nur `kapitel_freigabe.GET`) · P7 `coverage.GET`-WE + VK-Tools. Jedes neue Feld im selben Commit im MCP-Schema.

## Prinzip: Erst kreativ, dann erden — mit Anpassungs-Schleife

1. **Divergenz produkt-blind:** Kreativ-Phase erlaubt freie Ideen OHNE Bestandsprüfung; KI-Divergenz darf erfinden (Anker-Graph als Inspiration); Bestands-Vorschläge daneben, ersticken die Divergenz nicht.
2. **Erdung mit Anpassung statt Hard-Fail (E7):** Nicht erdbare Freitext-Idee → **Anpassungs-Schleife**: KI schlägt angepasste Version aus verfügbaren Produkten vor (kandidatenPool/#507/LA-First; Aroma-Treue via Anker-Graph), DRAFT mit sichtbarem Delta; alternativ LA-First-Mint (tentative GP + Beschaffungs-Hinweis). Beide Wege im Anlage-Protokoll.
3. **Kein stiller Kreativitätsverlust:** Original bleibt in `dish_ideas`/`quelle_meta`; Sortimentslücken-Signal als Follow-up (Signale-Cockpit).

## MCP-Lockstep

- `foodbook_blocks.POST`: +recipe_ref-Enum, +sales_recipe_id-Validierung, +presentation; **price_basis-Enum angleichen** (Tool `pro_person|pro_stueck|pauschal` ≠ Code `person|pauschal|staffel`).
- `foodbooks.POST/GET`: Dimension-Defaults + Fortschritts-/Anlage-Stand.
- `foodbook_kapitel.POST` + **NEU PUT**: Ziele + Zielgruppen + pricing_mode.
- **NEU** `leitstelle.GET`, `zielgruppen.GET(/POST)`, `kapitel_ideen.GET/POST/PUT`, `kapitel_freigabe.GET`. **Kapitel-Go OHNE MCP-Trigger.**
- `coverage.GET`: Kapitel-Befunde + WE-Sektion. `planning.*`/`phase.PUT` unverändert (Slots bleiben flach).

## Doku-Abgleich

Spec 08 Banner (Foodbook-Hälfte superseded; Go PRO KAPITEL; gültig Concept-standalone + P6) · Spec 03 (L7/L8 zweiter Konsument „Kapitel-Anlage (19)"; L3-v1 revidieren) · Spec 00 (Phase-4 splitten: E1–E5 deterministisch VOR L7/L8) · Spec 12 („Solver am Kapitel-Go/Preise-Phase") · ROADMAP Update-Absatz (Weg B nicht mehr exklusiv, ~237/~257) + Code-Doku FoodbookService:337–341 im E1-Commit · `_Spec_Status_Matrix` +19.

## Verifikation

- **Pest** (aus `sandbox-food-alchemist`): je Etappe Filter `Foodbook|FoodbookLeitstelle|Coverage|Concept` + neue Suiten (KapitelZiele, Skizzen, KapitelAnlage, ZielgruppenVokabular); MCP je mit Cross-Team-Negativtest; Etappen-Ende breite Suite ohne Regression.
- **MySQL-Smoke:** DarreichungResolver-Zwei-Darreichungen, Backfill-Idempotenz, Kaskaden-Deletes, JSON-Spalten.
- **Browser-Klickstrecke** (Sandbox, Fake-Provider): Kickoff → Struktur → Unterkapitel → Ziele → Einzel-Block + Paket-Fill → Skizzen → Anlage-Go → Panels → Vorschau → Konsole leer.
- **fb=1-Protokoll:** Vorher-Dump 8 Tabellen + SELECT-Protokoll; nach Migrationen Byte-Vergleich (alles opt-in); Feature-Durchlauf an NEUEM Test-Foodbook; Mixed-Test an fb=1 nur bewusst + mit Rücksetzung.

## Top-Risiken

1. Coverage-Tiefe (istFoodbook flach vs. kapitelAggregat rekursiv) — Fix E2 mit Eltern-Ziel/Enkel-Gericht-Test. 2. Doppel-Befunde Slot↔Kapitel — Vorrangregel konsistent. 3. Zwei Gate-Begriffe — „Anlegen" (Kapitel) vs. „Freigabe" (Foodbook); 7 Phasen NIE in PHASEN. 4. uebernehmeVorschlag-Wrapper bit-identisch. 5. fuerBlock nur bei gesetzter presentation/Servierform. 6. Verhaltensbruch Kreativ → „bereits angelegt"-Zustand. 7. Niveau-Vokabular-Falle (denormNiveauFuerConcept). 8. Keine neuen frei-slugs (Sektor-Kanonisierung = Follow-up).

## Nicht-Ziele

Kein platform-core/crm; KI-Neu-Erstellung = L7/L8 (nur Verdrahtung/Queue); R2.4-Solver = Follow-up; „+Topping", Farbcode-Ampel-UI, Slots-Hierarchisierung: raus.

---

## Fortschritts-Checkliste (Übergabepunkt der Routine — pro Lauf EIN Teilschritt, strikt der Reihe nach; nach Abschluss ☐→✅ + Datum + Commit-Hash)

### E0 — Setup + Doku-Abgleich
- [x] E0.1 Spec 19 angelegt (2026-07-23, diese Datei)
- [x] E0.2 Doku-Abgleich: `_Spec_Status_Matrix` +Zeile 19 · Spec-08-Banner · Spec 03 (L7/L8-Konsument, L3-v1) · Spec 00 (Phase-4-Split) · Spec 12 (Solver-Referenz) — ✅ 2026-07-23

### E1 — recipe_ref-Schreibpfad (S)
- [x] E1.1 `BLOCK_TYPES` + recipe_ref, Validierung (sales_recipe_id = echtes VK-Gericht, verkauf()-Scope, keine Slot-Variante — Muster `gerichtKandidaten`), Doktrin-Kommentar :337–341 + `docs/foodbook.md` + ROADMAP-Nachtrag — ✅ 2026-07-23
- [x] E1.2 `blockPreis`: price_basis (person|pauschal) für recipe_ref respektieren; Pest Mischkapitel (Konzept €/Gast + Einzel pauschal → Leiste korrekt) — ✅ 2026-07-23
- [x] E1.3 UI-Picker im Block-Editor („+ Gericht einfügen" neben „+ Concept einfügen", nutzt `gerichtKandidaten`) — ✅ 2026-07-23
- [x] E1.4 MCP `foodbook_blocks.POST`: recipe_ref-Enum + sales_recipe_id + price_basis-Enum-Angleich (`pro_stueck`-Falle); Pest MCP-Roundtrip + Tenancy-Negativ — ✅ 2026-07-24
- [x] E1.5 Dedup kapitelweit in `uebernehmeVorschlag` (Konzept-Slots ∪ recipe_ref-Blöcke); Pest — ✅ 2026-07-24

### E2 — Kapitel-Baum + Coverage-Tiefe (M)
- [x] E2.1 Unterkapitel-UI (anlegen unter Kapitel, verschieben; Service `addKapitel(parentId)`/`moveKapitel` existieren) — ✅ 2026-07-24
- [x] E2.2 Coverage Nachfahren-Scope: `istFoodbook`-Rollup (Kinder-Aggregation) + `slotScope` = Kapitel+Nachfahren; Model-Kommentar `FoodAlchemistFoodbook.php:32` fixen; Pest Eltern-Ziel/Enkel-Gericht → erfüllt — ✅ 2026-07-24
- [x] E2.3 `replaceStructure`-Rerun-Guard (Label-Match erhält chapter_id); Pest „Kickoff-Rerun zerstört keine Kapitel-Links" — ✅ 2026-07-24

### E3 — Zielgruppen + Foodbook-Defaults (M, parallel zu E1/E2 möglich)
- [x] E3.1 M1: target_groups + 3 Pivots (inkl. concept_target_groups) + Seeds; Models — ✅ 2026-07-24
- [x] E3.2 M2: Foodbook-Default-Spalten + foodbook_service_moments-Pivot; Models/FELDER — ✅ 2026-07-24
- [x] E3.3 Settings-CRUD (`ConcepterDimensionen::VOKABULARE` + zielgruppen) + Briefing-Tab Bedarf-Sektion (Defaults + Einsatzmomente-Pills) — ✅ 2026-07-24
- [x] E3.4 `leitplanken()`-Erweiterung (Kapitel-Param + neue Dimension-Keys + quellen); alle Aufrufer prüfen; Pest Kaskade — ✅ 2026-07-24
- [x] E3.5 MCP `zielgruppen.GET/POST` + `foodbooks.POST/GET`-Defaults; Pest + Tenancy — ✅ 2026-07-24
- [x] E3.6 (optional, abkoppelbar) Outlets-Vokabular + chapters.outlet_id als Tag — ✅ 2026-07-24

### E4 — Kapitel-Ziele + WE-Ampel (L)
- [x] E4.1 M3: Ziel- + Anlage-Spalten; `KAPITEL_FELDER`; `strukturAusGeruest`-Stempel (ziele_uebernommen) — ✅ 2026-07-24
- [x] E4.2 `kapitelZiele()` (Vererbung Kapitel→Eltern→Slot→Foodbook, quellen) — ✅ 2026-07-24
- [x] E4.3 `pruefeKapitel` + Vorrangregel (Kapitel-Ziel gewinnt, pruefeSlot skippt) + Kinder-Rollup; Pest inkl. keine Doppel-Befunde — ✅ 2026-07-24
- [x] E4.4 `wareneinsatzAmpel` (Ziel-Kaskade + Toleranz + partiell-Hinweis); Pest — ✅ 2026-07-24
- [x] E4.5 Backfill-Command Slots→Kapitel-Ziele (--dry-run/--apply, idempotent); MySQL-Smoke — ✅ 2026-07-24
- [x] E4.6 MCP `foodbook_kapitel.PUT` (Ziele+Zielgruppen+pricing_mode) + `coverage.GET`-Erweiterung; Pest — ✅ 2026-07-24

### E5 — Checkliste + Leitstelle-Rail (M)
- [x] E5.1 `LeitstelleService` (checkliste/kapitelStand/speisenBaum); Pest — ✅ 2026-07-24
- [x] E5.2 Checkliste-UI + Stepper auf Tab-Leisten-Ebene + Sprung-Event-Bus — ✅ 2026-07-24
- [x] E5.3 Rail-Panels als Nested-Livewire: Kopf = Fortschritt/Speisen/Kalkulation (Umschalter+Pin); Kapitel = Kapitel-Planung (Ziele-Editing) + Coverage + Kalkulation + Ideen-Stand — ✅ 2026-07-24
- [x] E5.4 MCP `leitstelle.GET`; Pest + Tenancy — ✅ 2026-07-24

### E6 — Kreativ-Skizzen (L)
- [x] E6.1 M4: dish_ideas + dish_idea_groups; Models — ✅ 2026-07-24
- [x] E6.2 `IdeenService` (CRUD, Gruppen, XOR-Guard, Bestand-Übernahme-als-Idee); Pest — ✅ 2026-07-24
- [x] E6.3 Kreativ-Tab-Skizzenfläche (3 Quellen, Paket-Bündelung per Mehrfachauswahl, verwerfen/reaktivieren) + Planung-Tab-Rückbau der Vorschlags-UI + „bereits angelegt"-Zustand für Bestands-Foodbooks — ✅ 2026-07-24
- [x] E6.4 Prompt `foodbook.kapitel_ideen` + Routings foodbook.plan/concept.plan + `kiDivergenz` (gegen Fake-Provider verdrahtet) — ✅ 2026-07-24
- [x] E6.5 MCP `kapitel_ideen.GET/POST/PUT` (ziel_form, paket_gruppe, paket_zielpreis_pp; GET gruppiert); Pest + Tenancy — ✅ 2026-07-24

### E7 — Kapitel-Go „Anlegen" (L; neu-Zweig L7/L8-gated)
- [x] E7.1 M5: blocks.presentation_id + `DarreichungResolver::fuerBlock` (null ⇒ bit-identisch); Pest + MySQL-Smoke Zwei-Darreichungen — ✅ 2026-07-24
- [x] E7.2 `uebernehmeGericht`-Refactor (+$conceptId; Wrapper bit-identisch); Regression-Pest Leitstellen-Kaskade — ✅ 2026-07-24
- [x] E7.3 `kapitelFreigeben` (Routing paket→Konzept/einzel→recipe_ref, Stempel via Pivots, idempotent, released_*, Protokoll); Pest — ✅ 2026-07-24
- [x] E7.4 Freitext-Queue (GenerateRecipeJob-Muster → L7/L8) + Anpassungs-Schleife-Verdrahtung (Graceful ohne Provider, „wartet auf KI"); Pest Graceful — ✅ 2026-07-24
- [x] E7.5 Anlage-Modal + Undo „Anlage zurückziehen"; Browser-Klick — ✅ 2026-07-24 (e0d0a3d)
- [ ] E7.6 MCP `kapitel_freigabe.GET`; Pest

### E8 — Preise + Abschluss (M)
- [ ] E8.1 Preise-Tab (Kapitel-Baum-Tabelle, Paket/Einzel, WE-Ampel, VK-Links)
- [ ] E8.2 Rail-Kalkulation-Ampel + R2.5-Snapshot-Badges
- [ ] E8.3 Vorschau/PDF depth-Überschriften + €/Gast-€/Pos-Konsistenz + Regression
- [ ] E8.4 Abschluss: breite Pest-Suite, Browser-Klickstrecke komplett, fb=1-Protokoll, ROADMAP-Abschluss-Update + Office-Dev-Issue Done, Memory-Update

> **Lauf-Protokoll** (jeder Routine-Lauf hängt eine Zeile an): `| Datum | Teilschritt | Ergebnis | Commit | Notizen |`

| Datum | Teilschritt | Ergebnis | Commit | Notizen |
|---|---|---|---|---|
| 2026-07-23 | E0.1 | ✅ Spec angelegt | — | Session „Foodbook-Leitstelle A–Z"-Plan |
| 2026-07-23 | E0.2 | ✅ Doku-Abgleich | s. Git-Log „E0.2" | 5 Touchpoints: Matrix +Z19 · Spec-08-Supersede-Banner · Spec 03 L7/L8-Zweitkonsument + L3-v1-Revision · Spec 00 Phase-4-Split (4a determ. vor L7/L8 / 4b provider-gated) · Spec 12 Solver am Kapitel-Go. Reine Doku, kein Code. |
| 2026-07-23 | E1.1 | ✅ recipe_ref-Schreibpfad frei | (s. Commit) | `recipe_ref` in `BLOCK_TYPES`; `pruefeRecipeRef()` (verkauf()-Scope, visibleToTeam, keine Slot-Variante) in addBlock+updateBlock; Doktrin-Kommentar :337ff. auf Entscheidung 5 umgeschrieben; docs/foodbook.md + ROADMAP nachgezogen. Stale-Test invertiert (BLOCK_TYPES→toContain) + neuer Guard-Test. Pest Foodbook 60/60, Coverage\|Concept 122/122 grün. MCP-Enum-Angleich ist E1.4 (separat). |
| 2026-07-23 | E1.2 | ✅ recipe_ref price_basis | (s. Commit) | `blockPreis()`: recipe_ref respektiert `price_basis` — `pauschal` → VK in flachen Anteil (kein ×Pax), EK ungezählt (konsistent zu header_frei_preis/pauschal, WE-Ampel „partiell" folgt E4.4); `person`/Default bit-identisch (Per-Person). Docblock aktualisiert. 2 Pest-Tests: Mischkapitel (Konzept 4,50 €/Gast + Einzel 2,50×3 pauschal → gesamt_vk 457,50 @100Pax) + Einzel per-Person bleibt ×Pax. Pest Foodbook 62/62, Coverage\|Concept 122/122. DarreichungResolver::fuerBlock-Anteil bleibt E7.1. |
| 2026-07-24 | E1.4 | ✅ MCP recipe_ref + price_basis-Angleich | (s. Commit) | `FoodbookBlocksPostTool`: type-Enum +`recipe_ref`, neues `sales_recipe_id` (Schema + $daten + Tenancy-Check via `verkauf()`+`variant_source_recipe_id`-Guard, spiegelt `pruefeRecipeRef`, typisiertes NOT_FOUND vor Write). price_basis-Angleich: `PRICE_BASIS_MAP` (pro_person→person · **pro_stueck→pauschal** · pauschal→pauschal + kanonische Pass-throughs) → Model-Const `FoodAlchemistFoodbookBlock::PRICE_BASES` (Vokabular-Pflicht). Unbekannte Basis = VALIDATION_ERROR. Beschreibung/Description auf Duality Paket/Einzel umgeschrieben. 2 Pest (Roundtrip: pro_stueck→pauschal DB-verifiziert + pro_person→person + recipe_ref-ohne-id=VALIDATION_ERROR · Tenancy: Kind-A-Gericht als Root=NOT_FOUND). Pest McpTools 6/6, Foodbook\|Coverage\|Concept 167/167. |
| 2026-07-24 | E2.1 | ✅ Unterkapitel-UI (Ein-/Ausrücken) | (s. Commit) | Anlegen-unter-Kapitel (`kapitelNeu($parentId)`) + Reorder ▲▼ waren schon da; Lücke war Parent-Wechsel. Neu in `Foodbooks\Index`: `kapitelEinruecken` (neuer Parent = vorheriges Geschwister, erstes Geschwister nicht einrückbar) + `kapitelAusruecken` (neuer Parent = Großelternteil/Top, Top nicht ausrückbar), gemeinsamer `kapitelUnter()` (nutzt Service `moveKapitel` inkl. Zyklus-Schutz + hängt via `reorderKapitel` ans Ende der neuen Geschwister, da moveKapitel `position` nicht anfasst). Blade: ⬅/➡-Buttons in der Tree-Zeile (⬅ disabled bei parent_id===null). Kein Service-/MCP-Change (Slots bleiben flach, MCP unverändert). 1 Livewire-Pest (Ein-/Ausrücken + beide Rand-Guards). Pest Foodbook 67/67, Coverage\|Concept\|Leitstelle 126/126. |
| 2026-07-24 | E1.5 | ✅ Dedup kapitelweit | (s. Commit) | `uebernehmeVorschlag`: neue `gerichtImKapitel()`-Helper prüft Union aus recipe_ref-Blöcken (`sales_recipe_id`) + Slots ALLER concept_ref-Konzepte des Kapitels. Prüfung läuft VOR jeder Anlage → Treffer legt weder leeres Konzept noch Slot an (concept_id = führendes Kapitel-Konzept oder 0). Alter Check sah nur EIN Konzept. Quer-Kapitel-Meldung bleibt WEICH → E7.2 (`uebernehmeGericht`). 2 Pest (Doppel-Übernahme = 1 Slot + gleiches Konzept · recipe_ref-Block dedupt kapitelweit, kein Konzept/Slot). Pest FoodbookLeitstelle 5/5, Foodbook\|Coverage\|Concept 169/169. **E1 komplett** → breite Suite. |
| 2026-07-23 | E1.3 | ✅ Gericht-Picker im Block-Editor | (s. Commit) | UI-only, kein neuer Service/MCP (MCP = E1.4). Livewire `Foodbooks\Index`: `$gerichtSuche` + `gerichtHinzu()` (spiegelt `conceptHinzu`, addBlock type=recipe_ref → pruefeRecipeRef validiert) + render-Data `gerichtKandidaten` (lazy: nur bei Suche+Kapitel). Blade: Button „+ Gericht einfügen" neben „+ Concept einfügen"; Modal `fb-gericht` (Freitext-Suche, kein Kategorie-Tree); recipe_ref-Switch-Case in Block-Zeile ([Gericht]-Pill + €/Pos·pauschal + Wording); Inline-Editor recipe_ref: Wording-Override + price_basis-Select (person/pauschal, E1.2). Empty-State-Hinweis ergänzt. `php -l` ok, Blade compileString ok, Pest Foodbook 62/62 + Coverage\|Concept 122/122 grün. |
| 2026-07-24 | E2.3 | ✅ replaceStructure-Rerun-Guard | (s. Commit) | Kickoff-Rerun (`replaceStructure` mit `slots`) löschte Slots samt `chapter_id` → 2. `strukturAusGeruest` mintete Duplikat-Kapitel. Fix in `PlanningFrameService::replaceStructure`: vor dem Löschen `label→chapter_id`-Map sichern (normalisiert via neuer `labelKey()`, case/trim-insensitiv), beim Neu-Anlegen chapter_id per Label-Match wieder setzen (nur wenn Payload keine eigene mitbringt). Nicht wieder zuordenbare Links → transiente `letzteStrukturWarnungen` (public, pro Lauf reset) + `Log::warning`. Kein Signatur-/MCP-Change (Slots bleiben flach, `planning.*` unverändert). 2 Pest: Rerun (case-insensitiv „HAUPTGANG") → 2. strukturAusGeruest angelegt=0/übersprungen=2, identische Kapitel-IDs · entfallenes Label „Käse-Station" → 1 Warnung. Pest PlanningFrame 13/13, Foodbook\|FoodbookLeitstelle\|Coverage\|Concept\|McpPlanning\|FoodbookStruktur 175/175. **E2 komplett** → breite Suite. |
| 2026-07-24 | E3.1 | ✅ M1 Zielgruppen-Vokabular + 3 Pivots + Models | (s. Commit) | Migration `2026_07_24_000001_create_foodalchemist_target_groups_tables` (Muster `…000006_concepter_dimensionen`): `foodalchemist_target_groups` (uuid, team_id-index, name, description, sort_order, is_inactive, timestamps, softDeletes, unique(team_id,name)) + 3 Pivots. **Namens-Deviation von Spec-Shorthand:** Pivots präfigiert `foodalchemist_foodbook_/chapter_/concept_target_groups` (Modul-Konvention + Kollisions-Schutz in geteilter Plattform-DB; Downstream nutzt Eloquent-Relations, nicht Roh-Namen). FK constrained+cascadeOnDelete (nur Intra-Modul-Refs). Seeds pro Team idempotent (Tagungsgast/Bankett-Gast/Mitarbeiter/VIP-Gala). Neues Model `FoodAlchemistTargetGroup` (foodbooks/chapters/concepts-BtM) + inverse `targetGroups()`-BtM auf Foodbook/Kapitel/Concept (+BelongsToMany-Import je Model). Idempotenz via hasTable/exists-Guards. Migration DBngin-MySQL gefahren (4 Tabellen, 4 Seeds, alle Relations = BelongsToMany). `php -l` 5/5, Pest Foodbook\|FoodbookLeitstelle\|Coverage\|Concept 171/171. Kein MCP-Change (Lockstep: `zielgruppen.GET/POST` = E3.5). Backup `PRE_E3.1_target_groups_*.sql`. |
| 2026-07-24 | E3.2 | ✅ M2 Foodbook-Dimension-Defaults + Einsatzmoment-Pivot | (s. Commit) | Migration `2026_07_24_000002_add_dimension_defaults_to_foodalchemist_foodbooks`: `foodalchemist_foodbooks` += `default_event_type_id`/`default_serving_form_id` (`unsignedBigInteger`+index statt FK, Spec-Regel für ALTERs auf Bestandstabellen) + `target_food_cost_pct`/`food_cost_tolerance_pp` decimal(5,2) nullable (Code-Default 5.0 folgt E4.4-`wareneinsatzAmpel`, kein DB-Default). Neuer Pivot `foodalchemist_foodbook_service_moments` (constrained FK, analog M1; Präfix-Deviation vom Spec-Shorthand). Model `FoodAlchemistFoodbook`: `defaultEventType()`/`defaultServingForm()`-BtT + `serviceMoments()`-BtM + Casts `decimal:2` für beide %-Spalten. `FoodbookService::FELDER` += 4 Spalten (updatebar). Migration DBngin-MySQL gefahren (4 Spalten + 1 Pivot verifiziert). `php -l` 4/4. 2 Pest (FELDER-Update+Cast+BtT-Relations · 1–n Einsatzmoment-Pivot). Pest Foodbook\|FoodbookLeitstelle\|Coverage\|Concept 173/173. Kein MCP-Change (Lockstep: `foodbooks.POST/GET`-Defaults = E3.5). Backup `PRE_E3.2_dimension_defaults_*.sql`. |
| 2026-07-24 | E3.3 | ✅ Zielgruppen-CRUD + Bedarf-Sektion | (s. Commit) | **Settings-CRUD:** `zielgruppen` in `ConcepterDimensionen::VOKABULARE` (Tabelle `foodalchemist_target_groups`, Name-Spalte `name`, generisches Blade rendert automatisch) + `referenzen()`-Case (Union aller 3 M1-Pivots Foodbook∪Kapitel∪Konzept → Lösch-Schutz). Intro-Text ergänzt. **Bedarf-Sektion (Briefing-Tab):** neue Card zwischen Stammdaten + Leitidee-Canvas — 4 Defaults (Eventtyp/Servierform per FK-Select · Ziel-Wareneinsatz % · Toleranz ±pp) via neuem `bedarfSetzen($feld,$wert)` (Whitelist, leer→null, int/float-Coercion) + Einsatzmoment-Pills (`toggleFbEinsatzmoment`) + Zielgruppen-Default-Pills (`toggleFbZielgruppe`). Service: `FoodbookService::toggleEinsatzmoment`/`toggleZielgruppe` (BtM `toggle()` + guard) + detail() eager-load `serviceMoments/targetGroups/defaultEventType/defaultServingForm`. **Kein MCP** (Lockstep: `zielgruppen.GET/POST`+`foodbooks`-Defaults = E3.5). 3 Pest (Zielgruppen-Toggle an/aus · Einsatzmoment-Pivot · bedarfSetzen FK+%+null). php -l 3/3, Blade compileString 2/2, Pest FoodbookLeitstelle 8/8, Foodbook\|Coverage\|Concept\|Dimensionen 177/177. |
| 2026-07-24 | E3.4 | ✅ leitplanken()-Dimensions-Kaskade | 38bc69a | `leitplanken()` += 4. Param `?FoodAlchemistFoodbookKapitel $kapitel` + 5 neue Keys (`zielgruppen[]`, `event_type_id`, `serving_form_id`, `service_moment_ids[]`, `quellen{}`). Neue private `zielgruppenKaskade()`: Kapitel → Eltern → … (Zyklus-Guard `$besucht`) → Foodbook-Default, erstes gestempeltes gewinnt; Quelle in `quellen`. Eventtyp/Servierform/Einsatzmomente vorerst nur Foodbook-Default (Kapitel-Overrides = M3/E4.1 — Docblock markiert die Andockstelle). Niveau/Convenience/Kundentyp bit-identisch. **Aufrufer:** alle 5 Callsites (Index.php 130/192/259/919 + interne :282) nutzen ≤2 Positional-Args → additiv abwärtskompatibel, keine Änderung nötig; neue Keys additiv (Blade liest keine Schlüssel-Zählung). Kein MCP-Change (Lockstep: `zielgruppen.GET/POST`+`foodbooks`-Defaults = E3.5). Casts via geladene Relation-Property (kein pluck-SQL-Ambiguität). 5 Pest (leere Keys · Foodbook-Default · Kapitel schlägt Foodbook · Kind erbt Eltern-Kapitel · Eventtyp/Servierform/Einsatzmoment + quellen). php -l 2/2, Pest Leitplanken 10/10, Foodbook\|Coverage\|Concept\|FoodbookLeitstelle\|DnaCascade\|Dimension 182/182. |
| 2026-07-24 | E3.5 | ✅ MCP zielgruppen.GET/POST + foodbooks-Defaults | (s. Commit) | 2 neue Tools `foodalchemist.zielgruppen.GET` (Vokabular-Liste, read-only, is_owned-Flag) + `foodalchemist.zielgruppen.POST` (team-eigen, Dedup case-insensitiv gegen eigenes Team, leer/dup=VALIDATION_ERROR) → Service `FoodbookService::zielgruppenListe`/`zielgruppeAnlegen`. `foodbooks.POST` += Bedarf-Defaults (default_event_type_id/default_serving_form_id FK-Vokabular-Guard VOR create() → kein verwaistes FB · target_food_cost_pct/food_cost_tolerance_pp via FELDER-Update · zielgruppen[]/einsatzmomente[] via toggle*, All-visible-Count-Guard). `foodbook.GET` += `defaults{}`-Block (Eventtyp/Servierform/WE-Ziel/Toleranz/Zielgruppen/Einsatzmomente) aus schon eager-geladenen Relations. Beide neuen Tools im ServiceProvider registriert. 3 Pest (Roundtrip+Dedup+Leername · Defaults-POST→GET-Spiegel · Tenancy: fremd-Team-Zielgruppe=NOT_FOUND + FB-Count unverändert + nicht in GET-Liste). php -l 7/7, Pest McpTools 9/9, Foodbook\|Coverage\|Concept\|Leitplanken\|DimensionDefaults 183/183. E3.6 (Outlets) bleibt optional/abkoppelbar offen. |
| 2026-07-24 | E3.6 | ✅ Outlets-Vokabular + chapters.outlet_id-Tag | (s. Commit) | Optionale M5-Hälfte (abkoppelbar). Migration `2026_07_24_000003_create_foodalchemist_outlets_tables`: neue `foodalchemist_outlets` (uuid, team_id-index, name, `color` string(7) nullable, sort_order, is_inactive, timestamps, softDeletes, unique(team_id,name)) + ALTER `foodalchemist_foodbook_chapters` += `outlet_id` `unsignedBigInteger`+index nullable (Spec-Regel für ALTERs auf Bestandstabellen — KEINE harte FK; loser Tag). **KEINE Seeds** (Outlets rein team-definiert, anders als M1-Zielgruppen). Model `FoodAlchemistOutlet` (chapters-HasMany) + Kapitel-`outlet()`-BtT. Settings-CRUD: `outlets` in `ConcepterDimensionen::VOKABULARE` (generisches Blade rendert automatisch, name-basiert; color über DB-Default null → Farb-Editor = spätere UI-Stufe) + `referenzen()`-Case (Zähler `foodbook_chapters.outlet_id`, Lösch-Schutz) + Intro-Text. **NICHT in leitplanken()/Kaskade** (Entscheidung 4 — reiner Tag). Die zweite M5-Hälfte (`blocks.presentation_id`+`DarreichungResolver::fuerBlock`) bleibt bewusst E7.1. **Kein MCP-Change** (Tag, keine Planungs-Dimension → nicht in `leitstelle.GET`/`foodbook_kapitel.PUT`). Migration DBngin-MySQL gefahren (outlets 10 Spalten + chapters.outlet_id verifiziert). Backup `PRE_E3.6_outlets_*.sql`. `php -l` 4/4, Blade compileString ok. 2 Pest (Kapitel-Tag + Relation beidseitig · Settings-CRUD anlegen + Lösch-Schutz bei Nutzung). Pest Foodbook\|Coverage\|Concept\|Dimension\|Leitstelle\|Leitplanken\|Outlet 186/186. **Breite Suite 997/1002** — die EINE rote (`KnowledgeBindToolTest::UNBIND … idempotent`) ist eine PRE-EXISTIERENDE Fremd-Regression im Wissen-Modul (deterministisch auch isoliert rot, von E3.6 unberührt — nichts mitgefixt, KEIN Push bis grün). **E3 komplett** (E3.1–E3.6). |
| 2026-07-24 | E4.1 | ✅ M3 Kapitel-Ziele + Anlage-Spalten + Slot-Ziel-Stempel | 8f3f20c | Migration `2026_07_24_000004_add_ziele_und_anlage_to_foodalchemist_foodbook_chapters` (Muster M2): 9 SOLL-Spalten (`target_count` int · `price_anchor/min/max` decimal(10,2) · `niveau` string KANONISCH · `service_moment_id`/`serving_form_id` unsignedBigInteger+index statt FK, Spec-Regel · `pricing_mode` string(12) · `target_food_cost_pct` decimal(5,2)) + 4 Anlage-Spalten (`released_at`/`released_by` users.id-ohne-FK/`release_note`/`release_result` json). `status` (Versand) unangetastet. Idempotent via hasColumn-Guards, additiv (alle NULL ⇒ heute). Model `FoodAlchemistFoodbookKapitel`: Casts (int/decimal:2/datetime/array) + Const `PRICING_MODES=[paket,einzel,gemischt]` (Vokabular-Pflicht, weiche Prüfung) + `servingForm()`/`serviceMoment()`-BtT. `FoodbookService::KAPITEL_FELDER` += 9 SOLL-Felder (release_* NICHT — setzt kapitelFreigeben E7.3). `strukturAusGeruest`: stempelt gesetzte Slot-Ziele (target_count/price_anchor/min/max) einmalig aufs neue Kapitel, Protokoll += `ziele_uebernommen[]`. Migration DBngin-MySQL gefahren (13 Spalten verifiziert). Backup `PRE_E4.1_kapitel_ziele_*.sql`. Kein MCP-Change (Lockstep: `foodbook_kapitel.PUT` Ziele+pricing_mode = E4.6). php -l 3/3. 1 Pest (Stempel target/preis + Slot-ohne-Ziele=leer + Protokoll-Spiegel + KAPITEL_FELDER-Edit niveau/pricing_mode/target_food_cost_pct). Pest FoodbookStruktur 5/5, Foodbook\|FoodbookLeitstelle\|Coverage\|Concept\|PlanningFrame 195/195. |
| 2026-07-24 | E4.2 | ✅ kapitelZiele() SOLL-Kaskade | 8735dfe | Neue `FoodbookService::kapitelZiele(Team,Kapitel): array` — aufgelöste SOLL-Sicht (target_count·price_anchor/min/max·niveau·serving_form_id·service_moment_id·pricing_mode·target_food_cost_pct) mit Vererbung **Kapitel→Eltern→Slot→Foodbook** + `quellen[<feld>]` ('kapitel'/'eltern'/'slot'/'foodbook'/null). Generischer `$resolve`-Closure über Kapitel-Kette (self+Eltern, Zyklus-Guard); Slot-Fallback nur für Menge/Preis (der flache Slot dieses Kapitels via `chapter_id`, greift wenn Kapitel-Feld leer — z.B. vor E4.1-Stempelung/nach Reset); Foodbook-Default für niveau (`default_niveau`)/serving_form (`default_serving_form_id`)/WE-Ziel (`target_food_cost_pct`); service_moment_id/pricing_mode nur Kapitel-Kette (Foodbook führt Einsatzmomente als 1–n-Pivot). Ergänzt `leitplanken()` (Zielgruppen/Dimensionen), ersetzt es nicht. Team-Scope via `ownedKapitel`. Kein Datenmodell-/MCP-Change (Lockstep: `foodbook_kapitel.PUT`+`coverage.GET`-WE = E4.6). php -l ok. 5 Pest (leer→null · Kapitel-Wert gewinnt · Kind erbt Eltern · Slot-Fallback Menge/Preis · Foodbook-Default Servierform/WE/Niveau). Pest KapitelZiele 5/5, Foodbook\|FoodbookLeitstelle\|Leitplanken\|Coverage\|Concept\|FoodbookStruktur 191/191. |
| 2026-07-24 | E4.3 | ✅ pruefeKapitel + Vorrangregel | (s. Commit) | Neue `CoverageService::pruefeKapitel(chapterId,titel,scope,ziele)` — Menge (target_count) + Preis-Anker/Spanne gegen Kapitel-**Rollup**-Scope (Kapitel+Nachfahren aus istFoodbook), Logik gespiegelt aus `pruefeSlot`; Befund-Shape identisch, `slot_id=null`+`chapter_id` gesetzt (befund() um additiven `chapter_id`-Key erweitert, alle Alt-Befunde =null). Neue private `kapitelKettenZiel()` löst M3-SOLL **kettenlokal** (self+Eltern, erstes gesetztes gewinnt) OHNE Slot-Fallback — der Slot ist der Gegen-Pfad. In istFoodbook 2. Durchlauf je Kapitel `ziele`/`hat_menge_ziel`/`hat_preis_ziel`. **Vorrangregel** in `pruefeSlot`: trägt das Slot-Kapitel eigene M3-Ziele, überspringt der Slot Menge/Preis (`$skipMenge`/`$skipPreis`) → kein Doppel-Befund am gestempelten Slot↔Kapitel-Paar; Dramaturgie/Regeln bleiben. Kein Datenmodell-/MCP-Change (Lockstep: `coverage.GET`-Kapitel-Sektion + WE = E4.6; befunde ist Passthrough-Read, chapter_id additiv). Model-Kommentar unberührt. php -l ok. 3 Pest (Kapitel-Mengenziel misst Rollup + chapter_id/fill_filter · Vorrang: genau 1 Menge-Befund vom Kapitel, Slot liefert keinen · Kapitel-Preisspanne Ø>max=verletzt). Pest Coverage 15/15, Foodbook\|FoodbookLeitstelle\|KapitelZiele\|Concept\|PlanningFrame 191/191. |
| 2026-07-24 | E4.4 | ✅ wareneinsatzAmpel | (s. Commit „E4.4") | Neue `FoodbookService::wareneinsatzAmpel(Team,$fb,Kapitel,?$pax): array`. **IST** = `kapitelAggregat().food_cost_percent` (rekursiv, EK÷VK der Per-Person-Anteile). **SOLL-Kaskade** über `kapitelZiele()['target_food_cost_pct']` (Kapitel→Eltern→Slot→Foodbook) → Fallback `TeamSettingsService::zielWareneinsatzPct` (Team-Setting oder 30 %-Default); `quelle` = Herkunft ('kapitel'/'eltern'/'foodbook'/'settings'). Toleranz = `$fb->food_cost_tolerance_pp` ?? 5,0 pp. Ampel: gruen ≤Ziel · gelb ≤Ziel+Tol · rot darüber · unbekannt ohne IST. **Partiell** = `agg['pauschal']>0` (Pauschal-Blöcke tragen VK, EK ungezählt → IST unterschätzt Food-Cost). kapitelZiele zuerst aufgerufen → erzwingt Team-Scope/Ownership vor Rechnung. Kein Datenmodell-/MCP-Change (Lockstep: `coverage.GET`-WE-Sektion = E4.6). php -l ok. 6 Pest (gruen/gelb/rot je Quelle · unbekannt+partiell nur-Pauschal · partiell trotz gruen bei Mix · settings-Default 30). Pest Wareneinsatz 6/6, Foodbook\|FoodbookLeitstelle\|Coverage\|Concept\|KapitelZiele 200/200. |
| 2026-07-24 | E4.5 | ✅ Backfill Slot→Kapitel-Ziele | (s. Commit) | Neue `FoodbookService::backfillSlotZiele(Team,?$foodbookId,$apply)` — holt für VOR-E4.1-Slot↔Kapitel-Kopplungen (die `strukturAusGeruest` nie stempelte, weil es nur bei Neu-Anlage stempelt) die 4 SOLL-Felder (target_count/price_anchor/min/max) vom Slot ans Kapitel nach. **Idempotent + verlustfrei:** füllt pro Kapitel NUR die Felder, die dort noch NULL sind (gesetzte Kapitel-Ziele unangetastet) → 2. Lauf schreibt nichts. Team-scoped via visibleToTeam+isOwnedBy. $apply=false=Dry-Run (Protokoll, kein Write). Command `foodalchemist:backfill-kapitel-ziele` (--team/--foodbook/--apply, default Dry-Run; ohne --team alle Teams; --foodbook erfordert --team) + im ServiceProvider registriert. **Kein Datenmodell-/MCP-Change** (E4.5-Checkliste nennt kein MCP; Kapitel-MCP = E4.6). php -l 4/4. 4 Pest (Dry-Run schreibt nicht · --apply+idempotent · nur-NULL-Felder/kein Überschreiben · Command-Roundtrip). **MySQL-Smoke** (transaktional, rollback — keine Fixture-Mutation): apply1=1 Kapitel/4 Felder, apply2=0 (idempotent), decimals korrekt (target=7, anchor=8.25) → SMOKE OK. Command live gegen Sandbox-MySQL = 0 Kandidaten (Fixture post-E4.1 schon gestempelt), lief fehlerfrei. Pest Backfill 4/4, breite Touch-Suite (Foodbook\|FoodbookLeitstelle\|Coverage\|Concept\|KapitelZiele\|Wareneinsatz\|FoodbookStruktur\|PlanningFrame) 217/217. |
| 2026-07-24 | E4.6 | ✅ MCP foodbook_kapitel.PUT + coverage.GET-WE | 52b695f | Neues Tool `FoodbookKapitelPutTool` (`foodalchemist.foodbook_kapitel.PUT`): pflegt SOLL-Ziele (target_count/price_anchor/min/max/niveau/serving_form_id/service_moment_id/target_food_cost_pct) + `pricing_mode` (Enum-Guard gegen `PRICING_MODES`, VOR Write) + Inhalt (title/consumer_title/claim/description) + `zielgruppen[]` (**PUT-sync** — []=leeren). Nur draft-Foodbook (ACCESS_DENIED sonst); FK-Vokabular-Guards (serving_form/service_moment/zielgruppen visibleToTeam→NOT_FOUND) laufen VOR jedem Write; `released_*` NICHT setzbar (Kapitel-Go=E7.3, human-only). Service: neue `setKapitelZielgruppen()` (ownedKapitel + `chapter_target_groups`-sync). **coverage.GET-Erweiterung:** `CoverageService::coverage()` += `wareneinsatz[]`-Sektion je Kapitel (foodbook-only, `wareneinsatzAmpel` E4.4 — status/ist/ziel/toleranz/quelle/partiell); Kapitel-Ziel-Befunde stecken schon in `befunde` (chapter_id, E4.3). Tool-Description beider erweitert. Im ServiceProvider registriert. 3 Pest (PUT-Roundtrip Ziele+pricing_mode+zielgruppen + kapitelZiele-Spiegel + sync-Leeren + Enum-Guard · Tenancy fremd-Zielgruppe=NOT_FOUND+Kapitel unverändert · coverage.GET wareneinsatz[]-Sektion status=unbekannt). php -l 5/5, Pest McpTools\|Coverage\|KapitelZiele\|Foodbook\|Wareneinsatz\|Concept 215/215. **E4 komplett** → breite Suite. |
| 2026-07-24 | E5.1 | ✅ LeitstelleService (checkliste/kapitelStand/speisenBaum) | (s. Commit) | Neuer read-only `LeitstelleService` (auto-DI, `FoodbookService`-Injektion). **`checkliste(Team,$fb)`**: 7 abgeleitete Schritte (bedarf→struktur→tiefe→kapitel_aufbau→kreativ→anlegen→preise), Status offen/teil/erledigt aus Bestand + Sprungziel `tab`+`anker` (Event-Bus E5.2). Freigabe/Versand NIE in der Liste (UX 1). Heuristiken: Bedarf=personen+≥1 Default-Dimension · Struktur=≥1 Kapitel · Tiefe=maxDepth≥2 sonst teil (flach optional) · Kapitel-Aufbau=Anteil Kapitel-mit-M3-Zielen/Zielgruppen · Kreativ=Ideen-Vorhandensein · Anlegen=Anteil befüllte Kapitel (Inhalts-Block ODER released_at) · Preise=Anteil bepreiste unter den befüllten. **`kapitelStand`**: kapitelZiele+kapitelAggregat+wareneinsatzAmpel+Zielgruppen+Inhalts-Zähler(pakete/einzel/ideen)+released. **`speisenBaum`**: pro Kapitel Paket (concept_ref→Konzept-Slots, €/Gast) · Einzel (recipe_ref, €/Pos, Status bepreist/angelegt/entwurf) · Ideen (Entwurf/ki_queue) mit Badges. **dish_ideas (M4/E6.1) existiert bei E5.1 noch NICHT** → alle Ideen-Zähler `Schema::hasTable`-guarded, Roh-DB (kein Model), liefern 0 bis E6. Kein Datenmodell-/MCP-Change (Lockstep: `leitstelle.GET` = E5.4). php -l 2/2. 5 Pest (7-Schritt-Reihenfolge+alle offen · Bedarf teil→erledigt · Struktur/Tiefe/Aufbau/Anlegen/Preise-Hochschaltung · kapitelStand-Shape · speisenBaum Paket vs Einzel). Pest LeitstelleService 5/5, Touch-Suite (Foodbook\|FoodbookLeitstelle\|Coverage\|Concept\|KapitelZiele\|Wareneinsatz\|PlanningFrame\|Leitplanken\|McpTools) 226/226. |
| 2026-07-24 | E5.2 | ✅ Checkliste-UI + Stepper auf Tab-Ebene + Sprung-Event-Bus | (s. Commit) | Neuer Partial `livewire/foodbooks/partials/leitstelle-checkliste.blade.php`: 7 Chips (Bedarf→Preise) aus `LeitstelleService::checkliste()` (in `Index::render()` als `checkliste` verdrahtet, DI via `app()`), Status-Farbe offen/teil/erledigt + Status-Punkt, `title`=hinweis, Chip-Klick dispatcht `fb-goto` {tab,anker}. Leitstellen-Leiste (`data-fb-leitstelle`) auf Tab-Leisten-Ebene (aus allen Tabs sichtbar) = Checkliste + Phasen-Stepper; **Stepper aus der Briefing-Karte (:132f) dorthin verschoben** (Versand-Status ≠ Checkliste, UX 1). **Sprung-Event-Bus:** Cockpit-Root `@fb-goto.window` — setzt `tab` NUR wenn `[data-fb-tab]` existiert (graceful: preise-Tab=E8 → bleibt stehen, kein Blank) + `$nextTick`-Scroll zum `[data-fb-anker]` (graceful: fehlender Anker=kein Scroll). Anker gesetzt: `bedarf` (Bedarf-Card), `kapitel` (Planung-Frame), `ideen` (Kreativ-Panel); `anlegen`/`preise` folgen E7/E8. Kein Service-/Datenmodell-/MCP-Change (`leitstelle.GET`=E5.4). php -l ok, Blade compileString 2/2. 1 Livewire-Pest (7 Chips, data-Attribute, fb-goto, erster/letzter Schritt). Pest FoodbookUi 4/4, Touch-Suite (Foodbook\|FoodbookLeitstelle\|LeitstelleService\|Coverage\|Concept\|KapitelZiele\|Wareneinsatz\|PlanningFrame\|Leitplanken\|McpTools\|FoodbookUi) 230/230. |
| 2026-07-24 | E5.3 | ✅ Leitstelle-Rail Nested-Livewire | 4fd1c73 | Neue Nested-Livewire `LeitstelleRail` (Muster `KundeDnaPanel`, auto-registriert als `foodalchemist.foodbooks.leitstelle-rail`) in der rechten activity-Sidebar, kontextsensitiv über `wire:key` (`foodbook.id`+kapitel\|'kopf', Re-Mount bei Selektionswechsel). **Kopf-Modus:** 3-Panel-Alpine-Umschalter (Fortschritt/Speisen/Kalkulation) mit localStorage-**Pin** + Auto-Default je Cockpit-Tab (Cockpit-Root dispatcht `fb-cockpit-tab` via `x-effect`; Rail folgt `tabMap`, solange nicht gepinnt). Fortschritt = kompakter N/7-Zähler (voller Chip-Strip bleibt in der Tab-Leiste, E5.2 — kein Doppel-Render) + **Kapitel-Matrix** (WE-Punkt · Ziele/Positionen/Preis-Badges · Go-Shortcut deaktiviert=E7) + **Komplexitäts-Hinweis** (>8 Kapitel \|\| >12 Positionen); Speisen = heterogener Baum (`speisenBaum`, Paket/Einzel/Idee-Badges, €/G vs. €/Pos); Kalkulation = Portfolio (`gesamt`) + WE-Ampel je Kapitel. **Kapitel-Modus:** Kapitel-Planung mit **Ziele-Editing** (M3: Niveau/pricing_mode/Einsatzmoment/Servierform/target_count/target_food_cost_pct/price_anchor/min/max via `updateKapitel`, leer→null) + Zielgruppen-Chips (`setKapitelZielgruppen`-sync) + Kapitel-Kalkulation + WE-Ampel + Kapitel-Coverage (Befunde gefiltert auf `chapter_id`, Scope Kapitel+Nachfahren) + Ideen-Stand (Go=E7 deaktiviert). Edits dispatchen `leitstelle-kapitel-geaendert` → neuer `#[On]`-No-op-Handler im Eltern-`Index` (Re-Render Kapitel-Kopf/Coverage). Neue read-only `LeitstelleService::kapitelMatrix()` (teilt Ziele-/Inhalts-Heuristik mit `checkliste()`; dient E5.4 als Kapitel-Sektion). Index-`render` um Portfolio/`kapitelAggregat`/`gesamt` entschlackt (zog in die Rail). **Kein MCP-Change** (Lockstep: `leitstelle.GET` = E5.4). php -l 3/3, Blade compileString 2/2. 3 Pest (kapitelMatrix Status+WE · Rail Kopf-Umschalter/Matrix/Progress · Rail Kapitel Ziele-Editing persistiert+Event+Zielgruppen-Toggle). E5.2-Test angepasst (Rail rendert Checkliste nicht mehr doppelt). Touch-Suite (Foodbook\|FoodbookLeitstelle\|LeitstelleService\|Coverage\|Concept\|KapitelZiele\|Wareneinsatz\|PlanningFrame\|Leitplanken\|McpTools\|FoodbookUi\|FoodbookStruktur) 233/233. |
| 2026-07-24 | E5.4 | ✅ MCP leitstelle.GET | (s. Commit) | Neues read-only Tool `LeitstelleGetTool` (`foodalchemist.leitstelle.GET`): führt eine KI durch die 7 Phasen. Args `foodbook_id` (Pflicht) + optional `chapter_id`. **Ohne chapter_id:** `foodbook{}`-Kopf + `checkliste` (7 Schritte via `LeitstelleService::checkliste`) + `kapitel_matrix` (`kapitelMatrix` — Planungs-Status je Kapitel + WE-Ampel). **Mit chapter_id:** statt Matrix `kapitel{}` (`kapitelStand` — Ziele-Kaskade/Zielgruppen/Aggregat/WE-Ampel/Inhalts-/Ideen-Zähler/Anlage) + `coverage_befunde` (Foodbook-Coverage-Befunde gefiltert auf chapter_id, Scope Kapitel+Nachfahren — spiegelt LeitstelleRail E5.3). READ-ONLY, Kapitel-Go bleibt human-only (kein MCP-Trigger, Spec MCP-Lockstep). Tenancy: FB visibleToTeam→NOT_FOUND; chapter_id muss zu diesem FB gehören (`foodbook_id`-Match)→NOT_FOUND. Im ServiceProvider registriert (bei E3.5-Zielgruppen-Tools). Kein Datenmodell-/Service-Change (reine Verdrahtung bestehender E5.1-Reads). php -l 2/2. 3 Pest (Überblick: 7-Schritt-Reihenfolge+Matrix, kein `kapitel` · Kapitel-Modus: Ziele/Zielgruppen/WE/coverage_befunde, keine Matrix · Tenancy: fremd-FB + fremdes Kapitel an eigenem FB = NOT_FOUND). Pest McpTools+E5.4 3/3, Touch-Suite (McpTools\|LeitstelleService\|FoodbookLeitstelle\|Coverage\|KapitelZiele\|Wareneinsatz\|Foodbook\|FoodbookUi\|Concept) 227/227. **E5 komplett** → breite Suite **1036 Tests, 1032 passed, 4 skipped, 0 failed**. |
| 2026-07-24 | E6.1 | ✅ M4 dish_ideas + dish_idea_groups + Models | 982168e | Migration `2026_07_24_000006_create_foodalchemist_dish_ideas_tables` (Muster M1, idempotent via hasTable-Guards). **`foodalchemist_dish_idea_groups`** (12 Sp.): uuid/team_id-index · chapter_id/concept_id nullable FK cascadeOnDelete (XOR-Owner, Guard = IdeenService E6.2) · name (→ concept.name beim Go) · target_price_pp decimal(10,2) · position · materialized_concept_id (loser Zeiger, index, kein Cascade) · timestamps/softDeletes. **`foodalchemist_dish_ideas`** (21 Sp.): uuid/team_id · chapter_id/concept_id XOR-FK cascade · position · title · description · sales_recipe_id (loser Zeiger) · target_form string(12) default 'einzel' · group_id FK **nullOnDelete** (Spec-explizit) · status string(20) default 'entwurf' · created_via · source_meta json · generation_status string(20) · generated_recipe_id (loser Zeiger) · materialized_at · materialized_ref json · timestamps/softDeletes. **Namens-Deviation vom Spec-Shorthand (dokumentiert im Migration-Docblock):** Content-Spalten anglisiert `titel→title`/`beschreibung→description`/`ziel_form→target_form`/`quelle_meta→source_meta` — die LIVE-Chapters-Tabelle beweist die Schema-Konvention (title/consumer_title/description EN); deutsche ENUM-**Werte** bleiben (einzel|paket, entwurf|verworfen|freigegeben) analog `pricing_mode`. Klassifikations-Strings per Model-Const gedeckelt (`TARGET_FORMS`/`STATUSES`/`GENERATION_STATUSES`, Vokabular-Pflicht). Ergebnis-Zeiger bewusst ohne Cascade (Materialisierungs-Delete reißt Skizze nicht mit, VK-Gericht bleibt löschbar). Models `FoodAlchemistDishIdea`/`FoodAlchemistDishIdeaGroup` (HasUuidV7/LogsActivity/BelongsToTeamHierarchy/SoftDeletes) + additive Inverse-HasMany `dishIdeas()`/`dishIdeaGroups()` auf Kapitel + Concept. **Kein MCP-Change** (Lockstep: `kapitel_ideen.*` = E6.5; Ideen erzeugen NIE Rezepte/GPs/Konzepte — Invariante). Migration DBngin-MySQL gefahren (Tabellen 12/21 Sp. verifiziert). Backup `PRE_E6.1_dish_ideas_*.sql`. php -l 5/5. Model-Smoke (Transaktion/rollback): UUID-Autogen · decimal/json-Casts · alle Relations (idea↔group, idea→chapter, kapitel→dishIdeas/Groups) · Consts · group_id nullOnDelete = alle OK. Touch-Suite (Foodbook\|FoodbookLeitstelle\|LeitstelleService\|Concept\|Coverage\|KapitelZiele) 218/218. |
| 2026-07-24 | E6.2 | ✅ IdeenService (CRUD/Gruppen/XOR/Bestand) | (s. Commit) | Neuer `IdeenService` (dependency-frei; `kiDivergenz`=E6.4). **CRUD:** `liste()` (Owner-XOR, gruppiert Paket-Gruppen mit Ideen + freie Einzel; verworfene per Default versteckt, `$inklVerworfen`) · `add()` (freie Skizze, Titel Pflicht, status=entwurf, source_meta.quelle=frei) · `update()` (Titel/Beschreibung/Position/Form — NICHT status/Owner/sales_recipe_id/materialized_*) · `setStatus()` (**nur entwurf\|verworfen** — freigegeben=E7.3-Go gesperrt). **Gruppen:** `addGruppe`/`updateGruppe` (Name Pflicht, target_price_pp→Konzept-€/Gast beim Go). **XOR-Guard** `ownerXor()`: exakt eines chapter_id/concept_id, sonst Fehler; `resolveOwner()` prüft Ownership übers Owner-Kapitel/-Konzept (visibleToTeam+isOwnedBy, D1). **Bestand-Übernahme** `uebernehmeBestand()`: spiegelt `pruefeRecipeRef` (verkauf()-Scope, keine Slot-Variante), loser `sales_recipe_id`-Zeiger, Titel-Default=Gericht-Name, created_via=bestand — **dupliziert NICHTS** (Invariante: Ideen erzeugen nie Rezepte/GPs/Konzepte). `normForm()`: group_id ⇒ target_form=paket + Gruppe muss team-eigen + selber Owner sein; paket ohne group_id=Fehler (Regel M4). Position ans Ende, owner-gescoped. **Kein Datenmodell-/MCP-Change** (Lockstep: `kapitel_ideen.*`=E6.5; UI=E6.3). **Env-Fix:** Sandbox-Symlink `vendor/martin3r/platform-foodalchemist` zeigte auf stale Worktree `sweet-gates-09e67b` (@ad721de=E5.4, VOR E6.1 → M4-Tabellen fehlten) → zurück auf main-Modul gepointet (reversibel, Worktree unangetastet). Trait `SeedsTeamHierarchy` += flush für DishIdea/DishIdeaGroup. php -l ok. 9 Pest (frei-Anlage+Position · XOR weder/beide · Leer-Titel add+update · Bestand-Übernahme+kein Dup · Nicht-VK abgelehnt · Paket-Bündelung+liste-Gruppierung · paket-ohne-group/Quer-Owner abgelehnt · verwerfen/reaktivieren+freigegeben-Sperre · Tenancy Cross-Team). Pest IdeenService 9/9, Touch-Suite (IdeenService\|FoodbookLeitstelle\|LeitstelleService\|Foodbook\|Coverage\|Concept\|KapitelZiele) 227/227. |
| 2026-07-24 | E6.3 | ✅ Kreativ-Skizzenfläche + Planung-Rückbau + „bereits angelegt" | (s. Commit) | **Kreativ-Tab** = Skizzenfläche (owner = links gewähltes Kapitel; ohne Kapitel Hinweis „links wählen"). 3 Quellen: freie Idee (Titel-Input + Enter/Button → `ideeHinzu`) · aus Bestand (VK-Gericht-Suche `gerichtKandidaten`-Reuse → `skizzeAusBestand`) · KI frei (disabled+Tooltip, provider-gated E6.4). **Paket-Bündelung per Mehrfachauswahl:** Checkbox-`ideeAuswahl` auf freien Einzel-Skizzen → „zu Paket bündeln" (`paketBilden`: `addGruppe`+`update(group_id)`); „aus Paket lösen" (`ausPaketLoesen` → group_id 0 + target_form einzel, sonst greift Paket-Guard) · „Paket auflösen" (`paketAufloesen` → neue `IdeenService::loescheGruppe`: Mitglieder lösen + Gruppe soft-delete). verwerfen/reaktivieren (`ideeVerwerfen`/`ideeReaktivieren` → setStatus entwurf\|verworfen; Papierkorb-Toggle `ideenPapierkorb`). Neuer Partial `partials/skizze-zeile.blade.php`. **Planung-Tab-Rückbau:** kompletter per-Slot-Vorschlags-Block (✨ Vorschläge + ✓/✕ + „Konzept füllen") entfernt → ein Forward-Button „🎨 im Kreativ-Tab skizzieren →" (dispatcht `fb-goto` tab=kreativ/anker=ideen). Livewire-Methoden `vorschlaegeFuerSlot`/`uebernehmeGericht`/`verwerfeGericht`/`slotFuellen` bleiben (Service-Pfad + FoodbookLeitstelleTest-Regression; E7 retired sie). **„bereits angelegt":** Kapitel trägt schon concept_ref/recipe_ref-Block, aber keine Skizzen → Sky-Hinweis „Slot-Übernahme, Ideen-Phase übersprungen" (UX 7, Bestands-Foodbooks). **Deviation:** `loescheGruppe` neu in E6.2-Service ergänzt (Gruppen-CRUD-Vervollständigung, die E6.3-UI braucht; kein MCP — `kapitel_ideen.*`=E6.5). php -l 2/2, Blade compileString 2/2 (index + skizze-zeile). 6 Pest (freie Idee entwurf/frei · Leer-Titel=Fehler+kein Insert · aus Bestand loser Zeiger/kein Slot · Paket bilden→lösen→auflösen · verwerfen↔reaktivieren). Pest FoodbookLeitstelle+IdeenService 22/22, Touch-Suite (FoodbookUi\|FoodbookStruktur\|PlanningFrame\|Coverage\|Concept\|LeitstelleService\|KapitelZiele\|Wareneinsatz\|Foodbook) 236/236. |
| 2026-07-24 | E6.4 | ✅ KI-Divergenz-Verdrahtung | (s. Commit) | **Prompt** `foodbook.kapitel_ideen` (Tier B, max_tokens 4000): produkt-blinde Divergenz, System betont „frei ideieren OHNE Bestandsprüfung, Erdung folgt beim Go", werte = {ideen:[{titel,beschreibung}]} (nur Skizzen — keine Mengen/Preise/Zutaten). In `AiGatewayService::FOOD_DNA_KEYS` aufgenommen → erbt die Food-DNA-Kette (Kontext-Vertrag, Spec §KI-Führung). **Routings** `foodbook.plan`/`concept.plan` (je cross_cutting:always + domain:discovery, KEIN pairing-grounding — Divergenz ist produkt-blind) in `KnowledgeImportCommand::seedRoutings` (insertOrIgnore, idempotent; erledigt Spec-08-P6). **`IdeenService::kiDivergenz(Team,$chapterId,$anzahl=5)`**: Ownership-Guard (visibleToTeam+isOwnedBy) VOR jedem Call; Kontext = `leitplanken()`+`kapitelZiele()`+`promptKontext()` des Foodbook-Frames (falls vorhanden)+`KnowledgeContextService::contextFor('foodbook.plan')`; `propose()` mit knowledge/food_dna_foodbook_id; jede gelieferte Zeile (titel|title, beschreibung|description) → Entwurf-Skizze `created_via='ai_gateway'`, `source_meta={quelle:ki_divergenz,confidence,call_log_id}`, leere Titel übersprungen. **Invariante gewahrt:** legt NUR dish_ideas an, nie Rezepte/GPs/Konzepte; ohne Provider wirft `propose()` typisiert (Aufrufer fängt, hier nicht geschluckt). anzahl geklammert 1–12. **Kein Datenmodell-/MCP-Change** (Lockstep: `kapitel_ideen.*`=E6.5; UI-KI-frei-Button war E6.3 schon disabled). php -l 4/4. 3 Pest (Divergenz→2 Entwürfe via Provider-Stub, engl./leere Keys robust, keine Rezepte/Konzepte · Fake-Echo ohne `ideen`-Key→0 Zeilen kein Fehler · Tenancy Cross-Team=Fehler+0 Insert) + KnowledgeImportTest-Zählung 8→12 nachgezogen (eigene Änderung, keine Fremd-Regression). Pest IdeenService 12/12, KnowledgeImport 4/4, Touch-Suite (IdeenService\|FoodbookLeitstelle\|LeitstelleService\|KnowledgeImport\|ConceptGenerator\|AiRetry\|Foodbook\|Coverage\|Concept\|KapitelZiele) 264/264. **Nachtrag:** die im E3.6-Protokoll vermerkte „Fremd-Rote" `KnowledgeBindToolTest::UNBIND` re-verifiziert = **stale** (Test 6/6 bzw. 1/1 grün in `:memory:`; `c1d2ba3` scopet die Assertions über die eindeutige `$docId` statt den festen Slug → backend-robust, kippt auch bei persistenter DB nicht). Push damit nicht länger blockiert. |
| 2026-07-24 | E6.5 | ✅ MCP kapitel_ideen.GET/POST/PUT | (s. Commit) | 3 neue Tools über `IdeenService` (E6.2). **GET** (`foodalchemist.kapitel_ideen.GET`, read-only): Owner chapter_id XOR concept_id → `liste()` gruppiert = `gruppen[]` (Paket + ihre Skizzen, `paket_zielpreis_pp`) + `einzel[]` + total; `include_verworfen`. **Cross-Team-Guard im Tool** (Deviation): `liste()` filtert Skizzen team-sichtbar (kein Leak), validiert den Owner selbst NICHT → fremdes Kapitel gäbe sonst leere Liste statt NOT_FOUND; deshalb explizite `visibleToTeam`-Existenzprüfung auf chapter/concept VOR liste(). **POST** (Diskriminator `objekt=idee|gruppe`): idee → `add` (title Pflicht) bzw. `uebernehmeBestand` (sales_recipe_id gesetzt); gruppe → `addGruppe` (name + paket_zielpreis_pp). MCP-Param→Service-Key-Map: `ziel_form`→target_form, `paket_gruppe`→group_id, `paket_zielpreis_pp`→target_price_pp; `created_via='mcp'`. **PUT** (`objekt`+`id`): idee → `update` (title/description/position/ziel_form/paket_gruppe) und/oder `setStatus` (nur entwurf\|verworfen — freigegeben gesperrt=Kapitel-Go); gruppe → `updateGruppe` bzw. `aufloesen=true`→`loescheGruppe`. Fehler-Map: ModelNotFound→NOT_FOUND, RuntimeException→ACCESS_DENIED bei „Geerbt" sonst VALIDATION_ERROR. Geteilte Serializer `skizzeArr()`/`paketArr()` in `FoodAlchemistTool`-Basis (MCP-Feldnamen ziel_form/paket_gruppe, nicht Roh-Spalten). Alle 3 im ServiceProvider registriert. **Invariante gewahrt:** legt NUR Entwürfe an, nie Rezepte/GPs/Konzepte (das Anlegen = human-only Kapitel-Go, kein MCP-Trigger). php -l 5/5. 3 Pest (POST/GET-Roundtrip: freie Idee + Paket-Gruppe + Paket-Zuordnung + GET gruppiert + paket-ohne-group=VALIDATION + XOR-Guard · PUT: umbenennen/Zielpreis + verwerfen↔reaktivieren + freigegeben-Sperre + Paket-auflösen→Einzel · Tenancy: fremdes Kapitel GET+POST=NOT_FOUND + 0 Insert). Touch-Suite (McpTools\|IdeenService\|FoodbookLeitstelle\|LeitstelleService\|Foodbook\|Coverage\|Concept\|KapitelZiele) 243/243. **E6 komplett** (E6.1–E6.5) → breite Suite. |
| 2026-07-24 | E7.1 | ✅ M5 blocks.presentation_id + DarreichungResolver::fuerBlock | (s. Commit) | Migration `2026_07_24_000007_add_presentation_to_foodalchemist_foodbook_blocks` (Muster M2): `foodalchemist_foodbook_blocks` += `presentation_id` `unsignedBigInteger`+index nullable, `after('sales_recipe_id')` (Spec-Regel für ALTERs auf Bestandstabellen — loser Zeiger auf `recipe_darreichungen`, KEINE harte FK; vgl. slot/paket_gericht.presentation_id). Idempotent via hasColumn-Guard, additiv. Model `FoodAlchemistFoodbookBlock`: `presentation()`-BtT + Cast `presentation_id`=integer. Resolver `DarreichungResolver::fuerBlock(Block, ?$servingFormId): ?Darreichung` — 3-Stufen analog `fuerSlot`: 1. block.presentation_id (expliziter Override) → 2. Gericht-Darreichung zur übergebenen Servierform (Kapitel-/Foodbook-Servierform aus der Dimensions-Kaskade, vom Aufrufer gereicht) → 3. `standardFuer()`; dish=null ⇒ null. **NOCH KEIN Money-Path-Caller** (blockPreis-Verdrahtung folgt, wenn presentation_id real geschrieben wird = E7.3/E7.5) → E7.1 rein additiv, garantiert bit-identisch (Top-Risiko #5). Kein MCP-Change (presentation_id wird in E7.1 von nichts geschrieben; MCP `foodbook_blocks.POST`-presentation zieht mit dem Schreibpfad E7.3). Migration DBngin-MySQL gefahren (Spalte bigint unsigned/Null=YES/index verifiziert). Backup `PRE_E7.1_presentation_*.sql`. php -l 3/3. 4 Pest (Standard-Fallback ohne presentation/Servierform · expliziter Zeiger zurückgegeben · recipe_ref ohne Gericht=null · Servierform-ohne-Match→Standard) — der distinguierende Zwei-Darreichungen-Fall (Match/Override schlagen Standard) ist SQLite-untragbar → **MySQL-Smoke** (transaktional/rollback): Servierform-Match=Buffet 2,32 · Override=Buffet 2,32 · Fallback=Standard 25 → SMOKE OK 3/3. Pest Darreichung\|Foodbook\|Coverage\|Concept\|McpDarreichungen\|VkSnapshot 232/232. **E7 gestartet.** |
| 2026-07-24 | E7.2 | ✅ uebernehmeGericht-Kern (+$conceptId) | 29cf87b | `uebernehmeVorschlag` in dünnen, **bit-identischen** Wrapper umgebaut + neuen Kern `uebernehmeGericht(Team,$fbId,$chapterId,$recipeId,?$rolle,$createdVia='foodbook_slot',?int $conceptId=null)` extrahiert (Spec-Signatur). Wrapper: fb→guard→slot-findOrFail→chapter_id-Check (Exception-Reihenfolge unverändert) → delegiert mit `$rolle=slot->label`, `createdVia='foodbook_slot'`, `conceptId=null` → heutiges Verhalten byte-gleich (Konzept-Name `trim($rolle?:$kapitel->title)?:'Konzept'`, addSlot role=$rolle). Kern: **$conceptId=null** = führendes Kapitel-Konzept finden/neu anlegen (concept_ref-Block) wie bisher; **$conceptId gesetzt** = Gericht gezielt in DIESES Konzept, KEIN concept_ref-Block (den setzt E7.3-Aufrufer), Ownership guardet `ConceptService::addSlot` selbst (visibleToTeam+guardOwner). Kapitelweite Dedup (`gerichtImKapitel`, Konzept-Slots ∪ recipe_ref) läuft in beiden Pfaden VOR Anlage. Kein Datenmodell-/MCP-Change (reiner Service-Refactor; MCP-Kapitel-Go bleibt human-only). php -l ok. Bestehende Wrapper-Tests (Niveau-Erbe/created_via/Doppel-Dedup/recipe_ref-Dedup) = Regression bit-identisch + 2 neue Pest (Kern mit $conceptId hängt gezielt ins Ziel-Konzept, kein 2. Konzept · Kern ohne $conceptId = Wrapper-Verhalten). Pest FoodbookLeitstelle\|LeitstelleService 21/21, breit Foodbook\|Concept\|Coverage 221/221 (0 Regressionen). |
| 2026-07-24 | E2.2 | ✅ Coverage Nachfahren-Rollup | 62cdef5 | `CoverageService::istFoodbook` in 2 Durchläufe geteilt: (1) direkte Gerichte je Kapitel + parent→children-Kanten (`$kinder[$parent_id ?? 0][]`); (2) `kapitelMap[$cid]['gerichte']` = Kapitel + alle Nachfahren (`unique('id')`), `vk_pp` bleibt aus `kapitelAggregat` (schon rekursiv). Neue private `nachfahrenIds()` (iterativ, spiegelt `FoodbookService::descendantKapitelIds`, aber DB-frei — Baum ist geladen). `slotScope`-Docblock ergänzt (Kapitel-Verweis = Kapitel+Nachfahren). Model-Kommentar `FoodAlchemistFoodbook.php:32` korrigiert (Relation liefert ALLE Kapitel flach, nicht nur Top). Kein MCP-Change (Slots/Coverage-Read-Shape unverändert; `coverage.GET`-WE-Sektion + `pruefeKapitel`-Vorrangregel bleiben E4.3/E4.4). 1 Pest (Eltern-Slot ohne eigene Blöcke zählt 2 Enkel-Gerichte → erfuellt + Enkel-Basisfall). Pest Coverage 12/12, Foodbook\|FoodbookLeitstelle\|Concept 161/161. |
| 2026-07-24 | E7.3 | ✅ kapitelFreigeben (Kapitel-Go „Anlegen") | (s. Commit) | Neuer `FoodbookService::kapitelFreigeben(Team,$kapitelId,?$note,?$userId): array` — transaktional + idempotent. Routing: **Paket-Gruppe → EIN Konzept** (`ConceptService::create` name/level, dann `$concept->update` created_via='kapitel_freigabe'/target_price_per_person aus Gruppe/serving_form_id [kapitelZiele-Kaskade]/event_type_id [leitplanken-Foodbook-Default] + `serviceMoments()->sync` [Kapitel-Override sonst Foodbook-Pivot] + `targetGroups()->sync` [concept_target_groups, Entscheidung 6], level via `denormNiveauFuerConcept`) + concept_ref-Block (nur wenn nicht vorhanden) + je Bestands-Mitglied ein Slot über `uebernehmeGericht($conceptId=…)` (kapitelweite Dedup). **Einzel-Idee+Bestand-Ref → recipe_ref-Block** (bereits vorhandener Block ⇒ weich verknüpft/übersprungen). **Freitext-Idee** (kein sales_recipe_id) → `generation_status='queued'` (echte KI-Erstellung = E7.4, Go scheitert NIE). Idempotenz: nur status='entwurf' angefasst, Gruppen reusen `materialized_concept_id`; Idee-Materialisierung setzt status='freigegeben'+materialized_at+materialized_ref ({concept_id,concept_slot_id} paket / {block_id} einzel). Setzt released_at/released_by/release_note/release_result(json) am Kapitel (LogsActivity-Trait loggt). Kein MCP-Change (Kapitel-Go human-only per Spec §MCP-Lockstep; `kapitel_freigabe.GET` = E7.6). php -l ok. 5 neue Pest (Paket→Konzept+Stempel+Slot · Einzel→recipe_ref · Freitext→queued · Idempotenz doppelter Go=0 Neuanlagen · released_*+release_result). Pest FoodbookKapitelAnlage 5/5, breit Foodbook\|Leitstelle\|Coverage\|Concept\|Ideen 246/246 (0 Regressionen). |
| 2026-07-24 | E7.4 | ✅ Freitext-Queue + Graceful | (s. Commit) | Neuer Freitext-Queue-Prozessor `FoodbookService::materialisiereFreitextIdee(Team,$ideaId)` (L7/L8): lädt EINE queued Freitext-Skizze (sales_recipe_id null), baut Erdungs-Kontext (`kapitelZiele`/`leitplanken`-Niveau + Titel/Beschreibung als description), ruft `RecipeGeneratorService::generiere(...,vkModus:true)` — der VK-Generator IST die Anpassungs-Schleife (GP-Grounding kandidatenPool/#507 bzw. LA-First-Mint). Bei Erfolg materialisiert: Paket-Mitglied (group_id + group.materialized_concept_id) → Konzept-Slot via `uebernehmeGericht($conceptId)` (kapitelweite Dedup), sonst Einzel → recipe_ref-Block; Skizze → generation_status='erstellt'/status='freigegeben'/generated_recipe_id/materialized_ref, source_meta merged (original_titel bleibt = kein Kreativitätsverlust). **Graceful (DoD):** catch `KiNichtVerfuegbar\|KiDeaktiviert` (beide RuntimeException) → bleibt `queued` + source_meta.generation_hinweis='wartet auf KI', kein Rethrow (Go scheitert nie); catch-all `\Throwable` → generation_status='fehlgeschlagen' + source_meta.generation_fehler, Original bleibt. Dispatcher `verarbeiteFreitextQueue(Team,$kapitelId,?$userId)` reiht je queued Skizze einen `MaterializeIdeaJob` ein (menschlich getriggert, KEIN MCP — „Kapitel-Go OHNE MCP-Trigger"). Neuer Job `MaterializeIdeaJob` (GenerateRecipeJob-Muster: timeout 300/tries 1, Auth-Restore für AiGateway-Team-Kontext, ruft den graceful Service → kein Fehler-Fallback/Retry nötig). **Kein Datenmodell-/MCP-Change** (generation_status/generated_recipe_id existieren aus M4; E7.6=`kapitel_freigabe.GET`). `kapitelFreigeben` (E7.3) unangetastet — bit-identisch. php -l 3/3. 4 Pest (Graceful ohne Provider→wartet_ki+bleibt queued+kein Rezept/Block · verarbeiteFreitextQueue→2 Jobs via Queue::fake+ideaId/teamId-Match · Fake-Echo unbrauchbar→fehlgeschlagen+Original bleibt · nicht-queued→uebersprungen). Pest FoodbookFreitextQueue+FoodbookKapitelAnlage 9/9; Etappen-breit (Foodbook\|Leitstelle\|Ideen\|Coverage\|Concept\|KapitelZiele\|Darreichung\|McpTools) 265/265 (0 Regressionen). |
| 2026-07-24 | E7.5 | ✅ Anlage-Modal + Undo „Anlage zurückziehen" | e0d0a3d | **Service:** neuer `FoodbookService::anlageZuruckziehen(Team,$kapitelId): array` — transaktional + idempotent, Undo von `kapitelFreigeben`. Guard: `released_at=null` → no-op ('nichts_anzulegen'); `snapshot_at≠null` ODER `status='sent'` → RuntimeException (eingefroren, Spec UX 4). Räumt NUR Anlage-Objekte: Anlage-Konzepte (created_via='kapitel_freigabe' & status='draft') samt Slots + concept_ref-Blöcken (Gruppe verliert `materialized_concept_id`); frische recipe_ref-Blöcke (materialized_ref.created===true) — VERKNÜPFTE Bestands-Blöcke (created===false) bleiben; Ideen → status='entwurf' (materialized_* geleert, queued/erstellt→generation_status null); KI-Rezepte bleiben als Draft (kein Datenverlust). Bearbeitete/aktivierte Konzepte (status≠draft) weich behalten. Setzt released_* zurück. **Provenienz-Nachzug in E7.3-Einzelpfad:** materialized_ref bekommt `created`-Flag (fresh=true/dedup-vorhanden=false), damit Undo den Bestands-Block nicht killt (Docblock-Zeilen ergänzt, `blockPreis`/Kaskade unberührt = bit-identisch). **UI:** `LeitstelleRail` — Ideen-Stand-Shortcut öffnet Anlage-Modal `kapitel-anlegen` (Zusammenfassung beider Wege, ☑-Liste Pakete/Einzel via `IdeenService::liste`, Niveau/Zielgruppen-Anzeige, optionale `anlageNote`) → `kapitelAnlegen()`; nach Anlage Undo-Button (`undoMoeglich` = draft+kein Snapshot) → Confirm-Modal `kapitel-zurueckziehen` → `anlageZuruckziehen()`; Matrix-Go im Kopf-Modus wird Navigations-Shortcut (`$parent.kapitelWaehle`). **Kein MCP-Change** (Kapitel-Go+Undo human-only, Spec §MCP-Lockstep; E7.6=`kapitel_freigabe.GET`). **Browser-Klick:** Klick-Pfade + Modal-Markup via Livewire-Komponententest abgedeckt (kapitelAnlegen→recipe_ref-Block+released_at; anlageZuruckziehen→leer+released_at null); volle Browser-Strecke = E8.4. php -l 4/4, Blade compileString ok. 6 neue Pest (5 Undo-Service: Round-Trip Konzept/Block/Slot+Re-Anlage · Freitext-Queue-Leerung · Dedup-Block bleibt · Snapshot/sent wirft · no-op; 1 Rail-Livewire Anlegen+Undo). Pest FoodbookKapitelAnlage 10/10, FoodbookUi 7/7; breit Foodbook\|FoodbookLeitstelle\|Coverage\|Concept 236/236 (0 Regressionen). |
